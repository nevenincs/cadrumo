# The portal surface the read cross-reference above is reached through. A
# revision declaring a live cross-reference must declare the portal link that
# owns it, so the operator surface and the guard policy name the same portal.

[[revisions."2024-y-siguientes".application_links]]
id = "modelo-200-portal"
surface = "portal"
consumer = "cadrumo.domain.portals.Portal.PORTAL_M200_SOCIEDADES_ANUAL"
requires_snapshot = true
legal_refs = ["ley-27-2014:art-124"]
source_refs = ["aeat-modelo-200-manual-2025"]
