# Modelo 233 record-design parity anchor. Declares the bundled orden-hac-1400-2018
# form-approval order as the layout authority. This scheduling/applicability-grade
# revision carries no calc formulas, so runner_required is false: the anchor pins
# the official form-approval provenance without asserting a verified fixed-width
# byte-position record design (no authoritative Modelo 233 DR is bundled).

[[revisions."2018-y-siguientes".workbook_parity_refs]]
id = "modelo-233-orden-static-layout"
workbook_source = "enrolled-modelo-233-layout"
fixture_id = "modelo-233-orden-hac-1400-2018-layout"
formula_coverage = "static_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["orden-hac-1400-2018:art-1", "ley-58-2003:art-93"]
source_refs = ["enrolled-modelo-233-layout"]
