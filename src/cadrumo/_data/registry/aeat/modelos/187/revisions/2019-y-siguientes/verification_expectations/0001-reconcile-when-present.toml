# First reconcile-when-present verification for Modelo 187 revision
# 2019-y-siguientes: the computed casilla 05 is value-reconciled
# against the engine WHEN the filing prints it (a filed-vs-computed
# divergence surfaces a discrepancy), but is EXCLUDED from the coverage
# denominator (min_coverage = 0), so enrollment can never lower coverage
# or flip a legitimate filing's verdict. Grounded in the casilla's own
# declared legal_refs/source_refs.
[[revisions."2019-y-siguientes".verification_expectations]]
id = "modelo-187-2019-y-siguientes-reconcile-when-present"
computed_casilla_ids = []
reconcile_when_present_casilla_ids = [
    "05",
]
tolerance = "0.01"
rounding = "money-2"
min_coverage = "0"
discrepancy_causes = ["extraction_unreliable", "unmodelled_rule", "rounding", "correctness_divergence"]
legal_refs = ["ley-35-2006:art-99", "orden-hap-1608-2014:art-1"]
source_refs = ["aeat-modelo-187-procedure", "boe-modelo-187-form-layout"]
