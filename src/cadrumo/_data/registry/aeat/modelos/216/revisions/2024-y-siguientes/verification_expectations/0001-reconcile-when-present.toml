# Modelo 216 reconcile-when-present verification: the computed totals (10, 13, 21)
# are value-reconciled against the engine WHEN the filing prints them, but are
# EXCLUDED from the coverage denominator (min_coverage = 0), so enrollment can
# never lower coverage or flip a legitimate filing's verdict. Grounded in the
# casillas' own declared legal_refs/source_refs.
[[revisions."2024-y-siguientes".verification_expectations]]
id = "modelo-216-2024-y-siguientes-reconcile-when-present"
computed_casilla_ids = []
reconcile_when_present_casilla_ids = [
    "10",
    "13",
    "21",
]
tolerance = "0.01"
rounding = "money-2"
min_coverage = "0"
discrepancy_causes = ["extraction_unreliable", "unmodelled_rule", "rounding", "correctness_divergence"]
legal_refs = ["trlirnr-rdleg-5-2004:art-24", "orden-eha-3290-2008:art-1"]
source_refs = ["aeat-modelo-216-procedure", "boe-modelo-216-form-layout"]
