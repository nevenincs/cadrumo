# Under-declaration advisory (no-silent-under-declaration). The design's own
# [22] total cuota devengada sums the eight cuota casillas; a declared
# positive base in the principal general-regime block against a zero total
# cuota is an arithmetic inconsistency the operator should see before filing.
# ADVISORY, not BLOCKING: implies_nonzero holds trivially when the
# antecedent is at or below zero.
[[revisions."2023-y-siguientes".verification_predicates]]
predicate_id = "modelo-309-cuota-devengada-cuando-base-declarada"
expression = 'implies_nonzero(["decl.rg-base-01", "decl.cuota-devengada-22"])'
finding_kind = "ADVISORY"
legal_refs = ["ley-37-1992:art-161", "orden-hac-3625-2003:apartado-1", "orden-hac-3625-2003:apartado-3"]
