# Calculation-completeness manifest for revision 2024: the casillas
# in this revision's calculation closure, which here is exactly the two header
# facts the verification expectation reconciles. The revision declares no
# formulas, so the closure is not a chain of derivations -- it is the set the
# verification surface touches, and nothing else enters it.
#
# Sibling modelo 720 carries the same manifest over the same two header facts.

[revisions."2024".completeness_manifest]
source_ref = "aeat-modelo-721-procedure"
legal_refs = [
    "ley-58-2003:da-18",
    "rd-1065-2007:art-42-quater",
    "orden-hfp-886-2023:art-1",
]
source_refs = ["aeat-modelo-721-procedure"]

[[revisions."2024".completeness_manifest.casillas]]
casilla_id = "ejercicio"
number = "ejercicio"

[[revisions."2024".completeness_manifest.casillas]]
casilla_id = "tipo-declaracion"
number = "tipo-declaracion"

