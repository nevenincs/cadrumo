# AEAT Modelo 130 instructions, casilla 15: same-ejercicio prior-quarter
# negative-result carry-forward under the RD 439/2007 art. 110 pago
# fraccionado framework. The selector declares
# `source_period_offset_from_target = -1` (prior period) with
# `max_year_delta = 0` (no cross-ejercicio reach): 2T pulls from
# 1T, 3T from 2T, 4T from 3T; 1T produces no anchor (absent-by-
# design) because AEAT's rule restricts carry-forward to prior
# trimestres within the same ejercicio. Casilla 15 at 1T is
# materialised as Decimal("0") through the absent-by-design path
# with provenance marker on the resulting CasillaObservation.
[[revisions."2019-y-siguientes".bindings]]
id = "modelo-130-resultados-negativos-anteriores"
provider = { kind = "previous_filing", source_modelo = "130", source_casilla_id = "saldo-negativo-fin-periodo", temporal = { kind = "target_period_offset", periods = -1, within_filing_year = true } }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "copy" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = [
    "importe (sin signo) de los resultados negativos",
    "cualquiera de las autoliquidaciones anteriores",
    "no hubieran sido deducidos anteriormente",
    "en ningún caso podrá figurar en la casilla 15 un importe superior",
]

# AEAT Modelo 130 instrucciones, casilla 05 ("Pagos fraccionados
# anteriores"): the same-ejercicio carry of prior-quarter payments.
# Verbatim rule: "la suma de las cantidades positivas consignadas en la
# casilla 07 ... correspondientes a los trimestres anteriores del mismo
# ejercicio, minorada en el importe de la suma de las cantidades
# consignadas en la casilla 16 ... No se computaran las cantidades
# negativas consignadas en la casilla 07". The selector enumerates every
# same-ejercicio quarter strictly preceding the target
# (prior_quarter_expanding_span; 2T->{1T}, 3T->{1T,2T}, 4T->{1T,2T,3T},
# 1T->empty/absent-by-design), bounded by max_year_delta = 0. The
# prior_pagos_fraccionados aggregation computes
# SUM max(0, casilla 07_q) - SUM casilla 16_q over those quarters: the
# per-quarter positive-part (a negative prior 07 contributes 0) and any
# observed casilla-16 minoracion. The registry presence policy requires 07;
# an absent 16 is treated as zero and surfaced by the application advisory.
# Casilla 07 = 04 - 05 - 06 reads the populated casilla 05 unchanged.
[[revisions."2019-y-siguientes".bindings]]
id = "modelo-130-pagos-fraccionados-anteriores"
provider = { kind = "previous_filing", source_modelo = "130", source_casilla_ids = ["07", "16"], required_source_casilla_ids = ["07"], temporal = { kind = "prior_quarter_expanding_span" } }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "prior_pagos_fraccionados" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = [
    "suma de las cantidades positivas consignadas en la casilla 07",
    "minorada en el importe de la suma de las cantidades consignadas en la casilla 16",
    "No se computaran las cantidades negativas consignadas en la casilla 07",
]
[[revisions."2019-y-siguientes".bindings]]
id = "irpf.previous_year_economic_activity_net_income"
provider = { kind = "previous_filing", source_modelo = "100", temporal = { kind = "filing_year_offset", years = -1, source_periods = ["0A"] }, source_casilla_ids = ["0224", "1479", "1553", "1577"], required_source_casilla_ids = [] }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "sum" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95"]
source_refs = ["aeat-dr-130-2019-v12", "aeat-modelo-130-instructions"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = ["Casilla 13.", "casillas 0224, 1479, 1553 y/o 1577"]
# Casilla 01 — "la totalidad de los ingresos íntegros fiscalmente
# computables" (AEAT M130 instructions). IVA repercutido is collected on
# behalf of Hacienda and is NOT fiscally computable income, so a
# transaction that carries an explicit IVA tagging contributes its
# IVA-exclusive base imponible (taxable_base); only an untagged
# transaction (no declared base) falls back to its gross transfer
# amount, the sole available measure. The per-observation fallback is
# the `ingresos_integros_sum` fact.
[[revisions."2019-y-siguientes".bindings]]
id = "modelo-130-actividad-economica-ingresos-cumulative"
provider = { kind = "ledger_renta_income_aggregation", modelo = "130", target_casilla_id = "01", fact = "ingresos_integros_sum" }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "sum" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95", "ley-35-2006:art-27", "ley-35-2006:art-28"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = ["Ingresos íntegros", "Ingresos"]

# Alternative income binding that uses the IVA-exclusive taxable_base
# (base imponible) from tagged invoices rather than the gross amount.
# Activated when transactions carry an explicit taxable_base field.
# Feeds the same casilla 01 as the ingresos_integros_sum binding above;
# the operator selects one binding or the other via the fact selector.
# A row carrying no declared taxable_base contributes nothing here: this
# fact sums DECLARED bases, and inventing one from cash would fabricate a
# legal figure.
[[revisions."2019-y-siguientes".bindings]]
id = "modelo-130-actividad-economica-ingresos-taxable-base-cumulative"
provider = { kind = "ledger_renta_income_aggregation", modelo = "130", target_casilla_id = "01", fact = "taxable_base_sum" }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "sum" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95", "ley-35-2006:art-27", "ley-35-2006:art-28"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = ["Ingresos íntegros", "Ingresos"]

# RD 439/2007 art. 110.2 — rendimiento neto de actividades económicas en
# estimación directa. Sums the IVA-exclusive base imponible (taxable_base)
# across actividad-económica income observations within the cumulative YTD
# window. Feeds casilla 03 directly from the ledger when transactions carry
# an explicit taxable_base; observations without taxable_base contribute 0.
[[revisions."2019-y-siguientes".bindings]]
id = "modelo-130-actividad-economica-rendimiento-neto-cumulative"
provider = { kind = "ledger_renta_income_aggregation", modelo = "130", target_casilla_id = "01", fact = "taxable_base_sum" }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "sum" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95", "ley-35-2006:art-28", "ley-35-2006:art-30"]
source_refs = ["aeat-dr-130-2019-v12", "aeat-modelo-130-instructions"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = ["Rendimiento neto", "Casilla 03"]

# NOTE ON `target_casilla_id = "01"` BELOW — read this before "fixing" it to "06".
# This field is doing double duty and reads as a contradiction against the
# `required_text` two lines down (which cites "Casilla 06"). It is not a typo.
# `target_casilla_id` here is the OBSERVATION-MATCH KEY, not the output casilla:
# `_renta_income_build_matcher` (domain/calculations/registry/_ledger_bindings.py)
# selects observations by `observation.target_casilla_id == selector.target_casilla_id`,
# and every RentaIncomeObservation is constructed with target_casilla_id="01" (the
# income casilla) regardless of which fact a binding reads off it. Retargeting this
# selector to "06" does not redirect the aggregate to casilla 06 -- it empties the
# match entirely (no observation is ever built with target_casilla_id="06"),
# silently resolving this binding to 0. Measured directly: retargeting to "06"
# also fails registry-build validation outright, since `_RENTA_130_INCOME_CASILLAS`
# (the allowed-target set for this whole binding family) is {"01", "03"} and does
# not include "06".
#
# The actual output casilla (06, per the required_text below) is set separately, in
# application code: `_m130_retenciones_backend_inputs` in
# application/aggregation/_modelo_bindings.py reads this binding's resolved value by
# id and writes it to casilla 06 directly, bypassing the registry's normal
# casilla-to-binding wiring. That function now also asserts casilla 06 is a real
# casilla on the M130 revision before writing to it (a snapshot cross-check, T-05
# pattern) rather than trusting the hardcoded constant blindly. A schema field
# expressing "match on X's observations, output to Y's casilla" honestly was tried
# and reverted: it reopens the cross-domain routing-table design T-05 governs, and
# that needs a superseding ADR, not an implementation-choice workaround.
[[revisions."2019-y-siguientes".bindings]]
id = "modelo-130-actividad-economica-retenciones-cumulative"
provider = { kind = "ledger_renta_income_aggregation", modelo = "130", target_casilla_id = "01", fact = "withheld_amount_sum" }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "sum" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = ["Casilla 06.", "Suma de las retenciones e ingresos a cuenta soportados"]
# Casilla 02 — "Gastos" (AEAT M130 instructions): the deductible business
# expenses (gastos fiscalmente deducibles) accumulated from the start of the
# fiscal year through the end of the declared quarter, the expense side of the
# estimación directa rendimiento neto (ingresos - gastos) per RD 439/2007
# art. 110.2. IVA soportado is recovered through Modelo 303 and is NOT a Renta
# gasto, so each transaction contributes its IVA-exclusive base imponible
# (taxable_base); an untagged transaction is surfaced (MISSING_TAXABLE_BASE)
# rather than gross-folded. Resolved by the ledger_renta_gastos_pago_fraccionado_aggregation
# source — the OUTGOING sibling of modelo-130-actividad-economica-ingresos-
# cumulative — over the same cumulative year-to-date quarterly window.
[[revisions."2019-y-siguientes".bindings]]
id = "modelo-130-actividad-economica-gastos-cumulative"
provider = { kind = "ledger_renta_gastos_pago_fraccionado_aggregation", modelo = "130", target_casilla_id = "02", fact = "deductible_amount_sum" }
value = { data_type = "money", channel = "decimal" }
aggregation = { op = "sum" }
legal_refs = ["rd-439-2007:art-110", "orden-eha-672-2007:art-1", "ley-35-2006:art-99", "rd-439-2007:art-95", "ley-35-2006:art-28", "ley-35-2006:art-30"]
source_refs = ["aeat-dr-130-2019-v12", "aeat-modelo-130-instructions"]

[[revisions."2019-y-siguientes".bindings.source_citations]]
source_ref = "aeat-modelo-130-instructions"
required_text = ["Gastos"]
