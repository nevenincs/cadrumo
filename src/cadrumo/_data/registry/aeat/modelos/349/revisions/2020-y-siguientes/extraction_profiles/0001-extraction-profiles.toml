[[revisions."2020-y-siguientes".extraction_profiles]]
id = "modelo-349-declaracion-pdf"
surface = "declaracion_pdf"
artefact_kind = "declaration_pdf"
accepted_artefact_kinds = ["declaration_pdf"]
parser = "cadrumo.adapters.inbound.declaracion.parse_declaracion"
target_casillas = [
    {casilla_id = "decl.numero-operadores", match_strategy = "named_label", value_kind = "amount", label_pattern = 'N[uú]mero\s+total\s+de\s+operadores\s+intracomunitarios(?!\s+con\s+rectificaciones)'},
    {casilla_id = "decl.importe-operaciones", match_strategy = "named_label", value_kind = "amount", label_pattern = 'Importe\s+de\s+las\s+operaciones\s+intracomunitarias'},
    {casilla_id = "decl.numero-rectificaciones", match_strategy = "named_label", value_kind = "amount", label_pattern = 'N[uú]mero\s+total\s+de\s+operadores\s+intracomunitarios\s+con\s+rectificaciones'},
    {casilla_id = "decl.importe-rectificaciones", match_strategy = "named_label", value_kind = "amount", label_pattern = 'Importe\s+de\s+las\s+rectificaciones'},
]
# Grounded against AEAT-published instructions PDF (instr_mod_349.pdf, pages 8-9).
# AEAT text (verbatim): "Casilla 01 Número total de operadores intracomunitarios.",
# "Casilla 02 Importe de las operaciones intracomunitarias.",
# "Casilla 03 Número total de operadores intracomunitarios con rectificaciones.",
# "Casilla 04 Importe de las rectificaciones."
# All four patterns match exactly once in the instructions document with no false positives.
confidence = "strict"
corpus_round_trip_verified = true
verification_source = "synthetic_from_aeat_published_text"
min_coverage = "0.5"
failure_semantics = "fail_hard"
legal_refs = ["orden-eha-769-2010:art-1", "orden-hac-174-2020:art-1", "ley-58-2003:art-93"]
source_refs = ["aeat-dr-349-2020-current", "aeat-modelo-349-procedure"]

[[revisions."2020-y-siguientes".extraction_profiles]]
id = "modelo-349-submitted-file"
surface = "export_record"
artefact_kind = "submitted_file"
accepted_artefact_kinds = ["submitted_file"]
parser = "cadrumo.domain.calculations.registry.parse_export_payload"
target_casillas = [
    {casilla_id = "decl.numero-operadores", match_strategy = "named_label", value_kind = "amount", label_pattern = 'N[uú]mero\s+total\s+de\s+operadores\s+intracomunitarios(?!\s+con\s+rectificaciones)'},
    {casilla_id = "decl.importe-operaciones", match_strategy = "named_label", value_kind = "amount", label_pattern = 'Importe\s+de\s+las\s+operaciones\s+intracomunitarias'},
    {casilla_id = "decl.numero-rectificaciones", match_strategy = "named_label", value_kind = "amount", label_pattern = 'N[uú]mero\s+total\s+de\s+operadores\s+intracomunitarios\s+con\s+rectificaciones'},
    {casilla_id = "decl.importe-rectificaciones", match_strategy = "named_label", value_kind = "amount", label_pattern = 'Importe\s+de\s+las\s+rectificaciones'},
    # Declarante identification and obligation fields. This profile parses the
    # SUBMITTED FIXED-WIDTH RECORD, so every declarante-section casilla is
    # recoverable from it and the profile is required to target them all; these
    # five were authored after the target list was written. They are located by
    # their own record positions rather than by a printed label, which is what
    # `numeric_casilla` means -- a label_pattern would be a PDF idiom borrowed
    # into a record surface that prints no labels.
    {casilla_id = "decl.numero-identificativo", match_strategy = "numeric_casilla", value_kind = "text"},
    {casilla_id = "decl.numero-identificativo-anterior", match_strategy = "numeric_casilla", value_kind = "text"},
    {casilla_id = "decl.complementaria-sustitutiva", match_strategy = "numeric_casilla", value_kind = "text"},
    {casilla_id = "decl.cambio-periodicidad", match_strategy = "numeric_casilla", value_kind = "text"},
    {casilla_id = "decl.nif-representante-legal", match_strategy = "numeric_casilla", value_kind = "text"},
]
confidence = "strict"
min_coverage = "1.0"
failure_semantics = "fail_hard"
legal_refs = ["orden-eha-769-2010:art-1", "orden-hac-174-2020:art-1", "ley-58-2003:art-93"]
source_refs = ["aeat-dr-349-2020-current"]

# --- Filing schedules (profile-driven cadence) ---

