# Modelo 592 record-design parity anchor. KNOWN GAP, permanent, not a
# worklist item (2026-08-15): enrolled-modelo-592-layout was retiered (it was
# never real layout content, any era) and AEAT's live Diseños de Registro
# index carries no Modelo 592 title anywhere (checked all page families,
# current and historical) -- no design has ever been published for this
# modelo, so no honest redirect target exists. This reference correctly
# fails registry validation until that changes.

[[revisions."2022-y-siguientes".workbook_parity_refs]]
id = "modelo-592-orden-static-layout"
workbook_source = "enrolled-modelo-592-layout"
fixture_id = "modelo-592-orden-hfp-1314-2022-layout"
formula_coverage = "static_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["orden-hfp-1314-2022:art-1", "ley-58-2003:art-93"]
source_refs = ["enrolled-modelo-592-layout"]
