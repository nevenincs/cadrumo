[[revisions."2007-y-siguientes".workbook_parity_refs]]
id = "modelo-182-orden-static-layout"
workbook_source = "enrolled-modelo-182-layout"
fixture_id = "modelo-182-orden-eha-3021-2007-layout"
formula_coverage = "static_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["orden-eha-3021-2007:art-1", "ley-58-2003:art-93"]
source_refs = ["enrolled-modelo-182-layout"]

# PROVISIONAL: the authoritative AEAT Modelo 182 Diseño de Registros
# (fichero-BOE fixed-width record layout) is NOT bundled in this corpus.
# Only the BOE-published Orden EHA/3021/2007 (approving the form and its
# January filing deadline) is bundled. This workbook_parity_ref exists to
# satisfy the per-revision parity-coverage gate honestly against that
# bundled source; it does NOT assert a verified fixed-width byte-position
# layout. The fichero-BOE export layout and its roundtrip test are blocked
# on enrolling the real AEAT DR PDF into the corpus.
