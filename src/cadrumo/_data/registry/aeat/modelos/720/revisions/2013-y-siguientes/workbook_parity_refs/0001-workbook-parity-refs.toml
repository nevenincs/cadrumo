[[revisions."2013-y-siguientes".workbook_parity_refs]]
id = "modelo-720-dr"
workbook_source = "aeat-dr-720"
fixture_id = "modelo-720-record-design-layout"
formula_coverage = "record_design_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["orden-hap-72-2013:art-1", "ley-58-2003:art-93"]
source_refs = ["aeat-dr-720"]

# Extraction note: the former M720 provisional declaración profile has been
# resolved in extraction_profiles/0001-extraction_profiles.toml. Its remaining
# decl.ejercicio target is synthetic-corpus round-trip verified against the
# AEAT-published record-design wording plus Orden HAP/72/2013 art. 7 language.
# This workbook parity reference remains layout-only: it proves the AEAT record
# design artefact is bundled and linked, not that an executable calculation
# workbook exists. M720 is informative-only, so no arithmetic runner is required.
