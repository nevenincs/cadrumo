# Redirected 2026-08-15 to the real bundled Diseño de Registro
# (aeat-dr-151-2015, era-matching this revision's 2015-2022 span) after the
# prior workbook_source (boe-modelo-151-layout, a 915-byte orden excerpt)
# was found to carry no layout content. This was checked as a candidate
# harvest gap before accepting a permanent gap: AEAT's historical index does
# publish a 2015-2022 Modelo 151 design (Orden HAP/2783/2015), it had simply
# never been bundled -- so the fix is a genuine re-point, not an
# unsatisfiable-condition writeoff.

[[revisions."2015-y-siguientes".workbook_parity_refs]]
id = "modelo-151-cuota-escala"
workbook_source = "aeat-dr-151-2015"
fixture_id = "modelo-151-cuota-integra-general-escala"
formula_coverage = "static_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["ley-35-2006:art-93"]
source_refs = ["aeat-dr-151-2015"]

# Cuota-íntegra escala parity (art. 93.2.e.1º): the cuota = lookup_bracket over
# the 24/47 two-tramo table. The enrolling E2E asserts the cuota against a
# worked example whose expected value is derived from the BOE escala arithmetic
# (600.000 × 0,24 + exceso × 0,47), not by re-running the registry formula.
# runner_required = false: the parity is asserted by the enrollment test, not a
# separate workbook runner.
