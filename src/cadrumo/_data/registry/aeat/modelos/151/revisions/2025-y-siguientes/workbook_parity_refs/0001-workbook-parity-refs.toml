[[revisions."2025-y-siguientes".workbook_parity_refs]]
id = "modelo-151-cuota-escala"
workbook_source = "boe-modelo-151-layout"
fixture_id = "modelo-151-cuota-integra-general-escala"
formula_coverage = "static_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["ley-35-2006:art-93"]
source_refs = ["boe-modelo-151-layout"]

# Cuota-íntegra escala parity: the general cuota = lookup_bracket over the 24/47
# two-tramo table (art. 93.2.e.1º) and the savings cuota = lookup_bracket over
# the 19/21/23/27/30 escala del ahorro (art. 93.2.e.2º). The enrolling E2E and
# the grounded savings-escala test assert the cuotas against worked examples
# whose expected values are derived from the BOE escala arithmetic, not by
# re-running the registry formula. runner_required = false: the parity is
# asserted by those tests, not a separate workbook runner.
