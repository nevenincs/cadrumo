# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-128"
domain = "modelo"
lifecycle = "draft"
domain_refs = ["modelo:128"]
created_at = 2026-07-30
updated_at = 2026-07-30

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 128, retencions i ingressos a compte sobre rendes de capitalització i assegurances de vida o invalidesa"

[language.en]
short_description = "Modelo 128, withholding and advance payments on capitalization and life or disability insurance income"

[language.hu]
short_description = "128-as modell: tőkésítési, élet- és rokkantsági biztosítási jövedelmek adólevonása és adóelőlege"

