# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-233"
domain = "modelo"
lifecycle = "draft"
domain_refs = ["modelo:233"]
created_at = 2026-07-30
updated_at = 2026-07-30

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 233, declaració informativa de despeses de custòdia en llars d'infants autoritzades"

[language.en]
short_description = "Modelo 233, information return on childcare costs at authorized nurseries and centres"

[language.hu]
short_description = "233-as modell: engedélyezett bölcsődékben és gyermekközpontokban felmerült gyermekfelügyeleti költségek tájékoztató bevallása"

