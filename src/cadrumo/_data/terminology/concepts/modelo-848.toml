# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-848"
domain = "modelo"
lifecycle = "draft"
domain_refs = ["modelo:848"]
created_at = 2026-07-30
updated_at = 2026-07-30

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 848, comunicació de l'import net de la xifra de negocis per a l'impost sobre activitats econòmiques"

[language.en]
short_description = "Modelo 848, communication of net turnover for the tax on economic activities"

[language.hu]
short_description = "848-as modell: a nettó árbevétel közlése a gazdasági tevékenységek adójához"

