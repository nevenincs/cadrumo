# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "gancho-preprocesado"
domain = "concepto"
lifecycle = "deprecated"
broader = ["manual-terminologia"]
related = ["barrido-rag"]
created_at = 2026-06-12
updated_at = 2026-07-22

[language.es]
short_description = "El contrato upstream para transformar fuentes PDF, XLS, HTML u otras antes de indexarlas."
definition = "Interfaz generica solicitada al servicio RAG de desarrollo para registrar preprocessors por patron de fichero, emitir una salida versionada con texto extraido o unidades pre-chunked, incluir identidad del preprocessor en la clave de cache y declarar fallos de forma explicita. Este contrato permite que los corpus BOE y AEAT sean indexables sin codificar conocimiento AEAT dentro del indexador."
scope_note = "Mientras el gancho upstream no exista, este proyecto usa sidecars de extraccion como camino interino."

[[language.es.term]]
label = "gancho de preprocesado"
term_status = "preferred"
part_of_speech = "noun"

[[language.es.term]]
label = "preprocess hook"
term_status = "admitted"
part_of_speech = "noun"

[language.en]
short_description = "The upstream contract for transforming PDF, XLS, HTML, or other sources before indexing."

[[language.en.term]]
label = "preprocess hook"
term_status = "preferred"
part_of_speech = "noun"

[language.ca]
short_description = "El contracte upstream per transformar fonts PDF, XLS, HTML o altres abans d'indexar-les."

[language.hu]
short_description = "Az upstream szerződés PDF, XLS, HTML és más források indexelés előtti átalakítása."
