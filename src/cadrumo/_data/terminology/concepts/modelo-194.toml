# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-194"
domain = "modelo"
lifecycle = "draft"
domain_refs = ["modelo:194"]
created_at = 2026-07-30
updated_at = 2026-07-30

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 194, declaració informativa anual de retencions sobre capital mobiliari i actius de capital prestat"

[language.en]
short_description = "Modelo 194, annual information return on withholding from movable capital and borrowed-capital assets"

[language.hu]
short_description = "194-es modell: ingó tőke és kölcsöntőke-eszközök adólevonásának éves tájékoztató bevallása"

