# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-216"
domain = "modelo"
lifecycle = "draft"
domain_refs = ["modelo:216"]
created_at = 2026-07-30
updated_at = 2026-07-30

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 216, autoliquidació de retencions de l'impost sobre la renda de no residents sense establiment permanent"

[language.en]
short_description = "Modelo 216, return for withholding under non-resident income tax without a permanent establishment"

[language.hu]
short_description = "216-os modell: a telephellyel nem rendelkező nem rezidensek jövedelemadójának adólevonási bevallása"

