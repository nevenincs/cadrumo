# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-038"
domain = "modelo"
lifecycle = "draft"
domain_refs = ["modelo:038"]
created_at = 2026-07-30
updated_at = 2026-07-30

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 038, declaració informativa d'operacions realitzades per entitats inscrites en registres públics"

[language.en]
short_description = "Modelo 038, information return on operations carried out by entities registered in public registers"

[language.hu]
short_description = "038-as modell: a nyilvános nyilvántartásokban bejegyzett szervezetek műveleteinek tájékoztató bevallása"

