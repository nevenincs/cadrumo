# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-308"
domain = "modelo"
lifecycle = "draft"
domain_refs = ["modelo:308"]
created_at = 2026-06-10
updated_at = 2026-06-10

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 308, sol·licitud de devolució de l'IVA per recàrrec d'equivalència o subjectes ocasionals"

[language.en]
short_description = "Modelo 308, VAT refund request for equivalence-surcharge taxpayers or occasional taxable persons"

[language.hu]
short_description = "308-as modell: áfa-visszatérítési kérelem egyenértékűségi feláras vagy alkalmi adóalanyok számára"

