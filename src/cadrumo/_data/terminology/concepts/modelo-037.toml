# Terminology Handbook concept fragment.
# Compiled by the strict loader in `dev.docs.terminology_handbook` into a frozen
# `ConceptRecord`. Curated prose fields (definition, scope_note,
# short_description) are hand-edited; structural operations go
# through the `dev.docs.terminology_handbook` CLI. `narrower` is derived at load
# from `broader` and must never be authored here.

[concept]
concept_id = "modelo-037"
domain = "modelo"
lifecycle = "retired"
domain_refs = ["modelo:037"]
replaced_by = "modelo-036"
created_at = 2026-06-10
updated_at = 2026-07-30

[language.es]
short_description = "(sin curar) draft pendiente de definicion"

[language.ca]
short_description = "Model 037, declaració censal simplificada d'alta, modificació i baixa, avui substituïda pel model 036"

[language.en]
short_description = "Modelo 037, simplified census registration, amendment, and deregistration return, now superseded by Modelo 036"

[language.hu]
short_description = "037-es modell: egyszerűsített bejelentés a cenzusba való bejelentkezésről, módosításról és kijelentkezésről, amelyet ma a 036-os modell vált fel"

