# Golden scenario: rectificar declaración (correct a filed return, WHEN layer).
#
# Asserts the amendment itinerary's own driven surface: confirm the active
# profile, then build the amendment over the reconciled filing record with the
# work-amend verb. Verification/export of the amended envelope are covered by
# the owning skills' scenarios. The runner additionally asserts the skill
# playbook cites each verb, the resolved 303 revision's casillas carry registry
# legal grounding, and the revision declares an AEAT-grounded verification
# contract containing `expected_computed_casillas` (the amendment corrects
# casilla values on the same grounded revision the original filing used).

name = "rectificar-declaracion-amendment"
modelo = "303"
filing_year = 2024
period = "1T"
skill_name = "cadrumo-rectificar-declaracion"
provenance_required = true
expected_computed_casillas = ["64", "66"]

expected_trajectory = [
  "overview.status",
  "modelo.work.amend",
]
