# Golden scenario: prepare Modelo 303 (IVA autoliquidación).
#
# Expands the operator golden eval to a second modelo lifecycle. Same assertions
# as the modelo-130 scenario: the trajectory resolves against the live CLI
# surface, follows the create->calculate->verify->export lifecycle, is consistent
# with the shipped `cadrumo-preparar-modelo-303` skill, the resolved revision's casillas
# carry registry legal grounding, and the revision declares an AEAT-grounded
# verification contract containing this scenario's `expected_computed_casillas`.
# No numeric casilla value is asserted (the figure-level oracle is a tracked
# follow-up); `expected_computed_casillas` are
# casilla IDS from the registry's AEAT-grounded computed set.

name = "modelo-303-iva-autoliquidacion"
modelo = "303"
filing_year = 2024
period = "1T"
skill_name = "cadrumo-preparar-modelo-303"
provenance_required = true
# Result-bearing IVA casillas from the revision's AEAT-grounded computed set.
expected_computed_casillas = ["64", "66"]

expected_trajectory = [
  "modelo.describe",
  "modelo.casillas",
  "modelo.work.create",
  "modelo.work.calculate",
  "modelo.work.revision",
  "modelo.work.verify",
  "modelo.export",
  "modelo.reconcile.pull",
]
