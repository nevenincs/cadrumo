# Modelo 220 record-design parity anchor. Declares the official layout authority
# (the BOE Orden HAC/657/2025 form spec covering the IS declaration models,
# including Modelo 220) as the parity source. This scheduling/applicability-grade
# revision carries no calc formulas, so runner_required is false: the anchor pins
# the official layout provenance without asserting a numeric parity run.

[[revisions."2024-y-siguientes".workbook_parity_refs]]
id = "modelo-220-record-design"
workbook_source = "boe-modelo-200-2025-form"
fixture_id = "modelo-220-record-design-layout"
formula_coverage = "record_design_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["orden-hac-657-2025:art-3"]
source_refs = ["boe-modelo-200-2025-form"]
