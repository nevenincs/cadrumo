# Modelo 038 record-design parity anchor. Declares the bundled orden-hac-66-2002
# form-approval order as the layout authority. This scheduling/applicability-grade
# revision carries no calc formulas, so runner_required is false: the anchor pins
# the official form-approval provenance without asserting a verified fixed-width
# byte-position record design (no authoritative Modelo 038 DR is bundled).

[[revisions."2002-y-siguientes".workbook_parity_refs]]
id = "modelo-038-orden-static-layout"
workbook_source = "enrolled-modelo-038-layout"
fixture_id = "modelo-038-orden-hac-66-2002-layout"
formula_coverage = "static_layout"
runner_required = false
tolerance = "0.00"
legal_refs = ["orden-hac-66-2002:art-1", "ley-58-2003:art-93"]
source_refs = ["enrolled-modelo-038-layout"]
