# scratch/

Canonical ephemeral-output directory. Nothing here is ever committed
(see .gitignore). Use it for PM triage dumps, one-off parsers, gh-issue
exports, REPL scripts, and any other short-lived artifact.

Authored artifacts that deserve history belong under `.vault/` via the
vaultspec pipeline (research → adr → plan → exec → audit), not here.
