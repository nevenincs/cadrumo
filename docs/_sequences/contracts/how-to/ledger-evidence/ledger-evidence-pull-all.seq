@step Fetch every PDF and image invoice in one Drive folder at once.
@static aeat app ledger evidence pull-all --folder <drive-folder-id-or-url> --note "Q1 supplier invoices"
@blocked external-service Fetches from a Google Drive folder under the drive.file scope; needs OAuth credentials and a live folder.
