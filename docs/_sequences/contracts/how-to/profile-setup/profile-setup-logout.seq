@step Sign out of the active profile.
aeat config logout
@step Confirm that no profile remains active.
@result aeat --format json config profile status
@expect status == "error"
@expect exit_code == 2
