@step Confirm a profile is currently active.
aeat config profile status
@step Close the active session and clear the active-profile selection.
aeat config profile logout
@step Confirm that no profile remains active.
@result aeat --format json config profile status
@expect result.configured == false
@expect exit_code == 0
