@step Confirm a profile is currently active.
aeat config profile status
@step Close the active session while keeping the profile selected.
aeat config logout
@step Confirm that the registered profile remains selected after its session closes.
@result aeat --format json config profile list
@expect result.active_profile == "docs-sequence-sandbox"
@expect exit_code == 0
