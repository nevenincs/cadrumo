@step Compare a modelo's boxes across two filing years.
@static aeat app modelo compare --modelo 100 --year 2024 --year 2025
@blocked unconverted Measured exit 2: creating the prior work units is not enough, each compared year must also be calculated, and a Modelo 100 calculation needs its full profile-binding set supplied.
