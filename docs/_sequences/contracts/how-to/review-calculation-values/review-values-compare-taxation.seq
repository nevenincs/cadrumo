@step Compare filing jointly as a family unit against filing individually.
@static aeat app modelo work compare-taxation --modelo 100 --year 2025 --period 0A
@blocked unconverted Measured exit 1: needs the prior-year negative-base carry binding, and the verb accepts no binding flag, so converting it means building a filed prior-year Modelo 100 chain first.
