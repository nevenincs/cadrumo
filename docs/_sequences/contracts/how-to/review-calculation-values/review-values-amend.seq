@step Amend an already-filed record as a complementaria.
@static aeat app modelo work amend --from-filing-record <filing-record-id> --kind complementaria --reason "corrected value" --set <casilla>=<decimal>
@blocked operator-artifact Measured exit 2: the amend path requires an imported AEAT-attested baseline, refusing a locally-filed record for having no external_evidence. Needs a real justificante the sandbox must not fabricate.
