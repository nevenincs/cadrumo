@step Create a taxpayer profile non-interactively.
@static aeat config profile create me --quiet --entity-type natural_person --tax-id 12345678Z --name "Ana" --surnames "Garcia Lopez" --activity "consultoria" --activity-start-date 2026-01-01
@blocked sandbox-posture Measured exit 2: every page sandbox pre-registers a profile already holding tax id 12345678Z, and one taxpayer may hold only one profile, so the create refuses on the injected identity itself.
