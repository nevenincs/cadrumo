@step Record the phone state you observed on one diagnostic.
@static aeat config auth diagnostics report <diagnostic-id> --phone-state app_prompted_not_accepted
@blocked operator-artifact Needs a real login-failure diagnostic, which only a live AEAT auth page-drive produces; it must not be fabricated as a committed fixture.
