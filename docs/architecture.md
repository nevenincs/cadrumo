# Architecture

The architecture guide lives at [Architecture overview](architecture/index.md).

Read it for the codebase map: the hexagonal layers, the registry authority
pipeline, the modelo lifecycle, persistence boundaries, and the local-first
safety model.
