[importlinter]
root_package = cadrumo
include_external_packages = False
# Exclude TYPE_CHECKING-guarded imports from the import graph so that
# type-annotation-only imports do not trigger layer-direction contracts.
# Runtime behaviour is unaffected; only import-time side effects are analysed.
exclude_type_checking_imports = True

# ── Cross-package domain isolation ───────────────────────────────────────────
# Domain peer-packages must not import each other's private internals.
# The calculations registry was found importing from domain.renta during
# the linkage-design audit (defect F7). It is resolved via Protocol-based
# dependency inversion: the registry validator declares the abstract
# CrossDomainSnapshotCheck Protocol and a register_cross_domain_snapshot_check()
# injection point; the concrete renta first-slice routing check lives in
# cadrumo.domain.renta._first_slice_routing_integrity and registers itself at
# renta package import time. The registry never names renta. The test edge
# below remains a legitimate cross-domain reference for integration tests.

[importlinter:contract:no-renta-in-registry]
name = Calculations registry must not import directly from domain.renta
unmatched_ignore_imports_alerting = error
# Scoped to DIRECT imports: the F7/S21 defects this guards were direct
# couplings; transitive chains through test files (test -> application -> ...)
# are sanctioned roundtrip noise, and the layered contract enforces the full
# ordering comprehensively. Production direct coupling here is zero.
allow_indirect_imports = true
type = forbidden
source_modules =
    cadrumo.domain.calculations
forbidden_modules =
    cadrumo.domain.renta
# Test modules that construct domain objects for registry integration tests
# are permitted to import domain.renta directly - they are not production code.
# The test_referential_integrity edge below is the chain origin of an
# indirect path (test -> application.diagnostics -> ... -> sede._declarations
# -> renta): sede._declarations imports the renta package for its
# cross-domain snapshot-check registration side effect, which is a legitimate
# adapter -> domain edge. Pinning the test-module chain origin keeps the
# contract enforcing genuine production calculations -> renta imports while
# excusing the test path. The same edge is already sanctioned in the
# domain-not-application contract.
ignore_imports =
    # ── W09: re-sanctioned test-file roundtrip/fixture/oracle edges (prior ignores went stale on test rename) ──
    cadrumo.domain.calculations.registry.tests.test_ledger_renta_expense_binding -> cadrumo.domain.renta
    # W09.S90: parity-scenario test consumers trigger the renta first-slice
    # registration directly (relocated out of the production _scenarios harness)

[importlinter:contract:domain-not-application]
name = Domain must not import application
# ── S21: Domain must not import application ───────────────────────────────────
# Production domain modules must not import from the application layer.
# The five violations (deadlines/_profiles, profile/_keys) were resolved in
# Wave 3 P04 S16 and S17: _profiles.py now parses canonical tokens directly;
# _keys.py now uses a register_profile_keys() injection point.
# Test files that import application modules for fixture setup are permitted.
unmatched_ignore_imports_alerting = error
# Scoped to DIRECT imports: the F7/S21 defects this guards were direct
# couplings; transitive chains through test files (test -> application -> ...)
# are sanctioned roundtrip noise, and the layered contract enforces the full
# ordering comprehensively. Production direct coupling here is zero.
allow_indirect_imports = true
type = forbidden
source_modules =
    cadrumo.domain
forbidden_modules =
    cadrumo.application
# Production domain -> application coupling is zero. The former
# domain.contribuyente._keys -> application.wizard._compiler lazy edge was
# eliminated by the DB-17 eager-registration bootstrap (importing
# cadrumo.application.wizard pushes PROFILE_KEYS at import time), and the topic
# catalogue loader now reads core.topics (a core -> core edge), so neither
# crosses this boundary any longer. Only sanctioned test/fixture edges remain,
# pinned individually below so a new non-deferred production edge is still caught.
ignore_imports =
    # ── W09: sanctioned cross-layer test/fixture/oracle edges (roundtrip discipline) ──
    cadrumo.domain.deadlines.conftest -> cadrumo.application.wizard._catalogue
    # ── conftest profile-key eager registration (wizard import side-effect; test scaffolding) ──
    cadrumo.domain.calculations.registry.tests.conftest -> cadrumo.application.wizard
    cadrumo.domain.contribuyente.conftest -> cadrumo.application.wizard
    # ── DB-16: per-domain repositories wrap the secure-storage substrate ──
    # Sanctioned by the secure-persistence-foundation ADR series
    # (2026-04-30-secure-persistence-foundation-adr, Wave-3/4 per-domain
    # repository pattern; e.g. domain.justificante._repository is Phase 4).
    # SecureBoundRepository is SQL/crypto-coupled so its base cannot move to
    # core; the repositories are deliberately co-located with their domain.
    cadrumo.domain.calculations.registry.tests.test_cross_boundary_roundtrip -> cadrumo.application.workflow
    cadrumo.domain.calculations.registry.tests.test_detail_record_modelo_coverage -> cadrumo.application.storage.calc_sheets
    cadrumo.domain.invoices.tests.test_reconciliation -> cadrumo.application.invoices
    cadrumo.domain.modelos.tests.test_work_unit -> cadrumo.application.modelo
    # ── registry/modelo test files pulling application fixtures (test-topology refactor) ──
    # W10: import-centralization refactor moved these test files onto the
    # top-level application.modelo / application.aggregation re-exports
    # (service-imports-via-top-level-reexports), so the prior private-submodule
    # ignores (._semantic_role_resolution, ._renta_income_ledger) went stale.
    cadrumo.domain.calculations.registry.tests.test_modelo_100_registry_roles_capital_gains -> cadrumo.application.modelo
    cadrumo.domain.calculations.registry.tests.test_modelo_100_registry_roles_prevision_social -> cadrumo.application.modelo
    cadrumo.domain.calculations.registry.tests._ledger_iva_aggregation_support -> cadrumo.application.calculations
    cadrumo.domain.calculations.registry.tests.test_binding_coverage_breadth -> cadrumo.application.aggregation
    cadrumo.domain.calculations.registry.tests.test_binding_coverage_breadth -> cadrumo.application.modelo._calculation_actions
    cadrumo.domain.calculations.registry.tests.test_ledger_renta_income_binding -> cadrumo.application.aggregation
    # W04.P08.S47: source-connectivity gate joins the domain registry inventory
    # against the live-mesh disposition taxonomy (application.aggregation public set).
    cadrumo.domain.calculations.registry.tests.test_source_enrollment -> cadrumo.application.aggregation
    cadrumo.domain.calculations.registry.tests.test_referential_integrity_part4 -> cadrumo.application.diagnostics
    cadrumo.domain.modelos.tests.test_work_unit -> cadrumo.application.user_profile

[importlinter:contract:domain-not-adapters]
name = Domain must not import adapters
# ── arch-remediation-ports-inversion (W04.P10.S20): declare the seam exhaustively ──
# The ports-inversion campaign migrated every production domain repository
# behind a Protocol port with its concrete under adapters.persistence.profile,
# so PRODUCTION domain -> adapters coupling is zero. This first-class forbidden
# contract (sibling of domain-not-application) replaces the per-production-edge
# exception list the layered contract carried: a new non-deferred production
# domain -> adapters import now fails loudly here rather than being absorbed as
# a layered-contract ignore. Only sanctioned test-file roundtrip/anti-tautology
# edges — which legitimately construct the concrete adapter to exercise the
# encrypted boundary end-to-end — remain, pinned individually below.
unmatched_ignore_imports_alerting = error
# Scoped to DIRECT imports: production direct coupling here is zero; transitive
# chains through test files are sanctioned roundtrip noise and the layered
# contract enforces the full ordering comprehensively.
allow_indirect_imports = true
type = forbidden
source_modules =
    cadrumo.domain
forbidden_modules =
    cadrumo.adapters
ignore_imports =
    # ── aeat-roundtrip-discipline carve-out (test-file-only) ──────────────
    # Test/conftest modules across the domain layer legitimately construct the
    # concrete persistence adapter to exercise the encrypted storage boundary
    # end-to-end (real adapters, not mocks -- aeat-roundtrip-discipline). Every
    # prior per-file exemption here was a test path; production domain ->
    # adapters coupling is zero (this contract enforces it: the
    # arch-remediation-ports-inversion campaign moved every production domain
    # repository behind a Protocol port). Two wildcards cover a domain test
    # module at any nesting depth (bare `domain.tests.*` and
    # `domain.<pkg...>.tests.*`); a new roundtrip test needs no new exemption
    # line here. See `2026-07-08-importlinter-test-carveout-adr`.
    cadrumo.domain.tests.** -> cadrumo.adapters.**
    cadrumo.domain.**.tests.** -> cadrumo.adapters.**
[importlinter:contract:core-not-outer]
name = Core must not import outer layers
# ── S22: Core must not import outer layers ────────────────────────────────────
# Core is the innermost layer and must not import from domain, application,
# adapters, or entrypoints. The production violations were resolved in
# Wave 3 P04:
# - core/errors/__init__.py: SiteHealthStatus TYPE_CHECKING import replaced
#   with SiteHealthStatusProtocol declared in core.
# - core/errors/_registry.py: entrypoints.cli._i18n import replaced with
#   core.i18n.
# - core/i18n/_render.py: application imports replaced with a registered
#   callback (register_profile_language_resolver).
# Core test files that pull application / domain fixtures for integration tests
# are permitted per project convention.
#
# core.resources._repos deferred loader imports (resource-management-api ADR,
# 2026-05-16): the resource-management-api ADR explicitly places the concrete
# Repository implementations under core/resources/ ("Repository implementations
# reference [domain models] but the Repository class hierarchy itself is
# core-layer infrastructure") AND chose lazy loading as the default (ADR Q5:
# "each Repository's get(key) loads on demand"; "the eager imports disappear").
# Each loader below is a *function-local* import inside a Repository._load /
# _resolve_authority method -- it runs only at first resource access, never at
# module-import time, so core/resources/ stays an acyclic leaf in the module
# graph. The runtime dependency direction is a deliberate, ADR-sanctioned
# consequence of co-locating the single resource boundary in core. Each edge is
# pinned individually (no wildcard) so a NEW non-deferred core->outer import is
# still caught. Test files importing domain errors are permitted per convention.
unmatched_ignore_imports_alerting = error
type = forbidden
source_modules =
    cadrumo.core
forbidden_modules =
    cadrumo.domain
    cadrumo.application
    cadrumo.adapters
    cadrumo.entrypoints
ignore_imports =
    # ── aeat-roundtrip-discipline carve-out (test-file-only) ──────────────
    # Core test/conftest modules pull domain/application/adapters/entrypoints
    # fixtures for integration and cross-layer seam tests (real dependencies,
    # not mocks); a new such test needs no new exemption line here. Production
    # core -> outer-layer coupling stays individually pinned below (the
    # core/resources/_repos deferred-loader block) so a NEW non-deferred
    # production edge is still caught loudly. See
    # `2026-07-08-importlinter-test-carveout-adr`.
    cadrumo.core.tests.** -> cadrumo.domain.**
    cadrumo.core.**.tests.** -> cadrumo.domain.**
    cadrumo.core.tests.** -> cadrumo.application.**
    cadrumo.core.**.tests.** -> cadrumo.application.**
    cadrumo.core.tests.** -> cadrumo.adapters.**
    cadrumo.core.tests.** -> cadrumo.entrypoints.**
    # ── core/resources/_repos deferred loader imports (ADR-sanctioned, see above) ──
    cadrumo.core.resources._repos.apoderamientos -> cadrumo.domain.auth.apoderamientos
    cadrumo.core.resources._repos.category_profiles -> cadrumo.domain.categories
    cadrumo.core.resources._repos.holiday_calendars -> cadrumo.domain.deadlines
    cadrumo.core.resources._repos.legal_parameters -> cadrumo.domain.calculations.registry
    cadrumo.core.resources._repos.manuals -> cadrumo.domain.manuals
    cadrumo.core.resources._repos.modelos -> cadrumo.domain.calculations.registry
    cadrumo.core.resources._repos.iva_catalogues -> cadrumo.domain.iva
    cadrumo.core.resources._repos.iva_rate_tables -> cadrumo.domain.iva
    cadrumo.core.resources._repos.recargo_bands -> cadrumo.domain.deadlines
    cadrumo.core.resources._repos.user_profile -> cadrumo.domain.user_profile
[importlinter:contract:layered]
name = AEAT layered architecture
# ── S23: Full layered architecture contract ───────────────────────────────────
# The broad layered contract enforces hexagonal direction across all layers.
# Layer order (outer → inner): entrypoints > adapters > application > domain > core
#
# Per the restructure ADR (2026-04-30 sec. 538), application/ is permitted to
# import from adapters/ directly (outbound adapter wiring). The exception
# ledger now pins each existing application source module individually;
# full inversion remains deferred, but new source modules fail loudly.
#
# Test files across all layers are permitted to cross layer boundaries for
# fixture setup; these are covered by individual entries below.
#
# Domain repositories that directly import the SQL persistence substrate are
# grandfathered in via individual narrow entries.
unmatched_ignore_imports_alerting = error
type = layers
layers =
    cadrumo.entrypoints
    cadrumo.adapters
    cadrumo.application
    cadrumo.domain
    cadrumo.core
exhaustive = false
ignore_imports =
    # ── aeat-roundtrip-discipline carve-out (test-file-only) ──────────────
    # Test/conftest modules across every layer legitimately cross layer
    # boundaries for fixture setup, oracle replay, and real-adapter
    # roundtrip/anti-tautology suites (real dependencies, not mocks); a new
    # such test needs no new exemption line here. Production cross-layer
    # coupling remains individually pinned below (the application -> adapters
    # ADR sec. 538 allowance, the per-repository ports-inversion edges, and
    # the core/resources/_repos deferred loaders) so a NEW non-deferred
    # production edge still fails loudly. The prorrata-owned production edges
    # (application._revision_persistence, application.calculations.
    # _prorrata_regularizacion, application.aggregation._iva_ledger ->
    # adapters.persistence.profile.prorrata_register / adapters.persistence.
    # storage) were inverted behind ProrrataRegisterRepositoryProtocol and now
    # ride the sanctioned construction-edge pins below (see the prorrata-register
    # ports-inversion block), superseding the deferral recorded in
    # `2026-07-08-importlinter-test-carveout-adr`.
    cadrumo.domain.tests.** -> cadrumo.adapters.**
    cadrumo.domain.**.tests.** -> cadrumo.adapters.**
    cadrumo.domain.**.tests.** -> cadrumo.application.**
    cadrumo.application.tests.** -> cadrumo.adapters.**
    cadrumo.application.**.tests.** -> cadrumo.adapters.**
    cadrumo.application.**.tests.** -> cadrumo.entrypoints.**
    cadrumo.core.tests.** -> cadrumo.domain.**
    cadrumo.core.**.tests.** -> cadrumo.domain.**
    cadrumo.core.tests.** -> cadrumo.application.**
    cadrumo.core.**.tests.** -> cadrumo.application.**
    cadrumo.core.tests.** -> cadrumo.adapters.**
    cadrumo.core.tests.** -> cadrumo.entrypoints.**
    # conftest.py modules sitting directly in a layer subpackage (not nested
    # under a tests/ folder) are the same roundtrip-discipline fixture pattern.
    cadrumo.application.**.conftest -> cadrumo.adapters.**
    cadrumo.domain.**.conftest -> cadrumo.application.**
    # test modules that route through the shared cadrumo.tests.* fixture helpers
    # (cadrumo.tests is a cross-cutting test-support package, not a declared
    # layer) which themselves transitively reach a forbidden outer layer.
    cadrumo.domain.**.tests.** -> cadrumo.tests.cli_runner
    cadrumo.application.tests.** -> cadrumo.tests.cli_runner
    cadrumo.application.**.tests.** -> cadrumo.tests.cli_runner
    cadrumo.domain.**.tests.** -> cadrumo.locales
    # ── D9 follow-up: local-import lifts made these concrete storage edges
    #    import-linter-visible; pin exact targets, not broad source modules. ──
    cadrumo.application.modelo._m145_communication_records -> cadrumo.adapters.persistence.storage
    cadrumo.application.modelo._review_package_recipient_encryption -> cadrumo.adapters.persistence.storage
    cadrumo.application.modelo._review_package_recipient_encryption -> cadrumo.adapters.persistence.storage.crypto
    cadrumo.application.modelo._review_package_recipient_registry -> cadrumo.adapters.persistence.storage
    cadrumo.application.modelo._review_package_recipient_replay_guard -> cadrumo.adapters.persistence.storage
    cadrumo.application.modelo._review_package_signing -> cadrumo.adapters.persistence.storage

    # ── application → adapters (permitted by architecture ADR sec. 538) ──
    # application/ may import adapters/ for outbound wiring; full inversion
    # would require a major refactor and is deferred.
    cadrumo.application.aggregation._modelo_bindings -> cadrumo.adapters.**
    cadrumo.application.aggregation._oss_ioss -> cadrumo.adapters.**
    cadrumo.application.aggregation._percepciones_observations_repository -> cadrumo.adapters.persistence.storage
    cadrumo.application.aggregation._retencion_observations_repository -> cadrumo.adapters.persistence.storage
    # _observation_window carries the shared replace-observations helper extracted from
    # the two observation repos above (P02.S08); it holds the same safe_repository_id
    # path-safety utility import - a plain storage helper with no port to invert.
    cadrumo.application.aggregation._observation_window -> cadrumo.adapters.persistence.storage
    cadrumo.application.aggregation._withholding_source -> cadrumo.adapters.persistence.storage
    cadrumo.application.auth -> cadrumo.adapters.outbound.aeat.auth
    cadrumo.application.auth._apoderado -> cadrumo.adapters.persistence.storage
    cadrumo.application.auth._certificate_secret_backend -> cadrumo.adapters.**
    cadrumo.application.auth._certificate_sources_operator -> cadrumo.adapters.**
    cadrumo.application.auth._diagnostics -> cadrumo.adapters.**
    cadrumo.application.auth._operator -> cadrumo.adapters.**
    cadrumo.application.auth._operator_probes -> cadrumo.adapters.**
    cadrumo.application.auth._operator_scope -> cadrumo.adapters.**
    cadrumo.application.auth._sessions -> cadrumo.adapters.**
    cadrumo.application.bienes_inversion -> cadrumo.adapters.**
    cadrumo.application.bucket_maintenance._manifest_digest -> cadrumo.adapters.**
    cadrumo.application.bucket_maintenance._sandbox -> cadrumo.adapters.**
    cadrumo.application.bucket_maintenance._service -> cadrumo.adapters.**
    cadrumo.application.calculations._iva_compensation_annual_partition -> cadrumo.adapters.**
    cadrumo.application.calculations._iva_compensation_history -> cadrumo.adapters.**
    cadrumo.application.calculations._multi_year -> cadrumo.adapters.**
    cadrumo.application.calculations._observations_repository -> cadrumo.adapters.**
    cadrumo.application.calculations._relation_prefill -> cadrumo.adapters.**
    cadrumo.application.diagnostics -> cadrumo.adapters.**
    cadrumo.application.diagnostics_run_health -> cadrumo.adapters.**
    cadrumo.application.evidence._service -> cadrumo.adapters.**
    cadrumo.application.filing._history_repository -> cadrumo.adapters.**
    cadrumo.application.filing._import -> cadrumo.adapters.**
    cadrumo.application.filing._runtime_repository -> cadrumo.adapters.**
    cadrumo.application.inventory._service -> cadrumo.adapters.**
    cadrumo.application.invoices._source_resolver -> cadrumo.adapters.**
    cadrumo.application.ledger._actions_common -> cadrumo.adapters.**
    cadrumo.application.ledger._actions_import -> cadrumo.adapters.**
    cadrumo.application.ledger._business_operation_invoice -> cadrumo.adapters.**
    cadrumo.application.ledger._evidence -> cadrumo.adapters.**
    cadrumo.application.ledger._evidence_draft -> cadrumo.adapters.**
    cadrumo.application.ledger._evidence_draft_vision -> cadrumo.adapters.**
    cadrumo.application.ledger._evidence_textlayer -> cadrumo.adapters.**
    cadrumo.application.ledger._llm_classification -> cadrumo.adapters.**
    cadrumo.application.ledger._llm_diagnostics -> cadrumo.adapters.**
    # ── arch-remediation-ports-inversion: usage_ratios persistence relocated to
    #    adapters.persistence.profile.usage_ratios; the ledger callers now import
    #    the adapter directly (narrow target keeps the source-module set flat) ──
    cadrumo.application.ledger._preflight -> cadrumo.adapters.persistence.profile.usage_ratios
    cadrumo.application.ledger._ratios -> cadrumo.adapters.persistence.profile.usage_ratios
    # ── arch-remediation-ports-inversion: submission repository relocated to
    #    adapters.persistence.profile.submission; the workflow gate constructs the
    #    concrete and injects it into the SubmissionEngine, tests seed via it ──
    cadrumo.application.modelo._workflow_gate -> cadrumo.adapters.persistence.profile.submission
    # ── arch-remediation-ports-inversion: justificante repository relocated to
    #    adapters.persistence.profile.justificante (no domain port; no domain
    #    caller); application/entrypoint callers import the adapter directly ──
    cadrumo.application.calculations._cross_period_clean_state -> cadrumo.adapters.persistence.profile.justificante
    cadrumo.application.modelo._external_import_actions -> cadrumo.adapters.persistence.profile.justificante
    cadrumo.application.ledger._review_projection -> cadrumo.adapters.**
    cadrumo.application.ledger._rule_repository -> cadrumo.adapters.**
    cadrumo.application.ledger._vision_classifier -> cadrumo.adapters.**
    cadrumo.application.live -> cadrumo.adapters.**
    cadrumo.application.live._borrador_100 -> cadrumo.adapters.**
    cadrumo.application.live._censo -> cadrumo.adapters.**
    cadrumo.application.live._errors -> cadrumo.adapters.**
    cadrumo.application.live._expedientes -> cadrumo.adapters.**
    cadrumo.application.live._filed_data -> cadrumo.adapters.**
    cadrumo.application.live._filed_data_capture -> cadrumo.adapters.**
    cadrumo.application.live._filed_observation_persistence -> cadrumo.adapters.**
    cadrumo.application.live._iva_remote_state -> cadrumo.adapters.**
    cadrumo.application.live._justificante -> cadrumo.adapters.**
    cadrumo.application.live._notifications -> cadrumo.adapters.**
    cadrumo.application.live._session -> cadrumo.adapters.**
    cadrumo.application.live._snapshot_base -> cadrumo.adapters.**
    cadrumo.application.live._verify -> cadrumo.adapters.**
    cadrumo.application.modelo._borrador_binding -> cadrumo.adapters.**
    cadrumo.application.modelo._m036_lifecycle -> cadrumo.adapters.**
    cadrumo.application.modelo._reconcile -> cadrumo.adapters.**
    cadrumo.application.prorrata_register -> cadrumo.adapters.**
    cadrumo.application.registry -> cadrumo.adapters.**
    cadrumo.application.repair_integrity -> cadrumo.adapters.**
    cadrumo.application.state_projection -> cadrumo.adapters.**
    cadrumo.application.storage.calc_sheets._parity_harness -> cadrumo.adapters.**
    cadrumo.application.user_profile._aggregate -> cadrumo.adapters.**
    cadrumo.application.user_profile._bundle -> cadrumo.adapters.**
    cadrumo.application.user_profile._bundle_encryption -> cadrumo.adapters.**
    cadrumo.application.user_profile._capabilities -> cadrumo.adapters.**
    cadrumo.application.user_profile._censo_sync -> cadrumo.adapters.**
    cadrumo.application.user_profile._custody -> cadrumo.adapters.**
    cadrumo.application.user_profile._custody_carry -> cadrumo.adapters.**
    cadrumo.application.user_profile._language_resolver -> cadrumo.adapters.**
    cadrumo.application.user_profile._orchestration -> cadrumo.adapters.**
    cadrumo.application.user_profile._profile_repository -> cadrumo.adapters.**
    cadrumo.application.user_profile._repository -> cadrumo.adapters.**
    cadrumo.application.user_profile._testing -> cadrumo.adapters.**
    cadrumo.application.verification._verify -> cadrumo.adapters.**
    cadrumo.application.workflow._adapters -> cadrumo.adapters.**
    cadrumo.application.workflow._models -> cadrumo.adapters.**
    cadrumo.application.workflow._persistence -> cadrumo.adapters.**
    cadrumo.application.workflow._profile_bucket_scan -> cadrumo.adapters.**
    cadrumo.application.workflow._profile_health -> cadrumo.adapters.**
    # The shared secure-SQL test helper wraps the real persistence adapter for
    # roundtrip-discipline tests; ignoring its adapter edge breaks the layer
    # path for every application/domain test that routes fixtures through it.
    cadrumo.tests.secure_sql -> cadrumo.adapters.**
    # The manual-ledger action support centralises its concrete adapter
    # constructors here so split application tests keep one test-support edge.
    cadrumo.tests.ledger_action_adapters -> cadrumo.adapters.**
    # Review-package roundtrip tests centralise raw storage/sql probes here so
    # application tests do not each own direct adapter pins.
    cadrumo.tests.review_package_adapters -> cadrumo.adapters.**
    # The custody-store recovery matrix centralises its real concrete adapter
    # constructors here so the application test owns one test-support edge
    # instead of eleven direct adapter pins.
    cadrumo.tests.custody_store_matrix_adapters -> cadrumo.adapters.**
    # The shared justificante parse-cache test helper imports the inbound
    # justificante adapter for roundtrip-discipline fixtures; same rationale.
    cadrumo.tests._justificante_parse_cache -> cadrumo.adapters.**
    # ── core/resources/_repos deferred loader imports ──
    # Function-local imports inside Repository._load / _resolve_authority
    # methods. The resource-management-api ADR (2026-05-16) places the
    # concrete Repository implementations in core/resources/ and chose lazy
    # loading (ADR Q5); the imports run only at first resource access, never
    # at module-import time, so core/resources/ stays an acyclic leaf. Each
    # edge is pinned individually so a new non-deferred core->outer import is
    # still caught. See the core-not-outer contract comment above for detail.
    cadrumo.core.resources._repos.apoderamientos -> cadrumo.domain.auth.apoderamientos
    cadrumo.core.resources._repos.category_profiles -> cadrumo.domain.categories
    cadrumo.core.resources._repos.holiday_calendars -> cadrumo.domain.deadlines
    cadrumo.core.resources._repos.legal_parameters -> cadrumo.domain.calculations.registry
    cadrumo.core.resources._repos.manuals -> cadrumo.domain.manuals
    cadrumo.core.resources._repos.modelos -> cadrumo.domain.calculations.registry
    cadrumo.core.resources._repos.iva_catalogues -> cadrumo.domain.iva
    cadrumo.core.resources._repos.iva_rate_tables -> cadrumo.domain.iva
    cadrumo.core.resources._repos.recargo_bands -> cadrumo.domain.deadlines
    cadrumo.core.resources._repos.user_profile -> cadrumo.domain.user_profile
    # ── application production edges: participation-index derived-cache co-write
    #    (module-granularity replaces the pre-refactor deep-submodule edge) ──
    cadrumo.application.ledger._participation_read -> cadrumo.adapters.persistence.profile.participation_index
    cadrumo.application.modelo._revision_persistence -> cadrumo.adapters.persistence.profile.participation_index
    cadrumo.application.modelo._participation_index_rebuild -> cadrumo.adapters.persistence.profile.participation_index
    cadrumo.application.modelo._verification_actions -> cadrumo.adapters.persistence.profile.participation_index
    cadrumo.application.modelo._export -> cadrumo.adapters.persistence.profile.modelos_verification_reports
    cadrumo.application.modelo._filing_actions -> cadrumo.adapters.persistence.profile.modelos_verification_reports
    cadrumo.application.modelo._history -> cadrumo.adapters.persistence.profile.modelos_verification_reports
    cadrumo.application.modelo._verification_actions -> cadrumo.adapters.persistence.profile.modelos_verification_reports
    # ── modelos_filing catalogue repository ports-inversion (W04.P09.S14):
    #    the concrete ModeloRecordCatalogueRepository moved to the persistence
    #    adapter behind ModeloRecordCatalogueRepositoryProtocol ──
    cadrumo.application.modelo._amendment_actions -> cadrumo.adapters.persistence.profile.modelos_filing
    cadrumo.application.modelo._export -> cadrumo.adapters.persistence.profile.modelos_filing
    cadrumo.application.modelo._external_import_actions -> cadrumo.adapters.persistence.profile.modelos_filing
    cadrumo.application.modelo._filing_actions -> cadrumo.adapters.persistence.profile.modelos_filing
    cadrumo.application.modelo._history -> cadrumo.adapters.persistence.profile.modelos_filing
    cadrumo.application.modelo._participation_index_rebuild -> cadrumo.adapters.persistence.profile.modelos_filing
    cadrumo.application.modelo._verification_actions -> cadrumo.adapters.persistence.profile.modelos_filing
    cadrumo.application.filing._complementaria -> cadrumo.adapters.persistence.profile.filing_drafts
    cadrumo.application.review._adapters -> cadrumo.adapters.persistence.profile.filing_drafts
    # ── filing amendment repository ports-inversion (W03.P08.S11):
    #    the concrete ModeloAmendmentRepository moved to the persistence
    #    adapter behind ModeloAmendmentRepositoryProtocol ──
    cadrumo.application.filing._complementaria -> cadrumo.adapters.persistence.profile.filing_amendments
    # ── invoices catalogue repository ports-inversion (W02.P04.S06):
    #    the concrete InvoiceCatalogueRepository moved to the persistence
    #    adapter behind InvoiceCatalogueRepositoryProtocol ──
    cadrumo.application.aggregation._renta_ledger -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.filing._review -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._bulk_import -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._creation -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._importing -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._lifecycle -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._linking -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._queries -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._reconciliation -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.invoices._wizard -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.ledger._actions_lifecycle -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.modelo._calculation_actions -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.review._adapters -> cadrumo.adapters.persistence.profile.invoices
    cadrumo.application.modelo._amendment_actions -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._calculation_actions -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._export -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._external_import_actions -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._filing_actions -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._history -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._iva_wallet_gate -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._iva_wallet_seed -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._participation_index_rebuild -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._selectors -> cadrumo.adapters.persistence.profile.modelos_calculation
    cadrumo.application.modelo._verification_actions -> cadrumo.adapters.persistence.profile.modelos_calculation
    # ── application production edges: modelos_work_units catalogue repository
    #    consolidation (import-centralization refactor; module-granularity) ──
    cadrumo.application.modelo._amendment_actions -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._calculate_input -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._calculation_actions -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._export -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._external_import_actions -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._filing_actions -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._history -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._iva_wallet_gate -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._iva_wallet_seed -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._participation_index_rebuild -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._selectors -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._taxation_comparison -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._verification_actions -> cadrumo.adapters.persistence.profile.modelos_work_units
    cadrumo.application.modelo._work_lifecycle -> cadrumo.adapters.persistence.profile.modelos_work_units
    # ── buckets event-history repository ports-inversion (W01.P03.S05):
    #    the concrete BucketEventHistoryRepository moved to the persistence
    #    adapter behind BucketEventHistoryRepositoryProtocol ──
    cadrumo.application.auth._operator -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.bucket_maintenance._service -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.inventory._service -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.ledger._actions_common -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.ledger._actions_split_merge -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.ledger._business_operation_invoice -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.ledger._evidence -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.ledger._llm_classification -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.ledger._review_projection -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.live._filed_observation_persistence -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.live._justificante -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._amendment_actions -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._calculation_actions -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._export -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._external_import_actions -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._filing_actions -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._history -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._iva_wallet_seed -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._m036_lifecycle -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._m145_communication_records -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._reconcile -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._verification_actions -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.modelo._work_lifecycle -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.user_profile._lifecycle -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.user_profile._orchestration -> cadrumo.adapters.persistence.profile.buckets
    cadrumo.application.workflow._events -> cadrumo.adapters.persistence.profile.buckets
    # ── transactions catalogue repository ports-inversion (W02.P07.S09):
    #    the concrete TransactionCatalogueRepository moved to the persistence
    #    adapter behind TransactionCatalogueRepositoryProtocol ──
    cadrumo.application.aggregation._impatriado_income_ledger -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.aggregation._iva_ledger -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.aggregation._renta_gasto_ledger -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.aggregation._renta_income_ledger -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.aggregation._renta_ledger -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.filing._review -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.invoices._linking -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.invoices._queries -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.invoices._reconciliation -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.ledger._actions_split_merge -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.ledger._preflight -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.modelo._art109_activity_income -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.modelo._calculation_actions -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.modelo._calculation_preparation -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.modelo._m349_ledger_guard -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.modelo._verification_actions -> cadrumo.adapters.persistence.profile.transactions
    cadrumo.application.review._adapters -> cadrumo.adapters.persistence.profile.transactions
    # ── prorrata-register repository ports-inversion (gate-drift-reconciliation
    #    P01): the concrete ProrrataRegisterRepository stays in the persistence
    #    adapter behind ProrrataRegisterRepositoryProtocol; these application
    #    modules retain only the sanctioned default-construction edge (ADR
    #    sec. 538). The storage edge is _prorrata_regularizacion's direct import
    #    of the ClassificationError/DecryptionError/EnvelopeVersionError boundary
    #    exception classes (plain exceptions, no port to invert). ──
    cadrumo.application.aggregation._iva_ledger -> cadrumo.adapters.persistence.profile.prorrata_register
    cadrumo.application.calculations._prorrata_regularizacion -> cadrumo.adapters.persistence.profile.prorrata_register
    cadrumo.application.calculations._prorrata_regularizacion -> cadrumo.adapters.persistence.storage
    cadrumo.application.modelo._revision_persistence -> cadrumo.adapters.persistence.profile.prorrata_register
