# LEDGER-01 shared-file handoff requests

## Shared integration owner lease — 2026-09-22

At source HEAD `9e05324019e8f37287626f377a6fa9dd4cd7dce4` (plus retained uncommitted income and withholding navigation edits), the shared TUI owner grants the **ledger lane** sole writer responsibility for `src/cadrumo/entrypoints/tui/ledger/controller.py`, `ledger/entries.py`, `ledger/routes.py`, `ledger/invoice_entry.py`, `ledger/models.py`, `ledger/tests/test_ledger_ingestion_surfaces.py`, `src/cadrumo/entrypoints/tui/ledger_doors.py`, and any new ledger detail/view files and their tests. Existing uncommitted hunks in `invoice_entry.py`, `models.py`, `test_ledger_ingestion_surfaces.py`, and `ledger_doors.py` must be retained. The shared owner has stopped writing those paths; no process writes source files for the active income installed runs (they read an already-installed wheel). The lease lasts through the ledger detail and linked-edit integration handoff; return ownership explicitly afterward. The ledger lane owns its domain screen/service tests and installed journeys.

For `src/cadrumo/entrypoints/tui/launcher.py`, grant the ledger lane a **hunk-scoped** lease only within `_ledger_generation_factory` and the existing ledger door/submitter helpers, for injected invoice/transaction detail operations. Keep the existing IVA category validation and income lifecycle hunks, and preserve the newly added withholding destination/factory. The shared integration owner retains all other launcher functions, `navigation.py`, `search.py`, and shared route refresh. Re-read live diffs immediately before writing. This lease is a concrete assignment, not evidence of installed acceptance. Retenciones owns its `withholding` screen/installed composition and acceptance driver; its route integration is described in the retenciones checkpoint. The generic CLI-runner extraction request below is a separate, unleased work item.

These requests target the current `tui/modelo` worktree. They are requests, not leases or completion receipts. Ledger will preserve every concurrent hunk and will verify the live file state immediately before integration.

## Income/TUI owner — invoice and transaction detail

- **Operation:** add canonical invoice detail/readback, supported invoice metadata update, and transaction-detail edit through registered ledger application operations. Preserve the existing invoice-entry capture, linking, income classification and Modelo navigation flows.
- **Files under current owner:** `src/cadrumo/entrypoints/tui/launcher.py`, `ledger/invoice_entry.py`, `ledger/models.py`, `ledger_doors.py`, `ledger/tests/test_ledger_ingestion_surfaces.py`; any new route/view files and their tests require a named single writer. Ledger can own new files and a narrow lease on `ledger/controller.py`, `ledger/entries.py` and `ledger/routes.py` only after route ownership is agreed.
- **Contract:** retain selected bucket/profile, record ID and baseline/revision through async submission; use typed shared application services, never CLI imports or screen tax arithmetic. Invoice update exposes only fields supported by `CatalogueInvoicePatch`; identity/totals changes and unsupported clears refuse. Accepted LEDGER-01 Decision A requires equivalent TUI refusal for a linked transaction ID change. A committed write refreshes detail; stale/conflicting/failed write reports the actual outcome.
- **Proposed delivery:** owner-applied patch in the listed dirty files, or explicit time-bounded lease naming the exact files and current commit/diff state. Ledger will supply clean shared-operation and test seams first.
- **Acceptance:** focused TUI navigation/view/update/refusal tests with explicit markers and `-n0`; installed TUI-only synthetic capture, update and fresh-process readback; both continuation directions after shared operations stabilize.
- **Handoff condition:** owner confirms writer has stopped on named files, identifies retained uncommitted hunks and active process handles, and records either applied patch+test receipt or granted file lease. A request alone does not transfer ownership.

### Income acceptance plumbing needed for installed ledger runs

- **Operation:** extract the generic installed CLI environment, stdin-only synthetic profile-secret delivery, sanitized command receipt and JSON command execution from `dev/acceptance/income_tax/cli_journey.py:44-258` into one canonical `dev/acceptance/installed_cli.py`. Keep income scenario/oracle/receipt logic in its package; migrate IVA's existing imports at `dev/acceptance/iva/cli_journey.py:20-21` to the new primitive. Ledger will consume it for its own driver and allocate run roots through `dev/test_runs/paths.py`.
- **Contract:** preserve current income/IVA executable behavior, effective-settings allowlist and redacted failure output; never put credentials in argv, logs or source; avoid a second ledger-specific environment runner or receipt schema.
- **Proposed delivery:** income owner applies and verifies the extraction, coordinating the narrow IVA import change, or grants a lease on those exact shared files and affected tests after its active installed run closes.
- **Acceptance:** focused income/IVA runner tests with exact markers and `-n0`, a fresh installed smoke under isolated synthetic storage, Ruff/typing/import-boundary checks, and unchanged prior receipts kept at their original source identity.
- **Handoff condition:** owner names the source state, test reservation/process status, and either completed extraction receipt or explicit lease on `dev/acceptance/income_tax/cli_journey.py`, `dev/acceptance/iva/cli_journey.py` and the new canonical module.

## IVA ledger writer — linked edit integration

- **Operation:** implement accepted LEDGER-01 Decision A in `src/cadrumo/application/ledger/actions_manual.py`: preserve explicit refusal of any linked transaction edit that changes its derived ID. Keep the booked-date pinned-authority `IvaCategory` membership guard and batch behavior. Surface the same typed refusal through CLI and the income-owned TUI integration.
- **Files under current owner:** `application/ledger/actions_manual.py` and `application/ledger/tests/test_manual_iva_category_membership.py`; ledger owns the already clean `actions_common.py` guard and focused replacement tests. The IVA checkpoint reports commit `9865586749` delivered; confirm no active writer remains before a narrow lease.
- **Contract:** validate before mutation, preserve tax facts and FX values, reject collision/missing source, keep edit lineage and evidence provenance for supported edits, and refuse linked ID changes before any catalogue write. The shared `actions_common.py` helper already refuses missing source, collision and changed direct `invoice_id` association. A failure must leave no one-sided link or false success. No linked identity correction is authorized for this lane.
- **Proposed delivery:** IVA owner applies the small action-path patch against the landed validator, or grants ledger an explicit lease on `actions_manual.py` after confirming the current category-validation receipt and no active edit.
- **Acceptance:** existing category-membership 5-case selection and the 270-row bulk/CLI load-once nodes after integration, linked ID-change refusal with unchanged encrypted catalogue and reciprocal invoice link, collision/missing-source refusal, targeted Ruff/typing/import boundary. Do not duplicate full IVA acceptance.
- **Handoff condition:** named owner confirms the source state/commit, releases the file and tests, and identifies any active test reservation. Ledger then becomes sole writer for its bounded edit.
