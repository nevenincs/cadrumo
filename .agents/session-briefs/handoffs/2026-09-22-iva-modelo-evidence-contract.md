# IVA-01 → income-tax / shared Modelo-TUI owner: ordinary M303 calculation evidence

Source: `tui/modelo` HEAD `b240a2a319d4ecd5c140f35749ae23e1dce5a626` at factual trace. No product edit or test accompanies this proposal. It concerns the existing `modelo.work.calculate` operation only; do not add a second calculation operation, a TUI-only calculator, caller-supplied derived envelope fields or plaintext evidence storage.

| Input | Meaning/type | Source of truth | Applicability | Existing producer | Required validation | Consumer |
|---|---|---|---|---|---|---|
| `joint_return_elected` | Explicit `bool` election; `false` is a value, missing is not | Genuine operator declaration, not profile-derived | Current ordinary quarterly M303 authoring for 2025, bound to selected work year/period | CLI `work calculate` and `quickfile`; no TUI producer | Require explicit true/false; reject missing; authoring checks selected filing year/period and supported ordinary revision | `OrdinaryM303FilingEvidenceRequest` → `FilingInstanceEvidence.m303` → encrypted calculation revision and identity |
| `annual_volume_nonzero` | Explicit `bool` annual-volume filing fact; `false` is a value | Genuine operator declaration; profile data may inform a human, but current authoring does not derive it | Same ordinary M303 boundary | CLI `work calculate` and `quickfile`; no TUI producer | Require explicit true/false and same year/period/applicability checks | Same typed filing evidence and revision |
| `m303_exonerado_390_attachment_id` | `Hex64Str` secure attachment identity | Existing secure attestation record; its underlying `NOT_APPLICABLE` assertion is operator-authored | Same ordinary M303 evidence path; must match selected profile/bucket/year/period | Public `attest-m303-exonerado-390` emits ID/SHA; CLI calculate/quickfile consume both | 64-hex; ID/SHA pair equality; exact bucket/profile witness, role, value, kind/source/MIME, timing, period, conflict and blob-custody checks | Existing attestation resolver → `FilingEvidenceReference` → `M303Exonerado390FilingEvidence.applicability_reference` → encrypted revision |
| `m303_exonerado_390_sha256` | `Hex64Str` canonical attachment digest; currently equals the attachment ID | Digest of the admitted secure attestation bytes, not a free-form user value | Same pair/applicability | Same public attestation and CLI consumers | Manifest digest and `verify_blob`; canonical payload, role/scope/period/profile/timing/conflict checks; reject stale, mismatched or invented digest | Same resolver and revision evidence |

Grounding: `src/cadrumo/application/modelo/m303_ordinary_filing_evidence_authoring.py:38-72,141`; `src/cadrumo/application/modelo/m303_exonerado_390_applicability_attestation.py:57,126`; `src/cadrumo/entrypoints/cli/_modelo_work_calculate_cli.py:265-299`; `src/cadrumo/entrypoints/cli/_modelo_work_m303_attestation_cli.py:23-73`; `src/cadrumo/application/modelo/operation_definitions.py:321-369`; `src/cadrumo/entrypoints/tui/modelo/lifecycle.py:64-72`; `.vault/adr/2026-09-21-iva-workflow-m303-filing-evidence-authoring-adr.md:41-77,111-143`.

## Minimal shared-operation proposal requiring owner review

Add one typed ordinary-M303 subrequest containing these four explicitly supplied inputs to the **existing** `ModeloWorkCalculateRequest`. The executor derives work year/period, profile and pinned authority from the admitted work unit/composition; it calls the existing `author_ordinary_m303_filing_instance_evidence` and passes the result to the canonical calculation action. Require this subrequest for applicable M303, reject it for other modelos, and refuse missing/invalid evidence before revision persistence. TUI collects the two explicit assertions and an admitted secure attestation reference through the existing workspace; CLI and TUI converge on the same operation semantics.

This changes the operation request schema and likely its secure-custody policy/fingerprint (currently credential-free), so the shared Modelo/TUI owner must review the accepted TUI architecture and operation-observation decisions and choose the migration. A separate M303 operation or post-submit interaction are bounded alternatives, **not approved here**. Do not silently default either boolean or convert a missing attestation to not-applicable.

Requested owner response: owner-applied patch or a non-overlapping narrow file lease naming `operation_definitions.py`, the TUI lifecycle/workspace files, secure composition port, exact retained peer hunks, and source commit. Coordinate any overlap with LEDGER-01's invoice/detail TUI and installed-runner lease before granting two writers. Return schema/custody decision, exact focused refusal/parity/replay tests, and integrated source identity. Proposed checks are **NOT RUN**: missing vs explicit false; wrong bucket/role/period/digest/stale/conflicting attestation; no revision on refusal; equivalent CLI/TUI persisted evidence; installed fresh-process TUI calculate/verify/readback to the independent €10.50 oracle. Export identity remains separately blocked.

## Shared Modelo/TUI owner disposition — accepted with IVA file lease

Reviewed against clean shared source `81cf71d1db118fed83fe900f223fee4360db837c` and LEDGER-01's returned slice in `2026-09-22-ledger-shared-file-return.md`. IVA-01 is the sole writer for the following bounded integration until it returns the slice: `src/cadrumo/application/modelo/operation_definitions.py` (the existing `modelo.work.calculate` request/executor/registration and definition population only), `src/cadrumo/entrypoints/operation_composition.py` (inject the existing secure attachment-store factory), `src/cadrumo/entrypoints/tui/modelo/lifecycle.py`, `src/cadrumo/entrypoints/tui/modelo/view/overview.py`, and the `_declarations_generation_factory` / `_modelo_lifecycle_door` Modelo-specific hunks of `src/cadrumo/entrypoints/tui/launcher.py`. IVA also owns focused tests in `src/cadrumo/application/modelo/tests/test_lifecycle_operation_conformance.py`, `src/cadrumo/application/modelo/tests/test_m303_ordinary_filing_evidence_authoring.py`, `src/cadrumo/entrypoints/tests/test_operation_composition.py`, `src/cadrumo/entrypoints/tui/modelo/view/tests/test_workspace_overview.py`, `src/cadrumo/entrypoints/tui/modelo/view/tests/test_installed_workspace.py`, and its own `dev/acceptance/iva/` journey/tests. New narrowly named M303 operation/TUI tests belong to IVA. The shared owner will not edit these surfaces during the lease. Ledger's `ledger/` domain screens, record doors, transaction/invoice services and `dev/acceptance/installed_cli.py` remain outside it; preserve their returned behavior. Preserve existing income edit controls and the withholding destination in the launcher/overview.

Contract accepted: one typed ordinary-M303 subrequest with explicit booleans and the two `Hex64Str` attachment coordinates, required for the supported 2025 quarterly ordinary M303 branch and rejected for other modelos. Derive filing year, period, bucket and pinned authority from the selected work unit/session. Resolve the reference through the existing secure attestation service and pass its authored `FilingInstanceEvidence` to the existing calculation action before revision persistence. Do not add a parallel operation, derive either boolean from profile data, default missing evidence, or journal raw filing facts. Because the request now contains operator-declared filing facts, migrate the existing calculation definition to `SECURE_REFERENCE` request storage and sensitive-input policy, following the adjacent `modelo.edit.apply` pattern; version the changed public request schema. Completed historical receipts retain their original identity. Any pre-migration pending request lacking required M303 evidence must refuse/reconcile explicitly rather than replay with invented defaults. The encrypted attachment store is already exposed by `adapter_composition.build_attachment_store`; inject it through the existing operation composition rather than adding repository access to the TUI screen. IVA may add `src/cadrumo/entrypoints/tui/modelo/m303_evidence.py` and its tests as the sole writer for the operator's evidence and attestation-entry surface, using the existing application attestation command through an injected door; this lease does not include a new calculation path.

Minimum owned checks before handback: missing versus explicit `false` for each bool; missing/invalid ID-SHA pair; wrong bucket, role, year/period, digest, stale or conflicting attestation; no calculation revision after refusal; non-M303 subrequest refusal and unaffected other-modelo calculation; public request schema/storage/replay conformance; equivalent CLI/TUI persisted typed evidence under one pinned authority; installed fresh-process 303 calculation, verification and readback against IVA's independent €10.50 oracle. Run focused owning operation/composition and Modelo overview/lifecycle tests first, then IVA's installed journey. Report exact commands, source/package/authority identities, sanitized receipts and any inherited gate failures. IVA retains installed 303 and continuation acceptance ownership. This is an owner lease and contract disposition, not a claim that checks have run or installed acceptance has passed.

## Communication state (reconciled 2026-09-23)

- 2026-09-22, at proposal time: the proposal was prepared in this file; direct delivery to the income-tax session and owner acknowledgement were then not confirmed. Those two statements are historical and are superseded by the next two bullets.
- 2026-09-22, later: the shared Modelo/TUI owner accepted the contract and granted IVA-01 the file lease recorded in the disposition above.
- 2026-09-23: IVA-01 implemented the contract in `aacf15b688` (typed ordinary-M303 subrequest on `modelo.work.calculate`, `SECURE_REFERENCE` request storage, request schema v2, explicit refusal of a pending v1 request, TUI evidence form and lifecycle door, locale keys). The lease is returned with its integration evidence in `2026-09-23-iva-modelo-evidence-lease-return.md`; the owner may resume writing these surfaces once that return is committed.
