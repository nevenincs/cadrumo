# LEDGER-01 import-gate delta to quality coordinator — 2026-09-22

Recipient: PROFILE/shared-quality coordinator. This is Ledger's delivered finding packet in the shared worktree; it does not claim a direct outside-session acknowledgement.

The latest available authoritative report inspected is `.logs/test-runs/2026-09-22/20260922T043004.335683Z-check-import-boundaries-66568-02167858/artifacts/import-health.json`. Its before/after source digest is the same (`280a764872c656bf7cb24f7e1a328ae36c890fa42cc333a37425408ec5e5c51a`), loadability is `2858/2858` with zero failed loads, and the gate is **red**: 172 hard findings (168 `PRIVATE_CROSS_PACKAGE`, 3 `REEXPORT_OR_ALIAS`, 1 `UNRESOLVED_DYNAMIC_TARGET`) and 10 new unapproved ratchet occurrences. The report predates the narrow provenance presentation change and is not a verdict on that later source.

Ledger-owned integration resolution: the checked-in `dev/quality/metadata/import_load_targets.json` now includes `dev.acceptance.ledger`, its `cli_journey`, `frontend_contract`, `installed_tui_journey` and `scenario` modules, plus `cadrumo.entrypoints.tui.ledger.record_doors` and `record_views`. `application_entrypoint_modules.json` also includes the two TUI modules. The candidate inventory for that report has **zero occurrences sourced from `dev.acceptance.ledger`, the new Ledger record routes, or the Ledger CLI presentation files**. The earlier missing Ledger load-target inventory complaint is therefore resolved in checked-in metadata; no Ledger-owned import-edge correction remains in this assigned integration slice. The later presentation edit introduces only the existing inward `pydantic.PositiveInt` and test imports, and its focused Ruff, formatting, typing and diff checks passed.

The report does contain older, broader modules with `ledger` in their names: adapter tests reaching entrypoint composition; `application.ledger.tests.test_evidence_routing_and_projection` and `test_batch_reader_roles` reaching inbound adapters; and TUI asset parity reaching a CLI asset adapter. Those are outside the assigned LEDGER-01 integration files and include other lane ownership. Please retain them in the quality owner's owner-by-owner worklist; this packet does not claim or suppress them.

Next formal action belongs to the quality coordinator: reserve one fixed-source import gate after active peer edits settle, compare exact occurrences with the authoritative report and assign only newly attributable findings to their owning writers. No global gate rerun or unrelated import rewrite was made by Ledger after the presentation edit.
