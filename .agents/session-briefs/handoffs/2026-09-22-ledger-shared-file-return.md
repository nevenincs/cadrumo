# LEDGER-01 shared-file return — 2026-09-22

Recipient: income/TUI and IVA writers through the coordinator. This file is the prepared return in the shared `tui/modelo` worktree; direct delivery to those outside sessions is not claimed. LEDGER-01 acknowledges IVA's narrow release in `2026-09-22-iva-ledger-release.md`. That release did not transfer income or other peer features.

## Source and exact slice

The shared integration was included in coordinator commit `01b78c10211601ea21d70ab60fae3ecec5552ae4` alongside unrelated lane changes. LEDGER-01 owns these contracts, not the surrounding income/IVA changes:

- `src/cadrumo/entrypoints/tui/ledger/{record_doors.py,record_views.py,controller.py,entries.py,overview.py,routes.py,workspace_injection.py}` and the narrow `launcher.py` record-door injection: canonical invoice catalogue/detail, reviewed notes update, transaction detail and reviewed description edit through typed application operations. The bound door retains profile/bucket and pinned authority; the opened record is the write baseline. Screens show refusal/failure and refresh from committed readback.
- `src/cadrumo/application/ledger/{actions_manual.py,actions_common.py,protocols.py}` plus `src/cadrumo/adapters/persistence/profile/transactions.py` and SQL secure-object revision guards: public manual update optionally accepts `expected_current`, refuses a stale opened record, and atomically co-commits one guarded replacement and audit history. The existing linked derived-ID refusal remains before mutation. Successful unlinked replacement keeps edit lineage; unchanged attachment manifests are not reread after commit.
- `src/cadrumo/application/invoices/catalogue_lifecycle.py`: optional opened-invoice baseline checked within guarded catalogue mutation. Existing invoice catalogue plus durable audit co-commit is retained. Audit event identity fields do not reconstruct complete before/after invoice content.
- `dev/acceptance/installed_cli.py` and affected income/IVA/ledger/assets/profile/retenciones callers: generic installed environment, stdin-only secret delivery, JSON command result and sanitized evidence moved to one shared module. Scenario logic remains in its package. No forwarding alias or ledger-specific subprocess runner was added.

The integrated installed wheel SHA-256 is `6c8d926c0e4274233cba86eb961bb29f1cce8890b92d2d9fc0b845e1b24c951b`; installed payload SHA-256 is `78f893663d2c184d8409f3eadb74557dfb13de37241717e9c65e2c5eb3c0b051`. The product imported from `site-packages`. The original CLI-only receipt stays bound to its earlier wheel SHA `4336de4f...`.

## Verification returned

- Integrated Ledger boundary selection: 15 passed, exit 0, log `C:/Users/hello/AppData/Local/Temp/.logs/test-runs/2026-09-22/20260922T003740.617560Z-pytest-54068-e51d3c60/run.log`. It includes mounted invoice/transaction detail, stale and linked encrypted persistence, invoice baseline and SQL batch rollback.
- IVA manual/category preservation selection: 32 passed, log `20260922T003730.649942Z-pytest-8856-cbc69579`. Scoped Ruff, format, `ty` and diff check passed. Booked-date pinned-authority IVA category validation from `9865586749` and combined TUI classification from `c33e3e48f6` remain in place. No full IVA lane was rerun.
- Shared installed-runner affected callers: 19 focused tests passed; scoped Ruff, `ty`, import smoke and compile checks passed. No full income lane was rerun.
- Installed journey 5 exited 0 with sanitized receipt `artifacts/ledger-01/installed-ledger-tui-wheel-6c8d-journey-5-receipt.json`: TUI-only invoice and transaction capture/detail/edit with fresh-process readback; CLI→TUI meaningful edits and visible linked identity refusal with unchanged reciprocal public readback; TUI→CLI distinct metadata update and fresh TUI readback; both two-row JSONL exports compared by amounts, direction, invoice links and digest. Failed attempts 1–4 remain failed evidence in the isolated synthetic run root and were not promoted.
- Supplemental TUI-only/public-CLI oracle exited 0 on the same wheel in a fresh isolated store, receipt `artifacts/ledger-01/installed-ledger-tui-wheel-6c8d-supplemental-tui-only-receipt.json` (SHA-256 `1d1cee2158c8178387301e9582c4d34d83a1c99e5df529dbd6c088a80ec9998b`). Four installed public JSON reads verify the TUI-created invoice's canonical identity, base/IVA/grand totals and absent links; the TUI-imported transaction's amount, incoming direction and absent link; and nonempty prior-ID edit lineage after its TUI description update. Fresh-process TUI readback also passed. The focused installed-journey/frontend-contract selection passed 11 tests; scoped Ruff, format, `ty`, and diff check passed. The first supplemental attempt failed before product assertions due to a wrong executable name; its failed receipt is retained separately.

## Limits and next owner actions

The TUI detail forms expose notes and description as a bounded supported patch subset. Invoice identity/totals and linked transaction identity-changing correction remain unsupported; both frontends refuse the linked edit. The event payload is not a full historical invoice reconstruction. The fresh-process installed test uses the public installed Login screen as a credential prelude because the headless launcher otherwise exits before an unadmitted workbench pilot.

The import-boundary gate is **red/unavailable** on the moving source: 7445 scanned files, 171 hard findings, 10 unapproved ratchet occurrences, stale load-target metadata, and source change during the run. Quality owner: enroll the committed `dev.acceptance.ledger` modules with canonical generators, resolve or classify the existing findings, then reserve one fixed-source rerun. Do not report a passing gate from the focused checks. Public JSON tracking does not expose import source filename/row; the supplemental TUI receipt therefore does not establish that particular provenance field, while the earlier encrypted-import tests establish custody in their exercised paths.

At return preparation, no active dirty income/TUI writer on the overlapping files was observed; this does not confirm the outside income session stopped. IVA's release names no active IVA writer at its stated source. Retenciones and other peer edits are disjoint. Income/IVA should inspect this shared diff, consume the typed application and runner contracts, and run only their smallest affected checks. The coordinator should confirm direct delivery/acknowledgement; this file alone does not prove either outside session received it.

Synthetic run roots from this journey and the earlier CLI receipt remain retained after automatic cleanup approval rejected deletion. They contain synthetic plaintext inputs/exports and encrypted stores. Secrets were delivered through stdin and omitted from receipts. Only coordinator/platform-authorized cleanup under the applicable policy may remove them.
