# RETENCIONES-01 installed TUI-to-CLI annual continuation — annual1 failure

The single isolated run root was
`C:\Users\hello\AppData\Local\Temp\retenciones-cli-to-tui-d0bdd2a36da147beb60da1d9ef1e76b5\annual1`.
Its parent chain exited; no retry was launched.

- `capture.json` is `proven`: the installed TUI captured the professional and
  urban-rent evidence.
- The only retained annual export artifact is Modelo 180. In the driver
  sequence, an artifact is independently parsed against the selected official
  layout, recipient/property rows, header count, and control fields before the
  next model begins. Therefore Modelo 180 passed that independent validation.
- No Modelo 190 export artifact was produced.
- `tui-to-cli-annual.json` is the preserved public failure receipt:
  `status=failed`, `stage=annual_continuation`,
  `code=RetencionesInstalledTuiError`.

The executed driver version did not preserve the message of its local annual
driver exception, and a read-only, token-only log scan found no additional
annual diagnostic code. This is an acceptance-driver observability gap, not a
grounded product refusal or an annual-success claim. The owned driver now has
a focused regression that preserves the exact safe local error string in a
future receipt, but this run is not retried here.

No live submission was requested. This artifact does not claim a TUI-only
lifecycle, a Modelo 190 export, or a complete annual continuation.
