# Duplication dispositions
#
# ===========================================================================
# READ THIS BEFORE QUOTING THE PERCENTAGE
#
# jscpd matches TOKEN SEQUENCES. It cannot see one concept implemented twice
# in different syntax, and that is the duplication this project's rules treat
# as a BLOCKER ("semantic overlap of one concept across different modules").
# So `duplicated_pct` below measures textual similarity, not duplication.
#
# This is measured, not theoretical. Five ledger projections carried the same
# casilla fold -- same algorithm, same output type, differing only in an
# accumulator loop versus a comprehension. jscpd reported NONE of the five,
# and instead flagged the import preambles those same files share. Most
# entries in this file are preamble matches. The real cluster was found by
# reading the concept and was consolidated into
# `application/aggregation/_grouping.py::fold_casilla_observations`.
#
# Therefore: a low percentage here is evidence that little COPY-PASTE
# survives. It is not evidence that little duplication survives, and it never
# has been. Semantic duplication needs a reader; this file is the residue of
# what token matching could reach.
# ===========================================================================
#
# One recorded disposition per clone group jscpd currently observes in the
# production tree (`src/cadrumo`). The clone COUNT is advisory debt, not an
# elimination mandate: jscpd output identifies candidates for semantic
# inspection, never a consolidation verdict. This file records the honest
# disposition of each candidate.
#
# This file is reconciled against a live `just audit-duplication` run, not
# hand-maintained prose: entries for groups a later consolidation resolved are
# removed rather than left stale, because "one disposition per OBSERVED group"
# means observed NOW. Removing a resolved entry records no loss of reasoning
# -- the clone it described no longer exists, so there is nothing left to have
# an opinion about.
#
# Every `[[group]]` carries exactly one `classification`:
#   * cluster-owned     - the group belongs to a named actionable
#                         consolidation cluster already in flight; `owner`
#                         names that cluster.
#   * intentional       - a recorded intentional non-consolidation: either an
#                         ADR "Intentional non-consolidations" entry or a
#                         constraint-divergent pair the substitutability
#                         pre-filter excludes; `rationale` cites why.
#   * advisory-residue  - low-value structural duplication left as VISIBLE
#                         advisory debt. NOT an elimination mandate; recorded so
#                         the count is honest rather than hidden.
#   * actionable        - a real substitutable redeclaration resolved in this
#                         commit; `rationale` names the fix.
#
# `where` uses paths relative to `src/cadrumo/` with jscpd line spans.

[meta]
generated_from = "just audit-duplication (jscpd@4.2.0)"
observed_groups = 14
duplicated_pct = "0.07"
files_analyzed = 1311
source_root = "src/cadrumo"
count_is_advisory = true

# What this record does NOT cover, stated so its silence is not read as
# evidence of absence:
#
#   * `dev/` is outside `source_root`. The standing runner scans the product
#     tree only, so the tooling that gates the product has never been measured
#     by it. A direct run over `dev/` observed 2 clone groups. The CloudFront
#     invalidate-and-wait pair (deploy/docs_static_site.py +
#     frontend_static_site.py) was substitutable and is consolidated. The
#     remaining one is the argparse main() emit tail shared by
#     packaging/installed_mcp_oracle.py and installed_tax_oracle.py: two
#     separate console entry points whose only shared tail is
#     parse-run-dump-print, left as advisory residue. Named here rather than
#     left invisible, since no scan reaches them.
#
#     The product-only scope is deliberate, not an oversight: the governing
#     audit scopes every instrument to "the intended production scope", and
#     the runner's docstring now says so. `dev/` remains scannable on demand
#     by passing a different `source_root`, which is how the two groups above
#     were found.
#
#   * Semantic duplication, per the banner at the top of this file.

# `observed_groups` is jscpd's own inventory size for this scan: it equals the
# sum of `cluster_owned` + `intentional` + `advisory_residue` below. The single
# `actionable` group sits below jscpd's min-lines/min-tokens floor (see its
# section) and is therefore outside that inventory -- the summary total is
# `observed_groups` plus that one extra, tracked group.
[summary]
cluster_owned = 1
intentional = 7
advisory_residue = 6
actionable = 1

# ---------------------------------------------------------------------------
# cluster-owned: tracked by a named actionable consolidation cluster
# ---------------------------------------------------------------------------

[[group]]
classification = "cluster-owned"
owner = "LLM review workflow consolidation"
where = ["entrypoints/cli/_ledger_llm_cli.py [762-790]", "entrypoints/cli/_ledger_llm_cli.py [694-720]"]
rationale = "Distributed LLM-review routing block; the other three instances this cluster previously carried have consolidated, leaving this last one pending the shared typed workflow."

# ---------------------------------------------------------------------------
# intentional: recorded intentional non-consolidation / constraint-divergent
# ---------------------------------------------------------------------------

[[group]]
classification = "intentional"
where = ["domain/calculations/registry/_aeat_nif_iva_oracle.py [22-50]", "domain/calculations/registry/_groi_oracle.py [41-69]"]
rationale = "The matched block is the shared import preamble. The behaviour these two oracles share is already consolidated behind _checker_oracle_flow and BaseCheckerOracle; what remains diverges by identifier rule (foreign-EU VIES proxy vs Spanish ROI), so the import list is the whole residue."

[[group]]
classification = "intentional"
where = ["application/modelo/_review_package_recipient_registry.py [53-68]", "application/modelo/_review_package_recipient_replay_guard.py [48-64]"]
rationale = "ADR intentional non-consolidation: recipient registry manages authorized recipients while the replay guard prevents repeated processing; distinct authorities."

[[group]]
classification = "intentional"
where = ["application/aggregation/_renta_gasto_ledger.py [34-49]", "application/aggregation/_renta_income_ledger.py [34-49]"]
rationale = "The matched block is the shared import preamble, not logic. What these modules genuinely shared has already been lifted out (_shared_issue_reasons, _business_proportion, _currency_predicates, and the fold in _grouping); the residue is the import list itself, which cannot be deduplicated without a re-export shim the architecture rules forbid."

[[group]]
classification = "intentional"
where = ["application/aggregation/_impatriado_income_ledger.py [40-70]", "application/aggregation/_renta_income_ledger.py [34-64]"]
rationale = "Import preamble again, plus the issue-reason enum heads. The reason MEMBERS already route through _shared_issue_reasons; each enum stays separate because a projection's reason set is closed over its own legal scope (the impatriado carries BECKHAM_FOREIGN_SOURCE_SEGREGATED, art. 93.2, which must never be constructible for a resident-IRPF projection). Merging them would widen each site's accepted value set."

[[group]]
classification = "intentional"
where = ["entrypoints/cli/_modelo_work_revision_payloads.py [49-66]", "entrypoints/cli/_modelo_work_wizard_payloads.py [57-74]"]
rationale = "Sibling payload models sharing field structure across distinct typed payloads; Typer/schema presentation structure, not one authority."

[[group]]
classification = "intentional"
where = ["adapters/outbound/aeat/sede/_censal_datos.py [49-76]", "adapters/outbound/aeat/sede/_notifications.py [36-63]"]
rationale = "Import preamble, the same residue two entries above already record for the oracle and ledger-projection pairs. Both are sede readers, so both import the same navigation substrate (browser session factory, storage state, wait constants, the three sede error types, the remote-state guard). What follows the preamble does not match: the censal reader is pinned to the consulta view and fails closed on a filing-tool or procedure-launcher landing, while the notifications reader pins a numbered host. A preamble cannot be deduplicated without the re-export shim the architecture rules forbid."

[[group]]
classification = "intentional"
where = ["adapters/inbound/tui/_form_screen.py [119-129]", "adapters/inbound/tui/_manager_screen.py [103-113]"]
rationale = "Constraint-divergent, excluded by the substitutability pre-filter. The matched block is the input row plus the actions row of a modal edit dialog, but the two dialogs disagree on both halves of what an edit MEANS: the manager variant starts a masked field EMPTY rather than pre-filled (pre-filling would submit the placeholder dots back as the literal new value) and dismisses with the raw input, while the form variant pre-fills and routes save through _submit() so a refusal can render in its #edit-refusal slot, which the manager dialog does not carry. Consolidating either direction would add masking where it is unwanted or drop validation where it is load-bearing."

# ---------------------------------------------------------------------------
# advisory-residue: low-value duplication left as visible debt (not a mandate)
# ---------------------------------------------------------------------------

[[group]]
classification = "advisory-residue"
where = ["domain/attachments/_service.py [172-213]", "domain/attachments/_service.py [104-147]"]
rationale = "Repeated attachment put/read shaping within one service; low-value residue."

[[group]]
classification = "advisory-residue"
where = ["application/overview/_calendar_models.py [309-321]", "application/overview/_calendar_models.py [168-180]"]
rationale = "Sibling calendar-model field blocks share structure; low-value residue."

[[group]]
classification = "advisory-residue"
where = ["application/modelo/_calculation_source_staging.py [254-267]", "application/modelo/_calculation_source_staging.py [180-193]"]
rationale = "Repeated source-staging block within one module; low-value residue."

[[group]]
classification = "advisory-residue"
where = ["application/ledger/_models.py [240-251]", "application/ledger/_models.py [92-103]"]
rationale = "Sibling ledger-model field blocks share structure; low-value residue."

[[group]]
classification = "advisory-residue"
where = ["application/diagnostics_run_health.py [663-670]", "application/diagnostics_run_health.py [571-577]"]
rationale = "Repeated health-metric shaping block within one module; low-value residue."

[[group]]
classification = "advisory-residue"
where = ["adapters/inbound/tui/_form_screen.py [187-199]", "adapters/inbound/tui/_form_screen.py [106-118]"]
rationale = "TextEditScreen and OneChoiceEditScreen share a modal chrome: BINDINGS, the field-holding __init__, and the compose head (Vertical#edit-dialog, Label#edit-label, the optional Static#edit-path hint) and tail (Horizontal#edit-actions with cancel and save). Unlike the cross-file TUI pair above, this residue IS substitutable -- roughly six lines of chrome plus __init__ could move to a shared base. Recorded as visible debt rather than consolidated here for two reasons: this is another campaign's live surface, and Textual compose bodies are declarative widget trees where hiding the head and tail behind helper generators trades a readable tree for a smaller line count. Named rather than classified as intentional so the owning campaign can take the extraction on its own terms; do not read this entry as a verdict that it should not happen."

# ---------------------------------------------------------------------------
# actionable: resolved in this commit (below jscpd's min-lines/min-tokens
# floor, so absent from jscpd's own inventory, but a real substitutable
# redeclaration confirmed by direct inspection)
# ---------------------------------------------------------------------------

[[group]]
classification = "actionable"
where = ["entrypoints/cli/_ledger_classify_cli.py [24-26]", "entrypoints/cli/_ledger_support.py [35-39]"]
rationale = "_TransactionRepo Protocol (single `bucket_id: str` property) was redeclared byte-identically in _ledger_classify_cli.py. Substitutable (identical shape). Fixed: deleted the local redeclaration and imported the canonical protocol from ._ledger_support, matching _ledger.py."
