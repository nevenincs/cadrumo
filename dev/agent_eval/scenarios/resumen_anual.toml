# Golden scenario: resumen anual (annual window, WHEN layer).
#
# Asserts the annual-season itinerary's own driven surface: confirm the active
# profile, read the annual window from the bounded calendar, and confirm one
# doubtful annual obligation explicitly. The delegated annual filings are
# covered by the owning per-modelo scenarios. The runner additionally asserts
# the skill playbook cites each verb, the resolved 390 revision's casillas
# carry registry legal grounding, and the revision declares an AEAT-grounded
# verification contract containing `expected_computed_casillas` — the annual
# IVA totals whose agreement with the four filed quarters is the season's
# defining reconciliation.

name = "resumen-anual-annual-window"
modelo = "390"
filing_year = 2023
period = "0A"
skill_name = "cadrumo-resumen-anual"
provenance_required = true
expected_computed_casillas = [
  "iva.anual.cuota-devengada-total",
  "iva.anual.cuota-deducible-total",
  "iva.anual.resultado-regimen-general",
]

expected_trajectory = [
  "overview.status",
  "overview.calendar",
  "overview.explain",
]
