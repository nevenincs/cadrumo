# Golden scenario: cierre de trimestre (quarter close, WHEN layer).
#
# Asserts the quarter-close itinerary's own driven surface: confirm the active
# profile, read what is next due from the agenda, then the bounded calendar for
# the presentation window, and confirm one doubtful obligation explicitly. The
# delegated quarterly filings are covered by the owning per-modelo scenarios
# (modelo_130.toml, modelo_303.toml). The runner additionally asserts the skill
# playbook cites each verb, the resolved 130 revision's casillas carry registry
# legal grounding, and the revision declares an AEAT-grounded verification
# contract containing `expected_computed_casillas` (the same grounded pago
# fraccionado casillas the modelo-130 scenario pins).

name = "cierre-trimestre-quarter-close"
modelo = "130"
filing_year = 2024
period = "1T"
skill_name = "cadrumo-cierre-trimestre"
provenance_required = true
expected_computed_casillas = ["03", "04", "07"]

expected_trajectory = [
  "overview.status",
  "overview.agenda",
  "overview.calendar",
  "overview.explain",
]
