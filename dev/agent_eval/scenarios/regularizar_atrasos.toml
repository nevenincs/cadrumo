# Golden scenario: regularizar atrasos (behind on obligations, WHEN layer).
#
# First life-situation scenario: asserts the situation itinerary's own driven
# surface, not a per-modelo spine (the delegated catch-up filing is covered by
# the owning per-modelo scenario, e.g. modelo_303.toml). The trajectory is the
# situation skill's sequence — confirm the active profile, surface the past-due
# backlog, confirm one surfaced obligation's applicability — and the runner
# additionally asserts the skill playbook cites each verb, the resolved 303
# revision's casillas carry registry legal grounding, and the revision declares
# an AEAT-grounded verification contract containing `expected_computed_casillas`
# (the same grounded IVA result casillas the modelo-303 scenario pins). No
# numeric casilla value is asserted; recargo figures are CLI-derived at runtime
# and the skill forbids the operator computing them.

name = "regularizar-atrasos-backlog"
modelo = "303"
filing_year = 2024
period = "1T"
skill_name = "cadrumo-regularizar-atrasos"
provenance_required = true
expected_computed_casillas = ["64", "66"]

expected_trajectory = [
  "overview.status",
  "overview.backlog",
  "overview.explain",
]
