##
## Declared channel for parsers of official regulatory prose.
##
## The SET is derived mechanically by
## ``dev.registry.analysis.regulatory_prose_parser_channel``; only the corpus and
## the reason are authored here. A module the derivation yields and this ledger
## does not name is a reconciliation failure, so a new grammar over AEAT or BOE
## text cannot ship undeclared.
##
## RULING: design-prose grammars are
## RETAINED, not migrated into the authoring tree, and the channel is declared
## here instead. Two facts settle it. First, the prose is the outside world's
## and keeps changing: ``no-legacy-compatibility`` explicitly keeps resilience
## for "AEAT portal variations, BOE corpus formats, PDF producer quirks", and a
## one-shot import would delete the ability to onboard the next AEAT design at
## all. Second, the grammar is not where the regulatory value lives: a value a
## grammar derives still lands in the registry authoring tree with its
## establishing provision, under ``aeat-calculation-grounding``. Enrolment here
## authorises READING prose. It authorises no value.
##
## The measured inventory also corrected the row's premise. The objection named
## one generator-side grammar in ``dev/``; the derivation found fifteen modules,
## eleven of them shipped under ``src/`` -- including a 4,600-line record-design
## parser that is the registry's own layout authority. Migrating "the" grammar
## was never the smaller change.
##

[[parser]]
module = "dev/registry/compiler/record_design_pdf_repairs.py"
corpus = "AEAT diseños de registro (PDF)"
reason = """\
The PDF half of the record-design authority, which was decomposed out of \
``record_design.py``. Turns published diseño tables into typed \
position/width/naturaleza facts, absorbing the extraction damage those \
documents arrive with -- reversed rows, stuttered rows, glued ordinals, orphan \
measures. This is the canonical reader for that damage; a second one would \
fork the layout authority."""

[[parser]]
module = "dev/registry/compiler/record_design_workbook.py"
corpus = "AEAT diseños de registro (XLSX)"
reason = """\
The workbook half of the same decomposition. Reads the footnote markers a \
diseño workbook carries beside its table -- numbered ``Nota`` rows and their \
symbol variants -- so a measure qualified by a footnote is not read as \
unqualified."""

[[parser]]
module = "src/cadrumo/adapters/inbound/borrador/_extractors/modelo_100_summary_v2025.py"
corpus = "AEAT borrador renta summary (extracted PDF text)"
reason = """\
Reads the taxpayer's own borrador summary, which AEAT publishes as prose \
rather than as a machine format: the casilla ordinals it lists and the \
ejercicio it declares. An imported borrador figure is an external value \
compared against the engine's own, never a substitute for one."""

[[parser]]
module = "dev/registry/compiler/record_design_schema.py"
corpus = "AEAT diseños de registro"
reason = "Schema-side companion to the record-design authority; validates the shapes that reader produces."

[[parser]]
module = "src/cadrumo/domain/calculations/registry/export_parse.py"
corpus = "AEAT diseños de registro"
reason = "Parses the fixed-width export field grammar the record designs declare."

[[parser]]
module = "dev/registry/compiler/validate_export_layout_coverage.py"
corpus = "AEAT diseños de registro"
reason = "Validates that a declared export layout covers every position its diseño states."

[[parser]]
module = "dev/registry/compiler/validate_layout_authority_content.py"
corpus = "AEAT diseños de registro"
reason = "Checks a layout authority's cited content against the design text it claims to mirror."

[[parser]]
module = "dev/registry/compiler/_validate_semantic_role_required.py"
corpus = "registry casilla declarations"
reason = "Matches semantic-role identifiers; reads registry-authored ids rather than official prose, and is enrolled because the detector matches its casilla vocabulary."

[[parser]]
module = "dev/registry/compiler/legal_grounding.py"
corpus = "BOE consolidated legal text"
reason = "Resolves legal-reference ids and their corpus anchors against bundled BOE text."

[[parser]]
module = "src/cadrumo/adapters/inbound/declaracion/_detect.py"
corpus = "AEAT declaración documents"
reason = "Detects which modelo and ejercicio an imported AEAT document is, before any typed parse."

[[parser]]
module = "src/cadrumo/adapters/inbound/justificante/_extract.py"
corpus = "AEAT justificante de presentación"
reason = "Extracts the CSV and filing coordinates AEAT prints on a submission receipt."

[[parser]]
module = "src/cadrumo/adapters/outbound/aeat/sede/declarations.py"
corpus = "AEAT sede consulta pages"
reason = "Reads the read-only consulta listing; the sede's own markup is external prose that changes without notice."

[[parser]]
module = "dev/registry/pipeline/_export_tree.py"
corpus = "AEAT diseños de registro"
reason = """\
Generator-side grammar over design value clauses ("N enteros y M decimales", \
quoted code enumerations, trailing Nota references). Feeds the export-tree \
renderer, whose output is proved against the published tree by \
``check_generated_export_tree`` before publication -- so what this grammar \
derives is never trusted on the grammar's word alone."""

[[parser]]
module = "dev/audit/legal_excerpt_vintage_screen.py"
corpus = "BOE consolidated legal text"
reason = "Audit tool screening bundled legal excerpts for vintage drift; reads BOE text to compare, writes nothing."

[[parser]]
module = "dev/docs/terminology/resolution.py"
corpus = "Terminology Handbook fragments"
reason = "Resolves glossary term references in authored documentation; enrolled because the detector matches its domain vocabulary, not because it reads official prose."

[[parser]]
module = "dev/docs/terminology/sweep_runner.py"
corpus = "Terminology Handbook fragments"
reason = "Sweeps documentation for enrolled terms; same disposition as its resolution sibling."

[[parser]]
module = "dev/registry/analysis/footnote_pointer_notes.py"
corpus = "AEAT record-design transcriptions"
reason = "Resolves a Contenido footnote pointer to the note text the design states beneath the table, so a reviewer authoring a representation rule can read what the pointer defers to. Enrolment authorises reading that prose and no value: the screen reports the note and derives nothing, and any scale or sign a note establishes still lands in the authoring tree with its provision. The design is located from the source reference's own corpus path rather than by globbing its directory, because one modelo bundles fifteen designs and a note resolved against the wrong year is worse evidence than none."

[[parser]]
module = "dev/locales/_casilla_keys.py"
corpus = "AEAT casilla label and help text, as the registry stores it"
reason = """Reads the edition-keyed chains a casilla's own AEAT label and help resolve through, so an inherited row shows the text its edition states. It reads key spellings over that prose; it establishes no value."""

[[parser]]
module = "dev/locales/modelo_casilla_catalogue.py"
corpus = "AEAT casilla label and help text, as the registry stores it"
reason = """Recognises the stock phrasings AEAT repeats across casilla help so a delta edition's inherited text is matched rather than restated per edition. Recognition only: the text it matches stays the registry's."""

[[parser]]
module = "dev/registry/analysis/casilla_lineage_seed.py"
corpus = "AEAT diseños de registro (printed box numbers)"
reason = """Reads the printed box number a design prints beside a casilla -- plain integers and the lettered, bis and dotted shapes -- to dispose of each successor row in a lineage. It proposes lineage seeds for review, never a filing value."""

[[parser]]
module = "dev/registry/analysis/unresolvable_note_pointers.py"
corpus = "AEAT diseños de registro (Contenido cells and their notes)"
reason = """Reads the ``Nota N`` pointer a Contenido cell carries when the rest of a field's meaning sits in a numbered note, and reports the pointers no note answers. A report, not a derivation."""

[[parser]]
module = "dev/registry/edition_delta_migration.py"
corpus = "Authored registry edition TOML (casilla sections)"
reason = """Reads the authored casilla section headers of an edition it is converting from a full copy to a delta. The prose vocabulary is the registry's own authoring spelling, and the conversion preserves declarations rather than deriving them."""

[[parser]]
module = "dev/registry/pipeline/render_profile.py"
corpus = "AEAT diseños de registro (Contenido cells and cell references)"
reason = """Reads the exact official Contenido cell a reviewed render profile may name, and the composite definitions that cell spells out. A profile is a reviewed input: this reads what it points at and infers nothing."""

[[parser]]
module = "dev/registry/analysis/record_design_enrollment.py"
corpus = "AEAT diseños de registro (ingest capture titles and authored sources rows)"
reason = """Reads the AEAT listing title a capture record stored beside an unregistered design, and the ``[sources."..."]`` headers already authored in the tree, to propose the source rows those designs are owed. The title's ejercicio names an identifier only; the candidate leaves applies_from/applies_to and record_design_epoch undeclared because a listing title states no legal window. It prints a candidate and installs nothing."""
