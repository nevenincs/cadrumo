import runner as process
from runner import run as invoke

LINT_IMPORTS_COMMAND = "lint-imports"
EXPECTED = "KEPT"


def audit() -> None:
    process.run([LINT_IMPORTS_COMMAND]).stdout.endswith("KEPT")
    invoke([LINT_IMPORTS_COMMAND]).stderr.startswith("BROKEN")
