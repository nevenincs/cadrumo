# Docstring references that name something absent, by module.
#
# NOT a debt ledger. Each of these is an accurate statement about the PAST:
# a sentence explaining what a module consolidated, or why two read paths were
# merged, naming code that correctly no longer exists. Rewriting one would turn
# a true statement about history into a false one about the present.
#
# The gate exists to keep any OTHER kind from joining them. Shrink-only.

[files]
"adapters/persistence/storage/envelope/secure_bound_repository.py" = 1
"core/decimal/coercion.py" = 2
"core/decimal/formatting.py" = 1
