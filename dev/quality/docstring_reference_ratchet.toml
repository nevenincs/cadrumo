# Docstring references that name something absent, by module.
#
# NOT a debt ledger. A reference here can be correct BECAUSE it names something
# absent: a sentence recording what a module consolidated, or why two read paths
# were merged, naming code that is properly gone. Rewriting one would turn a
# true statement about history into a false one about the present.
#
# Entries that are NOT of that kind are real debt and should shrink. The gate
# exists to keep new ones from joining either group. Shrink-only.

[files]
"adapters/persistence/storage/envelope/secure_bound_repository.py" = 1
"core/decimal/coercion.py" = 2
"core/decimal/formatting.py" = 1
"entrypoints/tui/components/theme.py" = 1
