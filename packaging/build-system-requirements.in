# PEP 517 backend authority for every Cadrumo wheel and sdist.
# Regenerate build-system-constraints.txt with the repository's pinned uv.
hatchling==1.32.3
# The root distribution's build hook compiles a missing source-tree authority,
# so its build-system.requires carries the canonical compiler's import closure.
# Hash-required builds need every one of those pinned here, at the locked
# versions.
beautifulsoup4[lxml]==4.15.0
lxml==6.1.3
defusedxml==0.7.1
openpyxl==3.1.5
pdfplumber==0.11.10
pydantic==2.13.5
pydantic-settings==2.15.0
pypdfium2==5.13.0
python-dotenv==1.2.3
python-i18n==0.3.9
PyYAML==6.0.3
rtoml==0.13.0
tzdata==2026.4 ; sys_platform == 'win32'
xlrd==2.0.2
