"""Placeholder 0.0.0 release of the ``aeat-data`` corpus-sources companion.

Exists only so the PyPI project exists ahead of its per-file size-limit
request; carries no corpus data. The real companion ships from
``packaging/aeat_data`` at 0.1.0 once the grant lands.
"""
