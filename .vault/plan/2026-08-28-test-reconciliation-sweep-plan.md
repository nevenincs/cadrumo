---
tags:
  - '#plan'
  - '#test-reconciliation-sweep'
date: '2026-08-28'
tier: L2
related:
  - '[[2026-08-28-test-reconciliation-sweep-adr]]'
modified: '2026-08-28'
body_schema: body-v2
body_hash: 'sha256:7db612a67844e5f5d10c588b6e23c0b0fd325ce5625e97dde99dc3b9836f2db4'
---

# `test-reconciliation-sweep` plan

## Description

## Steps

### Phase `P01` - reconcile stale gates and ungoverned writers

Bring every gate this campaign's renames invalidated back to a truthful green, without weakening an assertion

- [x] `P01.S01` - Narrow the CLASSIFIED_BY_MANUAL constants gate to its defining modules, since a package-namespace assertion demands the re-export the inert-namespace boundary forbids; `src/cadrumo/core/tests/`.
- [x] `P01.S02` - Allowlist the three committed google test modules with rationales read from their own docstrings; `src/cadrumo/adapters/outbound/google/tests/`.
- [x] `P01.S03` - Give the M303 filing-evidence document one canonical test-side home and pass it on every M303 calculate invocation; `src/cadrumo/entrypoints/cli/tests/`.
- [x] `P01.S04` - Grant the source-mesh purchase its INVOICE_EVIDENCE deduction authority so the input leg is asserted against real values; `src/cadrumo/entrypoints/cli/tests/`.
- [x] `P01.S05` - Repoint the M115 refusal assertion at the typed action projection that replaced the removed free-text field; `src/cadrumo/entrypoints/cli/tests/`.
- [x] `P01.S06` - Declare the Modelo 111 colegio-concertado attestation in the test profile, since readiness and the export producer both genuinely require it; `src/cadrumo/entrypoints/cli/tests/`.
- [x] `P01.S07` - Carry the Modelo 111 no-retenciones attestation path in the localised refusal message, following the Modelo 180 precedent, in all four catalogues; `src/cadrumo/application/aggregation/`.
- [x] `P01.S08` - Govern the four writers this campaign's renames pulled into the coverage predicate; `src/cadrumo/application/repair_integrity.py`.
- [x] `P01.S09` - Widen the coverage predicate to the post-split transport vocabulary, govern every leaf it newly selects, and delete the stale row in the same change; `src/cadrumo/application/repair_integrity.py`.
- [x] `P01.S10` - Author a per-modelo requiredness capability so the profile schema can express a modelo-scoped required fact without over-demanding it; `src/cadrumo/application/user_profile/`.

## Parallelization

## Verification
