---
tags:
  - '#plan'
  - '#current-schema-only-purge'
date: '2026-08-10'
modified: '2026-08-10'
body_hash: 'sha256:cbb65139a86f552a7deccaa518c3dd547642bd7392fd822064789c26859ae217'
tier: L3
related:
  - '[[2026-07-09-compatibility-lifecycle-adr]]'
  - '[[2026-05-06-secure-persistence-enforcement-adr]]'
  - '[[2026-06-10-zero-legacy-purge-research]]'
---

<!-- RETIRED: P02 -->

# `current-schema-only-purge` plan

## Description

Delete every confirmed read-tolerance, missing-marker default, and bare-payload
coercion that permits this pre-release application to hydrate or persist anything
other than its current schemas. The compatibility-lifecycle decision governs exact
current-version refusal and preservation of dormant forward-only lineage controls.
The secure-persistence decision governs fail-closed encrypted parsing and requires
schema mismatches to stop before key derivation or decryption.

This plan does not delete empty upgrader registries, regime gates, durability-floor
checks, or future-version refusal scaffolds because they read no obsolete shape. It
does not reinterpret AEAT regulatory revisions or external-source variability as
application legacy. Workflow action-detail compatibility remains exclusively owned by
the CLI action-envelope plan and is not duplicated here.

Version numbers appearing in this plan's Phase headings and Step rows are the
values this document ASSERTED when it was authored. They are a record of intent,
never an assertion about HEAD, and they must not be read as evidence that the
tree carries them. The profile phase is the worked example and it is worth
stating exactly: the plan says "version 4", the record's field defaulted to 1,
and the canonical user-profile schema in fact declares 5. All three numbers were
different, and the plan's was wrong when it was written -- not stale, wrong.

That is why every implementing change reads the current version from its schema
authority instead of inlining a literal at the call site. Had the remedy been
"set it to 4" the plan's own error would have been compiled into the code. A
literal 5 written today would be the same defect one revision later, and the
gate this plan installs found exactly that shape: hardcoded version literals in
seeded fixtures, refused the moment exact equality replaced the ceiling.

## Steps

## Wave `W01` - Pin domain records to current schema

Eliminate pre-current domain hydration and bare catalogue coercion before tightening
encrypted storage boundaries.

### Phase `W01.P01` - Require User Profile schema v4

Make live profile records and immutable snapshots accept exactly the canonical version
4 schema.

- [x] `W01.P01.S01` - Require exact schema id and schema version 4 for UserProfileRecord and UserProfileSnapshot; `src/cadrumo/domain/user_profile/_values.py`.
- [x] `W01.P01.S02` - Stamp newly created profile records explicitly with the canonical schema version; `src/cadrumo/application/user_profile/_lifecycle.py`.
- [x] `W01.P01.S03` - Prove current profile schema hydration and non-current marker refusal; `src/cadrumo/domain/user_profile/tests/test_payload_schema_identity.py`.
- [x] `W01.P01.S24` - Refuse a persisted profile payload that omits schema_version at both profile read boundaries, rather than making the field required. Required-ness was NOT taken because 229 of the 231 construction sites are in-memory test and harness constructions that are not the defect, while the defect is bytes hydrating as current. What required-ness would still buy, and what this row therefore does not deliver, is making the unstamped state unconstructable in memory as well as unreadable from disk; `src/cadrumo/application/user_profile/_repository.py at both the record load and the snapshot load, never in the shared SecureBoundRepository whose generic path serves other namespaces`.

### Phase `W01.P03` - Pin the active bucket pointer format

Require the exact current active-profile pointer marker at the TOML boundary.

- [x] `W01.P03.S04` - Define and require the exact current BucketPointer schema marker; `src/cadrumo/core/_bucket_pointer.py`.
- [x] `W01.P03.S05` - Prove current BucketPointer round trips and non-current marker refusal; `src/cadrumo/core/tests/test_bucket_pointer.py`.

### Phase `W01.P04` - Remove InvoiceCatalogue bare-payload coercion

Require the canonical invoices wrapper while preserving the explicit construction API.

- [x] `W01.P04.S06` - Delete mapping-without-invoices coercion from InvoiceCatalogue validation; `src/cadrumo/domain/invoices/_models.py`.
- [x] `W01.P04.S07` - Prove serialized catalogues require the canonical invoices wrapper; `src/cadrumo/domain/invoices/tests/test_catalogue.py`.

## Wave `W02` - Require cryptographic and persistence markers

Require every current storage discriminator at parsing time and before cryptographic
use without changing forward-only lineage machinery.

### Phase `W02.P05` - Harden encrypted wrapper markers

Make all encrypted wrapper format claims explicit and exact before key access.

- [x] `W02.P05.S08` - Require and explicitly write the exact current CipherEnvelope marker; `src/cadrumo/adapters/persistence/storage/envelope/_envelope.py`.
- [x] `W02.P05.S09` - Prove CipherEnvelope marker refusal occurs before master-key access; `src/cadrumo/adapters/persistence/storage/envelope/tests/test_cipher_envelope_version_gate.py`.
- [x] `W02.P05.S10` - Require and preflight the exact current WrappedMasterKey marker before decryption; `src/cadrumo/adapters/persistence/storage/master_key/_recovery.py`.
- [x] `W02.P05.S11` - Prove wrapped-master-key marker refusal precedes real unwrap; `src/cadrumo/adapters/persistence/storage/master_key/tests/test_recovery.py`.
- [x] `W02.P05.S12` - Require explicit current encrypted-bundle envelope payload and KDF markers; `src/cadrumo/application/user_profile/_bundle_encryption.py`.
- [x] `W02.P05.S13` - Prove encrypted-bundle marker refusal and current passphrase round trip; `src/cadrumo/application/user_profile/tests/test_bundle_export.py`.
- [x] `W02.P05.S25` - Gate the encrypted-bundle kdf_version marker against the current Argon2 version, promoting that version onto the master-key package facade as the precondition; `src/cadrumo/application/user_profile/_bundle_encryption.py and the master-key package facade that must export the Argon2 version constant`.

### Phase `W02.P06` - Harden local custody metadata

Require current index KDF and key-schedule markers on every local custody read and
write.

- [x] `W02.P06.S14` - Require and explicitly write the exact current SecretIndex marker; `src/cadrumo/adapters/persistence/storage/secret_store/_secret_store.py`.
- [x] `W02.P06.S15` - Prove missing and non-current secret-index markers refuse real store operations; `src/cadrumo/adapters/persistence/storage/secret_store/tests/test_secret_index_version_gate.py`.
- [ ] `W02.P06.S16` - Require the exact current KdfParameters version marker; `src/cadrumo/adapters/persistence/storage/master_key/_master_key_records.py`.
- [ ] `W02.P06.S17` - Stamp current KDF markers during key mint and recovery; `src/cadrumo/adapters/persistence/storage/master_key/_master_key.py`.
- [ ] `W02.P06.S18` - Prove file-fallback key loading refuses missing and non-current KDF markers; `src/cadrumo/adapters/persistence/storage/master_key/tests/test_master_key_file_fallback.py`.
- [ ] `W02.P06.S19` - Make BucketManifest key_schedule mandatory; `src/cadrumo/adapters/persistence/storage/bucket/_manifest.py`.
- [ ] `W02.P06.S20` - Prove real manifest reads require and preserve the current key schedule; `src/cadrumo/adapters/persistence/storage/bucket/tests/test_manifest_io.py`.
- [x] `W02.P06.S26` - Make the master-key KDF preflight model require a real version, replacing the optional-and-defaulting-to-absent field that lets a marker-less file pass the check the preflight exists to perform; `src/cadrumo/adapters/persistence/storage/master_key/_master_key_records.py preflight model and its single read call site, with no writer or derivation path touched`.

## Wave `W03` - Close the Modelo 303 observation write boundary

Prevent any official Modelo 303 observation from persisting without its resolved typed
result disposition.

### Phase `W03.P07` - Require disposition before persistence

Fail official Modelo 303 observation writes before repository mutation when
`result_disposition` is absent.

- [ ] `W03.P07.S21` - REOPENED AFTER REVERT, AND ITS HOLE FEEDS A LIVE DEFECT. Refuse an official Modelo 303 observation persisted with no resolved result disposition, at a boundary where the legitimate and under-declaring populations can actually be told apart. Two attempts at the envelope boundary both over-refused because the carry-casilla condition collapses to is-an-M303-observation and the legitimate population carries no header either. THIS ROW AND THE WALLET-GATE ROW ARE ONE HOLE FROM TWO ENDS: this end lets a non-canonical envelope be written because the normalise flag defaults off, and that end converts the resulting unreadable envelope into a proven first-period zero. The hole was rated a future-bypass risk and is in fact feeding a live laundering path; `boundary to be determined, NOT prepare_observation_envelope which has been tried twice and reverted. The static caller-side gate is now justified as cutting supply to a live defect rather than as closing a future bypass`.
- [x] `W03.P07.S22` - Require Modelo 303 result_disposition before any filing persistence write. Cause of the two failures standing against this row is now established and it is NOT this row. Both frames come from one pre-existing red test that calls persist_filed_revision directly, passing an observation repository and no disposition, so the guard in its ORIGINAL downstream position refused it identically. The hoist moved only where the refusal is raised, from after five repository writes to before them, which is what the row asked for and which improves that test's failure rather than causing it; `src/cadrumo/application/modelo/_revision_persistence.py and the extracted guard in _filed_revision_observation.py`.
- [ ] `W03.P07.S23` - Prove under-declared Modelo 303 observations are refused and current dispositions round trip; `src/cadrumo/application/calculations/tests/test_m303_carry_ingress.py`.
- [ ] `W03.P07.S27` - Resolve the red prorrata settlement-writeback test, which calls persist_filed_revision directly with an observation repository and no disposition. BLOCKED ON the ADR amendment: it is red for the same root cause the amendment exists to decide, a filing-boundary requirement sitting on a caller that is not filing, and a local repair here would paper over exactly that question; `src/cadrumo/application/calculations/tests/test_prorrata_regularizacion.py and whatever the amendment rules the filing requirement should key on`.
- [x] `W03.P07.S28` - Establish what the M303 carry normalisation path actually is, given that production READERS call it directly and are then required to supply a fact only a filing produces. The iva compensation history module, the annual partition loader and the iva wallet gate all call the normalise or validate functions without the door opt-in, so the gated ingress is not a filing boundary either and the remedy of moving the screen inside it is not available; `src/cadrumo/application/calculations/_m303_carry_ingress.py and its direct callers in _iva_compensation_history.py, _iva_compensation_annual_partition.py and modelo/_iva_wallet_gate.py`.
- [x] `W03.P07.S29` - SEVERE AND LIVE, ESTABLISHED NOT INFERRED. An operator CLI verb records a Modelo 303 local observation with no modelo restriction and without the normalise flag, which defaults off, so the envelope is non-canonical by construction. The wallet gate loads by modelo and period with NO source-kind filter, hits the fixed-point assertion, swallows the refusal, and returns nothing. Its single consumer does not branch and passes that nothing through alongside an activity-start first-period proof, so an uninterpretable envelope becomes a proven first-period zero on a compensacion. THE FIX IS ADDITIVE AND DELIBERATELY NARROW. It cuts the one link that cannot over-scope, which is the swallowed refusal being indistinguishable from a genuine absence at the consumer, and it leaves a genuine absence still proving first period because that is the population an additive change must not touch. IT DOES NOT CLOSE THE OTHER FIVE MOUTHS of the same collapse and must not be read as the resolution of them, which is a separate row on the shared return type. The two prior reverts in this campaign were both RESTRICTIVE changes at shared boundaries whose legitimate population nobody had counted, and additive is what structurally cannot repeat that. The measurement that makes the containment real: the resolver has exactly one caller and the activity-start predicate exactly two, all three inside this one module, so no shared signature and no consumer sweep is involved. Correct in the same commit the production comment at the flag consumer which asserts that only the genuinely-absent case is mapped to zero, because that sentence is precisely what the code does not do. THIS ROW AND THE REVERTED DISPOSITION-SCREEN ROW ARE ONE HOLE FROM TWO ENDS: that end leaves the write side open, this end launders the consequence. The reverted screen would NOT have caught this path anyway because it scoped to official sources and this write stamps OPERATOR_MANUAL, so the write side currently has no guard at all for any source kind and the eventual static gate must be scoped by what a caller writes into a shared persistence door rather than by source-kind officialness. The close condition includes proving a genuine absence still proves first period; `src/cadrumo/application/modelo/_iva_wallet_gate.py at the swallow and its single consumer, and the false only-the-genuinely-absent-case comment in _iva_wallet_reconciliation.py`.
- [ ] `W03.P07.S30` - Land a STATIC author-facing gate on the shared observation door. THE PROPERTY IS SETTLED AND NAMES NO MODELO: every production caller of the door passes the carry-ingress opt-in EXPLICITLY rather than relying on its default. Modelo 303 falls out of it because the compliant callers express the modelo condition in their own argument, which is where that knowledge belongs. THE CENSUS IS MEASURED, NOT ASSUMED, and it is the row's real deliverable. An earlier figure of fourteen call sites was the tests-included number and made this read as a large sweep. Production writers number FOUR and one of them is the door itself. The repository delegation forwards the flag and IS the door. The filed-revision observation writer passes the flag conditioned on its own work unit modelo. The live filed-observation persistence passes it unconditionally. The operator local-observation verb passes nothing and takes the default, and that verb is the writer that produced the live laundering defect, so two of the three real callers were already correct and neither had to be told. RESOLVED FORK, take option one and re-decide nothing: land the gate with the single known violator EXPLICITLY EXEMPTED, its reason stated and dated, and open the exemption as its own row. The gate then holds against NEW violators from the moment it lands, which does not wait on an operator-population question, and the violator becomes a visible justified entry instead of an invisible default. DO NOT REMEDIATE THE VIOLATOR INSIDE THIS ROW. Making that verb pass the opt-in requires every operator-recorded Modelo 303 observation to satisfy an ingress that accepts only official-AEAT or app_filing provenance and therefore refuses OPERATOR_MANUAL by design, so the verb would begin refusing a class of write it accepts today, over a population that depends on how operators actually use it and cannot be counted from the code. Both prior reverts in this campaign were that shape. Both runtime placement candidates remain measured out by the 2026-08-10 ADR amendment and neither may be re-proposed, and the validator remains a fixed-point assertion that runs normalisation as an oracle and returns the ORIGINAL envelope, so it cannot be repurposed to repair what it refuses. State a control proving both compliant callers still pass, and do not close on the refusal firing alone; `src/cadrumo/application/calculations/_observations_repository.py as the door, its three production callers, and a static gate under src/cadrumo/tests/`.
- [ ] `W03.P07.S31` - Stop the wallet refusal telling a taxpayer that no evidence exists when evidence exists and could not be read. MEASURED AND THE ROW IS SHARPER THAN DIAGNOSABILITY. The swallow no longer launders a zero, so this is not a correctness row about the value. It is that the refusal the taxpayer receives is FACTUALLY WRONG for this population. The unreadable case reaches the missing-authority branch of the domain reconciler, whose reason reads that no AEAT wallet observation or local recurrence is available for Modelo 303 prior compensation. An observation IS available. It was found, it could not be interpreted, and the operator is told the opposite of what happened. THE POPULATION REACHING THE CATCH IS MEASURED AND SMALL. The catch wraps the fixed-point carry validator, which refuses an envelope carrying no disposition or no normalised pair, and only writers that skip the normalise opt-in produce one. Of the production writers, the live capture passes the flag unconditionally, the local filing flow passes it for Modelo 303, and only the operator local-observation verb passes nothing. This is a pre-release tree with no legacy rows, so in ordinary operation nothing but operator-recorded rows reaches this catch. The disconfirming clause therefore does NOT fire and propagation would not break a live path. PROPAGATION IS STILL THE WRONG SHAPE. Raising out of the resolver would convert a returned blocked decision into an exception on a shared lazy path, which is restrictive and changes control flow for every caller to fix a message for one population. THE RULED SHAPE IS ADDITIVE AND HAS PRECEDENT IN THE SAME SIGNATURE. The first-period flag is already a caller-asserted fact the domain cannot derive, so an equivalent caller-asserted fact saying evidence was present but unusable follows the established pattern, defaults to false, and changes no existing behaviour. Only the reason on the misinformed population moves. TWO CONSTRAINTS ON WHOEVER BUILDS IT. The domain reason strings are plain English embedded in the reconciler rather than locale keys, so a new reason inherits that shape and MUST NOT silently extend it further than the one string it needs, nor be quietly fixed here as a side quest. And the new fact must state what was observed rather than name a mechanism, because a reason mentioning an ingress validator tells a taxpayer nothing. The disconfirming observation that decides the build: if the reason channel is not actually rendered to the operator on this path, the change fixes a field nobody reads and the row must move to whatever surface the operator does see; `src/cadrumo/domain/iva_compensation/_reconciliation.py at the missing-authority branch, its caller in application/calculations/_iva_wallet_reconciliation.py, and the evidence resolver in application/modelo/_iva_wallet_gate.py`.
- [x] `W03.P07.S32` - ANSWERED BY MEASUREMENT AND THE ANSWER IS THAT THE QUESTION WAS AIMED AT THE WRONG END. A source-kind filter on the carry gate's read is NOT the remedy and must not be built. The gate loads by a natural key of modelo and filing period, so there is exactly ONE observation slot per modelo and period, and the strict carry validator already refuses anything that is not official-AEAT or app_filing provenance. Since the laundering fix landed, an admitted operator-manual row blocks rather than laundering, so admission itself now costs a refusal rather than a wrong number. Filtering the read would change which refusal fires, not whether the taxpayer is protected. THE EXPOSURE IS AT THE WRITE AND IT IS ROWED SEPARATELY. Because there is one slot and the operator write path performs no read-before-write, no existing-row check and no provenance precedence, a later operator-manual write to a modelo and period that already holds official or app_filing evidence takes the slot. A read-side filter cannot help there because by the time the gate reads, the row it would have preferred is already gone from that key. Close this row on the measurement rather than on a change, and record that the disconfirming clause was never reached: no legitimate flow depending on the gate reading a non-official observation was found, but the question stopped mattering once the exposure moved to the other end; `measurement only, no production change; the write-side exposure carries its own row`.
- [x] `W03.P07.S33` - Establish which onboarding paths set the profile activity-start date and whether any completed onboarding can produce a profile without it. This is the code-answerable half of a question that was asked as a frequency and correctly refused as one, because how many real profiles carry the field is about taxpayer data rather than about branches. If every path sets it then ordinary-for-a-genuine-new-filer follows structurally with no real profile needed, and if some path leaves it unset that is a second finding about onboarding rather than a frequency estimate. Do not block the live-defect row on this and do not convert the answer into a severity label. The disconfirming observation that decides this row: if the field can be set after the fact by an ordinary profile edit rather than only at onboarding, enumerating onboarding paths does not answer the question at all and the row must say so; `the profile creation and censo application paths that write the activity-start field, read only`.
- [ ] `W03.P07.S34` - Make a complexity-allowlist entry fail when its key names something that no longer exists. LANDED. The allowlist enforced that an entry states a reason and a score and never that its key still names anything, so an acceptance could outlive its subject indefinitely while presenting as reviewed, and whatever later took that name would inherit an exemption nobody granted. One entry had already outlived a rename, so the class arrived before the gate did. A census of all 124 keys found exactly that one stale, which is why this is gate-hardening rather than a cleanup sweep. THE CHECK DISTINGUISHES STALE FROM UNRESOLVABLE AND THAT DISTINCTION IS THE ROW. A dotted key whose qualifier is defined locally while the member itself is inherited from a base in another module is legitimate and must not red. Without the split the gate would fire on correct entries, someone would loosen the match to quiet it, and it would go back to enforcing nothing, because a gate that reds on valid input gets weakened rather than obeyed. TWO EARLIER RESOLVERS WERE WRONG AND THE ROW RECORDS BOTH, because a record showing only the version that worked teaches nobody which attempts to skip. The first compared BARE names, so every Class-dot-method key read as missing and it reported 26 false positives. The second compared QUALIFIED names and still reported 2, and those two were not stale at all: both classes exist and the method is inherited from a shared base, which is exactly the disconfirming clause this row was written with, arriving in the real corpus rather than as a hypothesis. Both errors were caught by dry-running the resolver against the committed file rather than by reasoning about it, which is the only reason neither shipped. The gate carries a positive control proving the historical stale key still classifies stale, validates the inherited bucket by a PROPERTY rather than a count so nobody is trained to update a constant, and states its own limit: an AST-name resolver sees a decorated target fine but would red on a target that exists only at runtime, of which there are none today; `dev/audit/tests/test_complexity_allowlist.py`.
- [x] `W03.P07.S35` - Resolve two producers of the local Modelo 303 compensation recurrence, one of which still contributes to the result after a caller has declared it must not. THE SWITCH TURNS OFF THE VALUE AND NOT THE INFLUENCE. Found by semantic sweep and confirmed by targeted census, not by name. The generic extractor in the binding prefill module and the strict envelope resolver in the wallet gate both build the same recurrence carrier, and the reconciliation function selects between them with a boolean that defaults to using the generic one. The wallet gate passes it false on purpose, documented as stopping a legacy generic observation from regaining authority by fallback. THE SELECTION IS DOWNSTREAM OF THE CALL. The generic extractor executes UNCONDITIONALLY before the flag is consulted, so it runs a repository read and a full history reconstruction even on the strict path that has declared it must have no authority. Its recurrence is discarded there. ITS PREFILL REPORT IS NOT, and rides out on the returned reconciliation report regardless, so a resolver a caller switched off still contributes to the artefact that caller receives. NOT YET MEASURED and it must be measured before any change: whether that report is load-bearing for any consumer, and whether the discarded read is merely wasted work or an observable difference. Those are different findings with different severities and this row deliberately refuses to choose between them. The aggregation rule names exactly one canonical mechanism for this channel, the IVA wallet decision, which is why two producers behind a default-on boolean is a canonical-home question rather than a tidiness one, and it sits directly on the compensacion path the laundering row was about. THE DISCONFIRMING OBSERVATION IS THIS ROW'S DECISION PROCEDURE RATHER THAN COMMENTARY: if the two producers have genuinely different constraint shapes, the strict one refusing what the generic one accepts, then they are NOT substitutable and the remedy is to move the selection BEFORE the work rather than to collapse them. Merging two resolvers with different contracts is how the substitutability trap gets sprung. Reorder rather than merge is the expected answer and remains this row's job to establish rather than to assume; `src/cadrumo/application/calculations/_iva_wallet_reconciliation.py at the unconditional extract and the flag, and the two producers in _binding_prefill.py and modelo/_iva_wallet_gate.py`.
- [ ] `W03.P07.S36` - Surface the activity-start fact as UNCONTRASTED wherever it grounds a first-period zero, and do not build a contrast gate. THE DISCONFIRMING CLAUSE WAS MEASURED AND IT DOES NOT HOLD IN THE FORM IT WAS WRITTEN, and the real answer kills a different candidate. The clause supposed a genuine first filer might have no AEAT censo record to contrast against. That premise is false and irrelevant: a taxpayer who has started an activity has filed an alta, so a censo situation exists. What does not exist is anywhere in this codebase to put its date. The censo certificate model carries fiscal address, residence condition, representative identifiers, tax situation, periodic obligations and certified activities, and the certified activity carries a description, an IAE epigrafe and a local, WITH NO DATE ON EITHER. The parser is one function returning that model, and the censo reconciliation writes no activity-start fact. So the contrast is unavailable to EVERY taxpayer rather than to a first-filer population, which kills the require-a-contrast candidate on a stronger ground than the one the row anticipated, and kills the keep-it-unconditional candidate's stated REASON while leaving its conclusion standing for a different reason. THE REMEDY IS THE HONEST ONE. The value is operator-declared, the shipped operator text already says so in four locales, and the application cannot check it, so a first-period zero grounded on it must SAY it rests on an unverified declaration rather than imply the authority agreed. Absence stays fail-closed and unchanged, because the predicate already returns false on a missing date. The disconfirming observation that decides this row: if surfacing the advisory would fire on every ordinary first filing rather than only where the fact actually grounds a zero, it is noise that trains operators to ignore it and the row must narrow the trigger to the grounding site instead of the fact; `the first-period predicate and its refusal surface in src/cadrumo/application/modelo/_iva_wallet_gate.py, and the four locale catalogues`.
- [ ] `W03.P07.S37` - Decide whether the censo certificate model should carry the alta date it currently drops. Found while measuring whether an operator-declared activity-start date could be contrasted against the authority, and the answer was that nothing in the ingest chain models such a date. The certificate model carries fiscal address, residence condition, representative identifiers, tax situation, periodic obligations and certified activities, and the certified activity carries description, IAE epigrafe and local only. So the profile fact that drives obligation start year, pago-fraccionado eligibility and a first-period zero on the compensacion can never be reconciled against the document the operator supplies, no matter how complete that document is. GROUND THIS BEFORE BUILDING IT. Whether the physical AEAT certificado de situacion censal states an alta date per activity or per obligation is a claim about an official surface and MUST be confirmed against a real specimen or the bundled corpus rather than assumed from how the form is remembered, because a parser field added for a line the document does not carry is worse than the gap. If the document does carry it, extracting it turns an unverifiable declaration into a reconcilable one and is the precondition for any future contrast gate. The disconfirming observation that decides this row: if the certificate states an alta date for the CENSAL registration rather than for the economic ACTIVITY, it is not the same fact the profile field means and wiring it across would ground a calculation on the wrong date; `src/cadrumo/domain/censo/_certificado.py, src/cadrumo/adapters/inbound/censo/_parser.py and a real certificate specimen`.
- [ ] `W03.P07.S38` - Decide what happens when a second observation is written to a modelo and period that already holds one, and whether provenance may be downgraded silently. Measured, not assumed. Observations are keyed naturally by modelo and filing period, so there is exactly one slot per period. The shared persistence door performs an unconditional upsert on that natural id. The operator local-observation verb computes the key and writes with no read-before-write, no existing-row check and no provenance precedence, so an operator-manual row can take a slot that previously held AEAT-official or app_filing evidence. NOT MEASURED AND IT DECIDES THE SEVERITY: whether the encrypted secure-object substrate retains prior versions of an overwritten key. If it versions, the official evidence survives and this is a SHADOWING problem where the wrong row is selected. If it does not, the official evidence is DESTROYED by an ordinary operator command and the loss is unrecoverable. Those are different findings with different remedies and this row must establish which before proposing either. Candidate remedies to weigh only after that: refuse a write that would downgrade provenance, require an explicit replace intent, or make the key carry provenance so the rows coexist and selection becomes explicit. The disconfirming observation that decides this row: if an operator legitimately needs to correct a mistaken official-derived observation, an outright refusal removes their only repair path, so the remedy must distinguish correcting a row from silently displacing better evidence; `src/cadrumo/application/modelo/_local_observation_actions.py, the shared observation door in application/calculations/_observations_repository.py, and the secure-object substrate's overwrite behaviour`.
- [ ] `W03.P07.S39` - Refuse a write that would displace official AEAT evidence with non-official evidence, on BOTH edges, and name the replace intent in the refusal. RULED: refuse both the operator-manual and the app-filing edge. The frequency of the risky operator order is unmeasured and did not need measuring, because displacing a captured justificante with a local recalculation is an evidence downgrade in every branch. If the recalculation is right the operator must re-file with AEAT and re-pull, so the local recalc is not the authority. If they have not re-filed, a local recalculation displacing captured AEAT evidence is the silent under-declaration the standing rules forbid. The cost asymmetry decides it without operator data: a wrong refusal costs friction on a path that has a documented escape, a wrong permission costs unrecoverable AEAT evidence on a path measured to exist. THERE IS NO SINGLE WRITE DOOR AND THIS IS THE OBSTACLE THAT SHAPES THE BUILD. Two disjoint persistence paths reach the store. The operator verb goes through the repository save-observation method onto the base save. The local filing flow and the live justificante capture both prepare an envelope and then persist through the shared observation-and-history helper, which bypasses save entirely and writes through a batch of prepared secure-object writes so the observation and its IVA history land in one transaction. A guard on the base save would therefore miss BOTH official writers and the app-filing writer, catching only the operator verb, and would look complete while covering one of the two edges it was ruled to cover. The guard must sit on a check both paths call, and the batch path must call it before its writes are prepared rather than inside the transaction. FOUR WRITERS, enumerated: two official on the live capture path stamping the justificante and live-capture kinds, two non-official being the local filing flow stamping app-filing and the operator verb stamping operator-manual. The provenance taxonomy has MEMBERSHIP and NO ORDERING, so only the existing-is-official and incoming-is-not comparison is decidable today and a general downgrade rule would invent an ungrounded axis. Rejected candidates, recorded because a rejected option without its argument reads as an oversight on a data-loss surface: a provenance-bearing key changes the natural id and is a persisted-shape change on a live store, and a bare replace intent with no refusal protects only operators who already suspect the risk. THE REFUSAL MUST NAME THE REPLACE INTENT and must distinguish the two edges in what it says, because the same mechanism meets two different operator situations and a message written for one will confuse whoever meets the other. That means four locale catalogues and an operator-facing intent flag, so this is a build rather than an edit; `src/cadrumo/application/calculations/_observations_repository.py and _iva_compensation_history.py at both persistence paths, the operator verb in application/modelo/_local_observation_actions.py, and the four locale catalogues`.

## Parallelization

Waves remain ordered. Within W01, the profile, pointer, and invoice Phases are
file-disjoint and may execute in parallel. Within W02, each encrypted format may be
implemented independently, but every production Step precedes its paired real-behavior
test and KDF record validation precedes KDF minting. W03 waits for the current-schema
domain and storage boundaries so its final integration proof measures the completed
state. No worker may touch workflow action compatibility or dormant lineage machinery.

## Verification

- Parse current serialized records through production loaders and reject missing,
  pre-current, and future markers without coercion.
- Prove cryptographic marker refusal occurs before key derivation or decryption and
  retain real current-format positive round trips.
- Prove an under-declared Modelo 303 write leaves repositories unchanged, followed by
  a successful disposition-bearing round trip.
- Use production imports and real storage adapters; no fakes, mocks, stubs, patches,
  monkeypatches, skips, or expected failures.
- Run focused pytest, path-scoped Ruff and strict BasedPyright, plan and Vault checks,
  and a final semantic plus lexical sweep showing no listed compatibility branch or
  default remains.
- Complete a fresh-context code review before closing the campaign.
