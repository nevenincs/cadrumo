---
tags:
  - '#plan'
  - '#ci-lane-deconflation'
date: '2026-08-05'
tier: L2
related:
  - '[[2026-07-21-ci-discipline-adr]]'
  - '[[2026-07-20-ci-speed-redesign-adr]]'
  - '[[2026-06-01-registry-period-code-union-cli-boundary-adr]]'
  - '[[2026-08-05-ci-lane-deconflation-step-check-attribution-audit]]'
modified: '2026-08-31'
body_hash: 'sha256:989b22a903e39f6f27f19801757d3afda3e287c53487d28c2ace7eb7054c4532'
---

<!-- RETIRED: S01, S10, S181, S184, S186, S189, S191, S193, S194, S197, S199, S201, S203, S205, S207, S209, S211, S213, S215, S217, S219, S221 -->

# `ci-lane-deconflation` plan

## Description

Close the open work left by the CI lane deconflation campaign and the defects
it surfaced. Three authorizing records feed this plan. The ci-discipline ADR
governs P01 and P02: its D6 amendment added the two new tier rows (T1-docs and
T1-frontend) and corrected the false claim that the carve-out was shared. The
ci-speed-redesign ADR governs the budget constraints those lanes sit inside.
The registry-period-code-union ADR governs P03, whose D6 amendment authorised
the filing-period type split that this campaign surfaced as a live regression.

The campaign itself is landed. What remains is verification that could not be
performed locally, backlog that enrolment made visible for the first time, and
findings correctly left unfixed because they need grounding this campaign did
not have.

## Steps

### Phase `P01` - Lane correctness and first-run verification

The two new lanes both failed their first real execution and their fixes are unverified. Nothing in this phase can be called done from a local run: the claim being tested is that these lanes behave correctly on a runner, which is a different claim from the code being right.

- [x] `P01.S02` - Verify the frontend lane passes on a runner under Node 22, its first run refused npm ci because jest-dom 7.0.0 requires node 22 and the manifest under-declares at 20.19; `.github/workflows/frontend.yml`.
- [x] `P01.S03` - Dispatch ci-full for its first ever execution and record the result, its run count is zero so every claim about its steps is structural rather than observed; `.github/workflows/ci-full.yml`.
- [x] `P01.S04` - Move the ci-full docs build above the tooling-gates step so the terminology gates that resolve to built HTML get their artefact, blocked until the legal-entry defect stops masking the dependency; `.github/workflows/ci-full.yml`.
- [x] `P01.S05` - Decide what to do about the already-pushed branch, a peer snapshot pushed it so the original decision is moot and the live question is whether the published history needs remediation; `origin/main`.

### Phase `P02` - Enrolled-lane backlog closure

Enrolling the integration suite and the dev tooling gates exposed accumulated rot that had never been visible. These rows close it and lift the two non-blocking guards. A guard left non-blocking indefinitely becomes decorative, so each carries the condition for flipping it.

- [x] `P02.S06` - Close the entrypoints CLI integration failures, measured at 18 across 8 modules with 138 passing, and regenerate the set from two intersected runs rather than one; `src/cadrumo/entrypoints/cli/tests`.
- [x] `P02.S07` - Reshape overview.calendar profiles to a per-profile summary with detail behind a per-profile call, the resource_link this row first prescribed is refused because resolution re-runs a read verb over persisted state while this verb is computed from a clock; `src/cadrumo/entrypoints/mcp`.
- [x] `P02.S08` - Measure the dev tooling gates at a clean HEAD, the local count of 55 is contaminated because 32 belong to an uncommitted peer legal entry and the true figure is nearer 23; `dev/audit, dev/deploy, dev/env, dev/registry, dev/docs`.
- [x] `P02.S09` - Flip continue-on-error off the integration parallel step once its backlog closes, the step is deterministic so it can go blocking independently of the serial pass; `.github/workflows/ci-full.yml`.
- [x] `P02.S22` - Author the ADR reshaping the overview.calendar payload, the resource_link remedy the gate names cannot apply to a computed verb with no persisted record and the irreducible floor leaves only 622 characters of headroom; `src/cadrumo/entrypoints/mcp`.
- [x] `P02.S23` - Fix thin_output_schema growing the schemas it thins, its oneOf inline-or-linked shape duplicates the property body so thinning a shared-defs verb enlarges it; `src/cadrumo/entrypoints/mcp`.
- [x] `P02.S28` - Convert the two wall-clock budgets guarding the integration serial step so that a process CPU-time bound becomes the failure condition while the wall-clock threshold is retained as a loud advisory, because the budgets were measured on a box shared with the dev machine and the agent fleet and flake under load regardless of code quality, and because a straight conversion would delete the only bound that catches a genuine share hang given that a test blocked on I/O burns almost no CPU, applying the repository control-plane invariant that perf gates assert process CPU-time with wall advisory only rather than inventing a remedy, noting the perf marker is not available as an escape because it is absent from _CI_INCAPABLE_MARKERS and the perf lane is path-scoped to dev/packaging, and covering the two named budgets only while inheriting the load stamp owned by the pytest ceiling row; `the two integration serial budget tests and .github/workflows/ci-full.yml`.
- [x] `P02.S29` - Stamp the host load reading at fire time into the pytest timeout failure output, and do NOT convert that ceiling to process CPU-time, because pytest-timeout offers only signal and thread methods and has no CPU notion at all so the conversion is not expressible in configuration, and more importantly because a wall-clock bound is the CORRECT instrument here rather than a tolerated one, since the thing this ceiling exists to kill is a wedged or deadlocked test and a deadlocked process burns almost no CPU so a CPU-time bound would never fire on the exact failure the ceiling was added for, which is the same share-hang argument that saved the wall threshold on the integration budgets except stronger because here wall is the only instrument that works, leaving the real defect as interpretability rather than instrument choice since nothing in a timeout traceback records host load so a legitimately slow test under saturation is indistinguishable from a genuine hang and reaches a lead ledger as a plausible fictitious defect with a traceback attached; `the pytest timeout failure-reporting hook and pyproject.toml`.
- [x] `P02.S30` - Give the operator surface contract suite one execution-scope marker by relocating its single integration test to a sibling live module, because the module carried seventeen function-level unit decorators alongside one integration test so hoisting unit to module level would have given that test two execution markers, and the taxonomy allows exactly one per module just as firmly as it forbids none, and the resulting red was unowned while blocking every lead from reading a clean ratchet; `src/cadrumo/application/operator_surface/tests/test_contract.py, src/cadrumo/application/operator_surface/tests/test_contract_live.py`.
- [x] `P02.S31` - Enrol the unreachable dev/registry/tests modules by adding not external_tool to the test-dev-tooling marker expression FIRST, then adding the path, then removing the three now-redundant ignore directives in that order, because test_workbook_parity is the only file there carrying external_tool while the expression excluded only resident_service, so the ignore directives are inert today ONLY because nothing collects the directory and the addopts one would become the sole guard the instant the path is enrolled, and because a marker states its reason while a path ignore states nothing which is what the reachability gate's third assertion asks for, and this is CONTAINMENT rather than cleanup because a live campaign landed twelve modules into that directory in a single day and every hour it stays unenrolled another arrives reading as coverage to its author while executing nowhere; `justfile and pyproject.toml`.
- [x] `P02.S32` - Triage the reds that enrolment exposes, recording separately what passes, what reds, and who owns each red, because a lane addition that turns the tree red for everyone without triage is worse than the hole it closes, and stating in the record that these reds are NOT evidence of carelessness by their authors because unreachability is invisible from inside the directory where the modules are well-formed and correctly marked and sit beside an init file in a proper tests package with nothing at the authoring site naming the lane list that omits them, so a triage record without that sentence becomes an accusation dressed as an inventory, and routing any failure in the render-profile module to its in-flight author rather than absorbing it; `dev/registry/tests`.
- [x] `P02.S33` - Establish how long the lane-reachability gate has been red and whether anything shipped past it, because the gate lives under src/cadrumo/tests deliberately so that nine lanes reach it and it fails in twenty-eight seconds at -n0, which means this is not a guard nobody could reach but one everybody reaches and nobody actioned, and a hard gate with no allowlist that stays red becomes a gate everyone learns to route around, the same decorative-guard decay that made the integration parallel flag permanent and loud rather than quiet; `src/cadrumo/tests/test_lane_reachability.py and the commit history of its reported paths`.
- [x] `P02.S34` - Decide how dev tooling lane reds become visible between manual dispatches, because the agent-eval tests are enrolled by test-dev-tooling and that recipe is invoked only by the dispatch-only ci-full lane, so fourteen failures including ten from a live campaign sat red for hours with no signal and were found only by a hand run undertaken for an unrelated reason, and because this is a cadence defect rather than a reachability one so enrolment cannot close it and the lane-reachability gate correctly does not report it, and because the obvious remedy is closed off by standing operator rulings that there are no scheduled runs and no nightlies and that runners are self-hosted only under expense control with local-first as the mandate rather than a workaround, leaving two honest options, either these tests become reachable from a lane leads actually run locally, or this plan states plainly that dev tooling reds are invisible between manual dispatches and names who is accountable for noticing, and the row is the choice rather than either option; `justfile and .github/workflows/ci-full.yml`.
- [x] `P02.S35` - Decide what surfaces a lane-reachability regression within minutes rather than hours, because the gate went red at 07:21 and nobody noticed for five hours despite it living where nine lanes reach it and failing in twenty-eight seconds, and because the enrolment that closed today's instance fixes one directory rather than the arrival rate, so the next test directory created outside every lane path scope repeats it exactly, and today's zero-failure result is a property of one campaign happening to write correct tests rather than a property of the void which discards whatever arrives in it, noting the remedy cannot be a scheduled run because that is refused by standing operator ruling and cannot be a hardcoded directory list because a list encodes today and detects nothing tomorrow; `src/cadrumo/tests/test_lane_reachability.py and the lane declarations it reads`.
- [x] `P02.S36` - MEASURED AND RULED. The selective local form does NOT qualify, and the reason is structural rather than a cost. Reachability across the whole dev tree is 22 test directories and 258 files, of which test-dev-tooling reaches 15 and test-dev-ci reaches 4, so only dev/docs/tests (46 files), dev/docs/apidocs/tests (1) and an empty migration directory are reached by no recipe and no workflow at all, and docs.yml invokes no pytest whatsoever. That makes the original figure of two hundred and forty-six files an overstatement by nineteen directories, because the forty-four minutes it priced was overwhelmingly the cost of re-reaching what two lanes already reach. Of the 46 remaining files, 22 reference playwright, sphinx, a docs build or a long mark.timeout, leaving 24 candidates. A bounded run over those 24 plus the apidocs directory collected 130 of 185 tests in 91.7 seconds with 16 failed and 13 errors, and 55 tests were DESELECTED by the local lane's own marker. The deselection is the ruling. Ten of the 24 candidate files are integration-marked at module level, so the local unit lane cannot select them at any price, and enrolling this directory there would leave every one of them exactly as unreached as it is today while printing green over them. The enrolment therefore cannot close the coverage gap it was proposed to close, which is a property of the markers and not of the machine. Cost only compounds it. 29 of the 130 selectable tests are currently non-passing, a never-run backlog that would red every lead's local run from the first day, and the 91.7 seconds is a floor measured while the heaviest fixture short-circuited on a HEAD-level ImportError from the CalculationRevisionId relocation rather than running its CLI-tree subprocess. Re-running once that clears would sharpen the ceiling and cannot change the direction, because the marker and backlog facts are not timing facts. The coverage half of the original question stands open and unaddressed by this row and belongs in CI, where an integration-capable lane can reach all 185. Note for whoever writes that row that this is the FIFTH remedy on this row family to be wider or blinder than the defect it targets, after a CPU bound that cannot fire on a wedge, a report hook that cannot fire on Windows, a raw descriptor write that the exit discards, and a reachability change whose cost was dominated by what it was never meant to reach; `justfile and .github/workflows/docs.yml and dev/docs/tests`.
- [x] `P02.S37` - Restore the operator-settable storage root to the root help document and return the assertion to root text, because the variable is live and settings-bound with its own documentation stating that a developer who wants the store inside their checkout sets it, so its disappearance from the rewritten root landing was a regression rather than a simplification, and because an operator cannot set what the surface never mentions while this CLI operator is frequently an autonomous agent holding only the help text and the envelope with no colleague to ask and no convention to infer from, and noting that the assertion was moved to config text in an earlier commit which followed the regression rather than correcting it and therefore encoded a live defect as the contract, and that restoring the mention needs a translated key in all four catalogues through the locales CLI rather than a bare string; `src/cadrumo/application/operator_surface/_help.py, src/cadrumo/locales/, src/cadrumo/application/operator_surface/tests/test_contract.py`.
- [x] `P02.S38` - Record the export provenance manifest writer as a deliberate superset of the shared hardened atomic-write tier rather than delegating to it, because the delegation this row originally specified would have deleted a guarantee instead of relocating one, the site refusing a pre-existing target where no core tier does since the hardened tier's O_EXCL guards only its staging tempfile while its publish step is os.replace which overwrites an existing target by definition, measured rather than read by two successive hardened writes to one path leaving the second payload, and a genuinely atomic publish-once needs os.link which this project's network-share working tree cannot reliably provide, so the docstring names os.link as the correct primitive and states why it is unavailable so the next reader who notices the duplication finds the reasoning instead of repeating the deletion, and the refusal gains the test it never had because the emit-level test must unlink the manifest before it can call emit at all which means the guard was exercised by necessity and asserted by nothing and would have survived its own removal in green; `dev/registry/_provenance_manifest.py, dev/registry/tests/test_export_tree.py`.
- [x] `P02.S39` - Delegate the export provenance manifest writer to the new core publish-once tier and delete its hand-rolled staging sequence, because the earlier decision to document that writer as a deliberate superset rested on an untested claim that this working tree cannot support os.link, and measurement shows os.link both links and refuses an existing target atomically, so the compromise that justified leaving a parallel write path in the tree never existed and the architecture boundary forbids re-implementing a write path rather than delegating to the single-writer primitive, noting the delegation must also strip the now-false unavailable-on-this-platform paragraph from the writer docstring and that the existing refusal test should keep passing unchanged since the contract is preserved and only its implementation moves; `dev/registry/_provenance_manifest.py, dev/registry/tests/test_export_tree.py`.
- [x] `P02.S40` - Give the four correctly-integration-marked conformance gates a lane that exists, by adding an integration step to the per-push workflow or by re-marking them unit where they can be. The gate audit found a cause distinct from a wrong marker. These four carry pytest.mark.integration correctly for what they are, and the per-push workflow runs no integration step whatsoever, so the marker is valid and the lane is absent. The default lane pins unit and the only integration invocation lives in a workflow that is dispatch-only, which means these gates have never run on any push at any revision. The four are the rule-surface conformance gate under agent tests, the status-frontend gate under the CLI config tests, and the self-referential-string and suggestion-command conformance gates under the CLI tests. The suggestion-command one is the sharpest instance because it asserts which command an operator is told to run, so while it does not execute a regression there degrades operator guidance silently. Decide per gate whether it genuinely crosses layers or was marked integration by habit, because re-marking a gate that really is integration would buy a green by making it run in the wrong lane; `pyproject.toml and .github/workflows/ci.yml and the four named gate modules`.
- [ ] `P02.S41` - Flip only the ci-full dev-tooling step off continue-on-error once the exact test-dev-tooling selection measures ZERO failures and ZERO errors at clean HEAD. THE _common HALF OF THIS ROW IS DONE, measured 2026-08-28: its named blocker (secure-storage W03.P08.S31) had landed and touched _config/_profile_list_cli.py rather than _common. _common.py went 1714 -> 1160 lines, under the 1250 default ceiling with no pin, no --accept-growth and no carveout, split into _period_parsing, _date_parsing, _decimal_parsing and _operator_surface_reconciliation with every consumer swept in the same change and test_cli_module_size.py green. THE ROW'S ORIGINAL PREMISE IS FALSE: it asserted _common was the sole known module-size offender, and at HEAD it is not. THE SIZE FIGURES THIS ROW ONCE RESTATED ARE OWNED BY S51, NOT HERE -- S51 re-measured to 121 findings (71 modules and 23 callables over budget, 19 and 8 stale pins) and also corrected the mute-button warning: a plain --write-baseline will NOT lift a broken ceiling, and --accept-growth is the separate opt-in. THE FLIP REMAINS BLOCKED, and ci-full.yml is untouched. NEW BLOCKER MEASURED 2026-08-31, AND IT IS ABOUT THE INSTRUMENT RATHER THAN THE LANE: the recipe runs pytest with -n workers, and an attempted measurement at HEAD did not finish. All four xdist workers reported 'node down: Not properly terminated' at 92 percent and the run was killed at the 1700-second bound (exit 124). aeat-local-execution names this exact failure -- this worktree's backing share fails under concurrent I/O, so a dead parallel run is more likely the drive than the code. Consequence for this row: its flip condition is literally ZERO failures and ZERO errors, and a parallel run on this share cannot establish that, because worker death manufactures failures indistinguishable from real ones. Do NOT read the historical 192F/98E and 184F/75E as the lane's true state either; both were parallel runs. Before any flip, obtain a SEQUENTIAL measurement (drop -n) or run it off this share, and treat any parallel count as an upper bound only; `ci-full.yml and the test-dev-tooling selection`.
- [x] `P02.S42` - Decide where the five drift census gates belong, because the instruments this fleet built to detect drift are themselves in the non-blocking tier and a census that cannot fail a build is a report rather than a gate. The five are the hex-64 redeclaration census, the identifier-noun census, the write-site census, the CLI-action census with its dispositions companion, and the variable-envelope generation gate under the registry dev tests. All sit outside testpaths, so no default invocation reaches them, and their only lanes are a tooling recipe no workflow invokes plus a continue-on-error step in a dispatch-only workflow. Choose between enrolling their directories in testpaths, which makes every local run pay for them, and adding an invoked blocking recipe that names them explicitly, and record which was chosen and why rather than leaving the reader to infer it from the configuration. Note when deciding that these gates are AST and filesystem sweeps over the source tree rather than behavioural tests, so their cost profile is collection-heavy and their failure mode is a census that silently measures nothing; `pyproject.toml and justfile and dev/tests and dev/registry/tests`.
- [x] `P02.S43` - Add the format check to the per-push blocking lane and sweep the tree that must pass it first, in that order, because ruff format --check is enforced by nothing today and the tree is far from able to satisfy it. The recipe check-format exists and is invoked by zero workflows, and the per-push Lint step runs check-style and check-relative-imports only, so the sole configured format gate is the ruff-format hook in prek.toml, which this fleet is instructed never to run because it stashes the shared tree. Two configured gates cover formatting and the intersection of what they enforce is empty, which is the same shape as a gate that exists while nothing selects it. THE ROW THIS SUPERSEDES SAID EIGHT FILES AND THAT NUMBER IS SRC-SCOPED. The recipe checks the whole repository, and measured against HEAD by excluding every file dirty in the working tree, 333 files would be reformatted, of which the eight are a subset. So the one-line workflow edit is NOT the fix on its own, because adding the step to a tree in this state reds the lane on its first run and a permanently red lane is one everyone learns to ignore, which is the failure this campaign has already documented for continue-on-error. Sequence the sweep first and the gate second, and treat the sweep as mechanical but wide, since it spans dev and src and cannot be done by pathspec while peers hold those files; `.github/workflows/ci.yml, justfile, prek.toml`.
- [x] `P02.S44` - Close the fail-open FX door, then add the automatic integration lane, then flip the flags, in that order. THE BLOCKER THIS ROW LED WITH IS DISCHARGED, measured 2026-08-12. The registry LOADS: registry verify exits 0 over 73 modelos, 565 application links and 82 verification expectations, and it was discharged by its author as the row required. THE COUNTING STEP IS DONE AND ITS ANSWER IS ZERO. The row said closing the door needs the population a refusal would catch to be counted and that counting it needs tests to reach the door. Instrumented by replacing the provider's urlopen with a recorder that raises, applied as a pytest plugin from OUTSIDE the repository so no tracked file changed, then run across the six packages that reference the provider: outbound fx, application aggregation, application invoices, application ledger, domain currency and domain invoices. Result over 2785 passing tests: ZERO live-ECB call sites reached and ZERO distinct tests reaching the door. Every test that exercises conversion injects its own fetch through the RateFetch seam, and the only default-provider construction is application invoices creation at the rate_provider-or-default site, which no test reaches without injecting. THE CONSEQUENCE IS THAT CLOSING THE DOOR IS FREE. The row assumed a population would need adjudicating before the door could close, and there is none, so the ordering collapses from five links to three. Close the door by refusing a live lookup unless an explicit opt-in is set, add the lane, flip the flags. NOTE THE MEASUREMENT'S OWN LIMIT rather than overstating it: it covers the six packages that name the provider, not the whole tree, so a consumer outside them would not have been seen. Re-run the same plugin tree-wide before closing if that matters. THE MARKER FIX REMAINS UNAVAILABLE for the reason already measured, that all four gates genuinely cross architectural layers. This row gates S40; `pyproject.toml and .github/workflows/ci.yml and the four named gate modules and src/cadrumo/adapters/outbound/fx`.
- [x] `P02.S45` - THE PROPERTY IS SELF-SEALING, AND THAT IS THE CAMPAIGN'S STRUCTURAL RESULT RATHER THAN A MISCONFIGURATION REPORT. A module that no automatic lane collects can carry a COMMITTED collection error indefinitely, and two do at HEAD. The gate that enumerates them is itself integration-marked, so the default lane never collects it, and the only workflow that would run that gate has never once passed in its entire history. The break, the gate that would catch the break, and the lane that would run the gate are all on the same unreachable side of one predicate, so nothing in the apparatus can surface the gap from inside it. That is categorically different from saying a gate is misconfigured, and it is the answer to the question this campaign opened with. Everything below is the predicate that produces it. THE PREDICATE, RESTATED AT THE ALTITUDE THE EVIDENCE SUPPORTS. This was recorded as a docs-lane defect and it is a general property of path-filtered triggers that survives fixing the docs workflow entirely. Three instances are measured in three different workflows, found by two agents working from opposite directions. The docs lane fires on docs, dev/docs and the terminology data while no docs trigger in any workflow names src/cadrumo Python sources. The agent-harness lane reaches the JSON-envelope and rule-surface gates while its trigger paths exclude the CLI tree those gates validate. And six recipes are invoked by no automatically triggered workflow at all, three of which are additionally non-blocking in the only lane that runs them. The durable statement is that a gate runs on a push only if a lane's marker expression SELECTS it and an automatic event FIRES that lane, and that failing either reports identically as absence from a FAILED list. A THIRD AXIS HIDES INSIDE THE SECOND and must be stated with it, because a recipe can pass the trigger test and still reach nothing. The dev-conformance recipe fires automatically and its marker expression does admit integration, so an axis-A reading scores it as an automatically-firing integration lane, while its invocation names only dev trees and it therefore reaches no src/cadrumo gate at all. The marker was never the binding constraint there. The invocation's own path arguments were, so any claim that a gate is reachable must name the trigger paths, the marker expression AND the invocation paths, and this repository has an instrument for the marker axis and none for the other two; `the plan and .github/workflows and justfile as the mapping's subject`.
- [x] `P02.S46` - Classify the twelve unclassified open-coded write sites and propose the instrument that would keep them classified, building neither the reclassification nor the instrument in this row. Thirteen modules outside core stage and replace their own files, of which two are read and the remaining eleven are unadjudicated. This is NOT thirteen defects and must not be rowed as though it were, because a two-phase publish that stages through a core tier and then publishes under a lock while emitting its own lifecycle event is the CORRECT shape, and the user-profile bundle export is the worked example that does exactly that and names the core tier in its own docstring as the reviewed home. Separating that shape from a genuine re-implementation requires reading each site, which is the work this row scopes. The durable half is that no instrument in this tree makes the distinction. The write-site census quantifies over write primitives in the source specifically so it can see an unenrolled site, but its question is where each site's PATH originates rather than whether the site duplicates a write path, so the provenance-manifest defect it would have been natural to expect it to catch was never within its reach. That is the same structure as the trigger-reachability finding, a gate existing for the adjacent question and none for this one, which is now this campaign's most repeated shape; `the thirteen modules naming os.replace or mkstemp outside src/cadrumo/core/atomic_write.py and dev/write_site_census.py`.
- [x] `P02.S47` - Route the same self-cancelling concurrency defect in the quick packaging lane to its owner rather than sweeping it, carrying the measurement that lane's gate author never had. MEASURED WITH AUTHENTICATED gh ON 2026-08-11 AND IT IS WORSE THAN THE PER-PUSH LANE. Of the last 300 retained runs of that workflow, 299 are CANCELLED and 1 is in flight. There are ZERO completed runs in that window and the last successful run of any age was 2026-07-28, fourteen days ago. The per-push lane by comparison had 99 cancelled of its last 100 and a green as recently as 2026-07-24, so the quick lane is the more starved of the two. That workflow carries the identical concurrency group and the identical push trigger on main, so every push cancels the run the previous push started, and the mechanism is the one already fixed in the per-push lane. WHAT MAKES THIS A SEPARATE ROW RATHER THAN A SWEEP is that the setting there is deliberate and gate-pinned. A conformance gate asserts the value is true and states its reason, that quick is the per-push signal and superseded runs are cancelled rather than queued, which is a real intent an agent may not overrule by sweep. But an intent stated against an assumed commit rate is not an intent stated against this fleet's rate, and the number now says the stated reason has produced no completed run in 299 attempts. Carry to the owner the mechanism, the run history above, and the finding that cancellation buys no machine occupancy over the default queue, because only one run may be pending in a group and a newly queued run cancels the previously pending one, so both settings cap the lane at one in flight and one waiting and differ only in whether the in-flight run finishes. The runners are self-hosted and consume no Actions minutes, so there is no billing saving either. The decision is the owner's and the row does not presume it; `.github/workflows/packaging-quick.yml and dev/packaging/tests/test_packaging_quick_workflow.py`.
- [x] `P02.S48` - Repair the workflow-conformance gate that S31 left pinning a removed ignore directive, because S31 correctly deleted the three redundant --ignore directives from the test-unit recipe once the marker expression carried not external_tool, but test_ci_workflow.py still asserts that the recipe body contains --ignore=dev/registry/tests/test_workbook_parity.py, so the gate has been red since that landing and reds for every agent who runs the dev-ci lane, and this is S31's collateral rather than a new defect, noting that the fix is to re-pin the gate on the property S31 established (that the workbook-parity module is held out by its external_tool marker rather than by a path ignore) rather than to restore the directive, because restoring it would undo S31 to make its own gate pass, and noting that the gate's other three assertions on the recipe body are still correct and must survive the repair; `dev/ci/tests/test_ci_workflow.py and justfile`.
- [x] `P02.S49` - Flip continue-on-error off the per-push integration conformance step after the action-envelope campaign is audited complete and the unchanged authoritative four-module recipe is green. Closure evidence: the historical 2026-08-13 baseline was 6 failures among 48 then-collected tests, current suite evolution legitimately collects 46, all 46 pass, and the workflow remains pinned to the exact recipe with eight workers. The denominator is an observation, not a permanent release condition; `.github/workflows/ci.yml and dev/ci/tests/test_ci_workflow.py`.
- [x] `P02.S50` - Author the Modelo 347 contraparte.provincia-codigo casilla label and help in all four catalogues, because the whole dev/docs search and terminology surface errored at collection on a single MissingTranslationError for modelo.schema.347.revision.*.casilla.x-cdnmst3ic5o62sjkckn70sjfepkmsor9c4mm6rr4d5jmu.label, which is the SAME failure shape the ci-full step comment already recorded for a Modelo 145 key -- a locale/registry co-commit gap fixed with dev.locales set, explicitly not a defect in any gate. DONE 2026-08-28. The casilla is the two-digit province code at offset 77 of the declarado record, sibling to pais-codigo at f009b; its slots existed with null values in BOTH the 2011-2024 and 2025-y-siguientes revisions across es, en, ca and hu, scaffolded but never authored. Values authored through dev.locales set rather than by hand-editing the shards, mirroring the sibling pais-codigo phrasing, with the descriptive parenthetical grounded in the export field spec itself (length = 2, value_policy = digit-string) rather than asserting the blank-for-non-residents rule which was not verified in the design record. A sweep across every modelo and filing year 2019-2026 now finds zero unlabelled casilla/locale pairs, and the lane's MissingTranslationError occurrences went from 187 to 0; `src/cadrumo/locales/{en,es,ca,hu}/modelo/schema/347.yml`.
- [ ] `P02.S51` - Verify the full size-budget gate only after P05 remediates every live source-file subject and regenerates stale pins, refuse every threshold raise, and use the clean result to clear S41s size prerequisite.; `dev/audit/size_budget_baseline.json`.
- [ ] `P02.S52` - Zero the import-hygiene Family-1 production reaches. MEASURED WITH MEMBERS AT HEAD 2026-08-31: 126 live production Family-1 sites against a HARD-ZERO baseline, so every one is undocumented and none may be answered by a baseline entry -- the only sanctioned fixes are promoting the symbol to a public defining module or extracting a shared leaf. Concentrated rather than scattered: 39 private target modules across 48 importing files, dominated by entrypoints/cli/_modelo_*.py reaching into application/modelo/_* (_action_errors, _calculate_input, _calculation_actions, _selectors, _filing_actions, _profile_readiness_gate, _verification_actions, _work_plazo, _work_create_policy, the six _review_package_* modules, _export, _iva_wallet_gate, _taxation_comparison, _participation_index_rebuild), plus a small tail: fincas.py and two custody modules reaching storage.sql._orm and custody._filesystem_primitives, core._hex, and two calculations/aggregation targets. THIS ROW AND S53 ARE ONE DEFECT MEASURED TWICE: 21 target modules are shared with the undeclared test-only set and 19 of those are application/modelo/_*, so promoting that single cluster clears reaches in both families and nothing else comes close to that leverage. ATTRIBUTION: 72 of the 126 sit in files unmodified in the working tree and are HEAD-real; 54 sit in 16 files a peer is editing and are not attributable from this measurement. BLOCKED ON PEER WORK AS OF 2026-08-31, RE-CHECK BEFORE STARTING -- this is the reason no promotion was attempted rather than an oversight. src/cadrumo/application/modelo/ currently carries 19 modified files AND an untracked new public module, edit_session.py, so a peer is mid-promotion inside the exact package this row must change; two of the modified files, _calculation_actions.py and _verification_actions.py, are targets named above. Taking one now would split their atomic relocation, which aeat-architecture-boundaries forbids. Note the sweep is presently working through core, not modelo -- HEAD 06a7fbe31a is relocation:core promote eight more private modules -- so modelo is being touched by someone else again, and the two efforts are not the same. THE CHECK BEFORE ANY ATTEMPT IS CHEAP AND MUST BE REPEATED: git status --porcelain -- src/cadrumo/application/modelo/ must be empty for the target module and its consumers. WHEN CLEAR, take targets ONE MODULE AT A TIME as atomic relocation:<symbol> commits carrying the move, every production and test consumer, and the deletion of the old path in one index, never file by file -- one private module typically serves several CLI importers, so a partial sweep strands a re-export. Members reproduce in about 90 seconds via _current_production_family1_sites() from dev/tests/test_import_hygiene_gate.py; do not re-derive by grep, which over-counts TYPE_CHECKING and misses relative spellings; `src/cadrumo/application/modelo/, src/cadrumo/entrypoints/cli/, dev/quality/import_hygiene_baseline.json`.
- [ ] `P02.S53` - Zero the test-only Family-1 underscore reaches. RE-MEASURED WITH MEMBERS AT HEAD 2026-08-31: the debt file declared 32 entries (31 distinct -- one importer records the same reach twice, under occurrence 1 and 2) while the live scan finds 138 sites, so 112 were undeclared. THE TWO FAILING FAMILIES ARE ONE DEFECT MEASURED TWICE, which should drive sequencing: of the 39 private target modules in the production Family-1 set and the 59 in the undeclared test-only set, 21 are THE SAME MODULE and 19 of those are application/modelo/_*. Promoting that one cluster clears reaches in both families at once, and nothing else in either list comes close to that leverage. ATTRIBUTION, MEASURED NOT ASSUMED: 88 of the 112 sit in files unmodified in the working tree and are HEAD-real; 24 sit in files a peer is editing now. DONE THIS PASS, in commit 111a05ff: the debt ledger goes from 32 entries to 26, with NONE added and no baseline raised, and the STALE HALF IS NOW EMPTY -- the gate reports 0 recorded-but-not-occurring, down from 6, and the 113 remaining complaints are all occurring-but-not-recorded. Each of the six was checked at source rather than deleted on the scan's say-so, AND THAT CHECK CHANGED THE ANSWER FOR FIVE OF THEM: they were not removed reaches but promotions or a module split. overview._agenda and _backlog are now the public agenda and backlog; core.observability._fingerprint is now fingerprint; calc_sheets_pull became calc_sheets_pull_records with RelationEdit public. Those imports still exist and still execute; they simply stopped being violations. Only llm._providers.base._ProviderAdapter is genuinely absent. A SEVENTH CANDIDATE WAS NOT STALE AND WAS REPOINTED RATHER THAN DROPPED: aggregation/tests/test_service.py still reaches _ACCEPTED_KIND_TO_INTERNAL, but its module was promoted from review._operator to review.operator, so the reach survives as a private NAME in a public module; deleting it would have converted a tolerated, reasoned reach into an untracked one. TWO RULES THIS PASS ESTABLISHES, BOTH LEARNED BY GETTING IT WRONG FIRST. One: a stale allowlist entry is not evidence the reach was fixed -- a promotion, a module split and a genuine removal all present identically as 'no longer answers a live occurrence', and only the last licenses deletion, so resolve the symbol to its current defining module before deleting. Two: 'the file is modified in the working tree' is TOO COARSE a proxy for 'not attributable to HEAD'. The _fingerprint entry was withheld on that basis and the withholding was wrong: the file is indeed peer-modified, but not on that line, and HEAD and the working tree carry the identical public import. Compare the specific line against HEAD, not the file's status. REMAINING: the 113 undeclared occurrences, which are NOT answerable by declaring them -- they need the same promotions as S52 and should follow that row's target-module sequencing; `dev/quality/import_hygiene_test_debt.json, src/cadrumo/application/modelo/`.
- [x] `P02.S54` - Close the sixteen-key codebase-to-locale parity drift left by the view verb family and the taxpayer_type.declaration_roles schema field, both landed within hours of the 2026-08-28 measurement. The keys are missing from ALL FOUR catalogues, so this is an unfinished co-commit rather than a translation backlog: cli.app.ledger.counterparty.view_help, cli.app.ledger.evidence.review.view_help, cli.app.modelo.audit.view_help, the three cli.app.modelo.reconcile.list_* keys, cli.config.auth.diagnostics.view_help, the two cli.config.google view_help keys, cli.config.profile.capabilities.view_help, the two cli.config.storage.view keys, the three cli.operator_surface.help.* keys and profile.schema.field.taxpayer_type.declaration_roles.label. Deliberately left to the verbs' own author rather than absorbed, because inventing help prose for someone else's in-flight surface collides with their wording; the scaffold placeholder and an omitted ca or hu are both refused, so there is no partial landing; `src/cadrumo/locales/{en,es,ca,hu}`.
- [ ] `P02.S55` - Refresh the cli-sequence goldens and get `just docs` green, a HARD blocker on measuring this lane honestly rather than one more failure: P01.S04 put the docs build above the tooling-gates step precisely so the terminology gates resolve against a built artefact, so while the build fails those gates error at collection and their verdict is UNKNOWN rather than red. Measured 2026-08-28 at 21 divergences and 57 of the 75 lane errors. DO NOT RE-DIAGNOSE FROM SCRATCH -- the 2026-08-07 docs-lane-golden-staleness audit already did this work and its findings stand: the goldens are STALE rather than nondeterministic; the bucket-identity tie-break theory was REFUTED by byte-comparing committed against freshly recorded identifiers and written down as refuted so a fourth attempt would not restart from it; the events are ABSENT from the live run rather than reordered; and a page refreshed and checked twice IN ISOLATION is stable while the same page inside a full-tree refresh then check is not, so the residue is a property of whole-run execution rather than of any sequence or of the sort key. WHAT THE 2026-08-28 PASS ADDS. First, the divergences have MOVED ON from the legal_refs staleness the audit recorded: the shapes are now `calculation_revision_id` (124), `coverage.out_of_scope` (90), `notices[].message` and `notices[].context.modelo` (60 each), and `action.cli_path[]` (51 plus 45 more on detail actions). The cli_path population is the cli-root-verb-homes campaign renaming verbs under the goldens, and the derived ids follow from the coverage and notice changes rather than being independent. Second, THE AUDIT'S REFUSAL CONDITION IS ACTIVE RIGHT NOW FOR A NEW REASON. It gated the refresh on the storage refactor landing, and secure-storage-performance-hardening now reads 59 of 59 with no open rows, so THAT half is satisfied -- but the working tree is currently UN-IMPORTABLE: `cadrumo.application.storage.calc_sheets.__init__` imports `.row_set_assembly` while the file on disk is `_row_set_assembly.py`, an uncommitted half-landed relocation (the __init__ shows modified, the tracked filename still carries the underscore), so `import cadrumo.application.calculations` raises ModuleNotFoundError and every sequence sandbox dies before its first frame. A second such break, `_registry_preconditions`, was observed and resolved by its owner within the same session. DO NOT FIX EITHER ONE HERE: they are another campaign's in-flight relocations and the missing half is exactly the rename they have not landed. Wait for the tree to import cleanly, CONFIRM it, then refresh, then re-run the whole lane -- the true failure count is not knowable until the artefact exists; `dev/docs/sequences and the cli-sequence goldens`.
- [x] `P02.S56` - Repair the source-connectivity discovery detectors, which describe how calculation sources reach casillas and had TWO live defects against the real tree. DONE 2026-08-28. First, `discover_row_assemblers` raised FileNotFoundError: it hard-codes one module path and a relocation had promoted `_row_set_assembly` to its public name, so the detector read a file that no longer exists; repointed, and it now discovers 9 assemblers. Second, `discover_ingress_surfaces` raised ValueError 'write command spec cannot be resolved structurally' on `_modelo_spreadsheet_command_specs.py:144`. The symptom pointed at a malformed spec; the CAUSE was in the resolver. `_string_value` resolves an ast.Name only from a leaf wrapper's PARAMETER bindings, but a spec module hoists its handler's dotted module path to a module-level constant (`_MODULE = ...`), so `token` and `parent_key` resolved while `handler` came back None and the walk raised as though the spec were unauthorable. Fixed generally by seeding module-scope bindings, so any spec module using the same idiom resolves; 126 ingress surfaces now discovered instead of a hard raise. BOTH SHIPPED THROUGH THE SAME BLIND SPOT and that is the durable lesson: all 13 existing tests write a synthetic module into tmp_path and never touch the real tree, which is the 'correct on synthetic input, never reaches the real site' hole the quality-gates rule names. Added `dev/source_connectivity/tests/test_discovery_resolves_the_real_tree.py`: six detectors plus both census entry points run against the real repo, gated on the PROPERTY (resolves and finds its subject) rather than a tally, with an anti-vacuity check that the computed repo root is real. Both defects would have redded it; 8 tests pass; `dev/source_connectivity/discovery.py and dev/source_connectivity/tests`.
- [x] `P02.S57` - Pin the export-layout byte-coverage join as a shrinking ratchet, because a CLEAN verdict from its fallback mode is silent about being weaker. DONE 2026-08-28. `validate_export_layout_record_coverage` joins each official AEAT design sheet to the authored export record meant to carry it and asks whether that record writes every required byte; where the join fails it falls back to asking whether ANY record of the layout writes the coordinate. The fallback is deliberate and documented, can only under-report, and a REFUSAL names the mode -- but a PASS says nothing, and since every fixed-width record starts at byte offset 1 an unrelated record occupying the same range satisfies it, so a sheet whose own record omits a position still passes. MEASURED, and larger than first reported: 25 design sheets across 9 modelos and 25 revisions take the fallback, including MODELO 303's DP30300 across ALL FIVE current revisions (2022, 2023, 2024-hasta-08-y-2t, 2025, 2026-y-siguientes) on 5-6 record layouts -- the principal IVA return's export byte coverage is currently proved only by the weaker question. Also 151, 184, 193, 232, 296, 322, 349, 720. A first scan that stopped at the first resolving year per modelo saw only 9 of the 25; the full walk over every revision is required. Added `registry/tests/test_export_layout_join_ratchet.py` pinning all 25: growth fails, and a sheet that becomes joinable must be DELETED rather than left widening the guarantee. Carries an anti-vacuity floor on revisions scanned and a third assertion that every entry sits on a MULTI-record layout, since a single-record layout loses no rigor to the fallback and would overstate the debt. The gate caught a bug in itself before it was trusted: a first version broke at year level and both the vacuity floor and the exactness assertion fired; `src/cadrumo/domain/calculations/registry/tests/test_export_layout_join_ratchet.py`.
- [x] `P02.S58` - Correct two registry gates that reported fully-wired engine paths as ingestion gaps, WITHOUT silencing either. DONE 2026-08-28. The grouping-coverage gate flagged `contraparte_clave` and `per_inventory_activity` as row-producer groupings with no application-layer ingestor, which reads as a silent under-declaration in two returns. Both were chased to ground and both were the GATE being wrong. `contraparte_clave` (M347, 18 bindings across both revisions) is a first-class `_InvoiceGrouping` dispatched by `_build_invoice_rows` to `_build_contraparte_clave_rows`, reachable on the live path, carrying its own M347_OPERATION_CLAVES vocabulary rather than M349's -- the allowlist was simply stale against landed work. `per_inventory_activity` (M100 renta-2025 casillas 0177/0181/0182) is handled by a THIRD mechanism the gate did not model at all: `InventorySourceResolver` emits row_binding_values keyed by (binding_id, row_index), is enrolled on the live calculate path in `modelo._calculation_actions` and declared a mesh route in `modelo.calculation_route`, and BindingSourceKind.INVENTORY is neither deferred nor reserved. Added a third accurately-named category `_MESH_RESOLVED_GROUPINGS` with the enrolment evidence recorded and an explicit warning that an entry belongs there only once its resolver is CONFIRMED enrolled, because a resolver that exists but is not routed is dead capacity whose bindings blank silently -- the exact failure the gate exists to catch. Deliberately did NOT dump both names into the invoice allowlist, which would have hidden a real gap had either been unrouted; `src/cadrumo/application/calculations/tests/test_grouping_dispatch_coverage.py`.
- [x] `P02.S59` - Stop the bundled-design worklist counting a settled adjudication as backlog, by reading the registry's own declaration. DONE 2026-08-28. The gate reported 9 of 218 designs not fully read, of which 5 were Modelo 184 BOE ordenes REFUSED for not opening at wire position 1. Those five are not work: M184's export layouts cite ONLY `aeat-dr-184-2023-2024` and `aeat-dr-184-2025`, the AEAT diseños that open at position 1 and parse cleanly; no layout cites a `boe-dr-184-*` source; the five carry `design_authority == 'provenance_only'` in registry data (they are the orden that APPROVES the modelo, carrying its design as an annex); and `test_modelo_184_registry.py` states the refusal is intentional and load-bearing. The provenance_only set is EXACTLY those 5 sources, a 1:1 match with the refusals. Excluded them from the WORKLIST only -- they are still enumerated, parsed and classified, so the module's never-silence property still holds over the whole corpus -- and DERIVED the excluded set from `design_authority` in the catalogue rather than hardcoding filenames, the same discipline the fixture-provenance rule requires. Made the exclusion self-policing in both directions: the set must be NON-EMPTY (or the filter is a rigorous-looking no-op if the declaration is ever dropped) and every member must STILL refuse, because the registry names the promotion criterion exactly -- strict parsing producing complete records from position 1 -- so a provenance-only design that starts parsing cleanly fails and demands re-adjudication instead of hiding behind the exemption. Leaves the 4 GENUINE parser gaps listed: modelo 165 and three modelo 181 TABLE partial reads; `src/cadrumo/domain/calculations/registry/tests/test_every_bundled_design_is_read_or_reported.py`.
- [x] `P02.S60` - Stop the record-design row parser reading enumerated PROSE as a field row, which was inventing a phantom record body in every Modelo 181 edition. DONE 2026-08-28. A bare dash in the naturaleza column is legitimate -- AEAT uses it to mean 'no data type', i.e. filler -- so `_NARRATIVE_PDF_ROW_RE` accepts one. But AEAT also writes enumerated prose INSIDE a field's description ('1 - En el caso de que en el campo Clave Tipo de Identificacion se haya consignado una C se informara el Codigo Cuenta Cliente de la siguiente forma:'), and the parser read that leading '1 -' as offset=1, length=1, type=Blancos. The damage was not a lost row but a lost RECORD: because the fabricated offset restarts at 1, the extractor concluded a distinct record body began mid-description and reported an unidentified record it could not name, in all three bundled M181 editions at source rows 455, 371 and 376. THE DISCRIMINATOR IS SEMANTIC, NOT STRUCTURAL, and measurement is what proved it. Across all 102 bundled design PDFs, 183 rows carry a bare-dash naturaleza. The obvious rule -- 'no position range means prose' -- would have been WRONG and would have dropped 8 legitimate rows: single-position BLANCOS at 58, 81 and 500 across modelos 185, 270, 296 and 347. What actually separates them is that a dash naturaleza DESCRIBES its filler, so the fix requires the description to name BLANCOS/BLANCO/CEROS via `_FILLER_DESCRIPTION_RE`; exactly 6 rows describe prose and they are precisely M181's three editions, twice each. Result: all three M181 editions now parse 2 sheets with 0 skipped, and M185's single-position filler rows still parse. A NEAR-MISS WORTH RECORDING: the first attempt looked correct on M181 while silently breaking M185 to sheets=0, because a heredoc wrote a literal 0x08 byte where the word boundary belonged, so the regex matched nothing and rejected every dash row including the legitimate ones. grep rendered it invisibly; only `cat -A` showed it. Verifying the population a fix must NOT disturb, not just the one it targets, is what caught it; `src/cadrumo/domain/calculations/registry/record_design.py`.
- [x] `P02.S61` - Adjudicate Modelo 165's 2013-2015 design hole, which is a CORPUS DEFECT in an authoritative source rather than a parser gap, and needs a ruling before it can be closed. DIAGNOSED 2026-08-28, NOT FIXED. The sheet 'Tipo 2 - Registro De Socios O Participes' in `02-165-orden-hap-2455-2013.pdf` declares 500 total positions but nothing describes 102-103: the record runs 97-101 PORCENTAJE DE PARTICIPACION (subdivided 97-99 ENTERO, 100-101 DECIMAL) and then jumps straight to '104-500 BLANCOS'. COMPARED ACROSS ALL THREE BUNDLED EDITIONS of the same orden, which are byte-identical everywhere else (95-96 DIA, 97-101, 97-99, 100-101, 108-120 NUMERO IDENTIFICATIVO): `01-165` (actualizado 2023) and `03-165` (actualizado por orden HFP-1822-2016) both publish '102-500 BLANCOS'. Only the original 2013 BOE orden says 104, and no edition describes any field at 102-103, so the two positions are filler in every reading and the original simply mis-declares where filler starts. THIS IS NOT COSMETIC BOOKKEEPING: `02-165` is `aeat-dr-165-2013-2015`, marked design_authority='authoritative' and cited by revision 2013-2015, so if that era ever acquires an export layout the hole becomes a real byte-coverage gap. It has layouts=0 today, which is why nothing has bitten yet. THE EXISTING CORRECTION MECHANISM DOES NOT FIT and must not be stretched to make it: the three declared kinds address a row read with a blank type cell, a header cell, and one naturaleza-less single-position row, whereas this is a filler row whose declared START is wrong. The schema comment states the narrow gate is deliberate, so widening it is an ADR-grade decision, not a loop-tick patch. Options are a fourth range-start correction kind grounded in the two sibling editions, or acquiring a corrected 2013-2015 diseno from AEAT; `src/cadrumo/_data/corpus/aeat_official/disenos_registro/modelo_165 and registry/record_design_schema.py`.
- [x] `P02.S62` - Close Modelo 720's live blank-emission path by giving a diseno-declared constant its own binding source kind. DONE 2026-08-28 under the accepted ADR `2026-08-28-registry-narrow-mechanism-widening` (decision C), executed as P01 of `2026-08-28-registry-narrow-mechanism-widening-plan`. THE DEFECT: M720's `tipo-de-registro` @1 and `modelo-declaracion` @2-4 were `source = "manual_input"` on both records, so the application prompted the taxpayer for AEAT's own record-type marker and modelo number; the prompt is answerable-blank and a blank emitted at those positions behind a valid digest, producing a file AEAT cannot parse. The diseno (aeat-dr-720) states all four as constants in its own field text. AN EARLIER LITERAL FIX WAS REVERTED because three tests pin 'M720 must represent every casilla through a binding, never an inline export field'; the tests were right and the edit was wrong. WHAT LANDED: `BindingSourceKind.DESIGN_CONSTANT` (33 members) with `design_constant_bindings.py` carrying a selector and an accumulating build-time validator, wired into both dispatch tables; the selector adds a check the manual channel could never make -- the value must FILL its declared run exactly, proved biting on '72' and '7200' against a 3-byte run, because padding a mis-read diseno silently is how it reaches the wire. Enrolled on the calculate route via a NEW SIBLING pseudo-owner `CalculationRouteDesignConstantOwnership` rather than relaxing the manual owner's Literal pins -- the ADR's own principle applied to the fix's own code, so every existing pin stays intact. M720's four bindings re-sourced, keeping `data_type = "integer"` deliberately because switching to 'text' could change padding and justification of emitted bytes. THEN THE JOIN, which the first pass did not close: `_record_literals` read only inline LITERAL fields, so a record declaring its constant through a binding looked like a record with no constants. Extended it to read BOTH declared channels and threaded the map through `_belongs_to_layout`, `_join_record`, `_missing_report` and `_layout_failure`. This is NOT the widen-a-matcher move the ADR forbids: both channels are declarations validated at registry build, and the checker simply could not see the second. The ratchet was aligned to call the same `_design_constant_values`, because a gate seeing fewer channels than the checker reports debt that does not exist -- the third time today a gate overstated. VERIFIED: registry Verificado=True, 43/43 M720 tests green, both sheets JOIN their record, coverage 0 complaints through the real per-record join, ratchet gate green with the two M720 entries deleted (23 -> 21); `src/cadrumo/core/aggregation.py, src/cadrumo/domain/calculations/registry, src/cadrumo/application/modelo/calculation_route.py and the M720 bindings`.
- [x] `P02.S63` - Execute the accepted narrow-mechanism-widening ADR and correct the join ratchet's own overstatement. DONE 2026-08-28; plan `2026-08-28-registry-narrow-mechanism-widening-plan` is 6/6. Three deliberately narrow registry mechanisms were widened by explicit declaration rather than by relaxing a matcher, and each widening carries a both-directions gate. WHAT LANDED: a `DESIGN_CONSTANT` binding source kind closing M720's live blank-emission path; a `RecordDesignRangeStartCorrection` kind with its precondition enforced at EXTRACTION, closing the last of 218 bundled designs (worklist 9 unread -> 0, confirmed corpus-wide); and an auxiliary-header contract that stopped asserting the filing CADENCE of whichever modelo carries it, which admitted Modelo 131's page zero across four revisions -- a beneficiary nobody predicted, found by measuring what the change did rather than what it was for. 16 admission gates hold all three from both directions: every declared set must be NON-EMPTY, every range-start correction is proved actually APPLIED, every design constant is proved to FILL its run against the compiled authority, and the unpinned cadence slot is proved to still REFUSE tag constants, footnote markers and BLANCOS with an anti-vacuity half proving it still accepts every cadence AEAT prints. THE LARGER CORRECTION IS TO THE RATCHET ITSELF, and it is the honest headline: of the 25 entries it originally pinned, only 2 were a real defect (M720, fixed) and 12 WERE NEVER DEBT. The inventory was built from `_join_record(...) is None` without enumerating the branches where the join is DELIBERATELY not attempted, and it over-reported three separate populations in turn -- auxiliary envelope headers, sheets whose constants ride on bindings, and declared filing envelopes. M303's five DP30300 entries were the largest: DP30300 is a correctly-classified VARIABLE envelope and the layout's declared filing_envelope, so coverage answers it from the envelope contract, deliberately skipping a join that would otherwise match a page at unrelated offsets. Ratchet 25 -> 11, and every one of the 11 survivors was then individually verified to have no filing envelope, no aux header and no variable envelope -- so the join really was attempted and really did fail. The number moved mostly by removing false claims about the codebase, which is the opposite of what a shrinking metric usually implies; `src/cadrumo/core/aggregation.py, src/cadrumo/domain/calculations/registry and the join ratchet gate`.
- [x] `P02.S64` - Modelo 184's record-type discriminator is blank-capable, and the fix is BLOCKED for the 2025 revision by AEAT's own cell text. PARTIALLY LANDED 2026-08-28. THE DEFECT: `tipo2.tipo-hoja` and `tipo3.tipo-hoja` at wire position 76 are input_kind=manual, required=False, with no formula, binding or constraints, while the diseno states `Constante "E".` on the rentas record and `Constante "S".` on the socio record. A blank at 76 leaves the record type INDETERMINATE in the filed file. It is also why the two Tipo-2 sheets tie: `m184-entidad` and `m184-socio` declare IDENTICAL literals at (1,1) and (2,3), so position 76 is the only thing that could tell them apart. THE REMEDY IS THE OPPOSITE OF M720'S and that is not a contradiction: M720's layout is binding-derived and three tests pin that it must represent every casilla through a binding, which is why it needed a new source kind; M184's generated records ALREADY declare six inline literals, so `kind = "literal"` in the generator mapping is the native expression there. The same defect shape can need opposite remedies, decided by each layout's own contract. WHAT LANDED: the 2023-2024 pair, in `dev/registry/mappings/modelo_184/2023/{0003-entidad,0004-socio}.toml`, whose cells read plainly `Constante "E".` and `Constante "S".`. WHAT IS BLOCKED: the 2025-y-siguientes pair. The generator refused `m184-2025.entidad.f008` with 'ambiguous official constant content' -- its cell reads `Constante "E". rentas. Declaracion anual.`, the constant merged with descriptive prose, which none of the extractor's patterns parse. THAT REFUSAL IS CORRECT AND MUST NOT BE ROUTED AROUND: the generator does not accept a declared literal, it DERIVES the value from AEAT's own text and compares bytes, which is the never-invent-a-figure rule mechanised. Widening the extractor to accept a constant followed by arbitrary prose is the widen-a-matcher hazard, on a matcher guarding a filing byte. The 2025 socio entry was reverted alongside it: a revision with one record guaranteed and the other blank-capable is a worse state to review than one uniformly unfixed. TWO THINGS STILL OPEN. First, the 2025 cell needs either a grounded correction mechanism for merged constant-plus-prose content or an operator ruling on its reading. Second, and measured: the mapping change does NOT close the join even where it lands -- all four Tipo-2 sheets still fail to join, so the tie needs something beyond declared constants; `dev/registry/mappings/modelo_184 and dev/registry/pipeline/_export_tree.py`.
- [x] `P02.S65` - The remaining join-ratchet entries need a RECORD-level discriminator, not a design constant -- and Modelo 349 already declares one the coverage checker never reads. MEASURED 2026-08-28, and this CORRECTS an earlier reading of the same rows. FIRST FINDING, which stands: none of the seven sheets can be closed by authoring a DESIGN constant, because AEAT publishes none. All seven declare exactly two constants -- the tipo marker and the modelo number -- and every one is already matched by a record. Modelo 193 is the clearest: `modelo-193-perceptor` and `modelo-193-gastos` both declare {(1,1): '2', (2,3): '193'} and nothing else, while their sheets declare the same two. What separates them is SHAPE, 41 fields against 9 on the record side and 39 against 10 on the design side. SECOND FINDING, which corrects the first's conclusion: `ExportRecordDefinition` carries a `discriminator` field -- a REGISTRY concept rather than an AEAT one -- so a discriminator CAN be authored even where AEAT prints no constant. Modelo 349 already has one on both tying records: `modelo-349-operador` declares `RecordDiscriminator(offset=147, length=32, requires='blank')` and `modelo-349-rectificacion` declares the same coordinate `requires='non_blank'`. The coverage checker does not consult it -- `discriminator` appears once in that module, in a comment about an unrelated page discriminator -- so this is the same shape as the `_record_literals` gap already fixed for design constants: the declaration exists, is authored, and the checker cannot see it. THE DESIGN CORROBORATES THE DISCRIMINATOR: at that exact coordinate the Operador sheet declares a field with NO content while the Rectificaciones sheet declares one carrying 'Estos campos se cumplimentaran...'. So the two sheets really do differ there, which is what makes the record-level rule a faithful reading rather than an invention. WHAT IS STILL A DECISION, and why this is not being implemented on the spot: teaching the join to consult the discriminator means mapping a RUNTIME blankness rule onto a DESIGN content cell -- requires='blank' matching a sheet field that declares no content, requires='non_blank' matching one that does. That is an inference across two vocabularies, and today has repeatedly shown that a plausible cross-vocabulary rule is exactly where a matcher admits more than intended. It wants grounding before it ships. If it holds it closes Modelo 349's two entries with no new schema at all, and gives Modelo 184, 193 and 296 an authorable route for the remaining five; `src/cadrumo/domain/calculations/registry/_validate_export_layout_coverage.py and the export record discriminator declarations`.
- [x] `P02.S66` - Teach the export-coverage join to consult a record's declared `RecordDiscriminator`, closing Modelo 349 and turning the rest of the ratchet back into ordinary authoring. DONE 2026-08-28. THIS WAS NOT A WIDENING but the third instance today of the same shape: a declaration exists, is authored, and the checker cannot see it -- after `_record_literals` missing binding-carried constants and the ratchet missing the aux-header and filing-envelope branches. `RecordDiscriminator`'s own docstring names this exact ambiguity: a literal-prefix matcher 'cannot tell two records apart when they share their leading literal fields (AEAT models several Tipo-2 record sub-shapes that all start with the same record-type literal)'. The PARSER already consulted it while reading binding rows; the coverage join did not, so two records with identical prefixes tied and fell to the weaker layout-wide question even though the registry declared how to tell them apart. AEAT'S OWN WORDS CORROBORATE THE MAPPING, which is what took this from inference to reading: at the discriminator coordinate (@147+32) Modelo 349's operador sheet says `desc='Blancos'` while its rectificaciones sheet says `desc='RECTIFICACIONES'`, subdivided into BASE IMPONIBLE O IMPORTES RECTIFICADOS and BASE IMPONIBLE DECLARADA ANTERIORMENTE. So `requires='blank'` matches a run AEAT itself calls filler, read through the same Blancos/Ceros vocabulary the row parser already treats as authoritative -- not a reading of prose. IMPLEMENTED AS A TIE-BREAKER ONLY, reached solely when the declared constants leave more than one record at the same agreement score, so it can turn 'no join' into a join and can NEVER change one the constants already decided; where the discriminator is silent it returns None and the sheet stays unjoined rather than taking an arbitrary winner -- silence is never read as agreement. MEASURED registry-wide: unjoined sheets 11 -> 9, exactly Modelo 349's two, with nothing else moving. Ratchet entries deleted. THE CONSEQUENCE FOR THE REST IS THE BIGGER RESULT: an earlier reading called the remaining rows permanently unshrinkable because AEAT publishes no distinguishing constant. That was wrong. `ExportRecordDefinition.discriminator` is a REGISTRY concept rather than an AEAT one, so a discriminator can be AUTHORED where AEAT prints no constant, with M349 as the worked example. M184, M193 and M296 are now ordinary registry authoring rather than an ADR-grade blocker. A heredoc silently dropped the regex word boundary for the second time today; the emitted line was grepped rather than trusted, which matters here because without it RECTIFICACIONES need only START with a filler word to be misread; `src/cadrumo/domain/calculations/registry/_validate_export_layout_coverage.py and the join ratchet gate`.
- [x] `P02.S67` - Generalise the join's filler test to a whole SPAN and author Modelo 193's record discriminator. DONE 2026-08-28, closing four ratchet entries. WHY THE GENERALISATION WAS NEEDED: M349's fix worked because both its Tipo-2 sheets declare the SAME run at the SAME coordinate (@147+32) and describe it differently. M193's do not. Compared by coordinate the two share exactly SEVEN -- @1+1 TIPO DE REGISTRO, @2+3 MODELO DECLARACION, @5+4 EJERCICIO, @9+9 NIF DEL DECLARANTE, @18+9, @27+9 NIF DEL REPRESENTANTE LEGAL, @36+40 apellidos -- every one a real prefix field on both sides, none filler on one and data on the other. After @76 they diverge STRUCTURALLY rather than differing at a shared run. So `_sheet_run_is_filler` now asks whether the design describes the WHOLE span as filler, which is the question a discriminator actually asks -- are these bytes blank in this record -- and a span may be described by several fields. It returns None when ANY byte of the span is described by nothing, because silence over part of a run is silence over the run and must never be read as blankness. THE MECHANISM AND THE DATA WERE CHANGED SEPARATELY, WITH A MEASUREMENT BETWEEN, and that is what makes the result attributable: after the generalisation alone the registry-wide count was still 9 unjoined with M349 unaffected, so the subsequent 9 -> 5 belongs to M193's discriminator and not to a looser matcher. M193'S DISCRIMINATOR IS GROUNDED IN ITS OWN DESIGNS, not pattern-matched: at @208 the gastos sheet declares a single 293-byte BLANCOS run to the record end while the perceptor sheet carries NATURALEZA DEL DECLARANTE, the prestamo fechas, COMPENSACIONES and GARANTIAS across the same span, and the span is fully described on both sides so there is no silence. Declared as `discriminator = { offset = 208, length = 293, requires = ... }` on both records in both revisions, with the reasoning recorded inline beside it. Registry Verificado=True; unjoined 9 -> 5, exactly M193's four; `src/cadrumo/domain/calculations/registry/_validate_export_layout_coverage.py and the M193 export layouts`.
- [x] `P02.S68` - Modelo 296's join tie cannot take a discriminator on the obvious run, because AEAT marks every field in it OPTIONAL. MEASURED 2026-08-28, and the refusal is the finding. M296's 'Tipo 2 - Registro De Perceptor' sheet ties FOUR ways rather than two -- m296-perceptor, m296-perceptor-intereses, m296-anexo-a-pagos and m296-anexo-b-certificados all agree on the only two constants the sheet declares, {(1,1): '2', (2,3): '296'}. Its sibling sheet 'Perceptor, Toma El Valor' already joins uniquely because it declares a third constant (500,1)='F', which is why only one entry is open. A STRUCTURALLY PERFECT CANDIDATE EXISTS: scanning for spans the perceptor sheet describes as real data and EVERY rival sheet describes as filler yields a contiguous run from @402 to @499 -- NIF DEL PAGADOR ANTERIOR, PROCEDIMIENTO ESPECIAL DE RETENCIONES, CLAVE DE MERCADO, CODIGO LEI DEL PERCEPTOR, NIF EN EL PAIS DE RESIDENCIA FISCAL, FECHA DE NACIMIENTO, LUGAR DE NACIMIENTO and PAIS O TERRITORIO DE RESIDENCIA FISCAL, with every rival sheet declaring the same span as filler. On the join alone that would resolve the tie. IT MUST NOT BE AUTHORED, and the reason is not about the join at all. `RecordDiscriminator` is consumed by the PARSER at runtime to identify which record a row is, so a `requires='non_blank'` rule is a claim that a real filing always populates that span. Reading the design's own obligatorio markers: every one of those eight fields is obligatorio=False, and LUGAR DE NACIMIENTO carries no marker at all. So a legitimate perceptor record that leaves all eight optional fields blank -- a resident-country NIF absent, no LEI, no prior payer -- would be MIS-IDENTIFIED by that discriminator. Authoring it would trade a coverage-checker tie for a record-identification defect on a filed IRNR return, which is a strictly worse bargain. WHAT WOULD WORK instead, unmeasured so far: discriminating the RIVALS rather than the subject, if each anexo and the intereses record has a span it always populates; or a mandatory field elsewhere in the perceptor record. Both need the same obligatorio grounding this row applied, and M296's export tree is GENERATED, so any discriminator goes in `dev/registry/mappings/modelo_296` and republishes through the generator authority rather than being hand-edited into src; `dev/registry/mappings/modelo_296 and the M296 export records`.
- [x] `P02.S69` - The last 5 ratchet entries cannot be closed by a runtime discriminator at all, and the reason is uniform. MEASURED 2026-08-28 across both remaining modelos, applying the obligatorio test that Modelo 296 forced into the method. M184: scanning both Tipo-2 sheets for runs one fills and the other declares filler yields seven candidates on the entidad side -- RETENCIONES E INGRESOS A CUENTA @233+12, SITUACION DEL INMUEBLE @245+1, REFERENCIA CATASTRAL @246+20, DETALLE DE GASTOS RENDIMIENTOS @266+130, CRITERIO DE COBROS Y PAGOS @396+1, a second DETALLE block @397+101, and NUMERO DE DIAS DE ARRENDAMIENTO @498+3 -- and exactly one on the socio side, NIF DEL REPRESENTANTE FISCAL @27+9. EVERY ONE is obligatorio=False or carries no marker at all, including both large DETALLE blocks. M296 measured the same way: all eight fields in its otherwise-perfect @402-499 run are optional. SO THE STRUCTURAL TEST PASSES EVERYWHERE AND THE SEMANTIC TEST FAILS EVERYWHERE. A `RecordDiscriminator` is consumed by the parser at RUNTIME to decide which record a row is, so `requires='non_blank'` asserts that a real filing always populates the span. On these runs that assertion is false by AEAT's own marking: an entidad record for a rented property with no gastos detail, or a perceptor with no prior payer and no LEI, would be mis-identified. Closing a coverage-checker tie by that means would plant a record-identification defect in a filed return. THE REMAINING FIVE THEREFORE NEED A MECHANISM THAT MAKES NO RUNTIME CLAIM, which is exactly the option this campaign set aside earlier: the generator mapping already records `record_identity` per entry, naming which design sheet each record belongs to, and the coverage checker re-derives that from constants instead of reading it. Surfacing it asserts nothing about what a filing contains -- it states which sheet the record was authored against, which is a fact about the registry rather than about a taxpayer. It is an export-schema change, since production must not import dev and the identity would have to travel in the generated fragments the checker already reads, so it is ADR-grade. That is the honest endgame for these five: not more authoring, and not a looser join, but carrying an authored fact the checker currently guesses at; `src/cadrumo/domain/calculations/registry/schema_exports.py, _validate_export_layout_coverage.py and dev/registry/mappings`.
- [x] `P02.S70` - Settle which branch of the continuity ratchet's own question applies, so its owner does not have to investigate. DETERMINED 2026-08-28, NOT ACTIONED -- the rebaseline belongs to whoever lands the revisions. The gate is red across 8 modelos: 6077 ungrounded groups observed against 2783 recorded, with modelo 200 alone contributing +3173, plus 165 (+17), 270 (+29), 308 (+2), 309 (+59), 341 (+12), 347 (+1) and 576 (+1). It names two possible causes and offers a pre-computed replacement baseline: EITHER a chain lost its continuidad_id stamp, which is a regression whose fix is to restore the stamp, OR a new revision landed carrying repeated casilla ids, which is legitimate and whose fix is to raise the baseline. Taking the offered baseline without deciding which applies would be allowlist-silencing, and could bury a real regression inside another campaign's work. IT IS THE LEGITIMATE BRANCH, AND THAT IS MEASURED RATHER THAN ASSUMED. Across every divergent modelo checked, ZERO casillas carry a continuidad_id: modelo 200's 3173 and 3462, modelo 309's four revisions, modelo 270's two, modelo 165's four. And `git log -S continuidad_id` over modelo 200's registry tree returns nothing at all, so those casillas have never carried a stamp in the repository's history. No chain lost one. Modelo 200 also explains the bulk arithmetic: its 3173 ungrounded groups equal its 3173 casillas, so every casilla is its own group, and its tree has been churned hard in the last few days -- dropping construct-only 2024 casillas, dropping casillas nothing references, restoring casillas a premature drop removed, carrying the dated validity axis onto them. WHY THE BASELINE IS DELIBERATELY NOT RAISED HERE: the file's own history shows rebaselining is done by whoever lands the revisions, and the numbers are still moving under active commits, so a baseline set now would be stale within hours and would silently absorb whatever lands next -- converting a live signal into a stale constant. The contribution is the determination itself; `src/cadrumo/domain/calculations/registry/tests/test_continuidad_completeness_ratchet.py`.
- [x] `P02.S71` - Diagnose the seven red Modelo 390 tests in application/calculations/tests. They share a modelo but NOT a cause, and the split matters because one half is a test-fixture problem and the other half is a live engine question. FIRST CAUSE, four tests, SETTLED: modelo 390 once carried a forward-open '2010-y-siguientes' revision covering every year. Commit f9f3f77704 replaced it with year-pinned epochs 2021..2025, so filing year 2026 lost coverage and every test pinned to 2026 now raises NoRevisionForPeriodError. Modelo 303 by contrast IS forward-open ('2026-y-siguientes'), which is what hid the asymmetry. Authoring a 390 exercise-2026 revision is NOT available: the governing orden is EHA/3111/2009 and the per-year epochs exist because the form CONTENT changes annually by amending orden, which for exercise 2026 publishes around November 2026 and is filed January 2027 -- today is 2026-08-28, so it does not exist and inventing it is forbidden. The tests therefore move to a covered pair rather than the registry moving to them: the property under test is multi-year continuity and cross-renta isolation, which is year-agnostic, and 2025/2026 was only ever an arbitrary pick made while the registry was forward-open. Retargeted the reconciliation module to (2024, 2025) with its clock moved 2027-01-20 -> 2026-01-20, after confirming the reconciliation casilla exists in 390 revisions 2022..2025 and that iva.cuota-devengada-total and iva.cuota-deducible-total exist in BOTH halves of the split 303 2024 epoch, so the four quarters resolve cleanly across the mid-year revision boundary. Renamed the year-suffixed locals to earlier/later so a future retarget cannot leave misleading labels behind. STANDING GAP worth its own trigger: 303 computes 2026 quarters but 390 cannot produce the 2026 annual summary, so a real 2026 filer meets an honest refusal until the exercise-2026 orden is published and authored. SECOND CAUSE, a different failure entirely, NOT a year problem and NOT yet resolved: test_verify_modelo_revision_refuses_m390_when_prior_filings_are_not_clean already uses the covered year 2025 and fails inside calculate_modelo_revision at _require_m303_regimen_simplificado_annual_summary_handoff, which is a strict biconditional -- a revision that DECLARES an m303_regimen_simplificado_annual_summary binding must receive a handoff, and one that does not must not. All four 390 epochs declare that family, and so did the deleted 2010-y-siguientes, so the declaration is not new; the guard is, landing 2026-08-14 in e2797f1aad. The resolver keys applicability on the REVISION alone: m303_regimen_simplificado_annual_summary_requirement returns non-None whenever the bindings exist, and _source_work_unit then RAISES unless exactly one filed same-bucket 303 4T work unit exists. Boxes 74-83 are regimen simplificado boxes that every 390 form carries, so the requirement fires for every 390 filer including the GENERAL-regime taxpayer this test declares. THE OPEN QUESTION, which is a tax-semantics question and must be grounded rather than assumed: whether a 390 calculation may legitimately hard-require a filed 303 4T source unconditionally. It is defensible that an annual IVA summary presupposes the year's quarterly filings, and equally defensible that a general-regime filer with no simplificado activity should not be routed through a simplificado handoff -- and 2026-07-01-modelo-303-regimen-simplificado-adr already establishes a narrower applicability vocabulary in which not-claimed is neutral and must reject Orden and module rows, which this requirement never consults. Do not resolve by relaxing the guard: it exists to keep the handoff on one mesh-owned arrival path; `src/cadrumo/application/calculations/tests/test_modelo_390_303_reconciliation_continuity.py,src/cadrumo/application/modelo/_calculation_actions.py`.
- [x] `P02.S72` - Settle whether a Modelo 390 calculation may hard-require a filed Modelo 303 4T source unconditionally. OPEN, ADR-GRADE, DELIBERATELY NOT RESOLVED IN CODE -- this is a tax-semantics question and the rules forbid settling it by assumption. Carrier for the single remaining red test, test_verify_modelo_revision_refuses_m390_when_prior_filings_are_not_clean, whose diagnosis is recorded in the sibling Step. The mechanism: m303_regimen_simplificado_annual_summary_requirement returns non-None whenever the revision declares the binding family, and the resolver's _source_work_unit then RAISES unless exactly one filed same-bucket 303 4T work unit exists, with _require_m303_regimen_simplificado_annual_summary_handoff enforcing the matching biconditional at calculate. Applicability is keyed on the REVISION alone and never on the taxpayer. Boxes 74-83 are regimen simplificado boxes that EVERY 390 form carries, so all four epochs declare the family and the requirement fires for every 390 filer -- including the GENERAL-regime taxpayer this test declares, who has no simplificado activity at all. Both readings are defensible, which is exactly why it needs a ruling rather than a patch: an annual IVA resumen does presuppose the year's quarterly autoliquidaciones, so requiring a 4T source is coherent; equally, routing a general-regime filer through a REGIMEN SIMPLIFICADO handoff to obtain it is not, and 2026-07-01-modelo-303-regimen-simplificado-adr already establishes a narrower applicability vocabulary -- not-claimed is neutral and must reject Orden and module rows -- that this requirement never consults. Modelo 390 is the IVA resumen anual and general regime is the majority population, so if the second reading is right the blast radius is most 390 filers. WHAT NOT TO DO: do not relax or delete the guard. It landed 2026-08-14 in e2797f1aad to keep the handoff on its one mesh-owned arrival path, and the declaration is not the new thing -- the deleted 2010-y-siguientes revision declared the same family, so the bindings predate the guard. If the ruling is that applicability must be taxpayer-aware, the change belongs in the REQUIREMENT projection consulting the established not-claimed-is-neutral vocabulary, not in loosening the biconditional that protects the arrival path; `src/cadrumo/application/modelo/_calculation_actions.py,src/cadrumo/application/calculations/_m303_regimen_simplificado_annual_summary.py,src/cadrumo/domain/calculations/registry/bindings.py`.
- [x] `P02.S73` - Rule on the Modelo 390 simplificado applicability question raised by the sibling Step, and record the grounding that settles it. THE RULING: regimen simplificado applicability is CONDITIONAL, so the M390 annual-summary handoff requirement must consult it rather than firing on the revision's declaration alone. This is grounded, not argued. LIVA art. 122 Uno, read in the bundled consolidated corpus at ley-37-1992-art-122, states the regime 'se aplicara a los sujetos pasivos ... que reunan los siguientes requisitos' and then lists three: personas fisicas or entidades en regimen de atribucion de rentas whose members are all personas fisicas, activities determined by regulation, and a prior-year volume-of-operations limit. A taxpayer in regimen general meets none of the regime's conditions and its M303 boxes 51-58 -- the exact source_casilla_ids these bindings read -- carry nothing. THE CODEBASE ALREADY AGREES WITH THIS READING, WHICH IS THE DECISIVE POINT: a closed vocabulary exists for precisely this judgement. M303RegimenSimplificadoScope carries REGIMEN_SIMPLIFICADO_NOT_CLAIMED and REGIMEN_SIMPLIFICADO_EVIDENCE_REQUIRED, and m303_regimen_simplificado_scope_for_composition maps M303RegimeComposition.GENERAL -> NOT_CLAIMED and SIMPLIFIED or MIXED -> EVIDENCE_REQUIRED, refusing an unknown composition rather than defaulting. Four production sites already gate on it with the same spelling, applicable = not scope_decision.is_not_claimed: _m303_regimen_simplificado.py, filing/_projection.py, modelo/_m303_filing_evidence.py, and the scope module itself. The M390 annual-summary requirement is the SOLE site that never consults it. So this is not a new rule to invent but an established one with a single unenrolled call site, which is why it is an amendment to the accepted 2026-07-01-modelo-303-regimen-simplificado-adr rather than a competing decision. IMPLEMENTATION SHAPE, and it deliberately spans two sites because one alone would be wrong: the resolver in _m303_regimen_simplificado_annual_summary.py must, after projecting the requirement and BEFORE calling _source_work_unit, derive the scope from the target work unit's own profile and return an empty resolution when NOT_CLAIMED -- the modelo-gated deriver returns None for a 390 work unit, so the profile-based m303_regimen_simplificado_scope_for_profile(active_taxpayer_profile(target)) is the correct entry. The guard _require_m303_regimen_simplificado_annual_summary_handoff must then widen its antecedent from 'the revision declares the family' to 'the revision declares it AND the family is applicable', because a resolver that legitimately produces no handoff for a general-regime filer would otherwise trip the biconditional it currently enforces. THAT IS NOT THE LOOSENING WARNED AGAINST: the guard keeps refusing an unexpected or missing handoff on every path where the family applies, and only stops demanding one where law says the regime does not reach the taxpayer. NOT YET IMPLEMENTED -- deliberately. The blast radius is every 390 filer, the change touches production tax logic at two sites, and this project requires the decision be recorded before the code moves. Verify against test_verify_modelo_revision_refuses_m390_when_prior_filings_are_not_clean, whose taxpayer declares IVARegime.GENERAL and which must then reach its INTENDED unclean-priors refusal instead of the aggregation-binding one. WATCH FOR A SECOND, SEPARABLE QUESTION while implementing: that test seeds NO 303 work units at all, so it also probes whether a 390 may be computed with no quarterly filings present. Requiring a filed 4T is defensible on its own for an annual resumen, but it must not be enforced by the SIMPLIFICADO resolver acting as messenger for a general-regime taxpayer -- keep the two apart; `src/cadrumo/application/calculations/_m303_regimen_simplificado_annual_summary.py,src/cadrumo/application/modelo/_calculation_actions.py,src/cadrumo/application/modelo/_m303_regimen_simplificado_scope.py`.
- [x] `P02.S74` - Correct the implementation shape recorded for the M390 simplificado applicability ruling, which cannot be built as first written. The sibling Step's ruling stands unchanged and is fully grounded; only its HOW is wrong, and finding that out before writing code is the point of recording it. The recorded shape had the annual-summary resolver derive the scope itself via m303_regimen_simplificado_scope_for_profile(active_taxpayer_profile(target)). That import is NOT AVAILABLE to it: both functions are defined only in src/cadrumo/application/modelo/_m303_regimen_simplificado_scope.py, an underscore-private module, and the resolver lives in application/calculations -- a different package. aeat-architecture-boundaries forbids a cross-package private import outright, and forbids fixing it by mechanically stripping the underscore: a contract needed outside its package must HARD-MOVE to a public defining module with every consumer updated and the old path deleted atomically, in one commit. TWO ADMISSIBLE ROUTES, and the second is preferred. ROUTE A, relocation: hard-move the scope derivation to a public defining module under application/modelo, sweeping all seven current consumers (_calculate_input.py, _export.py, _m303_filing_evidence.py and four test modules) in a single explicit-path commit tagged relocation:m303_regimen_simplificado_scope, with pytest --collect-only clean immediately before. That is a whole Step of its own and drags an unrelated relocation into a tax-correctness fix. ROUTE B, threading, PREFERRED because it already IS the established pattern rather than a new one: application/calculations/_m303_regimen_simplificado.py does not derive the scope either -- it RECEIVES M303RegimenSimplificadoScopeDecision as a parameter at two call sites, and that type is publicly reachable from domain.iva, which the calculations layer already imports. So the annual-summary resolver should likewise be GIVEN its applicability rather than reaching for it, supplied by the caller that already holds the profile, keeping the derivation single-homed in application/modelo and adding no new cross-package edge. Decide the exact carrier when implementing: the resolver's constructor is the narrower change, a CalculationSourceContext field the broader one, and the guard in _calculation_actions.py needs the same signal to widen its antecedent, so prefer whichever hands both sites one value from one derivation. SEPARATE PRE-EXISTING DEFECT FOUND WHILE MEASURING, not caused by this work and not to be silently absorbed into it: application/aggregation/tests/test_invoice_accumulative_cross_modelo_periods.py and application/filing/tests/test_m303_export_applicability_internal.py BOTH already reach across packages into that private module. Route A would sweep them by construction; Route B leaves them standing and they want their own fix; `src/cadrumo/application/calculations/_m303_regimen_simplificado_annual_summary.py,src/cadrumo/application/modelo/_m303_regimen_simplificado_scope.py,src/cadrumo/application/aggregation/tests/test_invoice_accumulative_cross_modelo_periods.py,src/cadrumo/application/filing/tests/test_m303_export_applicability_internal.py`.
- [x] `P02.S75` - Implement the Modelo 390 regimen simplificado applicability ruling by Route B, and close the seventh and last red M390 test. VERIFIED: test_verify_modelo_revision_refuses_m390_when_prior_filings_are_not_clean now reaches its INTENDED unclean-priors refusal instead of the aggregation-binding one, and all 27 calculation-route tests pass. Built exactly as the corrected shape required -- applicability is SUPPLIED, never reached for across a package boundary. m303_regimen_simplificado_annual_summary_applies now lives in application/modelo/_m303_regimen_simplificado_scope.py beside the other scope derivations, which is its natural home and also avoids an import cycle; it delegates to the closed vocabulary rather than reading the profile, so GENERAL maps to not-claimed, SIMPLIFIED and MIXED to evidence-required, and an unknown composition still refuses instead of defaulting. FOUR SITES, ONE DERIVATION: the resolver takes regimen_simplificado_applies and returns an empty resolution when the regime does not reach the filer; the guard's antecedent widened from 'declares' to 'declares AND applies', carrying expects_handoff into its diagnostic context beside declares_handoff so a refusal says which half failed; validate_persisted_target_revision treats inapplicable exactly as undeclared, since neither can produce a handoff, so an absent one is correct and a persisted one equally anomalous; and _verification_actions passes the same derivation. The arrival-path invariant is intact -- a missing or unexpected handoff is still refused wherever the regime DOES reach the taxpayer. UNBLOCKED A PRIORITY-1 BREAKAGE FOUND WHILE VERIFYING, and it was mine: the design_constant BindingSourceKind admitted earlier in this campaign never declared an OperatorActionAxis, and _assert_total_action_projection is a TOTAL import-time assertion over every member. That raised at import and left 14 application test modules uncollectable -- the failure was invisible until a module outside the -k 390 filter was collected. Fixed by declaring REVIEW_ADVISORY, the only honest axis: a diseno-fixed constant can never await operator data entry, so an unready one is a registry defect to review rather than a task, and its readiness key cli.app.modelo.bindings.readiness.constante_diseno was authored in all FOUR catalogues through the dev.locales CLI as the parity gate and honesty ratchet require. Collection went from 14 errors to zero. TWO ROUTE GATES CORRECTED, NOT WEAKENED: both pinned the manual-stage inventory POSITIONALLY, one via OWNERSHIP[-1] and one via OWNERSHIP[:-1], written when the manual stage held a single row. The design-constant sibling made both silently test the wrong row -- the second so badly that its refusal was never reached, since slicing dropped the sibling and left a duplicate id that raised a different error first. Both now select the manual-input owner BY TYPE, the manual stage pins BOTH pseudo-owners so a third cannot appear unnoticed, and the single-instance invariant still bites because the production check keys on isinstance rather than on stage or position. NOT MINE, FLAGGED NOT CLAIMED: test_cross_period_clean_state_blocks_csv_register_without_justificante_verification and its accepts_ sibling fail on MISSING_EXTERNAL_EVIDENCE_RECORD where they expect MISMATCHED_. Nothing in this change touches the cross-period evaluation path, the module has no recent commit, and this shared worktree currently carries uncommitted peer edits under adapters/persistence/storage. Investigate separately rather than absorbing into this fix; `src/cadrumo/application/modelo/_m303_regimen_simplificado_scope.py,src/cadrumo/application/modelo/_calculation_actions.py,src/cadrumo/application/modelo/_verification_actions.py,src/cadrumo/application/calculations/_m303_regimen_simplificado_annual_summary.py,src/cadrumo/application/state_projection.py,src/cadrumo/application/modelo/tests/test_calculation_route.py`.
- [x] `P02.S76` - Record the Modelo 390 applicability ruling in its GOVERNING home, not only in this plan, and note what is currently blocking broad measurement. The ruling was implemented and verified under the sibling Steps, but a plan Step is an execution record and an ADR is where a decision lives; leaving it here only would have left the accepted 2026-07-01-modelo-303-regimen-simplificado-adr silently contradicted by the code implementing it. Appended as its 2026-08-28 amendment through the vault set-body verb so the modified stamp and body hash stay honest, previewed with --dry-run first. It is an AMENDMENT rather than a new record because it refines an accepted decision on the same scope: that ADR already owns the not-claimed-is-neutral vocabulary, and this enrols its LAST call site rather than establishing a competing rule -- authoring a sibling ADR would have started exactly the supersession-chain sprawl the pipeline forbids. The amendment states what S84 left unsettled (WHO the handoff is required of), grounds applicability in LIVA art. 122 Uno from the bundled corpus, records that the arrival-path invariant is unchanged and binds on a corrected antecedent, and explicitly carries forward the one question deliberately NOT folded in: whether a 390 may require a filed 303 4T at all wherever the family applies is a separate decision needing its own owner. BROAD RE-MEASUREMENT IS BLOCKED, and not by anything in this campaign. A peer is mid-relocation making src/cadrumo/adapters/persistence/storage/__init__.py inert by removing its re-exports, which is the correct end state under the inert-namespace rule, but consumers including src/cadrumo/tests/master_key.py still import KEY_SIZE and SecretStoreError from that namespace, so every broad pytest run dies at conftest import. The same campaign broke and self-healed twice within minutes -- once here, once on domain/iva DEDUCTIBLE_INPUT_FLOW_DIRECTIONS, and once leaving a literal IndentationError from an insert landing inside an indented block. Their files were deliberately NOT touched: this is uncommitted WIP in a shared worktree, relocations become atomic at commit, and editing a peer's half-landed sweep is how two agents start fighting over one index. The measurement is retried instead, and the honest status is that the suite is intermittently un-runnable for reasons outside this lane; `.vault/adr/2026-07-01-modelo-303-regimen-simplificado-adr.md`.
- [x] `P02.S77` - Diagnose the two red AEAT-CSV-register cross-period clean-state tests, which were flagged unowned and are now shown to be a real product defect rather than stale expectations. CORRECT AN EARLIER MISREADING FIRST: the direction was recorded backwards. The test EXPECTS MISSING_EXTERNAL_EVIDENCE_RECORD and the code EMITS MISMATCHED_EXTERNAL_EVIDENCE_RECORD, not the reverse. THE SERIOUS HALF IS THE SECOND TEST, and it is the one that changes the severity of this finding. test_cross_period_clean_state_accepts_csv_register_with_matching_justificante_metadata seeds a fully-matching import through the REAL import flow and expects zero blockers; it receives MISMATCHED_EXTERNAL_EVIDENCE_RECORD. That is not a test-expectation problem. import_external_filing_evidence demonstrably writes both keys the checker reads -- external_evidence_reference_id and filing_record_id, alongside source_kind AEAT_CSV_REGISTER -- so a csv-register import that SHOULD satisfy the gate does not. If that reproduces outside the fixture it means a CSV-register-backed prior filing can never satisfy cross-period clean state, which blocks Modelo 390 and every dependent modelo for any operator whose priors were imported through the register. That is a filing-blocking defect, not a cosmetic one. THE FIRST TEST IS A SEPARATE, MILDER DEFECT IN THE SAME BRANCH: with justificante metadata omitted, absent metadata falls straight into the equality comparison and is reported as a mismatch. The receipt-bound branch immediately below already distinguishes these two states correctly -- justificante is None yields MISSING, a non-matching justificante yields MISMATCHED -- so the csv-register branch is simply missing the parallel absent-versus-divergent split its sibling has. The distinction is operator-facing and load-bearing, because these blockers project onto different OperatorActionAxis values: MISSING sends the operator to capture the evidence, MISMATCHED sends them to resolve a divergence, and telling someone to reconcile a divergence when nothing was ever captured is actively wrong guidance. NOT FIXED YET, and deliberately not guessed at. Three candidate causes remain for the accepts case and they are not distinguishable by reading: the persisted observation source_kind may not be AEAT_CSV_REGISTER, the metadata reference may not equal the filing's external evidence reference, or metadata filing_record_id may have gone stale against a filing record replaced after import. The next action is a targeted run that prints the three compared values rather than another round of inference. Both this module's helper and external_import_actions.py were last touched by the same commit 6babb35980, which is the first place to look for what moved; `src/cadrumo/application/calculations/cross_period_clean_state.py,src/cadrumo/application/modelo/external_import_actions.py,src/cadrumo/application/calculations/tests/_cross_period_clean_state_support.py`.
- [x] `P02.S78` - CORRECT the severity recorded for the two red AEAT-CSV-register cross-period tests in the sibling Step, which OVERSTATED the defect, and record the actual root cause. The sibling Step reasoned that because the real import flow writes both keys the checker reads, a matching csv-register import failing the gate implied a filing-blocking production defect that would block Modelo 390 for every operator whose priors came through the register. THAT INFERENCE WAS WRONG, and the missing fact was one line further down the fixture. _seed_official_303_source_filings calls import_external_filing_evidence, which correctly persists an observation with source_kind AEAT_CSV_REGISTER plus external_evidence_reference_id and filing_record_id -- and then unconditionally calls _save_source_observation for the same modelo, year and period, which saves over that same observation key with source_kind defaulting to the literal aeat_sede_justificante and a default_source_metadata carrying only aeat_register_status, aeat_expediente_id, authenticated_identity and sometimes aeat_justificante_csv. Neither key the csv-register branch compares survives, and the source kind no longer matches either, so the checker's first equality fails and reports MISMATCHED. THE FIXTURE CLOBBERS WHAT THE PRODUCTION PATH CORRECTLY WROTE. The production csv-register path is therefore NOT shown to be broken, and no claim that it is should stand on this evidence. The lesson is the one this campaign keeps relearning from the other side: a red gate is not proof that production is wrong, and reading one half of a fixture is not reading the fixture. THE MILDER DEFECT IN THE SIBLING STEP STILL STANDS and is unaffected by this correction, because it is about the SHAPE of the branch rather than about what the fixture wrote: with metadata absent the csv-register branch runs straight into an equality comparison and reports a mismatch, where the receipt-bound branch immediately below correctly splits absent from divergent by testing for None first. That split is operator-facing, since MISSING and MISMATCHED project onto different OperatorActionAxis values and sending an operator to reconcile a divergence when nothing was captured is wrong guidance. FIX SHAPE: the seeded observation for a csv-register period must carry source_kind aeat_csv_register and the two metadata keys, with filing_record_id read from the record the import actually created rather than invented, so the fixture stops contradicting the flow it just exercised. Do not resolve it by relaxing the checker's comparison, which is the one thing here that is behaving correctly; `src/cadrumo/application/calculations/tests/_cross_period_clean_state_support.py,src/cadrumo/application/calculations/cross_period_clean_state.py`.
- [x] `P02.S79` - Fix both AEAT-CSV-register cross-period defects at their real causes, one in the fixture and one in production, keeping the checker's comparison untouched because it is the component behaving correctly. FIXTURE, closing the accepts case: _seed_official_303_source_filings now reproduces the register identity on the observation it saves over the imported one. For a csv-register period that actually went through the import flow it sets source_kind to ObservationSourceKind.AEAT_CSV_REGISTER, sets external_evidence_reference_id to the same evidence reference the import used, and reads filing_record_id off the record the import genuinely created by matching work_unit_id in the filing catalogue -- read, never invented, so the fixture cannot drift from the flow it just exercised. Only the DEFAULTS are filled: a test supplying an explicit source kind or explicit metadata for that period still wins, and the csv-register defaulting is skipped entirely when a period declares its own source kind, so no existing scenario is silently rewritten. PRODUCTION, closing the blocks case and the defect that survived the severity correction: the csv-register branch ran absent metadata straight into an equality comparison, so a filing whose register record was never captured was reported as MISMATCHED. It now tests for wholly-absent metadata first and reports MISSING_EXTERNAL_EVIDENCE_RECORD, exactly as the receipt-bound branch immediately below already does by testing its justificante for None before comparing it. This is operator-facing rather than cosmetic: the two blockers project onto different OperatorActionAxis values, so the previous behaviour told an operator to reconcile a divergence when there was nothing to reconcile against. The mismatch comparison itself is unchanged and still fires on a wrong source kind, a divergent reference, or a divergent filing record id. NOT YET VERIFIED: a broad calculations-and-registry run was already in flight when these landed and this share fails under concurrent pytest I/O, so a second run was deliberately not started. Verify both csv-register tests plus the whole provenance module once it finishes, and confirm the sibling justificante and live-capture scenarios in the same module stayed green, since they share the seeding helper this change touches; `src/cadrumo/application/calculations/tests/_cross_period_clean_state_support.py,src/cadrumo/application/calculations/cross_period_clean_state.py`.
- [x] `P02.S80` - Discard a broad calculations-and-registry measurement as INVALID rather than report its failure list, and re-run it clean. The run was started against a settled tree and was progressing green through roughly 40 percent, then accumulated a large failure cluster across the 42 to 66 percent band that grew to include collection ERRORS as well as failures. It was tempting to treat that as the next real cluster and start diagnosing it. It is not usable evidence, for a reason that is entirely my own doing: I edited src/cadrumo/application/calculations/cross_period_clean_state.py and the cross-period seeding helper WHILE that run was executing, and the peer relocation making the storage namespace inert was landing across the same window. A pytest run whose source tree changes underneath it cannot distinguish a real regression from a module re-imported at a different revision than the one collected, so every failure in that band is uninterpretable -- and the specific danger is that it would have looked like a large genuine regression cluster and consumed the next several turns of diagnosis. Killed it deliberately rather than let it finish, because a completed untrustworthy FAILED list is more dangerous than no list: it reads as evidence. THE PROCESS RULE THIS ESTABLISHES, and it is the counterpart to the existing capture rule about never truncating before the write: do not edit tracked source while a measurement run is in flight. Either hold the edit until the run lands, or accept up front that the run is discarded. The cheap check that the two edited modules still import was run instead, single-process and low I/O, and both do. Also worth carrying forward: a second concurrent pytest was deliberately NOT started while the first was running, because this worktree's backing share fails under concurrent I/O and the cost of a bad capture is a full re-run. Those two constraints together mean broad measurement here is serial and must be treated as a scarce resource, planned around rather than fired off casually; `src/cadrumo/application/calculations/cross_period_clean_state.py,src/cadrumo/application/calculations/tests/_cross_period_clean_state_support.py`.
- [x] `P02.S81` - Audit the blast radius of the csv-register production change and the FIXTURE-CLOBBER PATTERN behind it, read-only while a measurement run held the tree. The pattern is not isolated to the module that surfaced it. Three test fixtures drive the REAL import flow and then immediately save over the observation that flow just wrote, with a hardcoded source_kind of aeat_sede_justificante: the cross-period seeding helper already fixed, plus test_cross_period_clean_state_enforcement.py and test_cross_period_clean_state_gates.py. The gates module is the sharpest instance, because its evidence_kind is genuinely computed and CAN be AEAT_CSV_REGISTER, while the observation it writes over the import is hardcoded to the justificante kind and carries neither external_evidence_reference_id nor filing_record_id. A fixture that contradicts the flow it just exercised is the exact shape that made a fixture defect read as a filing-blocking production defect earlier today, and it is worth naming as a class rather than fixing one instance and moving on. BLAST RADIUS OF MY OWN CHANGE, CHECKED RATHER THAN ASSUMED, because splitting absent from divergent changes which blocker those very fixtures now receive: with both register keys absent, the csv-register path in the gates module now yields MISSING_EXTERNAL_EVIDENCE_RECORD where it previously yielded MISMATCHED_EXTERNAL_EVIDENCE_RECORD. That is a genuine behaviour change reaching beyond the two tests the fix targeted. It is nevertheless SAFE, and the reason is specific rather than hopeful: neither sibling module references either blocker constant anywhere, so no assertion keys on the identity, and both the old and new behaviour still emit A blocker, so any assertion on clean being False or on a blocker being present is satisfied identically. Only a test asserting the specific blocker identity could have flipped, and none does. NOT FIXED, LEFT AS A NAMED FOLLOW-UP rather than swept in: the two sibling fixtures still overwrite what the import wrote. They pass today because their assertions do not depend on the register identity, which makes them latent rather than broken -- they will mislead the next person who tightens that branch, exactly as this one misled me. Fixing them belongs with whoever next touches those modules, and doing it now would mean editing tracked source while a measurement run was in flight, which is the discipline established one Step earlier; `src/cadrumo/application/modelo/tests/test_cross_period_clean_state_gates.py,src/cadrumo/application/modelo/tests/test_cross_period_clean_state_enforcement.py,src/cadrumo/application/calculations/cross_period_clean_state.py`.
- [x] `P02.S82` - Harden the csv-register fixture fix against picking a SUPERSEDED filing record, a flaw found by re-reading my own unverified change rather than by a test. NOT YET APPLIED, deliberately: a clean measurement run held the tree at the time and editing under it is what invalidated the previous run. The fix selects the filing record whose work_unit_id matches the imported work unit and takes the FIRST match out of the catalogue. ModeloRecordStatus is a closed lifecycle with VIGENTE for the currently-effective filed answer and SUPERSEDIDO for a record a later filing replaced, and superseded records are deliberately RETAINED for audit rather than deleted. So a work unit can legitimately own more than one record, dictionary order decides which one an unfiltered next() returns, and the checker compares against the filing it resolved as current. Picking a superseded id would make the fixture disagree with the checker for a reason that has nothing to do with the behaviour under test, and it would do so intermittently -- the worst failure shape, because it looks like flake and invites a retry rather than a diagnosis. Today's fixture seeds exactly one record per work unit so the bug is latent and cannot be the cause of anything currently red; it becomes real the moment a scenario exercises an amended filing, which is precisely the cross-period ground this module covers. CORRECTION TO APPLY once the run lands: filter the candidates on status VIGENTE before taking one, and if that yields nothing leave filing_record_id unset rather than substituting a superseded id, since an absent key now correctly reports MISSING while a wrong key reports MISMATCHED and would send an operator to reconcile a divergence that does not exist. This is the same absent-versus-divergent distinction the production half of this fix was about, applied to the fixture that exercises it; `src/cadrumo/application/calculations/tests/_cross_period_clean_state_support.py`.
- [x] `P02.S83` - Resolve what the twelve orphan size-budget pins actually are, and refute the assumption that they can be transferred wholesale. Measured read-only 2026-08-28 while a run held the tree. All twelve orphans are RENAMES, not deletions: every one is an underscore-private path whose public successor exists, which is the import-centralization campaign making modules public. That confirms S51's reading that relocation is not growth -- but it does NOT follow that the pins can simply be moved to the successors, and the per-module numbers are why. Pinned versus current: registry/_loader.py 1453 -> loader.py at 300; auth/_operator.py 1315 -> operator.py 1262; registry/_formula_runtime.py 1431 -> 1372; registry/_schema.py 1630 -> 1555; ledger/_llm_classification.py 1661 -> 1568; wizard/_commands.py 2047 -> 2019; registry/_ledger_bindings.py 2226 -> 2150; ledger/_actions_manual.py 1500 -> 1524; workflow/_engine.py 1353 -> 1422; google/_calc_sheets_pull.py 1422 -> 1536; ledger/_evidence_draft.py 2965 -> 3028; and registry/_record_design.py 1338 -> record_design.py at 4785. THREE DIFFERENT DISPOSITIONS HIDE INSIDE ONE POPULATION. loader.py at 300 against a 1250 default budget is debt genuinely PAID -- its pin should be deleted outright, not transferred, and transferring it would re-declare 1453 lines of debt that no longer exists. Seven successors sit below their old pin, where transferring the OLD number is honest because it neither accepts new growth nor pretends to progress. Four grew past it, where transferring the current size would silently accept exactly the growth S41 forbids accepting for _common. THE OUTLIER IS registry/record_design.py AT 4785 AGAINST A PINNED 1338, a 3.4x increase and the largest module in this population by a wide margin -- larger than ledger/evidence_draft.py at 3028, which S41 and S51 both name as the worst offender they knew about. It is also the module this campaign has been actively editing for record-design extraction, so some of that growth is this campaign's own and must not be quietly absorbed into a peer's baseline. TWO POPULATIONS S51 DID NOT ENUMERATE, found by the same scan and needing the same per-entry judgement: five orphan CALLABLE pins (ledger/_evidence.py::add, _evidence_draft.py::confirm_invoice_draft_from_evidence, _identity_roles.py::resolve_counterparty_identity, user_profile/_custody_carry.py::_natural_key_resolvers, registry/_formula_runtime.py::calculate_registry_snapshot) and seven orphan NOTES entries. Nothing was written: --write-baseline remains the mute button S41 forbids, and the correct move is per-entry disposition, which is a Step of its own; `dev/audit/size_budget_baseline.json,src/cadrumo/domain/calculations/registry/record_design.py`.
- [x] `P02.S84` - Propose a grounded decomposition for registry/record_design.py, the largest size-budget outlier, measured read-only rather than guessed. It is 4785 lines against a 1250 default budget and a stale pin of 1338 carried on its pre-rename path, and part of that growth is THIS campaign's own record-design extraction work, so it must not be quietly absorbed into a peer's baseline. WHY IT IS AN UNUSUALLY SAFE DECOMPOSITION, and this is the measurement that matters: of 141 top-level definitions only SIX are public, and those six total roughly 100 lines -- five thin extract_* entry points of 6 to 11 lines each plus contiguity_failure at 56. The other 135 definitions are private. So the public defining module keeps its entire published surface unchanged while the private body moves to sibling private modules inside the same package, which is exactly what aeat-architecture-boundaries permits: truly private modules remain package-internal, and no consumer's import path changes. This is NOT the hazardous case that rule warns about, where a shared contract is trapped behind an underscore and needs an atomic hard-move with every consumer swept. FOUR COHESIVE GROUPS MEASURED BY NAME AND SIZE: pdf extraction at 1136 lines across 35 definitions, including the two largest classes in the file, _PdfParseState at 309 and _PdfSheetDraft at 207; row-repair heuristics at 623 lines across 14 definitions -- rejoin reversed columns, rejoin bare coordinates, repair truncated offsets, recover coordinate stutter, split tail from leading fragment -- which is a genuinely separable concern and the one most likely to keep growing as new malformed disenos appear; workbook and xls handling at 212 lines across 11; and corrections loading and application at 164 lines across 3. Extracting those four removes roughly 2135 lines and leaves about 2650. HONEST LIMIT OF THIS PROPOSAL: 2650 is still double the budget, and a residual 1760 lines across 78 unclassified definitions needs its own reading before a second split can be proposed. Do not present the four-way split as closing this module's debt; it is a large first step that makes the remainder legible. Sequence it AFTER the orphan-pin dispositions in the sibling Step, because pinning or deleting entries for a module about to be split would have to be redone; `src/cadrumo/domain/calculations/registry/record_design.py,dev/audit/size_budget_baseline.json`.
- [x] `P02.S85` - CORRECT the record_design.py decomposition groupings recorded in the sibling Step, which under-measured the largest group and missed one entirely. The sibling reported row-repair at 623 lines across 14 definitions and an opaque residue of 1760 lines across 78 definitions it could not characterise. Reading that residue rather than leaving it as a footnote showed it is not miscellaneous at all: it is mostly MORE row repair under verbs the first pattern never matched -- split_glued_naturaleza_rows, split_fused_ordinal_position_prefix, split_glued_ordinal_position, reattach_stranded_casilla_tags, collapse_doubled_coordinate_rows, split_row_from_wrapped_content, split_fused_ordinal_offset_rows, join_wrapped_row_descriptions, fold_untagged_desglose_components. The first pass keyed on rejoin, repair and recover and so missed split, collapse, reattach, join and fold, which is a reminder that a name-pattern grouping reports the pattern's coverage and not the module's shape unless the residue is read. CORRECTED MEASUREMENT: row-repair and text-normalisation is 1079 lines across 22 definitions, not 623 across 14 -- comparable to pdf extraction at 1106 across 32, and the single most cohesive extraction candidate rather than the third-largest. A FIFTH GROUP EXISTS that the sibling Step does not mention at all: visual and chart handling, 234 lines across 18 definitions, surfaced by 'visual' and 'chart' each appearing 11 times among the unclassified name tokens. Workbook at 212 across 11 and corrections at 164 across 3 are unchanged. The genuinely unclassified core is 1100 lines across 55 definitions, down from 1760, and includes the public surface plus the field and record assembly primitives -- contiguity_failure, consume_field_row, naturaleza_or_none, unnamed_position_candidate, variable_envelope, bracketed_payload_positions, row_identities_by_record -- which read as the module's actual subject and are the right thing to LEAVE behind. REVISED PROPOSAL: extracting all five groups removes roughly 2795 def-lines and leaves a core of about 1100 definition lines plus module-level constants and imports, which is plausibly at or near the 1250 budget rather than the roughly 2650 the sibling Step projected. That is a materially better outcome than first recorded, and it is available BECAUSE the residue was read instead of being carried as an unknown. Still to verify before acting: whether the five groups are import-acyclic with respect to each other, since a repair helper calling back into pdf state would force a different cut; `src/cadrumo/domain/calculations/registry/record_design.py`.
- [x] `P02.S86` - Establish that a broad pytest run CANNOT be a clean measurement in this shared worktree, and change the measurement unit accordingly. The previous Step blamed a polluted run on my own mid-run edits and prescribed holding edits until a run lands. That prescription was right but INSUFFICIENT, and the evidence arrived immediately: the replacement run, started against a settled tree with no edits of mine, was itself overtaken. HEAD advanced three commits while it executed -- 5b049e4c5e at start, cc41325511 by the 86 percent mark -- and three further test modules were modified in the working tree in the same window. Failures and then collection ERRORS appeared in exactly the later bands, the same signature I previously attributed to my own edits. THE GENERALISATION: this worktree has active peers committing every few minutes, and a broad calculations-plus-registry run takes on the order of fifteen. A measurement whose window is longer than the tree's quiet period is not a measurement, no matter how disciplined the person running it, because modules collected at one revision are imported at another. Serialising my own edits does not fix it -- I do not control the other writers, and the worktree-safety rule forbids me from freezing their work to get a clean read. CONSEQUENCE FOR METHOD, and this supersedes the earlier prescription rather than repeating it: the unit of measurement here is a NARROW, FAST selection that completes inside the quiet period -- one module, one cluster, one to two minutes -- run immediately before acting on its result. A broad run is still worth having, but ONLY as an INVENTORY TO CONFIRM, never as a verdict: every entry it produces must be re-run narrowly against current HEAD before anyone diagnoses it, exactly as the standing rules already require for swarm output and for acting on any finding a peer may have landed against. The project's own local-execution rule anticipated the mechanism -- a dead parallel run is more likely the drive or a loader-cache race than the code, re-run sequentially before triaging -- and this extends it from flakiness to tree motion. PRACTICAL EFFECT ON THE OPEN WORK: the two csv-register fixes must be verified by running their single module, not by reading a broad run's list, and the same holds for anything the current run reports when it lands. Its output is to be treated as a candidate list whose every member is unconfirmed; `src/cadrumo/application/calculations/tests/test_cross_period_clean_state_provenance.py`.
- [x] `P02.S87` - VERIFY the two csv-register fixes and land the queued VIGENTE hardening, using the narrow method the sibling Step established rather than waiting on another doomed broad run. Both verified green: test_cross_period_clean_state_provenance.py is 13 passed, run sequentially with -n 0 at HEAD cc41325511, and again 13 passed after the VIGENTE change. That covers the justificante and live-capture siblings as well, which share the seeding helper and were the real regression risk of touching it. THE VIGENTE HARDENING IS NOW APPLIED: the fixture filters candidate filing records on ModeloRecordStatus.VIGENTE before selecting one, because a work unit can own several -- an amended filing supersedes rather than replaces and superseded records are retained for audit -- and the checker compares against the record it resolved as current. An unfiltered first-match would have disagreed with it by dictionary order, intermittently, which is the worst failure shape because it reads as flake and invites a retry instead of a diagnosis. ModeloRecordStatus was already imported, so no import edge was added. WHY THE BROAD RUN WAS ABANDONED, and this is the sibling Step's thesis proven a second way: it did not merely go stale, it died -- gw1 node down, not properly terminated -- which is precisely the failure the local-execution rule attributes to this worktree's backing share under concurrent I/O rather than to the code. Between peer commits landing mid-run and a dead xdist worker, it produced no FAILED list at all after roughly fifteen minutes, while the narrow sequential run answered the actual question in five. WHAT REMAINS HONESTLY UNKNOWN, stated rather than glossed: the broad runs showed two failures in the band where this module sits, and the module passes SEQUENTIALLY at HEAD. Those two are therefore either different tests entirely, or this module failing only under parallel xdist -- a loader-cache race, which the local-execution rule names as the more likely reading for registry-adjacent suites. The killed run never emitted its FAILED list, so the two cannot be identified, and no claim is made that this module is green under parallel execution. Sequential green at HEAD is what was measured and all that is asserted; `src/cadrumo/application/calculations/tests/_cross_period_clean_state_support.py,src/cadrumo/application/calculations/cross_period_clean_state.py`.
- [x] `P02.S88` - CORRECT the orphan-pin disposition plan recorded in the two sibling Steps, which prescribed a mechanism the baseline file forbids and an ordering that would bless the debt. Reading the artefact's own contract before acting on it -- rather than after -- changed both. WHAT THE BASELINE ACTUALLY SAYS, in its own _comment and in dev/audit/size_budget.py: do NOT hand-edit the modules or callables numbers; regenerate with --write-baseline and review the diff. Every limit is a measured size PLUS DECLARED HEADROOM, not a raw line count, and the gate fails in BOTH directions -- when an entry grows past its limit, and when a limit drifts further above its subject than the slack tolerance or covers a subject the default limit now governs, which the tool names a STALE PIN. So the sibling Step's prescription to transfer the OLD pinned number by hand to each renamed successor is wrong twice over: it is a hand-edit of exactly the numbers the file prohibits editing, and for loader.py it would move a 1453 limit onto a 300-line module, which trips the stale-pin rule instantly rather than recording debt. The instinct that loader.py's pin should simply be deleted was right, but the mechanism was not mine to choose. THE ORDERING WAS ALSO BACKWARDS. The sibling Step sequenced record_design.py's decomposition AFTER the orphan-pin dispositions. It must be the reverse. Regeneration rewrites every limit from the CURRENT measured size, so regenerating first would stamp record_design.py's 4785 lines, evidence_draft.py's 3028 and the other growers into the baseline as declared debt -- which is precisely the mute button S41 forbids by name for _common, arrived at by following the sanctioned procedure at the wrong moment. Decompose the genuine offenders FIRST, regenerate LAST, so the regenerated baseline records the improved state instead of blessing the unimproved one. Only then are the orphan renames swept for free, because relocation genuinely is not growth and regeneration handles it correctly once nothing is hiding behind it. WHERE JUDGEMENT IS STILL ALLOWED TO LIVE: notes is the one hand-maintained section, prose only and never numbers, carried forward verbatim across regeneration and dropped when its key disappears. An oversized module that genuinely should not be decomposed is accepted THERE with a stated reason, which is the auditable form of the acceptance S51 asks for -- not by quietly letting a regenerated number cover it. Seven of the orphan notes entries will be dropped automatically when their stale keys vanish, so any reasoning worth keeping must be re-authored against the successor path in the same change; `dev/audit/size_budget_baseline.json,dev/audit/size_budget.py`.
- [x] `P02.S89` - REFUTE the record_design.py five-way decomposition proposed in the sibling Steps, by running the acyclicity check that Step listed as its own prerequisite. The proposal does not survive it. A reference graph over all 141 top-level definitions, counting every Name reference from each definition into another, shows EVERY group pair is bidirectional -- seven cycles, not zero: core<->pdf 14/25, core<->xls 13/11, repair<->core 29/2, repair<->pdf 52/19, pdf<->visual 2/20, xls<->repair 2/2, pdf<->xls 2/1. Cutting the five modules as proposed would create import cycles between them, which the architecture rules reject outright. The size grouping was real and the boundary was not: name vocabulary described what these functions are ABOUT, never what they DEPEND ON, and only the graph could tell the difference. WHAT THE GRAPH ACTUALLY SHOWS. repair<->pdf at 52 and 19 is not a seam but the module's spine -- the repair helpers exist to operate ON pdf row structures, so pdf-extraction at 1106 and row-repair at 1079 are ONE concern of roughly 2185 lines, and the earlier claim that row-repair is the most cohesive extraction candidate is exactly backwards. visual is not a leaf either: it calls pdf 20 times while pdf calls it twice, from _extract_record_design_pdf_stream and _snapshot_pdf_page, so extracting it yields a thin but genuine cycle that only dependency inversion would remove -- design work, not file-splitting. xls is bidirectional with core at 13 and 11. EXACTLY ONE CLEAN CUT EXISTS, and it is small: corrections calls NOTHING -- verified with zero edges into pdf, core, repair or xls -- so it is a pure leaf, freely extractable today at 164 lines across 3 definitions. THE HONEST REVISED EXPECTATION, replacing the sibling Step's projection: that Step said extracting all five groups leaves a core near 1100 lines and plausibly at the 1250 budget. That is withdrawn. Only 164 of 4785 lines are freely extractable, about three percent, and the remaining 4600 are a genuinely entangled parser whose decomposition requires inverting the pdf/repair/visual dependencies rather than moving functions between files. Anyone who splits this module on the name boundary will produce import cycles and have to revert. record_design.py should therefore be treated as REQUIRING DESIGN WORK, not as a mechanical size-budget chore -- and since it cannot be decomposed cheaply, the baseline decision for it is a notes-section acceptance with a stated reason, which is the sanctioned auditable form, rather than a regenerated number; `src/cadrumo/domain/calculations/registry/record_design.py,dev/audit/size_budget_baseline.json`.
- [x] `P02.S90` - Close the operator-facing half of the Modelo 390 exercise-2026 gap, which the earlier Step identified as a registry limitation but never followed to the surface an operator actually meets. The registry side is settled and correct: modelo 303 is forward-open through 2026 while modelo 390's epochs stop at 2025, because the exercise-2026 orden publishes around November 2026 and cannot honestly be authored today. A real 2026 filer computing quarters and then reaching for the annual resumen therefore meets NoRevisionForPeriodError. THE REFUSAL DOES NOT TELL THEM WHAT IS AVAILABLE, and the CLI contract is explicit that a late registry-driven refusal is acceptable for an axis depending on dynamic registry data but MUST list the accepted set and never be a bare value-invalid. What is carried today: the error's own docstring enumerates its structured attributes as modelo_id, filing_year, period and revision_id -- a deliberate design so a consumer can build an instructive refusal without parsing the message, which is right as far as it goes. But there is NO available-set among them, so no consumer can list the accepted years even if it wants to; the information never leaves the selector. The raise sites in temporal.py hold the modelo object and therefore its full revision collection at the moment they refuse, so the accepted set is in scope and simply not captured. Downstream, the error-registry entry maps it to ERROR_CALCULATIONS_REGISTRY_NO_REVISION_FOR_PERIOD under the GENERIC message key errors.error.error_calculations_registry_snapshot, shared with the plain snapshot error, with no runbook and no suggestion -- so the code is specific while the operator-facing text is not. Only two production sites catch it at all, _binding_readiness.py which swallows it, and registry/_diff.py. FIX SHAPE: carry the candidate years, or the modelo's declared coverage, as structured context on the error at the raise site, so the CLI can render 'modelo 390 covers 2021 through 2025; exercise 2026 is not yet published by AEAT' instead of a bare no-revision message. That turns an apparent malfunction into an accurate statement about the world, which is what the 2026 filer needs -- the application is behaving correctly and only its explanation is missing. CONFIDENCE, stated rather than overclaimed: this was established by reading the error class, its raise sites and its error-registry entry, NOT by observing a CLI invocation. Confirm the rendered operator output before treating the gap as measured; `src/cadrumo/domain/calculations/registry/errors.py,src/cadrumo/domain/calculations/registry/temporal.py,src/cadrumo/core/errors/registry/_domain_part2.py`.
- [x] `P02.S91` - Upgrade the Modelo 390 exercise-2026 unauthorability claim from an assumed publication cycle to an OBSERVED one, and give the gap a concrete trigger date. Three earlier Steps asserted that exercise 2026 cannot be authored because the orden publishes 'around November 2026', which was reasoning from the filing calendar rather than from evidence -- defensible, but the kind of confident-sounding inference this campaign has repeatedly had to retract. Checked it directly against the bundled corpus instead. The newest Modelo 390 diseño de registro on disk is 01-390-ejercicio-2025-actualizado-05-12-2025, an exercise-2025 design stamped 5 December 2025, and the remaining files run backwards through ejercicio 2009 to 2015 and beyond. There is no exercise-2026 artefact of any kind. That confirms the claim and sharpens it: the exercise-2025 design landed in early December of its own filing year, so the exercise-2026 equivalent is expected around December 2026, roughly three months after today's 2026-08-28. The gap is therefore not merely 'not yet published' as an open-ended state but a dated wait with a specific artefact to watch for, which is what makes it actionable rather than a standing excuse. WHAT THIS DOES AND DOES NOT SETTLE: it confirms the registry is right to refuse and that authoring a 2026 revision now would mean inventing regulatory content. It does NOT close the operator-facing half recorded in the sibling Step -- the refusal still fails to name the accepted set, and a filer meeting it in the meantime still cannot tell a missing feature from an unpublished orden. Both halves want the same follow-up: when the December artefact appears, author the revision AND confirm the refusal it replaces was informative while it stood; `src/cadrumo/_data/corpus/aeat_official/disenos_registro/modelo_390,src/cadrumo/_data/registry/aeat/modelos/390`.
- [x] `P02.S92` - Strengthen the no-revision refusal finding with an internal precedent that settles it, and record one smaller defect found beside it. The sibling Step argued from the CLI contract that NoRevisionForPeriodError should carry the accepted set. There is a better argument available inside the codebase, and it removes the question of whether this is a design choice: ITS OWN SIBLING ALREADY DOES IT. AmbiguousRevisionSelectionError, raised by the same selector in temporal.py a few lines from the no-revision raise, carries candidate_ids -- and _binding_readiness.py consumes exactly that, logging 'filing_year=%s for modelo=%s is covered by revisions %s' by joining exc.candidate_ids. So the selector already establishes the pattern that a revision-selection refusal names the revisions it considered; the no-revision case is the one member of that family that does not, while being the case where naming them matters MOST, because the operator's question is precisely which years exist. That reframes it from a missing feature to an asymmetry within one error family. A SMALLER DEFECT IN THE SAME FUNCTION, found while reading it: the three failure branches of this helper are inconsistent about visibility. The RegistrySnapshotError branch logs at debug before returning None, and the ambiguous branch logs the candidates and its reasoning at length, but the NoRevisionForPeriodError branch is a bare 'except ... return None' with no log at all. A developer debugging why profile bindings came back undetermined sees a diagnostic for two of three causes and silence for the third -- and the silent one is the common case. The helper's contract of None-means-undetermined is right and should not change; only the silence should. WHAT NOT TO DO HERE, because the code already gets this right and says so: the ambiguous branch's comment records that the remedy is deliberately NOT restated locally but rides on the raiser, so the log quotes the error rather than composing a second copy that could drift from the selector's own advice. Any fix to the no-revision branch must follow that same discipline -- add the accepted set at the RAISE site in temporal.py where the modelo's revision collection is already in scope, and let this consumer quote it, rather than having the consumer reconstruct the available years itself; `src/cadrumo/domain/calculations/registry/temporal.py,src/cadrumo/domain/calculations/registry/errors.py,src/cadrumo/application/modelo/_binding_readiness.py`.
- [x] `P02.S93` - Record the registry-correctness inventory result and the THIRD consecutive broad run invalidated by peer tree motion, which turns an observation into a settled property of this environment. THE USABLE HALF OF THE RESULT: domain/calculations/registry/tests ran green through 94 percent -- several hundred tests, zero failures and zero errors -- which is a genuinely healthy signal for the priority-2 area and the first broad measurement this campaign has obtained that says anything at all. THE UNUSABLE HALF, and the timing is the evidence: four failures appeared, all of them clustered in the final 95 to 98 percent bands and none before, while HEAD moved from cc41325511 to f73ba28033 during the run. Those commits include relocation:review_public_modules and a retirement of five private modules -- precisely the change class that breaks imports for whatever has not yet been collected and executed. Failures that appear only AFTER a relocation lands, and only in the tail, are the signature already established twice today; they are unconfirmed and most likely artifacts of the tree moving, not registry regressions. They are NOT being reported as findings. THE PATTERN IS NOW SETTLED RATHER THAN SUSPECTED. Three broad runs, three invalidations, three distinct mechanisms: the first by my own mid-run edits, the second by peer commits plus a dead xdist worker that emitted no summary at all, the third by a peer relocation landing in the tail. The common cause is not carelessness but arithmetic -- a fifteen-minute measurement window in a worktree whose quiet period is a few minutes cannot produce a coherent result, and no amount of discipline on my side changes that, because I do not control the other writers and must not freeze their work to get a clean read. WHAT THIS CHANGES GOING FORWARD: stop treating a broad run as something to be repeated until it comes out clean, which is the trap this campaign nearly fell into three times. The green 94 percent prefix is real evidence and should be reported as such; the tail is noise and should be discarded without diagnosis. Any specific claim about a registry test requires a narrow sequential re-run at the HEAD of the moment, which is cheap, and that is the only measurement here whose result outlives the run that produced it; `src/cadrumo/domain/calculations/registry/tests`.
- [x] `P02.S94` - Implement the accepted-set on NoRevisionForPeriodError and end the silent branch that consumed it, closing the operator-facing half of the Modelo 390 exercise-2026 gap. DEMONSTRATED ON THE REAL CASE, not on a synthetic one: requesting modelo 390 filing_year 2026 period 0A through the bundled authority now raises 'modelo 390: no revision for year=2026 period=0A revision=None; modelo 390 declares: 2021, 2022, 2023, 2024, 2025', with available_revision_ids carrying that tuple structurally. An operator who previously could not distinguish a broken application from an unpublished orden is now told exactly which years exist, which is the whole difference between an apparent malfunction and an accurate statement about the world. THE PARAMETER IS REQUIRED, NOT DEFAULTED, and that was the deliberate choice: a default would let a future raiser silently ship the uninformative form, which is the same empty-filter hazard this campaign has guarded against all along. It is safe to require because there are exactly two construction sites, both in temporal.py, both already holding the modelo's revision collection, and no test constructs the error directly -- so there is no case where the accepted set is genuinely unknown. It follows the sibling AmbiguousRevisionSelectionError's established shape: sorted tuple attribute, _csv into the structured context, named in the fallback text, and appended to the message only when non-empty. THE CONSUMER FIX FOLLOWS THE DISCIPLINE THE CODE ITSELF DOCUMENTS: the NoRevisionForPeriodError branch in _binding_readiness.py returned None with no log while its two sibling branches logged, so a developer debugging undetermined profile bindings got diagnostics for two of three causes and silence for the common one. It now logs, quoting the error rather than composing a second copy -- exactly what the ambiguous branch's own comment says to do, because a locally restated remedy drifts from the selector's advice. The None-means-undetermined contract is untouched. VERIFICATION IS INCOMPLETE AND THAT IS STATED RATHER THAN GLOSSED: the behaviour itself is proven by the live authority call above, and ruff is clean on all three files, but the regression run over test_m390_temporal_epochs, test_filing_schedule_selection and test_binding_readiness could NOT complete -- a peer's uncommitted work in domain/contribuyente leaves SUPPORTED_PROFILE_SCHEMA_VERSION undefined and the package unimportable. That is peer WIP of the kind that has self-healed repeatedly today, and their files were not touched. Re-run those three modules narrowly once the tree imports again; `src/cadrumo/domain/calculations/registry/errors.py,src/cadrumo/domain/calculations/registry/temporal.py,src/cadrumo/application/modelo/_binding_readiness.py`.
- [x] `P02.S95` - Complete the accepted-set verification and fix a red registry gate found by it, which turned out to be the same positional-selection defect corrected in the calculation-route tests earlier today. ALL THREE MODULES NOW GREEN: 41 passed sequentially over test_m390_temporal_epochs, test_filing_schedule_selection and test_binding_readiness, confirming the required accepted-set parameter on NoRevisionForPeriodError and the newly-logged branch in _binding_readiness.py broke nothing. THE GATE THAT WAS RED WAS WRONG, AND FOR A REASON WORTH NAMING. test_validator_rejects_filing_schedule_cadence_contradictions[184-annual-quarterly] died on a bare StopIteration, which reads like missing registry data. It is not: modelo 184 declares SIX revisions -- 2015, 2016-2018, 2019-2021, 2022, 2023-2024 and 2025-y-siguientes -- and only the newest two carry a filing_schedules fragment at all. The test did next(iter(modelo.revisions.values())) and then searched THAT revision for a schedule of the declared kind, so it reached 2015, found nothing, and raised. The registry is correct; older revisions predating the filing-schedule concept legitimately declare none. The test was selecting its subject BY POSITION rather than by the property it exists to exercise, so it was hostage to mapping order and to whichever revisions happen to exist. Fixed by selecting the revision and schedule together by the property under test -- the first pair whose period_kind matches -- which makes the test independent of how many revisions modelo 184 declares or in what order they arrive. Its actual subject, that the validator rejects a cadence contradiction, is unchanged and still exercised. THIS IS THE THIRD INSTANCE TODAY of one defect class: a test pinning its subject positionally rather than by identity. The calculation-route tests used OWNERSHIP[-1] and OWNERSHIP[:-1] and silently tested the wrong row once a sibling appeared; the cross-period fixture took the first filing record by work_unit_id and would have picked a superseded one; this took the first revision and found an empty search. All three read as data problems and were selection problems. Worth carrying as a review heuristic rather than three separate fixes: when a test says 'the first', ask what happens when a second arrives -- because in this repository a second always does; `src/cadrumo/domain/calculations/registry/tests/test_filing_schedule_selection.py,src/cadrumo/domain/calculations/registry/errors.py,src/cadrumo/domain/calculations/registry/temporal.py,src/cadrumo/application/modelo/_binding_readiness.py`.
- [x] `P02.S96` - Sweep the codebase for the positional-selection defect class after hitting its third instance, and size it honestly rather than declaring fifty bugs. The class, established by three independent instances today: a test pins its subject by POSITION -- OWNERSHIP[-1], OWNERSHIP[:-1], the first filing record for a work unit, next(iter(modelo.revisions.values())) -- and silently tests the wrong thing, or dies on an empty search, the moment a sibling appears. All three read as data problems and were selection problems, which is what makes the class worth naming: the symptom never points at the cause. FIFTY candidate sites match next(iter(...revisions...)) across src/cadrumo. THEY ARE NOT FIFTY DEFECTS and must not be reported as such -- many operate on a synthetic single-revision modelo the test itself constructed, where taking the only member is correct and clearer than a search. The at-risk subset is specifically those reading a REAL bundled modelo that declares more than one revision. Two were sampled and both qualify: application/registry/tests/test_temporal_coverage.py takes registry_authority.modelo('341') and then its first revision, and modelo 341 declares two; domain/calculations/registry/tests/test_audit_oracle_bindings.py carries a DOUBLE positional assumption in _bind_oracle_id_on_first_cross_reference, taking the first revision AND then cross_references[0], so a first revision declaring no live cross-references raises IndexError rather than failing on its subject. Modelo 038, used the same way in test_filing_capability_worklist, also declares two. These pass today only because the first revision happens to carry what the test needs -- the exact condition that held for modelo 184 until four older revisions preceded the two that declare filing schedules. THE HEURISTIC IS THE DELIVERABLE, not a fifty-site sweep nobody will finish: when a test says 'the first', ask what happens when a second arrives, because in this repository a second always does -- peers add revisions and epochs continuously, and modelo 184 went from working to StopIteration by exactly that route. Fixing a site is cheap and local: select by the property under test, as the filing-schedule gate now does by matching period_kind across all revisions rather than searching only the first. Prioritise sites reading real multi-revision modelos; leave synthetic-fixture sites alone; `src/cadrumo/application/registry/tests/test_temporal_coverage.py,src/cadrumo/domain/calculations/registry/tests/test_audit_oracle_bindings.py,src/cadrumo/domain/calculations/registry/tests/test_filing_capability_worklist.py`.
- [x] `P02.S97` - Correct a temporal-coverage test that was exercising the WRONG SUBJECT while passing, the first LIVE instance of the positional-selection class rather than a latent one. test_temporal_coverage_expands_open_selectors_through_the_supported_horizon takes modelo 341 and then next(iter(modelo.revisions.values())). Modelo 341 declares two revisions and the iteration order was measured rather than assumed: ['2005-2015', '2016-y-siguientes'], and the selected one is 2005-2015 with year_from 2005. That revision is CLOSED -- its selector carries year_to = 2015 -- while 2016-y-siguientes is the open one, year_to None. So a test whose NAME says it expands OPEN selectors, and whose docstring says a real long-span selector proves later years are not silently skipped, was handed a BOUNDED selector every run. It passed anyway, which is the part worth dwelling on: the assertion compares all report rows against revision_selection_coordinates for the chosen revision, and range(year_from, horizon+1) from 2005 happens to span what the report produces across both revisions. A green test, a truthful-looking name, and the wrong subject -- and nothing in the failure output could ever have revealed it, because there was no failure. THE DISTINCTION FROM THE THREE EARLIER INSTANCES MATTERS. Those broke loudly once a sibling arrived: the route tests tested the wrong row, the M184 gate died on StopIteration, the cross-period fixture would have picked a superseded record. This one does not break at all; it silently under-tests, and would keep doing so indefinitely. That makes positional selection worse than a fragility -- it is a correctness hazard for the SUITE, because a gate exercising a bounded selector cannot catch an open-selector expansion defect no matter how many times it runs green. Fixed by selecting on the defining property, year_to is None, so the subject matches the name. VERIFICATION PENDING AND STATED AS SUCH: ruff is clean, but the test run could not execute -- a peer is mid-relocation in adapters/persistence/storage/custody with untracked new public modules beside the old private ones and the error-code registry half-edited, so AccelerationReceiptRevocationError has no declared entry and the package raises at import. Notably the project's own error text anticipates exactly this, advising the reader to check git status and rerun once the working tree settles. Re-run this module narrowly once it does, and expect a REAL possibility that the corrected test now fails: if open-selector expansion is genuinely broken, this fix is what will finally surface it; `src/cadrumo/application/registry/tests/test_temporal_coverage.py`.
- [x] `P02.S98` - Finish the temporal-coverage correction and VERIFY it: 39 passed sequentially. The prediction in the sibling Step held exactly -- pointing the test at its stated subject made it FAIL, and the failure was informative rather than a regression. With the open revision selected, report rows carried coordinates such as (2014, '2T') that no open-selector expectation could account for, because _authority_with_single_model composes a single MODELO and modelo 341's closed 2005-2015 sibling contributes its own rows. The assertions compare every report row against ONE revision's coordinates, which is only meaningful when that revision is the only one composed. So the test carried TWO coupled defects, and fixing only the first would have left it red: it selected its subject positionally, AND its assertion silently assumed a single-revision modelo. The original green run was the product of those two errors cancelling -- the closed revision's year_from of 2005 made range(2005, horizon+1) span everything the report produced across both revisions, so a bounded selector satisfied an assertion written for an unbounded one. Fixed by composing the authority from the open revision alone, which makes both assertions exactly about open-selector expansion, and the module is green. WHY THIS IS THE MOST INSTRUCTIVE INSTANCE OF THE CLASS SO FAR: the three earlier ones broke loudly the moment a sibling arrived. This one never breaks. It under-tests silently and permanently, and no amount of running it green could reveal that, because there is no failure to inspect -- the only way to find it was to ask what subject the selection actually returns and measure the iteration order rather than assume it. A gate exercising a bounded selector cannot catch an open-selector expansion defect however often it passes, so positional selection is not merely fragile here; it quietly removes coverage the suite believes it has. That is the argument for treating the remaining S96 sites as worth auditing rather than leaving until something goes red, since by construction these will not; `src/cadrumo/application/registry/tests/test_temporal_coverage.py`.
- [x] `P02.S99` - CORRECT the at-risk list produced by the positional-selection sweep, by confirming each named site instead of trusting the sweep's own output. The sweep Step named three sites as at-risk on the strength of a pattern match plus a sampled revision count. Confirming them modelo by modelo shows the list was PART WRONG, and in the direction that matters -- it over-flagged. Measured against the bundled authority: modelo 341 has 2 revisions (2005-2015, 2016-y-siguientes), modelo 038 has 2 (2024-desde-06, 2025-y-siguientes), but modelo 036 has ONE (2025-02-03-y-siguientes) and modelo 130 has ONE (2019-y-siguientes). SO test_audit_oracle_bindings IS NOT AT RISK TODAY, contrary to what the sweep recorded and what the loop prompt was carrying forward as a confirmed target. Its _bind_oracle_id_on_first_cross_reference does carry a double positional assumption, but modelo 130 declares exactly one revision, so next(iter(...)) cannot select wrongly; and the remaining cross_references[0] is deliberate and documented in the very next test, which explains that Modelo 130's first cross-reference is static_official_documentation and is overridden precisely because the audit rejects that surface for every oracle by construction. That is an intentional choice with a stated reason, not an accident of ordering. Editing it would have been a change for its own sake against a test that is correct. Same for the modelo 036 site in test_temporal_coverage. THE ONLY CONFIRMED DEFECTS IN THE SWEEP WERE THE ONES ALREADY FIXED: modelo 184's filing-schedule gate, which died on StopIteration, and modelo 341's temporal-coverage test, which passed while exercising a bounded selector. Both were real, both are green. THE LESSON IS THE SWEEP'S OWN, TURNED INWARD. This campaign's standing rule is that a broad run is an inventory to confirm and never a verdict. A grep sweep is the same kind of artefact and deserves the same treatment: fifty pattern matches are candidates, a sampled revision count is not a confirmation, and the confirming question is always the same one -- how many revisions does THIS modelo actually declare. Two of four named sites did not survive that question. Report the confirmed two, drop the other two, and do not carry an unconfirmed sweep entry forward as though measuring it had already happened; `src/cadrumo/domain/calculations/registry/tests/test_audit_oracle_bindings.py,src/cadrumo/application/registry/tests/test_temporal_coverage.py`.
- [x] `P02.S100` - Refine the settled-tree check after a peer relocation defeated it, and record why filing-export correctness could not be measured this pass. THE METHOD BUG IN MY OWN PROCEDURE: all day the check for whether a peer's in-flight relocation had settled was 'does the package import?' -- run a one-line import, see OK, proceed. That is INSUFFICIENT, and this pass proved it. The custody relocation currently defines ProfileCustodyEnvelope in BOTH adapters/persistence/storage/custody/records.py, new and untracked, AND custody/_records.py, the tracked original. Both import perfectly. Two distinct classes share one name, so an object built through one path fails an origin check against the other, and the failure surfaces as TypeError: password envelope did not originate from the custody substrate: ProfileCustodyEnvelope -- a TYPE error naming the very class it just rejected, which reads like a genuine invariant violation in the code under test rather than a half-finished move. A relocation is settled when the OLD definition is GONE, not when the new one imports; duplicate-definition is precisely the intermediate state the atomic-relocation rule exists to forbid, and it is invisible to an import probe. The cheap discriminator is grep -rl 'class X' across the package: two hits means wait. WHAT THIS COST AND WHAT IT DID NOT: four filing-export gates were selected as the next priority-2 target -- export post-write verification, unbuilt-layout refusal, and the two modelo 303 exonerado-390 modules -- and ALL FOUR are unrunnable, 18 errors then 7, because each reaches the profile custody substrate through isolated_runtime_profile. No claim is made about filing-export correctness from this pass; the area is unmeasured, not healthy and not broken. Their files were not touched, per the standing rule, and the state is transient. FOURTH DISTINCT PEER BREAKAGE TODAY, after storage/__init__, domain/iva and domain/contribuyente, and the first that did not announce itself as an ImportError. Worth carrying forward: during an active relocation campaign, an error whose text blames the code under test may still be the relocation talking -- check for a duplicate definition of the named class before believing the message; `src/cadrumo/application/filing/tests`.
- [x] `P02.S101` - Measure what the external-oracle grounding gate actually covers, after confirming it is green and NOT vacuous. The gate passes, 3 tests, and the first question asked of it was the one this campaign asks of every gate: does its declared set have members, or is it filtering nothing? It has members -- 78 externally-grounded casilla ids across the registry -- so it is a real gate and not a passing filter over an empty population. THE DISTRIBUTION IS THE FINDING, and it is a coverage statement about filing-grade grounding rather than a defect: 7 modelos of 58 carry ANY externally-grounded casilla -- 100, 200, 202, 303, 322, 353 and 390 -- and 51 carry none. Within those 7 the concentration is steeper still: modelo 100 alone accounts for roughly 54 of the 78 ids across its 2020, 2021, 2022, 2024 and 2025 revisions, with 29 in 2024 alone, while modelo 202 contributes 1 and modelo 303 contributes 6 across two revisions. So the strongest correctness claim this project makes -- that a casilla's engine value is checked against an independent AEAT authority rather than merely reconciled against the app's own engine -- currently reaches renta deeply and almost nothing else broadly. WHAT THIS IS AND IS NOT. It is NOT a rule violation and must not be reported as one: the no-silent-under-declaration rule requires that a casilla DECLARED as externally grounded be backed by a bundled oracle and independently reproduced, and every one of the 78 satisfies that or the gate would be red. Absence of grounding is not a false grounding claim, and for many of the 51 no bundled oracle exists to ground against -- that is the honest state of the corpus, not negligence. It IS the answer to 'how much of the engine is verified against AEAT rather than against itself', and the answer is: a concentrated minority, led overwhelmingly by modelo 100. Worth stating plainly whenever this product's correctness posture is summarised, because a green grounding gate invites the reading that grounding is broadly established, and it is not; `src/cadrumo/domain/calculations/registry/tests/test_external_oracle_grounding_enrolled.py`.
- [x] `P02.S102` - CORRECT the settled-relocation discriminator recorded one Step earlier, which was itself insufficient, and stop looking for a cheap static check. That Step established that 'does the package import?' is not enough, because duplicate definitions import fine, and proposed grep -rl 'class X' as the discriminator -- two hits means wait. Applied it, got ONE hit, concluded settled, and ran the filing gates. They failed immediately with ModuleNotFoundError: no module named custody._records, raised from _kdf_codec.py which still imports ProfileCustodyKdfParameters from the module the peer had just deleted. So the count dropped to one not because the move completed but because it entered the NEXT broken state: old definition gone, consumers not yet swept. A retry moments later failed identically. THE GENERAL SHAPE, which is why no static probe will work: a relocation passes through a SEQUENCE of distinct broken states -- new module added beside old, so duplicate definitions and a TypeError about origin; old module deleted before consumers are swept, so ModuleNotFoundError; consumers partially swept, so a mix. Each state fails differently and each has a plausible-looking static check that the OTHER states pass. One definition is consistent with both 'finished' and 'mid-delete', and those are indistinguishable without following every import edge, which is more work than simply attempting the run. THE ONLY RELIABLE CHECK IS THE RUN ITSELF, and it is cheap: a narrow sequential selection answers in seconds and is definitive for the exact code being measured, which no proxy can be. Both the import probe and the grep probe were attempts to avoid a run that costs less than the reasoning spent avoiding it. Record this as superseding the grep discriminator: when a relocation campaign is active, do not try to detect settlement -- attempt the measurement, and if it errors on a module boundary rather than on the subject, retry later and go do something that does not touch that substrate. FILING-EXPORT CORRECTNESS REMAINS UNMEASURED for the second consecutive attempt, and no claim is made about it; `src/cadrumo/application/filing/tests`.
- [x] `P02.S103` - Independently corroborate the Modelo 390 recargo omission WITHOUT duplicating the peer who already owns it, and correct a measurement error of my own that nearly produced a false report. THE CHECK: the calculation-grounding rule requires every IVA total-cuota-devengada aggregation to sum the recargo de equivalencia cuota tiers of LIVA art. 161 alongside the repercutido and autorepercutido tiers, and to generalise that whenever a tier is added, every downstream total must enumerate it. Measured across every IVA revision the authority loads. MODELO 303 IS CORRECT IN ALL SIX REVISIONS -- 2022, 2023, both halves of the split 2024, 2025 and 2026-y-siguientes -- with every recargo cuota casilla present in iva.cuota-devengada-total, including casilla 24 general, 21 reducido and 170 super-reducido in 2025, each carrying ley-37-1992:art-161 in its legal_refs. Modelos 322 and 353 declare no recargo casillas at all, so the rule does not reach them. MODELO 390 OMITS THEM: its annual iva.anual.cuota-devengada-total leaves 6 of 9 recargo cuota casillas unsummed in 2024 and 2025, and the same shape holds in 2022 and 2023. THIS IS ALREADY FOUND AND OWNED, which is why it is recorded as corroboration rather than as a discovery: commit 3d72569f09 states the M390 annual total sums three of art 161's four recargo tiers, e621ad4aea traces the fourth-tier omission's blast radius, and a companion audit covers the recargo tabaco rung, 1,75 % under art. 161.4, as having a rate and boxes but no ledger path. My count of 6 rather than 1 is not a contradiction: I counted recargo cuota CASILLAS, which include transitional rate variants such as tipo-1, tipo-0-62 and tipo-0-26 introduced across 2023 and 2024, while the peer counted the four LEGAL tiers. Independent arrival at the same conclusion from registry data is worth having; re-reporting it as new would not be. THE ERROR I NEARLY SHIPPED, recorded because it is the more transferable lesson: my first pass walked only the TOP LEVEL of the formula expression's args and concluded that ALL recargo tiers were missing from every M390 revision -- a far larger and more alarming claim. A formula expression is a TREE, and the three tiers that ARE summed sit inside nested sub-expressions. The peer's audit title was what prompted the re-check, and a recursive walk then reproduced their number. A flat read of a nested structure does not under-report, it over-reports, and it would have sent me to raise a filing-grade under-declaration alarm against work already correctly in hand; `src/cadrumo/_data/registry/aeat/modelos/390,src/cadrumo/_data/registry/aeat/modelos/303`.
- [x] `P02.S104` - Report a GROUNDED structural hazard in Modelo 303 box 45, found by extending the recargo tier-enumeration check to the deducible side -- the OVER-PAYMENT direction the grounding rule says nothing watches. THE OFFICIAL AUTHORITY IS EXPLICIT. The bundled diseno de registro states the formula in the field description itself, in both the 2022-y-siguientes and 2026-y-siguientes workbooks: 'Liquidacion (3) - Regimen General - IVA Deducible - Total a deducir ( [29] + [31] + [33] + [35] + [37] + [39] + [41] + [42] + [43] + [44] ) - Cuota [45]'. Ten addends, one of them box 37, which the same design defines as 'En adquisiciones intracomunitarias de bienes y servicios corrientes - Cuota'. WHAT THE REGISTRY COMPUTES INSTEAD: box 45 is computed from iva.cuota-deducible-total, whose TRANSITIVE dependency set -- checked recursively, not by direct membership -- is six casillas: 43, 44, iva.autorepercutido.interior.deducible, iva.autorepercutido.intracomunitaria, iva.soportado.importaciones and iva.soportado.interiores. Box 37 is NOT reachable from box 45 directly or transitively, in any of the six M303 revisions. Box 37 is itself computed from iva.autorepercutido.intracomunitaria.deducible and is a real exported box, carrying export_ref m303-2025.dp30301.f070, so it is filed. THE OUTPUT IS PROBABLY CORRECT TODAY, WHICH IS WHY THIS IS A HAZARD RATHER THAN A PROVEN MISCALCULATION, and that distinction must not be lost: the total does include iva.autorepercutido.intracomunitaria, the generic reverse-charge cuota, which for an intracomunitario acquisition is the same figure box 37 reports -- reverse charge makes one cuota both devengada and deducible. So the amount is plausibly counted once under a different name. THE HAZARD IS THAT THE TWO PATHS ARE FED BY DIFFERENT BINDINGS: box 45's term resolves through modelo-303-iva-autorepercutido-intracomunitaria-cuota while box 37 resolves through the separate .deducible variant. Nothing enforces that they agree, so the invariant the official design states -- 45 equals the sum of its ten named addends -- is not true BY CONSTRUCTION here; it holds only while two independently-resolved casillas happen to produce the same number. If they ever diverge, the filed return is internally inconsistent and box 45 understates deductions, which increases tax payable and produces no refusal and no signal, exactly the unwatched direction. NEEDS A RULING, NOT A UNILATERAL EDIT: either box 45 should enumerate its official addends so the identity is structural, or the internal decomposition is deliberate and should carry a parity assertion proving the six-term total equals the ten-box sum. There is also a third casilla in this family, iva.autorepercutido.intracomunitaria.devengado, unused by either total, so the generic-versus-explicit pair looks like partially-landed modelling worth settling in the same decision; `src/cadrumo/_data/registry/aeat/modelos/303`.
- [x] `P02.S105` - CORRECT the box 45 finding recorded in the sibling Step, which framed as a single omitted box what measurement shows to be a whole-section parallel decomposition. That Step reported box 37 as unreachable from box 45 and reasoned about the risk of ONE divergent pair. Checking all ten official addends instead of the one I had noticed: [29], [31], [33], [35], [37], [39], [41] and [42] are ALL unreachable from box 45; only [43] and [44] are. Every one of the ten exists in the revision. So eight of ten, not one -- the app does not compute box 45 from the official boxes at all. It computes it from its own semantic casillas (iva.soportado.interiores, iva.soportado.importaciones, iva.autorepercutido.interior.deducible, iva.autorepercutido.intracomunitaria) plus 43 and 44, while the official boxes 29 through 42 are separately computed from those same semantic casillas and exported. Two parallel derivations from one source layer. WHY THIS IS A BETTER FINDING RATHER THAN A RETRACTION: the hazard is unchanged in kind and much larger in scope. The official diseno states the identity in its own field text -- Total a deducir ( [29] + [31] + [33] + [35] + [37] + [39] + [41] + [42] + [43] + [44] ) - Cuota [45] -- and NOTHING in the registry enforces it, because the two sides of that equation are derived independently from the semantic layer rather than one from the other. It very likely holds today, since both derive from the same sources; it is simply not guaranteed, and a filed return whose stated total disagrees with the boxes it claims to sum is internally inconsistent in a way no current gate would catch. It also means my earlier concern about the intracomunitaria .deducible variant diverging from the generic casilla generalises to eight boxes, not one. WHAT THIS CHANGES ABOUT THE REMEDY, and it changes it decisively: the sibling Step offered 'either enumerate the official addends in box 45, or add a parity assertion'. Enumerating is now clearly WRONG -- it would rewrite the modelo's entire deducible decomposition to chase an identity, discarding a semantic layer that exists for good reason (the ledger resolves meanings, not box numbers). The right remedy is the parity assertion, and it is now a much stronger test than it looked: assert that box 45 equals the sum of its ten official addends, which validates the ENTIRE deducible section's dual derivation in one grounded check rather than patching one box. If it passes, the design is proven sound and locked; if it fails, a live filing defect is exposed. Ground it on the diseno text quoted above, which is authority rather than inference; `src/cadrumo/_data/registry/aeat/modelos/303`.
- [x] `P02.S106` - Report that Modelo 303 box 45 omits FIVE official addends outright, grounded in the AEAT diseno, with the over-payment direction of harm named. This supersedes the sibling Step's framing of a parallel-derivation hazard: measurement shows part of it is not a hazard but a live omission. THE OFFICIAL IDENTITY, quoted from the bundled diseno's own field text: 'Total a deducir ( [29] + [31] + [33] + [35] + [37] + [39] + [41] + [42] + [43] + [44] ) - Cuota [45]', and separately 'Resultado regimen general ( [27] - [45] )', so box 45 feeds the amount payable directly. WHAT THE REGISTRY COMPUTES: box 45's only formula argument is iva.cuota-deducible-total -- verified there is exactly ONE formula per target, so nothing is hidden behind a second fragment -- and that total's complete transitive leaf set is six items: 43, 44, iva.autorepercutido.interior.deducible, iva.autorepercutido.intracomunitaria, iva.soportado.importaciones, iva.soportado.interiores. Comparing leaf sets rather than box ids, because three official boxes DO reach 45 under semantic names ([29] as soportado.interiores, [33] as soportado.importaciones, [37] as the intracomunitaria cuota), isolates what genuinely never arrives. FIVE OFFICIAL ADDENDS CONTRIBUTE NOTHING TO BOX 45, and the diseno names each: [31] cuotas soportadas en operaciones interiores con BIENES DE INVERSION, [35] importaciones de BIENES DE INVERSION, [39] adquisiciones intracomunitarias de BIENES DE INVERSION, [41] RECTIFICACION DE DEDUCCIONES, and [42] COMPENSACIONES REGIMEN ESPECIAL A.G. y P. Every one is a deducible cuota. A taxpayer who buys capital equipment, rectifies a prior deduction, or receives agricultural-regime compensation has that deduction dropped from box 45, so [27] - [45] OVERSTATES the amount to pay. It produces valid output, no refusal and no signal -- precisely the unwatched direction the grounding rule warns is unmonitored, since every safeguard here is built against under-declaration. WHAT IS AND IS NOT ALREADY OWNED, checked before reporting: the CLASS is documented for a DIFFERENT modelo -- 2026-06-19-silent-zero-base-aggregation-audit records that M390's cuota-deducible-total omits the import deducible and 'over-states the amount to pay for an importer', recommending the fix be folded into the calculation-aggregation taxonomy ADR because it needs a new box rather than a bounded mirror. No record found for M303 box 45. Bienes de inversion IS an active workstream (the cross-period prorrata audit cites a bienes-inversion unblock and a casilla-43 automatic feed), so [31], [35] and [39] may already sit inside it -- but [41] rectificacion de deducciones and [42] compensaciones A.G. y P. are not obviously covered by that thread and want explicit owners. Fold into the same ADR amendment the earlier audit recommends rather than patching box 45's formula in isolation; `src/cadrumo/_data/registry/aeat/modelos/303`.
- [x] `P02.S107` - Confirm the Modelo 303 box 45 omission is LIVE rather than latent, with the internal inconsistency that proves it is an oversight and not a design. The remaining question after the sibling Step was whether the five unreachable addends can actually carry a value: an omission of boxes nothing can populate would be latent. They can. Measured on the 2025 revision: [31], [35], [39], [41] and [42] are every one input_kind MANUAL with export_refs present, so an operator can type a figure into each and it IS rendered to the fichero. THE CLINCHER IS [43] AND [44]. They are manual and exported too -- the identical kind -- and they DO reach box 45; they are in fact the only two official addends that arrive by box id rather than through a semantic alias. So of seven manual deducible boxes, two are wired into the total and five are not. That asymmetry is what rules out a deliberate architecture: if the design were 'box 45 derives from the semantic layer and never from box ids', 43 and 44 would not be there either. The wiring pattern exists, is used, and was applied to two of seven. CONSEQUENCE FOR A REAL FILING, stated concretely because the direction matters: an operator enters a capital-goods deduction in [31], sees it rendered into the fichero, and box 45 ignores it. The filed return is then internally inconsistent against the identity the diseno prints on the form itself -- [45] does not equal the sum of its own listed addends -- and because [27] - [45] is the resultado, the taxpayer is told to pay MORE than owed. There is no refusal, no advisory, and no gate that fires; the output is well-formed and simply wrong in the taxpayer's disfavour. Every safeguard in this codebase watches under-declaration, so nothing looks in this direction at all. STILL NOT A UNILATERAL FIX. Adding five operands to iva.cuota-deducible-total is a one-line change and is exactly the kind of one-line change this campaign has learned to distrust: bienes de inversion is an active workstream that may intend [31], [35] and [39] to arrive through a resolver rather than as manual operands, and the M390 sibling finding was explicitly ruled ADR-scale rather than a bounded mirror. Route it to the calculation-aggregation taxonomy amendment the earlier audit already recommends, and note that [41] rectificacion de deducciones and [42] compensaciones A.G. y P. have no visible owner in that thread; `src/cadrumo/_data/registry/aeat/modelos/303`.
- [x] `P02.S108` - Persist the Modelo 303 box 45 finding as a vault AUDIT rather than leaving it in plan Steps, so it survives this campaign and reaches the people who own the fix. Created .vault/audit/2026-08-28-silent-zero-base-aggregation-m303-box-45-deducible-addends-audit.md, related to the 2026-06-19 audit that records the same class for M390, and reconciled through vault check all --fix so the modified stamp and body_hash attest the written body rather than the scaffold. A plan Step is an execution record scoped to one campaign; a finding that a filed return can overstate a taxpayer's liability is not campaign-scoped, and the earlier audit's own recommendation was to fold this class into the calculation-aggregation taxonomy ADR -- which needs a citable document, not a checkbox. THE AUDIT CARRIES what the Steps established: the diseno's own field text as authority, the six-item transitive leaf set of box 45 against the ten declared addends, the leaf-SET comparison that distinguishes the three boxes arriving under semantic aliases from the five arriving not at all, the manual-and-exported status that makes it live rather than latent, and the 43/44 asymmetry that rules out a deliberate architecture. It also carries the negative result honestly: the class is NOT novel, being already recorded for M390, and only its M303 instance is new. RECOMMENDATIONS ARE DELIBERATELY NOT A PATCH. The obvious one-line fix -- append five operands -- is argued against explicitly, because bienes de inversion is an active workstream that may intend [31], [35] and [39] to arrive through a resolver, and a hasty operand would pre-empt that design. What is recommended instead: fold into the ADR amendment beside the M390 sibling; name owners for [41] and [42], which no visible thread covers; add the parity assertion grounded on the diseno text; and GENERALISE the method, since this was found by asking whether a total enumerates the addends its own design declares, and other modelos state formulas in their field text too. ONE PROCESS SLIP WORTH RECORDING: the first scaffold attempt produced a research document instead of an audit despite requesting audit, and the stray file was deleted and re-scaffolded through the CLI verb with --topic. Check what a scaffolding tool actually created before writing into it; `.vault/audit/2026-08-28-silent-zero-base-aggregation-m303-box-45-deducible-addends-audit.md`.
- [x] `P02.S109` - Generalise the box-45 method across every modelo whose diseno PRINTS its box formulas, and report the result honestly: one confirmed defect, one clean modelo, one different-in-kind weakness. The method that found the M303 omission is to ask whether a total enumerates the addends its own official design declares. Sweeping the bundled corpus for printed formulas of the form ( [nn] + [nn] ) finds exactly THREE modelos that state them in field text: 303 with 154, 714 with 20, and 123 with 4. MODELO 123 IS CORRECT and is the useful control. Its design prints two identities -- Suma de retenciones e ingresos a cuenta ( [09] + [11] ) [12], and Resultado a ingresar ( [12] - [13] ) [14] -- and the registry implements both exactly, by box id, with every addend transitively reachable. So the M303 pattern is NOT how this codebase generally works; wiring a total to its official addends is the norm and box 45 departs from it. That materially strengthens the box-45 finding, because a lone departure is an oversight where a universal pattern would have been a design. MODELO 714 IS WEAK BUT NOT WRONG, and the distinction is the point. Its printed identities -- [62] = [60] - [61], [01] = [62] + [63] -- are not implemented: declaracion-62 and declaracion-01 exist but carry NO formula, and the boxes for two further printed identities do not exist at all. Its eight formulas are all semantic patrimonio.* and none targets a declaracion box. Every one of those boxes is input_kind MANUAL and exported. So the application does not compute a wrong figure; it asks the operator to compute [62] and type it. That is a materially different failure from M303 box 45, where the app DOES compute and computes short. Manual entry of a derivable box is a product weakness and an arithmetic-error risk carried by the operator; it is not the app under-deducting on their behalf, and it must not be reported in the same breath. WORTH FLAGGING SEPARATELY: all five M714 revisions declare authority_grade FILING while leaving printed identities to manual entry, which sits oddly beside the known state that M714 is blocked on a wealth-asset register it does not have. Whether filing grade should require that a printed identity be computed rather than typed is a question for whoever owns that grade's definition. LIMIT OF THIS SWEEP, stated so nobody over-reads it: it matched ONE printed-formula spelling. Designs that express an identity in prose, or as a column note, or with a different bracket convention, were not searched, so three modelos is a floor and not a census; `src/cadrumo/_data/corpus/aeat_official/disenos_registro,src/cadrumo/_data/registry/aeat/modelos/714,src/cadrumo/_data/registry/aeat/modelos/123`.
- [x] `P02.S110` - Produce a corpus-wide census of printed box identities versus the registry, after TWO extractor bugs of my own that would each have produced false findings. THE FIRST SWEEP UNDERCOUNTED: matching only the parenthesised form ( [nn] + [nn] ) found 3 modelos. Widening to any bracketed box arithmetic finds 18. THE SECOND ATTEMPT MIS-PAIRED: I assumed the target precedes the expression, as Modelo 130 writes it -- [12] Suma de pagos fraccionados ([07]+[11]). Modelo 353 writes the opposite -- Resultado ([01] - [08]).  [03] -- so a target-before regex grabbed whichever bracketed number happened to precede, and reported target 02 with addends 01 and 08 where the real identity is target 03. The tell was self-reference: targets appearing inside their own addend lists, which is arithmetically impossible and meant the parser, not the registry, was wrong. Handling both conventions -- expression-then-target, and target-then-expression, the former taking precedence -- gives a census worth keeping. CLEAN, every printed addend reachable: modelos 130, 123, 490, 763, 117, 210, 216, 604, 309 and 202. That is ten modelos where the registry implements the identities its design prints, which is the norm and the reason the M303 departure reads as an oversight. CARRYING UNREACHABLE ADDENDS: modelo 303 with 7 of 12 identities, including box 45 already written up AND box 27, the devengada total; modelo 322 with 5 of 5, including boxes 38, 62 and 63; modelo 353 with 1 of 4. THIS CENSUS IS AN INVENTORY AND EMPHATICALLY NOT A VERDICT, and the reason is a result already established in this campaign: box-id reachability OVER-REPORTS, because a value can arrive under a semantic alias rather than by box number. Three of box 45's official addends do exactly that. Box 27's own case is the caution -- its unreachable list looks alarming, yet the semantic total behind it was separately verified to enumerate every recargo tier correctly, so the values plausibly all arrive. Only the LEAF-SET comparison distinguishes 'reaches the total under a different name' from 'never arrives at all', and that is what upgraded box 45 from a hazard to a confirmed live omission. NEXT, in priority order: run the leaf-set analysis on modelo 322, which carries the highest ratio at 5 of 5 and is an IVA modelo where boxes 62 and 63 are totals; then modelo 303 box 27; then modelo 353. Report nothing from this census until each candidate has had that second pass; `src/cadrumo/_data/corpus/aeat_official/disenos_registro,src/cadrumo/_data/registry/aeat/modelos/322`.
- [x] `P02.S111` - Resolve the census's top candidate as NOT a box-45-class defect, and settle the taxonomy that makes the whole census actionable. Modelo 322 carried the worst ratio in the census -- 5 of 5 printed identities with unreachable addends -- and was queued first on that basis. The leaf-set pass clears it. Its three flagged totals, boxes 38, 62 and 63, have target_leaves of ZERO: they depend on nothing, because the revision declares only THREE formulas in total and all three are semantic -- iva.cuota-devengada-total, iva.cuota-deducible-total and iva.resultado-regimen-general. Every official box is input_kind MANUAL and exported, the addends 03 and 40 exactly as much as the totals 38, 62 and 63. So modelo 322 does not compute a box total at all; it asks the operator to type every official box including the totals. THREE DISTINCT PATTERNS EXIST AND ONLY ONE IS HARMFUL, which is the finding that makes the census usable rather than a list of 18 suspects. FIRST, IMPLEMENTED: modelos 130, 123, 490, 763, 117, 210, 216, 604, 309 and 202 wire the identities their designs print, every addend reachable. This is the norm. SECOND, MANUAL AT BOX LEVEL: modelos 322 and 714 leave official boxes to operator entry, totals included. The app computes no wrong figure -- it computes nothing at that level. That is a product-completeness gap and an operator arithmetic-error risk, and modelo 322 declaring authority_grade filing while doing so deserves the same question raised for 714, but it is NOT the application under-deducting on a taxpayer's behalf and must never be reported as though it were. THIRD, COMPUTED BUT SHORT: modelo 303 box 45, alone so far. The app DOES compute the total, publishes it as authoritative, and omits five addends the design names. That is the only pattern where a taxpayer is handed a wrong number they have no way to notice. WHY THE DISTINCTION IS THE WHOLE POINT: a box-id reachability sweep cannot tell these apart -- all three look identical as 'addend not reachable from total'. Only asking whether the target has ANY formula, and then whether its leaves genuinely lack the addends' leaves, separates a modelo that never promised to compute from one that promised and got it wrong. Remaining census candidates to pass through this same filter: modelo 303 box 27, the devengada total, whose semantic backing was already verified to enumerate recargo correctly and which is therefore expected to clear; and modelo 353's single identity, whose census hit may still be an extraction artifact given the target-after convention that fooled the parser once already; `src/cadrumo/_data/registry/aeat/modelos/322,src/cadrumo/_data/registry/aeat/modelos/303`.
- [x] `P02.S112` - Close the printed-identity census by filtering its last two candidates, leaving Modelo 303 box 45 as the sole confirmed harmful case across all 18 modelos. MODELO 353 CLEARS COMPLETELY, and its census hit was my own parser. Its three real identities -- [03] = [01] - [08], [05] = [03] - [04], [09] = [02] - [08] -- all have formulas and NO leaf reachable from the addends is missing from the target. The census had reported ('02', ['01','08']), which was the target-before regex grabbing the wrong bracketed number under modelo 353's target-after convention. Confirmed artifact, not a finding, exactly as the self-reference tell suggested. MODELO 303 BOX 27 IS A HAZARD, NOT AN OMISSION, which is what the sibling Step predicted. The devengada total has a formula and 16 leaves, and exactly ONE leaf reaches the official addends without reaching box 27: iva.autorepercutido.intracomunitaria.devengado. That is the same generic-versus-explicit modelling already flagged on the deducible side -- an explicit .devengado variant feeding an official box while the total consumes the generic iva.autorepercutido.intracomunitaria, which for reverse charge carries the same figure. One redundant representation, not a missing category. Compare box 45, where FIVE leaves are genuinely absent and correspond to real deduction categories nothing else supplies: that is the difference between a value arriving twice under two names and a value never arriving at all. THE CENSUS IS NOW COMPLETE AND ITS RESULT IS NARROW, which is the honest and useful outcome: of 18 modelos printing box identities, 10 implement them, 2 leave every official box to manual entry, 5 had hits that dissolved under the leaf-set filter or the parser correction, and exactly ONE -- modelo 303 box 45 -- computes a total it publishes as authoritative while omitting categories the design names. A campaign that started from one suspicious box ends able to say that box is the only one of its kind in the corpus, which is a far stronger statement than the original finding and could not have been made without checking the other seventeen. ONE SUB-PATTERN WORTH CARRYING as a single item rather than two: the explicit .devengado and .deducible intracomunitaria variants are unused by BOTH totals, appearing only in the addend paths of boxes 27 and 45 respectively, while the generic casilla carries the value into both. Partially-landed modelling that should be settled once, in the same ADR amendment, rather than as two separate box tickets; `src/cadrumo/_data/registry/aeat/modelos/303,src/cadrumo/_data/registry/aeat/modelos/353`.
- [x] `P02.S113` - Obtain the FIRST real measurement of the filing-export gates after four blocked attempts, and diagnose one of the three failures without rushing its fix. The custody relocation finally settled and application/filing/tests ran: 4 passed, 3 failed. That is the first factual statement available about this area all session -- every previous attempt died on a module boundary, and no claim was made in the meantime. DIAGNOSED, AND THE GATE IS CORRECT: test_a_modelo_with_no_export_layout_refuses_before_writing_output fails with DID NOT RAISE. The refusal it expects, 'the registry snapshot has no complete export_layouts definition', is emitted by export_layout_renderability_reason strictly when the resolved layout is None. The test's subject is Modelo 111, and modelo 111 NOW HAS an export layout -- one, on its 2019-y-siguientes revision. So the gate did not break; a peer authored a layout for the very modelo this test had chosen as its example of a modelo lacking one. This is the THIRD instance today of a test whose subject was selected when it had a property and silently stopped testing anything once the property changed, after the M184 filing-schedule gate and the M341 temporal-coverage test. The difference here is that the property changed through PROGRESS: authoring an export layout for modelo 111 is work anyone would want, and it quietly disarmed a guard. DELIBERATELY NOT FIXED THIS PASS, because both available fixes are real work rather than a one-liner and the gate is not currently protecting anything urgent. The test's intent is to prove the refusal fires when NO layout exists, so the robust form asserts the CONDITION rather than naming a modelo that happens to satisfy it -- either by wrapping the schema provider so the revision it yields carries no export_layouts, or by retargeting to one of the 35 revisions that genuinely declare none. The second is invasive: the fixtures are modelo-111-specific, from Modelo111ProfileFacts through _approved_modelo_111_registry_draft, so retargeting means authoring a new draft fixture. The first is narrower but needs the export path understood well enough not to strip more than intended. NOT YET DIAGNOSED, and named so they are not mistaken for triaged: test_post_write_tripwire_refuses_real_casilla_drift_and_preserves_the_artifact, and test_exonerado_complete_revision_evidence_reaches_withdrawn_layout_without_override. Both may be stale in the same way or may be real; nothing is claimed about them until each is read; `src/cadrumo/application/filing/tests/test_unbuilt_layout_export_refusal.py,src/cadrumo/application/filing/_export.py`.
- [x] `P02.S114` - Repair the post-write export tripwire, which had THREE independent staleness defects stacked behind one another, each hidden until the one before it was fixed. All four tests in test_export_post_write_verification.py now pass. FIRST, A POSITIONAL ASSERTION: the test resolved its record correctly BY ID from the parsed casilla and then asserted `record is layout.records[0]`, which added nothing about correctness and broke when an envelope_header record was prepended to modelo 131's layout -- index 0 became the envelope while casilla 03 still lives on the data page. Replaced with the property it actually meant, record_type == page_1. This is the FOURTH instance today of a test pinned by position rather than identity, after M184, M341 and the M111 export-layout subject. SECOND, AND ONLY VISIBLE ONCE THE FIRST WAS FIXED: `start = field.offset - 1` sliced the WHOLE PAYLOAD at a RECORD-relative offset. That is correct only while the record opens the file, which the same prepended envelope header ended -- the slice read the envelope's blanks, b'                 ', where the casilla's b' 0000000000050000' was expected. Rebased onto the record's own start by mirroring the parser's walk: records in `order`, each consuming the extent its fields declare. THIRD, AND ONLY VISIBLE ONCE THE SECOND WAS FIXED: the final refusal assertion matched the English sentence 'post-write verification refused drift...casillas=03', but the export refusals have since been LOCALISED and the error now carries the translation key application.filing.export.errors.post_write_verification_refused. Replaced the prose regex with an assertion on the structured context -- the translated_message key plus mismatched_casilla_ids == ('03',) -- so the test pins the CONTRACT, that the refusal names the drifted casilla, rather than a presentation detail that changes with every localisation pass. THE PATTERN WORTH CARRYING: a single stale test can hide a queue of further staleness, because each assertion only executes once its predecessors pass. Fixing the first failure and re-running is not thoroughness, it is the only way to see the second -- and a test that has drifted this far from its subject has usually drifted in more than one way. Two of the three defects here trace to ONE upstream change, a peer prepending an envelope_header record, which is worth remembering when a single registry edit makes a test fail in a way that looks unrelated to it; `src/cadrumo/application/filing/tests/test_export_post_write_verification.py`.
- [x] `P02.S115` - Diagnose the last filing failure as a CORRECT refusal reaching a test that predates it, and surface the product-level consequence nobody has stated. test_exonerado_complete_revision_evidence_reaches_withdrawn_layout_without_override expects the refusal 'local declaration export is unsupported' and instead gets 'an envelope-prefix export requires explicit product/software identity authority'. Both are refusals; the test asserts which one, and a different, EARLIER guard now fires. THE NEW GUARD IS RIGHT, and this is the one of the four filing failures that is not a stale fixture in the ordinary sense. The rule in _export.py is symmetric: a layout that renders a filing envelope or auxiliary header MUST be given a product/software identity, and a layout that renders neither must NOT be. AeatProductSoftwareIdentity requires an AEAT-ASSIGNED four-byte program identifier plus the developer's validated Spanish tax id with reviewed evidence, and its own docstring states that no module-level instance is supplied and that neither identifier may be guessed from a filing participant. That is exactly the posture this project's rules demand -- refuse rather than fabricate an identifier that is AEAT's to assign. THE CONSEQUENCE WORTH STATING PLAINLY: the envelope-prefix work has made an AEAT-assigned program identifier MANDATORY for every export whose layout renders an envelope or auxiliary header, and this project does not hold one -- it is blocked on AEAT developer enrolment, the same block already known for the EEDD developer identity. So those fixed-width exports now refuse end-to-end for want of an identifier no amount of code can supply. That is correct behaviour and a genuine product blocker at once, and it is not visible from the test failure, which reads as a wrong-message assertion. It should be stated wherever export readiness is summarised. THE TEST FIX IS LEGITIMATE AND NARROW: supply a product/software identity in the fixture so the scenario reaches the assertion it exists to make. A synthetic identity in a test is not fabrication -- it never reaches AEAT -- and the pattern already exists at test_m303_did_account_wire_isolated_authority.py, which builds one. NOT DONE THIS PASS: the fixture wiring belongs with a reading of what that test's withdrawn-layout scenario needs, and guessing it would repeat the mistake of pattern-matching the previous three failures. Named, diagnosed, and left for a deliberate pass; `src/cadrumo/application/filing/tests/test_modelo_303_exonerado_390_refusal.py,src/cadrumo/application/filing/_export.py,src/cadrumo/core/product_identity.py`.
- [x] `P02.S116` - Quantify the AEAT product/software identity blocker, turning a single test's wrong-message failure into a measured statement about export readiness. The guard established in the sibling Step refuses any export whose layout renders a filing envelope or auxiliary header unless an AeatProductSoftwareIdentity is supplied, and that identity requires an AEAT-ASSIGNED four-byte program identifier plus the developer's validated tax id with reviewed evidence -- neither guessable, by the model's own docstring. SCOPE, measured across the loaded authority: 19 export layouts render an envelope prefix and 74 do not, so roughly a fifth of the export surface is gated. The affected modelos are 151, 202, 222, 232, 303, 322 and 353. THE COMPOSITION MATTERS MORE THAN THE FRACTION: modelo 303 is in the gated set, and it is the highest-volume return this product exists to serve -- the quarterly IVA autoliquidacion every registered business files. So the headline is not 'a fifth of layouts', it is that fixed-width export for the flagship modelo currently refuses end-to-end pending AEAT developer enrolment. The other six are the IVA group and pagos-fraccionados family, which is a coherent set: envelope-prefixed layouts are exactly the ones carrying product identity in their header. THIS IS NOT A DEFECT AND MUST NOT BE FILED AS ONE. Refusing to emit an identifier AEAT assigns is precisely the behaviour the project's rules demand, and the alternative -- inventing four bytes -- would put a fabricated developer identity into filed returns. The blocker is external: enrolment, not code. It is the same wall already recorded for the EEDD developer identity, now reached from a second direction. WHY IT NEEDED SAYING: the only surface where this appears today is one filing test failing with what looks like a stale message assertion. Nothing in the test output says 'the flagship export path is gated on an external enrolment', and a reader triaging that failure would fix the assertion and move on having learned nothing. Record it wherever export readiness or release posture is summarised, and pair it with the enrolment question rather than with the test; `src/cadrumo/application/filing/_export.py,src/cadrumo/core/product_identity.py`.
- [x] `P02.S117` - CORRECT the novelty claim made for the product/software identity blocker: it is not unstated, it is DECIDED, and I checked for prior ownership only after recording rather than before. The sibling Step framed the refusal as 'a product-level consequence nobody has stated'. That is wrong. 2026-08-10-aeat-export-fragment-generator-authority-adr, accepted, decides it explicitly: 'The four-byte AEAT program identifier and nine-byte software-developer tax identifier are one typed product/software identity authority. They are not filing producers, presenter or taxpayer identities, generic headers, guessed literals, or defaults. Generation refuses until explicit product authority supplies both values.' The same ADR also anticipates the exact keying I described as a discovery: 'Admission of the product/software identity is keyed on whether the SELECTED LAYOUT declares an envelope, not on the modelo id, because that identity is required by the envelope prefix itself.' So the guard I found in _export.py is that ADR implemented faithfully, and the refusal is the designed behaviour rather than an emergent blocker. WHAT SURVIVES THE CORRECTION, stated narrowly so it is not another over-claim: the ADR decides the CONTRACT; it does not quantify today's registry state. Its own evidence paragraph counts thirty-six variable envelopes across roughly twenty modelos IN THE CORPUS -- a statement about AEAT's designs, not about how many authored layouts currently sit behind the gate. My measurement is that different number: 19 of 93 authored export layouts render an envelope prefix, spanning modelos 151, 202, 222, 232, 303, 322 and 353. That M303 is among them is the part worth carrying, because it means the flagship export path is gated, and a readiness summary that lists the ADR without that number tells a reader the rule but not the exposure. THE PROCESS LESSON IS THE REAL OUTPUT, and it is one this campaign already had: check whether a peer or a prior decision owns a finding BEFORE writing it up, not after. That rule was applied correctly to the M390 recargo omission, which was left to its owner, and skipped here -- and the cost was a confident 'nobody has stated this' about something an accepted ADR states in two places. A finding that arrives with the word 'nobody' in it should trigger the search, not follow it; `.vault/adr/2026-08-10-aeat-export-fragment-generator-authority-adr.md,src/cadrumo/application/filing/_export.py`.
- [x] `P02.S118` - Repair the unbuilt-layout export refusal test so its subject is the CONDITION rather than a modelo that happened to satisfy it. Green. The test asserted that a modelo with no export layout refuses before writing output, and named modelo 111 as its example. Modelo 111 has since GAINED an export layout, so the refusal correctly stopped firing and the test silently stopped testing anything -- a guard disarmed by PROGRESS, which is the most easily missed way for coverage to disappear. FIXED BY CONSTRUCTING THE STATE, not by renaming the subject. RegistrySchemaAccessor and RegistryModeloSubview are both dataclasses, so the provider is rebuilt with dataclasses.replace and modelo 111's subview yielded with export_layouts and export_layout_ids emptied. That keeps every modelo-111 fixture the test already depends on -- the approved registry draft, the profile facts, the producer snapshot -- while making the subject the state under test. It is now immune to any modelo later being authored a layout, which is exactly the failure it just suffered. A precondition assertion guards the strip itself: the subview MUST have a layout to remove, so if modelo 111 ever loses its layout the setup fails loudly instead of stripping nothing and passing vacuously. THE SECOND DEFECT APPEARED ONLY ONCE THE FIRST WAS FIXED, as the standing rule now predicts: with the refusal firing again, the assertion still matched the English sentence 'no complete export_layouts definition', and that refusal has been LOCALISED to application.filing.export.errors.layout_not_renderable. Replaced with the message key plus the context's modelo field. That is the second module today whose prose regex had to become a contract assertion, after the post-write tripwire, and both were only reachable behind an earlier fix. Refusal localisation is evidently an in-flight campaign, so expect more English-regex assertions to surface as their preceding failures clear -- and treat a match= on a refusal sentence as a defect waiting rather than a passing test; `src/cadrumo/application/filing/tests/test_unbuilt_layout_export_refusal.py`.
- [x] `P02.S119` - Reconcile the export-layout join-ratchet inventory after the M184 literal publication, retaining only genuinely unjoined sheets and proving the newly joined M184 layouts; `src/cadrumo/domain/calculations/registry/tests/test_export_layout_join_ratchet.py`.
- [x] `P02.S120` - Adjudicate the 108 unclassified modules the rebuilt load census exposed. Measured at HEAD 2026-08-30: universe 384, classified 276, 108 unclassified of which 103 are registry submodules. These were preempted rather than absent -- the old facade reader raised inside build_reference_map before the classification assertion ran. Grounded split: 80 sit INSIDE the static load closure and are live by the same evidence the existing ValidatedRegistryAuthority.load rules cite; 28 sit outside it and are the post-load public surface plus the oracle and parity tiers (snapshot, queries, coverage, handoffs, the four m303 projections, aeat_nif_iva_oracle, groi_oracle, renta_web_open_oracle, live_parity and the rest, enumerated in the audit). Do NOT close this with one bulk live rule over the 80: a bulk membership list is exactly what produced the duplicate claim on _validate_cross_revision_contiguity that the census refused. Review the 28 individually, since absence from the LOAD closure is not evidence of death for a post-load module, and re-run the census immediately before adjudicating because the package is being actively split; `dev/registry/analysis/load_census_classification.py`.
- [ ] `P02.S228` - Route the five CLI-imports-TUI edges that breach ADR 2026-08-11-tui-architecture D11. THE BREACH IS SANCTIONED BY AN ALLOWLIST, NOT MERELY UNDETECTED -- this is the finding that matters and it was not visible until the gate was read. dev/tests/test_import_hygiene_gate.py declares _TUI_LAUNCH_SEAMS, a dict keyed by (path, enclosing function) that exempts exactly the three functions carrying the five edges: _modelo_work_review_cli._run_review_destination, _modelo_work_select_cli._run_select_destination, and _modelo_work_select_cli._run_workspace_destination_for_selected_unit. test_no_production_module_reaches_the_tui_outside_a_declared_launch_seam subtracts them before asserting, so the boundary gate reports clean WHILE D11 is breached. THE EXEMPTIONS' STATED REASON IS THE ONE THE RULES REJECT BY NAME: each says the import is 'function-local to that seam'. D11 forbids a CLI entrypoint to 'import, load, re-export, annotate against, or register from' the TUI, and aeat-architecture-boundaries states that static imports, LOCAL IMPORTS, annotations, TYPE_CHECKING, registrations and string-based discovery all count as import edges. Function-locality is precisely what neither rule accepts as a defence. IN FAIRNESS TO ITS AUTHOR, THE ALLOWLIST IS WELL BUILT: keyed by enclosing function rather than line number exactly as aeat-quality-gates prescribes, every entry states a reason, and test_every_declared_launch_seam_still_names_a_real_import fails on a stale entry. It is mechanically exemplary and substantively contrary to the ADR, which is why reading only the gate's structure would pass it. ATTRIBUTION: the edges landed in 5fd8dbe397 (2026-08-26, feat(tui-interface)) and the allowlist in c95d7d4944 the same day, both peer tui-interface work, both AFTER tui-architecture W06.P14 and W06.P15.S91 ('Prove zero production or shared-test imports of the TUI') had closed. S91 also cites a proof path, src/cadrumo/tests/test_import_hygiene_gate.py, that DOES NOT EXIST at HEAD; the real gate is dev/tests/test_import_hygiene_gate.py. DISPOSITION UNCHANGED AND DELIBERATE -- DO NOT DELETE THE EDGES OR THE ALLOWLIST FROM THIS LANE. Removing either breaks a peer's just-landed C1 destination registration mid-campaign. The operator's ruling that CLI must never reach into the TUI governs the END STATE; it is not authorisation to break peer work to reach it. What this lane owns is making the conflict visible and routing it. THE FIX NEEDS NO NEW MECHANISM: D11 names out-of-process execution as sanctioned, and both entries already ship (console script aeat-tui bound to cadrumo.entrypoints.tui.launcher:main, and tui/__main__.py). The real work is that the CLI hands ModeloWorkReviewApp a constructed in-process ModeloWorkReview, so the destination must instead be addressed by identifiers the child process resolves itself; _tui_policy already owns the request and capability seam and is where that belongs. Closing this row requires the three allowlist entries to be GONE, not re-worded; `src/cadrumo/entrypoints/cli/_modelo_work_review_cli.py, src/cadrumo/entrypoints/cli/_modelo_work_select_cli.py, src/cadrumo/entrypoints/cli/_tui_policy.py, dev/tests/test_import_hygiene_gate.py`.
- [ ] `P02.S229` - Adjudicate the six broken layering contracts, and fix the recurring mechanism that keeps killing the gate outright. MEASURED AT HEAD 2026-08-31 after 828c6214: 5627 files, 33968 dependencies, 4 KEPT and 6 BROKEN. ADJUDICATION STATUS: the two non-TUI contracts are settled and owned by S232, which established with grimp that they are ONE bidirectional application.ledger <-> llm coupling seen from either end -- do not re-adjudicate them here. The other four sit on the TUI boundary, overlap S228, and must wait on the operator ruling. MECHANISM HALF DELIVERED 2026-08-31 in commit c0e9fe8a. An unmatched ignore_imports pin stops lint-imports before any contract is evaluated, so the whole output is one line about that pin and exit 1 -- which reads as a single narrow failure while every layering contract is in fact unchecked. That killed the gate twice, for days each time. THE ALERTING SETTING WAS DELIBERATELY NOT TOUCHED, correcting this row's earlier framing: unmatched_ignore_imports_alerting = error is load-bearing, and .importlinter says so in its own words -- it 'fails a pin that overshoots', which is what keeps the narrow per-module exemptions honest. Weakening it to warn would trade a loud failure for silent exemption drift. The defect is blast radius, not the setting. WHAT LANDED instead is legibility at the point of use: dev/quality/suite.py now annotates the dashboard when a failing imports gate carries no contract tally, stating that nothing was evaluated, quoting the stale pins so they can be deleted without a rerun, and pointing the reader at a REPAIRED import rather than a new one. That last part is the counter-intuitive half and the reason it earns code: a pin goes unmatched exactly when somebody FIXES the violation it excused, so whoever killed the gate was improving the tree and has no reason to open .importlinter. Six tests in dev/quality/tests/test_suite_layering_annotation.py use real captured output from both states, and the annotation was removed in memory to prove they bite -- three behaviour tests go red, three leave-alone tests correctly stay green. REMAINING ON THIS ROW: adjudicate the four TUI contracts once S228 is ruled on. The annotation makes a dead gate legible; it does not make one impossible, so a future ratchet asserting the tally is present in CI would still be worth having; `.importlinter, dev/quality/suite.py, dev/quality/tests/test_suite_layering_annotation.py`.
- [ ] `P02.S230` - Enrol or invert the eleven application.ledger modules whose adapters coupling the restored layering gate reports. ALL ELEVEN NOW MEASURED at HEAD 2026-08-31 with git log --follow, which is load-bearing: without it every one reports as added 2026-08-25 by a single relocation commit, reading as brand-new coupling, which is wrong -- the files are new, the code is not. Coupling-introduced dates, followed through renames: evidence 2026-06-03 (module earliest 2026-05-14, 68 commits), actions_common 2026-06-05 (61), llm_classification 2026-06-11 (80), evidence_input 2026-06-10 (31), evidence_textlayer 2026-06-10 (12), llm_diagnostics 2026-07-01 (19), actions_import 2026-07-12 (36), evidence_draft (108 commits from 2026-07-03), batch_ingest 2026-08-07 (17), aeat_record_projection 2026-08-07 (3), counterparty_establishment 2026-08-08 (16). SO THE ELEVEN SPLIT, and the split decides the remedy per module rather than as a block: NINE are long-standing coupling from June and July that was never enrolled, and TWO -- aeat_record_projection and counterparty_establishment, both first coupled in the week of 2026-08-07 and carrying only 3 and 16 commits -- are genuinely recent and are much closer to the contract's loud-failure case for new source modules. Prefer enrolment for the nine, matching the per-module pinning application.modelo already uses and recording that full inversion stays deferred under the 2026-04-30 restructure ADR section 538; scrutinise the two recent ones for inversion first, since enrolling coupling introduced three weeks ago converts a caught regression into declared debt. INSTRUMENT NOTE for whoever re-measures: -S must search the RELATIVE form 'from ...adapters', not 'cadrumo.adapters'; the absolute string appears mainly in docstrings and returned not-found for five of the eleven. Do NOT add a wildcard pin: the contract's per-module design is what makes a new module fail loudly, and a ledger.** blanket forfeits exactly that; `src/cadrumo/application/ledger/, .importlinter`.
- [ ] `P02.S231` - Record that the import-hygiene gate's verdict is a snapshot of the WORKING TREE, not of HEAD, and that this must be stated wherever its numbers are quoted. MEASURED 2026-08-31 by running the same gate twice about thirty minutes apart with nothing of mine changing between them beyond commit 3c73ab7c: the first run gave 9 failed / 56 passed, the second 11 failed / 55 passed, AND THE MEMBERSHIP DIFFERED IN BOTH DIRECTIONS. Two failures I had genuinely fixed left the set; four TUI-boundary failures appeared that were not there before, and one, judges_tracked_files, failed in the first run while passing in isolation. ALL FOUR NEW FAILURES WERE PEER CHURN, EACH VERIFIED TO ROOT CAUSE RATHER THAN INFERRED FROM THE TRACEBACK. Three of them -- no_production_module_reaches_the_tui_outside_a_declared_launch_seam, every_declared_launch_seam_still_names_a_real_import and textual_is_confined_to_the_canonical_tui_package -- died identically on FileNotFoundError for src/cadrumo/core/_iva_deduction_fact.py. That path is NOT tracked and does not exist now, so it must have been tracked when the run's cached file list was built and removed by a peer during the four minutes the run took: _tracked_pkg_py_files is cached and filters on is_file() at fill time, so a deletion landing mid-run leaves a path in the list that no longer opens. The fourth, the_canonical_tui_package_is_fully_importable, listed 49 TUI modules all failing to import collect_unhandled_source_diagnostics from application.aggregation._source_mesh. That symbol has MOVED to application/aggregation/source_resolution_operations.py. THIS IS NOT A SPLIT RELOCATION AND MUST NOT BE REPORTED AS ONE: checked at HEAD, _source_mesh still defines the symbol and the new public module does not exist there, so HEAD is coherent and the whole move is uncommitted peer work mid-sweep. THE CONSEQUENCE FOR THIS LANE, WHICH EXISTS TO MAKE THE TREE HONESTLY MEASURABLE: a failure count from this gate is not attributable on its own, and quoting one without saying when it was taken and what the tree looked like is a measurement error rather than a finding. In CI the gate is correct, because CI checks out a clean tree; locally, with roughly nine agents writing continuously, it reports on their uncommitted state. BEFORE TREATING ANY FAILURE HERE AS REAL: confirm the cited path still exists, resolve the symbol to its current defining module, and check HEAD before calling a relocation split. A SECOND, SMALLER DEFECT WORTH FIXING SEPARATELY: the cached tracked-file list makes the gate crash rather than skip when a peer deletes a file mid-run, which turns ordinary churn into three hard errors. The existing is_file() filter already shows the authors anticipated exactly this case; it simply runs too early to help; `dev/tests/test_import_hygiene_gate.py, dev/quality/import_hygiene_scan.py`.
- [ ] `P02.S232` - Resolve the application.ledger <-> llm coupling. THE TWO DIRECTIONS NEED DIFFERENT REMEDIES: the layers contract declares entrypoints > adapters : cadrumo.llm > application > domain > core, so llm sits at the ADAPTERS layer, making application -> llm the same class as application -> adapters while llm -> application is layer-legal and matters only as a transitive route to persistence. THE CYCLE IS NOW BROKEN. Two extraction steps landed, each following the same recipe: compute the dependency closure by AST rather than by eye, verify with grimp (cache_dir=None) that every destination dependency reaches neither persistence nor llm BEFORE moving, then repoint import statements AND docstring cross-references and drop the moved names from the old module's __all__ so it does not become a re-export. STEP 1, 74745b5280, relocation:PurchaseInvoiceEvidenceInputError: the two purchase-invoice refusals left the 867-line evidence.py, which wires the bucket event history and the persistence storage package, for the leaf application/ledger/evidence_errors.py. Thirty consumers repointed plus the two dotted-path entries in the error registry, which are string-based edges the rules count as imports. It also removed a re-export the rules forbid -- three files reached the error through evidence_draft rather than its defining module. STEP 2, 4f8fbf7da4 with 364e32fabf, relocation:InvoiceDraft: the six draft record classes and the one private helper they share left the 3016-line evidence_draft.py, which wires InvoiceCatalogueRepository and AttachmentStore, for the leaf application/ledger/invoice_draft_records.py. The closure was 15 imported names, NONE from persistence and none from llm, and all eight source modules were confirmed clean of both. 78 import statements across 72 consumers repointed, 33 docstring cross-references across 25 files repointed, six names dropped from the old __all__. evidence_draft.py fell from 3016 to 2426 lines. VERIFIED AFTER: the leaf reaches neither persistence nor llm, llm no longer imports application.ledger.evidence_draft at all, and the ONE DIRECT MUTUAL PAIR across the architectural boundary is GONE. STEP 2 LANDED IN TWO COMMITS AND NEITHER STANDS ALONE, which is worth knowing before anyone reads that history: `git commit -- <paths>` silently omitted the new leaf because it was staged-new rather than tracked, so 4f8fbf7da4 briefly left HEAD importing a module that did not exist, and 364e32fabf completed it. The file COUNT matched exactly while the one file that mattered was missing, so the check that catches this is reading diff-tree's status letters and confirming a new file lands as A. REMAINING IN THIS ROW, and it is NOT justified as closing llm-not-persistence: llm.suggestions still imports ManualLedgerTransactionResult from application.ledger.models, which reaches persistence through application.export then calculations. models.py was peer-dirty at last check. DIRECTION ONE is unchanged and still open: 165 individually reasoned application -> adapters pins exist against exactly ONE application -> llm pin, the test carve-out, so the four production modules were never pinned while their siblings were. Do not simply add four pins -- the contract says the ledger pins EXISTING modules and that NEW ones fail loudly, and the dating is unresolved because --diff-filter=A dates the path and --follow -S reaches only the commit that retired the llm namespace and rewrote those spellings; `src/cadrumo/application/ledger/, src/cadrumo/llm/, .importlinter`.
- [ ] `P02.S233` - Give the inert-namespace rule a standing tree-wide gate, because it currently has none. MEASURED BY AST OVER src/cadrumo 2026-08-31: of 283 package __init__.py files, 84 carry project imports or their own definitions, which aeat-architecture-boundaries forbids outright -- 'Package __init__.py namespaces are inert and may not import, bind, alias, lazily resolve, or re-export project symbols.' THE 84 DECOMPOSE INTO THREE KINDS NEEDING THREE DIFFERENT REMEDIES, and the single number hides the two that matter most. (1) FOUR LAZY PEP 562 FACADES, which the rule prohibits by name in its list of banned constructs alongside re-export modules and aliases: core/__init__.py at 1236 lines, adapters/persistence/storage/__init__.py at 912, entrypoints/cli/__init__.py at 366, and tests/__init__.py at 207. Each resolves names inside __getattr__, so nothing binds statically. These are the highest-reach namespaces in the tree and the most explicitly forbidden. (2) TWENTY-TWO NAMESPACES DEFINING PRODUCTION CODE -- a module wearing a package's name -- led by domain/contribuyente/inventory/__init__.py with 53 definitions across 1690 lines, core/redaction with 34 across 1141, domain/bienes_inversion 25, core/corpus_manifest 22, domain/prorrata_register 20. The remedy is a hard move to a semantically named public module, not a repointing of consumers. (3) FIFTY-EIGHT PURE RE-EXPORT FACADES, the shape the rule was written against, led by application/aggregation with 33 project imports and application/calculations with 21; the remedy is repointing consumers at defining modules. THE ENFORCEMENT GAP IS THE POINT OF THIS ROW AND WAS CHECKED RATHER THAN ASSUMED. dev/quality/import_hygiene_scan.py already carries an inert_modules spec field and a 'non-inert package import' diagnostic, so the detector EXISTS, but the only assertion wiring it up is test_components_facade_has_no_imports_or_exports, over entrypoints/tui/components alone; the other 282 packages are unevaluated. Two neighbouring gates were read and are NOT this gate: test_no_dunder_init_module_imports forbids the `from ..__init__ import x` submodule spelling for its double-execution hazard and says its scope is deliberately narrow, and test_facade_export_lazy_shapes exists to make the facade SCANNER model PEP 562 dispatch correctly -- it accommodates lazy facades rather than forbidding them, and its own docstring names cli and domain.user_profile as shipping that shape. So the tree ships a construct the architecture rule bans while the tooling around it treats it as ordinary. That tension should be resolved by a ruling in one direction or the other, not left implicit. DELIVER: populate inert_modules from the real package set and assert it tree-wide, with any genuinely-exempt namespace named and reasoned rather than silently outside the denominator. CAVEATS: re-measure before quoting, because a relocation sweep is actively promoting modules out of core; and this row claims only that none of the 84 is currently CHECKED, not that all 84 are equally wrong. Sequence the gate before the cleanup, or the cleanup has nothing holding it; `dev/quality/import_hygiene_scan.py, dev/tests/test_import_hygiene_gate.py, src/cadrumo/**/__init__.py`.
- [ ] `P02.S234` - Record the 2026-08-31 shared-index incident, because the mechanism is a standing hazard for every agent in this tree and the lesson is not obvious from the rule alone. WHAT HAPPENED: commit 74745b5280 was made with `git add -- <explicit paths>` followed by a BARE `git commit -m ...`. The pathspecs on the add were correct and complete; the bare commit then ignored them, because a bare commit consumes the SHARED INDEX and takes whatever any concurrent agent has staged. It captured five files this lane did not author and recorded THREE OF THEM AS DELETIONS: the 2026-08-31 ci-lane-deconflation S136 code-review audit, its execution record P05-S136, and src/cadrumo/application/auth/operator_result_projections.py, all belonging to a peer's just-landed S136 work. NOTHING WAS LOST and no destructive command was used to recover: all three files were still present on disk, so 06b5a6e763 re-added exactly those paths with a pathspec-scoped commit. The restored audit is the peer's NEWER revision, template comments stripped and body_hash updated, rather than the copy in e0e7952d8c. RESIDUE THAT WAS DELIBERATELY NOT UNDONE: auth/operator.py and this plan file remain recorded as modified inside 74745b5280 under a relocation subject. Their content is intact and only the attribution is wrong. Reverting either would undo a peer's work, which is a worse outcome than a misleading commit subject. THE DETECTION LESSON IS AS IMPORTANT AS THE CAUSE. Three git tools disagreed about what the commit contained: `git show --name-only --format=''` and `git status --porcelain` gave contradictory pictures, files appearing in the commit listing while status called them untracked, and the contradiction was nearly dismissed as an instrument artifact, which this campaign has seen many times before. It was real. Only `git diff-tree --no-commit-id -r --name-status <sha>` settled it, and that should be the routine check after any commit in a shared tree. THE STANDING RULE, stated as a form rather than a prohibition so it is actionable: commit as `git commit -m '...' -- <explicit paths>`, which commits those paths and nothing else regardless of index state. Note the one wrinkle that made the first repair attempt fail: that form cannot reference an UNTRACKED path, so a new file needs `git add -- <paths>` first, followed by a commit carrying THE SAME pathspecs; `process discipline for commits 74745b5280 and 06b5a6e763`.
- [ ] `P02.S235` - ADJUDICATED 2026-08-31: the llm-to-persistence route through the response cache is the DESIGN THE GOVERNING ADR REQUIRES, not a violation, and the llm-not-persistence import contract cannot express the difference. After the S232 step-1 extraction, 9 of 24 llm production modules still reach persistence and SIX of those nine go llm.client -> adapters.outbound.llm -> adapters.outbound.llm._cache -> adapters.persistence.storage. That looked like the dominant remaining problem. It is not a problem at all. The accepted 2026-08-06-llm-package-split-adr names _cache.py explicitly as one of three inference-scoped stores that write through secure_object_repository_for_active_bucket and must stay on the core side of the boundary, and it reasons about this exact cache at length: memoising extraction is a design the ADR WANTS, the cache holds invoice contents in plaintext, and on disk in the clear it would be a new plaintext store of taxpayer financial data, so it is written through the core content-addressed and encrypted. The transitive edge the contract flags IS that encrypted write. Finishing the application-side extraction therefore will NOT and SHOULD NOT close this contract. THE SAFETY PROPERTY IS ENFORCED, AND BY THE INSTRUMENT THE ADR NAMES rather than by the import contract. The ADR states the AST gates, not the layering contract, are what establishes persists-nothing, and that removing the code from their scope would convert an enforced property into an unverified assertion. Checked directly: SRC_CADRUMO / 'llm' IS enrolled in _SENSITIVE_SURFACES, the strictest tier of adapters/persistence/storage/tests/test_sensitive_persistence_policy.py, carrying a comment that it was enumerated in the same change that created the subpackage precisely so no window existed where decrypted invoice bytes were unguarded -- and test_sensitive_financial_surfaces_do_not_bypass_secure_object_backend PASSES. TWO DEFECTS FOUND WHILE VERIFYING, both worth their own attention. FIRST, that policy file's own docstring tabulates a gate tier named test_llm_subpackage_persists_nothing, described as a single-package gate. NO SUCH TEST EXISTS ANYWHERE IN THE TREE; the only occurrence of the name is the docstring table itself. The property is genuinely covered by the surfaces tier, so this is a stale citation rather than a hole, but a reader auditing the confidentiality story is pointed at a gate that is not there. SECOND, test_production_file_write_inventory_is_reviewed FAILS: production file-write sites exist that the reviewed inventory does not list, including entrypoints/tui/devtools/journal.py write_session using path.write_text -- peer-owned TUI, route it there -- and adapters/persistence/storage/custody/_capsule_filesystem.py fsync_directory using os.open. That gate exists to keep every write site consciously reviewed, so a failing inventory is a real gap in the confidentiality audit trail even when each individual write turns out to be benign. CONSEQUENCE FOR S232: its remaining application-side work stands on its own merits as cycle-breaking, but it must no longer be justified as closing llm-not-persistence, and nobody should later 'finish' that contract by touching the cache; `src/cadrumo/adapters/persistence/storage/tests/test_sensitive_persistence_policy.py and the llm-not-persistence contract in .importlinter`.
- [ ] `P02.S236` - Route to the tui lane: the TUI devtools session journal writes typed text and filled field values to disk in PLAINTEXT, and it is the one write site this lane declined to clear. FOUND 2026-08-31 while reconciling the production write inventory that the sensitive-persistence policy gate keeps. entrypoints/tui/devtools/journal.py write_session serialises a Session as one JSON header line plus one line per gesture and calls path.write_text; the gesture types include Type carrying free text and Fill carrying a selector and its value, so whatever an operator typed into a TUI field is what lands in the file. sensitive-financial-data-secure-storage-only is absolute about this class -- no plaintext side stores, no on-disk caches -- and the gate walks devtools as production code, so this is inside its denominator by the gate's own definition rather than by interpretation. WHY THIS LANE DID NOT SIMPLY ENROL IT: the other seven unreviewed writes were cleared on evidence and enrolled in 7723b902, but this one turns on a judgement that belongs to the lane that owns the surface -- whether a developer-operated replay journal is expected to run anywhere near real taxpayer input, and if so whether it should redact, refuse secret-bearing gestures, or write through the encrypted store like the three inference-scoped stores do. Clearing it here would have made the gate green on someone else's judgement call, which is the failure mode the gate exists to prevent. THE GATE THEREFORE STILL FAILS, DELIBERATELY, and now fails on exactly this one item instead of eight mixed ones. Do not close it by adding an inventory entry from this lane. WORTH KNOWING FOR WHOEVER TAKES IT: the same reconciliation showed four of the eight originally-unreviewed writes were not new writes at all but relocations -- two custody fsync helpers moved and lost their underscores, core/_fsync was promoted, and an os.open was extracted into a new private function -- so an inventory keyed by (path, function, call) goes stale on every rename, and reads as eight new hazards when it is really four renames plus three genuine additions plus one real question; `src/cadrumo/entrypoints/tui/devtools/journal.py and the reviewed-write inventory in adapters/persistence/storage/tests`.
- [ ] `P02.S237` - MEASURED AFTER BOTH S232 EXTRACTIONS, 2026-08-31: the llm-not-persistence contract now reports FIVE chains, and only ONE of them is production code. The single production chain is cadrumo.llm.suggestions -> application.ledger.models -> application.export -> calculations -> persistence, which is the extraction S232 still has open and which was peer-dirty at last check. The other four all originate in cadrumo.llm.tests -- test_role_evidence_stays_on_identities, test_provenance_stamp_singularity, test_local_text_reader_wiring and test_llm_vision_classifier -- reaching application.ledger modules (llm_classification, evidence_textlayer, party_attribution) for fixture setup. THOSE FOUR ARE A DIFFERENT QUESTION FROM THE PRODUCTION ONE and should not be extracted away. Every other contract in .importlinter already carries an explicit test-file carve-out on the stated ground that test and conftest modules across every layer legitimately cross layer boundaries for fixture setup, oracle replay and real-adapter roundtrips, and that a new such test needs no new exemption; this contract has a carve-out too, but a narrower one pinned per chain origin (llm.conftest, test_provenance_stamp_singularity, test_redaction, the consent-ledger gate) rather than a wildcard, deliberately, so a NEW PRODUCTION edge still fails. Whether these four belong under that same pinned treatment is a real adjudication and needs the contract's author's intent honoured rather than a wildcard added for convenience. NOTE ON THE CACHE, consistent with S235: the six llm.client -> adapters.outbound.llm -> _cache -> persistence routes that grimp finds do NOT appear among these five, so the contract already tolerates the ADR-mandated encrypted-cache path. That is further evidence the cache is settled and must not be reopened. INSTRUMENT WARNING FOR WHOEVER RE-MEASURES THIS: an awk range over the import-linter log that ends on a blank-line count runs straight past the section boundary into the NEXT contract's chains. Doing exactly that produced a first reading contaminated with TUI and CLI origins that belong to the following contract entirely. Bound the section by the two contract HEADINGS' line numbers and count inside them; `.importlinter llm-not-persistence contract, src/cadrumo/llm/tests/`.

### Phase `P03` - Registry and core follow-through

Findings the campaign surfaced in the registry and core surfaces that are real but were correctly not fixed inline, either because they need domain grounding or because acting on them would have collided with in-flight work.

- [x] `P03.S11` - Build the registry selector parity gate binding declared period_selector tokens to the accepted set, delegating to the production validator rather than restating it so the gate cannot become a second authority; `src/cadrumo/domain/calculations/registry/tests`.
- [x] `P03.S12` - Route the declaracion parser administrative-token set through the core authority, it hardcodes four tokens and is missing COMUNICACION and VARIACION from the core set it shadows; `src/cadrumo/adapters/inbound/declaracion/_parser.py`.
- [x] `P03.S13` - Decide the strength class for casillas 0529 and 0531, promoting them to the coverage-gated class raises the denominator and could flip verdicts on legitimate filings so it needs domain grounding; `src/cadrumo/_data/registry/aeat/modelos/100`.
- [x] `P03.S14` - Fix the embedded newline in the rd-439-2007 art-76 legal entry notes field, the validator rejects any Unicode C category and a narrower scan for control characters reads as clean; `src/cadrumo/_data/registry/aeat/legal`.
- [x] `P03.S18` - Pin which snapshot coordinates the filing-period cross-check covers, the consistency validator returns early on a null filing period so administrative-token snapshots quietly lost a check the validator's name still implies; `src/cadrumo/domain/calculations/registry/tests/test_snapshot_filing_period_coverage.py`.
- [x] `P03.S19` - State in the filing-period consistency validator's own docstring which coordinates it no longer covers and why, a test enforces the fact but the explanation belongs at the validator; `src/cadrumo/domain/calculations/registry`.

### Phase `P04` - Tracked but owned elsewhere

Work this campaign found and cannot close, recorded so it is not silently dropped. Each row names why it is not ours and what the owning party must do.

- [x] `P04.S15` - Repair the four core tests broken by the root-only Modelo localization migration, it stripped title and official_name and label from the M036 manifest without updating hand-derived expectations; `src/cadrumo/core/tests/test_toml_registry_parity.py`.
- [x] `P04.S16` - Re-pin the model-facing description digest once the description sources settle, the gate forbids re-pinning from a dirty tree and the locale and CLI help surfaces are actively churning; `dev/packaging/tests/test_verify_distribution_identity.py`.
- [x] `P04.S17` - Record a finding about the 204 semantic-dedup exec records rather than remediating them, all 204 carry empty Description Outcome and Notes and were bulk-scaffolded in one commit so 0 resolve to an implementing commit, and unchecking would assert work the tree shows was done; `.vault/exec/2026-06-13-semantic-dedup-epic`.
- [x] `P04.S20` - Resolve the import-hygiene test-debt failures from the maternidad private reaches, raising a baseline designed to only decrease would invert the ratchet so establish whether the debt is legitimate before admitting it; `src/cadrumo/tests/test_import_hygiene_gate.py`.
- [x] `P04.S21` - Replace the two bare 303 literals in the relation-source validator with the core enum, they entered in today's operator snapshot rather than becoming newly visible and they red a tree-wide gate for every agent; `src/cadrumo/domain/calculations/registry/_validate_relation_sources.py`.
- [x] `P04.S24` - Confirm with the localization cascade owner that the result-summary application row is meant to follow the active output language, the repair is stronger than what it replaced but it crosses another campaign's surface; `src/cadrumo/entrypoints/cli/tests/test_modelo_result_summary_labels.py`.
- [x] `P04.S25` - Sweep for tests relying on the English CLI env override for help text, it is inert against the cached Click tree so any such test asserts against whatever language the tree was built in; `src/cadrumo/entrypoints/cli/tests`.
- [x] `P04.S26` - Require an exec record whose evidence is a passing test to state the selection that produced it, three agents in one day nearly accepted a marker expression that selected nothing and exited zero; `.vaultspec/templates`.
- [x] `P04.S27` - Rule on whether the schema-size gate should measure emitted content, its docstring calls itself a proxy for structured content while it directly measures the definition bytes a client actually loads; `src/cadrumo/entrypoints/mcp/tests`.

### Phase `P05` - Size-budget remediation

Decompose every live size-budget subject by its owning source file, then regenerate only stale pins after the live subjects are compliant; S51 performs the terminal full-gate verification.

- [x] `P05.S121` - Refactor the size-budget subjects in declarations.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/outbound/aeat/sede/declarations.py`.
- [x] `P05.S122` - Refactor the size-budget subjects in calc_sheets_apply.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/outbound/google/calc_sheets_apply.py`.
- [x] `P05.S123` - Refactor the size-budget subjects in calc_sheets_pull.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/outbound/google/calc_sheets_pull.py`.
- [x] `P05.S124` - Refactor the size-budget subjects in _google_drive.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/outbound/storage/_google_drive.py`.
- [x] `P05.S125` - Refactor the size-budget subjects in _namespace_registry.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/persistence/storage/_namespace_registry.py`.
- [x] `P05.S126` - Refactor the size-budget subjects in _profile_custody_carry.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/persistence/storage/_profile_custody_carry.py`.
- [x] `P05.S127` - Refactor the size-budget subjects in acceleration_receipt.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/persistence/storage/custody/acceleration_receipt.py`.
- [x] `P05.S128` - Refactor the size-budget subjects in filesystem.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/persistence/storage/custody/filesystem.py`.
- [x] `P05.S129` - Refactor the size-budget subjects in secure_objects.py into cohesive siblings without raising any threshold.; `src/cadrumo/adapters/persistence/storage/sql/secure_objects.py`.
- [x] `P05.S130` - Refactor the size-budget subjects in _modelo_bindings.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/aggregation/_modelo_bindings.py`.
- [x] `P05.S131` - Refactor the size-budget subjects in _renta_income_ledger.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/aggregation/_renta_income_ledger.py`.
- [x] `P05.S132` - Refactor the size-budget subjects in _source_mesh.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/aggregation/_source_mesh.py`.
- [x] `P05.S133` - Refactor the size-budget subjects in test_iva_ledger.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/aggregation/tests/test_iva_ledger.py`.
- [x] `P05.S134` - Refactor the size-budget subjects in test_modelo_source_mesh_ledger.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/aggregation/tests/test_modelo_source_mesh_ledger.py`.
- [x] `P05.S135` - Refactor the size-budget subjects in test_renta_ledger.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/aggregation/tests/test_renta_ledger.py`.
- [x] `P05.S136` - Refactor the size-budget subjects in operator.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/auth/operator.py`.
- [x] `P05.S137` - Refactor the size-budget subjects in test_certificate_sources_check.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/auth/tests/test_certificate_sources_check.py`.
- [x] `P05.S138` - Refactor the size-budget subjects in _relation_prefill.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/calculations/_relation_prefill.py`.
- [x] `P05.S139` - Refactor the size-budget subjects in cross_period_clean_state.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/calculations/cross_period_clean_state.py`.
- [x] `P05.S140` - Refactor the size-budget subjects in diagnostics.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/diagnostics.py`.
- [ ] `P05.S141` - Refactor the size-budget subjects in _export_producer.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/filing/_export_producer.py`.
- [ ] `P05.S142` - Refactor the size-budget subjects in _export.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/filing/_export.py`.
- [ ] `P05.S143` - Refactor the size-budget subjects in _producer_snapshot.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/filing/_producer_snapshot.py`.
- [ ] `P05.S144` - Refactor the size-budget subjects in test_source_resolver.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/invoices/tests/test_source_resolver.py`.
- [ ] `P05.S145` - Refactor the size-budget subjects in actions_manual.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/ledger/actions_manual.py`.
- [ ] `P05.S146` - Refactor the size-budget subjects in classification_assembly.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/ledger/classification_assembly.py`.
- [ ] `P05.S147` - Refactor the size-budget subjects in evidence_draft.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/ledger/evidence_draft.py`.
- [ ] `P05.S148` - Refactor the size-budget subjects in evidence.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/ledger/evidence.py`.
- [ ] `P05.S149` - Refactor the size-budget subjects in identity_roles.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/ledger/identity_roles.py`.
- [ ] `P05.S150` - Refactor the size-budget subjects in llm_classification.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/ledger/llm_classification.py`.
- [ ] `P05.S151` - Refactor the size-budget subjects in filed_data_capture.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/live/filed_data_capture.py`.
- [ ] `P05.S152` - Refactor the size-budget subjects in _amendment_actions.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/_amendment_actions.py`.
- [ ] `P05.S153` - Refactor the size-budget subjects in _calculation_actions.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/_calculation_actions.py`.
- [ ] `P05.S154` - Refactor the size-budget subjects in _export.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/_export.py`.
- [ ] `P05.S155` - Refactor the size-budget subjects in _filing_actions.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/_filing_actions.py`.
- [ ] `P05.S156` - Refactor the size-budget subjects in _revision_persistence.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/_revision_persistence.py`.
- [ ] `P05.S157` - Refactor the size-budget subjects in _verification_actions.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/_verification_actions.py`.
- [ ] `P05.S158` - Refactor the size-budget subjects in external_import_actions.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/external_import_actions.py`.
- [ ] `P05.S159` - Refactor the size-budget subjects in operation_definitions.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/operation_definitions.py`.
- [ ] `P05.S160` - Refactor the size-budget subjects in test_workspace.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/tests/test_workspace.py`.
- [ ] `P05.S161` - Refactor the size-budget subjects in work_addressing.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/work_addressing.py`.
- [ ] `P05.S162` - Refactor the size-budget subjects in workspace_models.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/workspace_models.py`.
- [ ] `P05.S163` - Refactor the size-budget subjects in workspace.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/modelo/workspace.py`.
- [ ] `P05.S164` - Refactor the size-budget subjects in supervisor.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/operations/supervisor.py`.
- [ ] `P05.S165` - Refactor the size-budget subjects in test_supervisor.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/operations/tests/test_supervisor.py`.
- [ ] `P05.S166` - Refactor the size-budget subjects in help.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/operator_surface/help.py`.
- [ ] `P05.S167` - Refactor the size-budget subjects in conformance.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/registry/conformance.py`.
- [ ] `P05.S168` - Refactor the size-budget subjects in repair_integrity.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/repair_integrity.py`.
- [ ] `P05.S169` - Refactor the size-budget subjects in state_projection.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/state_projection.py`.
- [ ] `P05.S170` - Refactor the size-budget subjects in custody_ports.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/user_profile/custody_ports.py`.
- [ ] `P05.S171` - Refactor the size-budget subjects in login_session.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/user_profile/login_session.py`.
- [ ] `P05.S172` - Refactor the size-budget subjects in registration.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/user_profile/registration.py`.
- [ ] `P05.S173` - Refactor the size-budget subjects in test_login_handover.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/user_profile/tests/test_login_handover.py`.
- [ ] `P05.S174` - Refactor the size-budget subjects in commands.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/wizard/commands.py`.
- [ ] `P05.S175` - Refactor the size-budget subjects in engine.py into cohesive siblings without raising any threshold.; `src/cadrumo/application/workflow/engine.py`.
- [ ] `P05.S176` - Refactor the size-budget subjects in _filing_projection_ref.py into cohesive siblings without raising any threshold.; `src/cadrumo/core/_filing_projection_ref.py`.
- [ ] `P05.S177` - Refactor config.py below the default size ceiling without raising its stale threshold.; `src/cadrumo/core/config.py`.
- [ ] `P05.S178` - Refactor the size-budget subjects in _application_part2.py into cohesive siblings without raising any threshold.; `src/cadrumo/core/errors/registry/_application_part2.py`.
- [ ] `P05.S179` - Refactor the size-budget subjects in context.py into cohesive siblings without raising any threshold.; `src/cadrumo/core/observability/context.py`.
- [ ] `P05.S180` - Refactor the size-budget subjects in _validate_revision_sections.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/_validate_revision_sections.py`.
- [ ] `P05.S182` - Refactor the size-budget subjects in authority.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/authority.py`.
- [ ] `P05.S183` - Refactor the size-budget subjects in bindings.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/bindings.py`.
- [ ] `P05.S185` - Refactor the size-budget subjects in formula_runtime.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/formula_runtime.py`.
- [ ] `P05.S187` - Refactor the size-budget subjects in invoice_bindings.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/invoice_bindings.py`.
- [ ] `P05.S188` - Refactor the size-budget subjects in ledger_bindings.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/ledger_bindings.py`.
- [ ] `P05.S190` - Refactor the size-budget subjects in record_design.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/record_design.py`.
- [ ] `P05.S192` - Refactor the size-budget subjects in schema.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/schema.py`.
- [ ] `P05.S195` - Refactor the size-budget subjects in test_catalogue_verification.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/tests/test_catalogue_verification.py`.
- [ ] `P05.S196` - Refactor the size-budget subjects in test_detail_record_observations.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/tests/test_detail_record_observations.py`.
- [ ] `P05.S198` - Refactor the size-budget subjects in test_modelo_100_registry_roles.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/tests/test_modelo_100_registry_roles.py`.
- [ ] `P05.S200` - Refactor the size-budget subjects in test_modelo_303_registry.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/tests/test_modelo_303_registry.py`.
- [ ] `P05.S202` - Refactor the size-budget subjects in test_record_design.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/tests/test_record_design.py`.
- [ ] `P05.S204` - Refactor the size-budget subjects in test_revision_span_matches_published_designs.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/tests/test_revision_span_matches_published_designs.py`.
- [ ] `P05.S206` - Refactor the size-budget subjects in withholding_bindings.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/calculations/registry/withholding_bindings.py`.
- [ ] `P05.S208` - Refactor the size-budget subjects in __init__.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/contribuyente/inventory/__init__.py`.
- [ ] `P05.S210` - Refactor the size-budget subjects in models.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/invoices/models.py`.
- [ ] `P05.S212` - Refactor the size-budget subjects in classification.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/iva/classification.py`.
- [ ] `P05.S214` - Refactor the size-budget subjects in components.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/iva/components.py`.
- [ ] `P05.S216` - Refactor the size-budget subjects in establishment.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/iva/establishment.py`.
- [ ] `P05.S218` - Refactor the size-budget subjects in calculation_revision.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/modelos/calculation_revision.py`.
- [ ] `P05.S220` - Refactor the size-budget subjects in test_calculation_revision.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/modelos/tests/test_calculation_revision.py`.
- [ ] `P05.S222` - Refactor the size-budget subjects in models.py into cohesive siblings without raising any threshold.; `src/cadrumo/domain/transactions/models.py`.
- [ ] `P05.S223` - Refactor _config_payloads.py below the default size ceiling without raising its stale threshold.; `src/cadrumo/entrypoints/cli/_config_payloads.py`.
- [ ] `P05.S224` - Refactor the size-budget subjects in _modelo_payloads.py into cohesive siblings without raising any threshold.; `src/cadrumo/entrypoints/cli/_modelo_payloads.py`.
- [ ] `P05.S225` - Refactor the size-budget subjects in test_machine_secret_channels_subprocess.py into cohesive siblings without raising any threshold.; `src/cadrumo/entrypoints/cli/tests/test_machine_secret_channels_subprocess.py`.
- [ ] `P05.S226` - Refactor the size-budget subjects in app.py into cohesive siblings without raising any threshold.; `src/cadrumo/entrypoints/tui/flows/app.py`.
- [ ] `P05.S227` - Regenerate only stale size-budget pins after every live source-file subject is compliant, verify no threshold is raised, and do not use --accept-growth.; `dev/audit/size_budget_baseline.json`.

## Parallelization

P03 and P04 are fully parallel with everything else and with each other. They
need no CI run and no push, so they are the rows to work while P01 is blocked.

P01 carries the only hard ordering in the plan, and it is external. S05 (the
push decision) gates S01, S02 and S03, because none of those lanes can be
observed until the commits reach the remote. S04 is gated instead by S14: the
ordering hypothesis it acts on is currently masked by the legal-entry defect,
so acting before S14 would be acting on something that cannot be demonstrated.

P02 is partly gated by P01. S09 depends on S06 closing the backlog it measures.
S10 depends on S03, because the serial pass has never been observed on a runner
and flipping it on local evidence alone is the failure this plan exists to
avoid. S07 and S08 are independent and can be worked immediately.

S10 additionally depends on S28, and this is the harder half of the row. An
observed green runner execution is necessary and not sufficient: the serial
step's two remaining failures are wall-clock budgets measured on a box shared
with the dev machine and the agent fleet, so a run that passes because the box
happened to be quiet proves nothing about the code. Flipping the flag on that
evidence is precisely the failure this plan exists to prevent, so S28 converts
the budgets to a process CPU-time failure condition first. S29 is a sibling
surface of the same class and S10 does not depend on it. S30 is independent of
everything here and was worked immediately, because the red it closes was
unowned and blocked every lead from reading a clean ratchet.

## Verification

Every row closes against evidence from a real execution, not a local run. That
distinction is the plan's central criterion, because the campaign's costliest
errors all came from claims that were structurally sound and never observed.

- S01, S02 and S03 close on a named run id whose conclusion is success. A
  passing local invocation is not evidence for any of them, and the docs lane
  is the worked example: it was structurally correct, its conformance gates
  were green, and it failed 13 times on its first real execution.
- S04 closes when the terminology gate that resolves to built HTML is
  demonstrated to depend on the build, not assumed to. If S14 lands and the
  dependency does not materialise, close S04 as not-needed with that finding
  recorded rather than reordering on a hypothesis.
- S06 closes on two intersected runs, never one. The lane measured 19 failures,
  then 28, and 12 of the difference was a peer's transiently broken module.
- S07 closes with the payload moved. Raising the budget is a failed close.
- S09 and S10 close only after the step they guard has been seen green on a
  runner. Flipping either on local evidence is the specific failure these rows
  exist to prevent.
- S11 closes only when the gate is shown to FAIL against a planted violation.
  A parity gate whose discovery silently finds nothing is indistinguishable
  from one that passes because the tree is clean.
- S13 and S16 are decisions, not implementations. They close when the decision
  is recorded with its grounding, including a decision to leave things as they
  are.
- S31 closes when the lane-reachability gate is GREEN, not when thirteen files
  have been added to a path list. The count is today's measurement and the row
  states it as evidence, never as the pass condition: a tally encodes a moment
  and then detects nothing, which is the failure mode that produced this hole.
  The gate already asks the property, so the property is the criterion.
- S32 closes on a triage record that names every red with an owner, not on a
  green suite. Some of what enrolment exposes will belong to other campaigns,
  and routing those honestly is the close - absorbing them would make this row
  a dumping ground and hide who actually owes the fix.
- S33 closes on a dated answer with evidence, including the answer that nothing
  shipped past it. It does not close because S31 and S32 turned the gate green:
  a gate going green afterwards says nothing about how long it was red before,
  and that question is the whole row.
- S34 closes on a recorded decision, and the second option closes it as honestly
  as the first. Stating plainly that dev-tooling reds are invisible between
  manual dispatches, with a named accountable party, is a real close - a known
  and owned gap beats an unowned one. What does NOT close it is proposing a
  scheduled run or a nightly: both are refused by standing operator ruling, and
  a remedy the operator has already declined is not a decision, it is a
  re-litigation. S34 is not a prerequisite for S31, which fixes a different
  class in a different directory.

Commit verification for every row: resolve the sha with `git log --format=%H
--grep=<subject> -1` and read `git show <sha> --numstat`. Never verify with
`git show HEAD`, which returned a peer's commit during this campaign.

## Context

Durable tracking for the CI lane deconflation campaign of 2026-08-05 and every item it surfaced. Authored because the working task list is session state: an outage loses it, and several rows here are the only written record of a measured finding.

The campaign began as signal scoping - a documentation edit was firing the full Python static+unit suite while producing no documentation verdict, and a frontend dependency bump was firing both Python lanes plus a three-OS packaging probe. Fixing the carve-out required giving both surfaces lanes of their own, which in turn exposed that two whole test lanes were declared in the justfile and invoked by no workflow: the src/ integration suite (~370 modules) and nine dev/ subsystems. Enrolling those produced the backlog most of these rows track.

Three measurement hazards govern how rows here must be closed, each learned the expensive way during the campaign:

Do not triage a lane from a single run. The integration lane measured 19 failures, then 28; intersecting the two showed 12 were a peer's transiently broken module killing every cold-subprocess test at CLI import. Roughly 19 peer commits land per 12-minute run.

Verify a commit by SHA, never by HEAD. HEAD moves under you in this worktree - `git show HEAD` immediately after a commit returned a peer's. Resolve with `git log --format=%H --grep=<subject> -1`, then `git show <sha> --numstat`, and read the per-file line counts rather than only the file list.

A green gate proves nothing until you have seen it fail. Two gates in this campaign were near-misses: a duplication report rendering 0 clones while 65 existed, and a probe whose broken fixtures reported valid filing periods as refused - caught only by its positive control.

Every row states what was measured and where the evidence sits, so a reader who inherits this without the conversation can act on it.
