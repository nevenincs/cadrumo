---
tags:
  - '#exec'
  - '#distribution-installation-readiness'
date: '2026-07-16'
modified: '2026-07-16'
body_hash: 'sha256:17403871afaa8a7954cb5c61b451c95108d6bb178f0910915fe0d53064452cfc'
related:
  - "[[2026-07-15-distribution-installation-readiness-plan]]"
---

# `distribution-installation-readiness` `W01.P02` summary

The phase now proves that one immutable three-wheel Python cohort can be installed into
a clean environment and complete the same grounded Modelo 200 itinerary through both
the public CLI and public MCP protocol. The MCP server resolves and supervises the
sibling CLI from its own installation, operates with checkout imports and product
commands removed from `PATH`, and retains CI-uploaded evidence binding the source
commit, artifact digests, grounded result, persisted resource, and actual child
executable attestations.

- Modified: `.github/workflows/packaging-smoke.yml`
- Modified: `.vault/index/distribution-installation-readiness.index.md`
- Modified: `.vault/plan/2026-07-15-distribution-installation-readiness-plan.md`
- Modified: `dev/packaging/installed_mcp_oracle.py`
- Modified: `dev/packaging/installed_tax_oracle.py`
- Modified: `dev/packaging/tests/test_installed_oracles.py`
- Modified: `dev/packaging/tests/test_packaging_smoke_workflow.py`
- Modified: `pyproject.toml`
- Modified: `src/cadrumo/application/workflow`
- Modified: `src/cadrumo/entrypoints/mcp`
- Created: eleven `W01.P02` step execution records and this phase summary.

## Description

The installed CLI oracle creates an encrypted profile, creates and calculates Modelo
200 work, reloads persisted observations, and requires
`DP200014:00562=23000.00` with the expected formula, legal references, source
references, notices, revision, and work-unit identity. The MCP oracle repeats that
claim through direct public tool dispatch and exact observation-resource identity.

Cold-session profile reads now reopen production custody safely, while failed
master-key unlocks remain non-repairable and cannot clear the active pointer. MCP
subprocess execution is bound to the installed scripts directory and fails closed
instead of consulting ambient `PATH`.

The final cohort gate archives one exact commit, builds and hashes the command wheel
and both mandatory corpus wheels, installs them together, verifies installed metadata
and direct artifact URLs, and runs both oracles from the same environment. Production
telemetry records a one-way digest of the exact `argv[0]` passed to every supervised
subprocess. The gate requires complete and identical attestations for profile creation,
work creation, calculation, and observation retrieval, writes the complete evidence
document to a stable path, and uploads it from packaging CI even when later workflow
steps fail.

Verification completed with Ruff and ty on every touched Python surface, focused
workflow and MCP suites, nineteen packaging-workflow conformance tests, a full
scrubbed-path installed MCP run, and the final two-oracle cohort run. The final cohort
used source commit `107f64f72daa9dc2a932e55b092120e0ae2a982d`, retained all three
wheel digests and per-command child attestations, and passed in 339.48 seconds. Formal
review found no remaining high- or medium-severity phase findings.
