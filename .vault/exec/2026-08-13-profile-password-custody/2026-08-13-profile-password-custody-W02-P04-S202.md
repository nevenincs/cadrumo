---
tags:
  - '#exec'
  - '#profile-password-custody'
date: '2026-08-18'
modified: '2026-08-18'
body_schema: 'body-v1'
body_hash: 'sha256:569f181b18045d8d479df40dc42bcc1649fb3f296bbf0f5b763c5504e6846221'
step_id: 'S202'
related:
  - "[[2026-08-13-profile-password-custody-plan]]"
---

# Have Sol Medium carry the codebase-to-locale parity debt to the registry campaign that owns it, being several hundred missing modelo casilla keys and a set left stale by a revision-id rename that re-keys rather than retires them, the population having grown by an order of magnitude while this campaign ran because the concurrent authority sweep adds modelos faster than their Spanish terminology is authored, since closing it requires real AEAT terminology the grounding rules forbid inventing and deleting the stale set would destroy real Catalan Spanish and Hungarian labels the renamed revisions still need

## Scope

- `src/cadrumo/locales/ and src/cadrumo/_data/registry/`

## Description

## Outcome

Carried to the registry campaign that owns it: the codebase-to-locale parity debt — several hundred missing modelo casilla keys under `src/cadrumo/locales/*/modelo/schema/` and a set left stale by the revision-id rename (re-keyed rather than retired), the population having grown while the concurrent authority sweep adds modelos faster than Spanish terminology is authored. Closing it requires real AEAT terminology the grounding rules forbid inventing, and deleting the stale set would destroy real labels the renamed revisions still need — it is therefore the registry campaign's population, not this one's.

## Notes

Handover completed as a recorded carry-forward with the enumeration inherited from the parity-suite diagnostics; the owning campaign's plan must pick it up. This row closes on the handover, not on the debt.
