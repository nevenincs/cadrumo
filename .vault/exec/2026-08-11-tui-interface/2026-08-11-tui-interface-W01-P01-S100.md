---
tags:
  - '#exec'
  - '#tui-interface'
date: '2026-08-30'
modified: '2026-08-30'
body_schema: 'body-v2'
body_hash: 'sha256:d99a0dad6a529cba13987a67da4fe0c616f7aae105ab133ce34e562efeb35487'
step_id: 'S100'
related:
  - "[[2026-08-11-tui-interface-plan]]"
---

# Sweep the bare single-code campaign identifiers from production source, then flip that pattern to production scope at the END of the sweep and not before. RE-MEASURED 2026-08-30 AND THE COUNT BELOW IS STALE: the live figure is 16 sites across 9 production modules, not 68 across 22. The difference is not progress against this row -- it is `W05.P23.S322` in the tui-architecture plan, which swept 17 sites across 12 files. THESE TWO ROWS ARE THE SAME SWEEP ADDRESSED FROM TWO PLANS THAT DO NOT REFERENCE EACH OTHER; each reads as independently coherent, which is why the duplication survived review. Neither can be executed correctly without knowing the other exists. THE 9 THAT REMAIN ARE A COORDINATION BOUNDARY, NOT A LEFTOVER, and will not shrink on their own: they are exactly the files S322 declined as peer-held, plus `_edit_models.py` handed to the receipt lane. Live set: entrypoints/cli/_common.py 4; application/modelo/_edit_models.py 2; application/user_profile/bundle.py 2; domain/modelos/calculation_revision.py 2; domain/modelos/calculation_revision_m303_handoff.py 2; application/live/filed_observation_persistence.py 1; application/modelo/_m303_filing_evidence.py 1; application/modelo/_m303_regimen_simplificado_scope.py 1; application/modelo/_verification_predicates.py 1. MEASURE BEFORE SCOPING, AND SAMPLE THE MEMBERS RATHER THAN REPORTING THE COUNT: a fresh measurement of this set first returned 75 modules / 123 sites, wrong because its suppression stripper removed `# noqa` but left `: S106`, so every ruff code counted as a campaign identifier -- the exact defect this row already documents, reproduced in new tooling minutes after the warning was read. It was caught by looking at the top hits, not by checking the number. A population count is not a measurement until you have inspected members of the population. JUDGE EACH, DO NOT SUBSTITUTE MECHANICALLY: user_profile/bundle.py reads as section banners 'Serialiser - S105' and 'Deserialiser - S106', and those two codes are also real ruff rule identifiers, so a mechanical sweep is wrong in either direction there. Land the scope flip only once the set is empty; `the 9 production modules named above, coordinated with their owning lanes, then dev/tests/_marker_metadata_patterns.py`.

## Scope

- `the 22 production modules named above`
- `then dev/tests/_marker_metadata_patterns.py`

## Changes

- `M` `dev/tests/_marker_metadata_patterns.py`
- `M` `dev/tests/test_marker_integrity.py`
- `verify:` `bare-S pattern over production, through the gate's own stripper` -> `0 hits`
- `verify:` `production-scoped cases` -> `4 -> 5`
- `verify:` `pytest dev/tests/test_marker_integrity.py` -> `32 passed; the guard this flip contradicted now passes`

## Notes

THE SWEEP HALF WAS ALREADY DONE, BY THE OTHER PLAN. This row and
`W05.P23.S322` in the tui-architecture plan are the same sweep addressed from
two plans that do not cross-reference, which this row states outright. The nine
modules and sixteen sites it lists as the live set are exactly the ones swept
when S322 closed. Production now measures ZERO bare campaign identifiers.

WHAT REMAINED WAS THE FLIP, AND ITS TWO BLOCKERS HAD CLEARED. `W01.P01.S97`
production-scoped the dotted step-notation cases and deliberately deferred the
bare single-code one: it hit 68 files, of which 55 were the file-level
suppression form the stripper could not see because its pattern required the
directive to follow the hash directly. The stripper now accepts an optional
tool prefix, so ruff's own rule codes no longer read as campaign ids.

THE GATE'S OWN META-TEST HAD TO MOVE WITH THE FLIP, AND THAT IS THE POINT.
`test_production_scope_excludes_the_families_that_false_fire_on_domain_prose`
listed `closed by S08` among the families that must NOT reach production, with
the stated reason that the pattern reaches 22 unswept production modules. That
exclusion was empirical, not principled, and both facts it rested on had
changed. The probe moved to the production-scoped set and the comment now
records the measurement rather than the stale count. Leaving the probe where it
was would have meant a gate asserting the opposite of the table it guards.

FOUR FAILURES IN THAT SUITE ARE NOT THIS ROW'S. Three are pytestmark placement
and validity violations in peer modules; one is a campaign-metadata hit in a
test module's docstring. All four are TEST modules, which every pattern already
scanned before this flip, so widening a pattern to PRODUCTION scope cannot
reach them. The arithmetic confirms it: the suite failed 5 before this change
and 4 after, and the one that disappeared is exactly the guard above.
