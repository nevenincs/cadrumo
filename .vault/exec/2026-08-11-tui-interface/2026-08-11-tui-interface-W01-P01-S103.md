---
tags:
  - '#exec'
  - '#tui-interface'
date: '2026-08-30'
modified: '2026-08-30'
body_schema: 'body-v2'
body_hash: 'sha256:f2df622c1e429cc2363519c6839c4c5557003b28d09aa21eaed7b4a8e46a3cda'
step_id: 'S103'
related:
  - "[[2026-08-11-tui-interface-plan]]"
---

<!-- Machine-owned: the filename, the frontmatter, the title heading and the
     Scope list are all filled by `vaultspec-core vault add exec` from the
     originating Step row; never hand-edit them. Add no frontmatter fields.
     Wiki-links belong in `related:` only, never in the body. -->

# Action the gates-that-report-clean family against the audit that now records it, rather than fixing five gates independently. The audit is 2026-08-30-repo-gate-integrity-wrong-subject-gates-audit and it carries the measured exemplar plus the sub-species distinction: a gate that DRIFTED into blindness was once right, so a staleness check can catch it, while a gate authored blind to the population it names was never right about it and nothing about it will ever go stale. FIVE instances, not six -- a sixth was reported and then refuted, and the audit records the refutation so nobody rediscovers it. The five: the strict-frozen config gate that proves the constant and never that anything uses it (the exemplar, 62 to 221 production sites depending on the filter); a TUI route census built on a hardcoded literal set; an executor conformance matrix asserting a twenty-item registry against its own thirteen-item tuple; a staleness message whose remediation instruction completes the defect it should prevent; a workspace remnant scanner whose pass condition contradicts a legitimate docstring; and a relocation-parity test whose name pins an exact consumer count. Four of those are RELAYED and unverified in the audit -- each needs its own measurement before it earns action. The counter-example worth copying is in the same tree: the marker scan's scope gate asserts BOTH directions, so a family that quietly stops matching fails as loudly as one that leaks, which is what a gate that cannot go vacuous looks like

## Scope

- `.vault/audit/2026-08-30-repo-gate-integrity-wrong-subject-gates-audit.md and the gates it names`

## Changes

- `M` `.vault/audit/2026-08-30-repo-gate-integrity-wrong-subject-gates-audit.md`
- `M` `dev/tests/test_workspace_assembly_forbidden_paths.py`
- `verify:` `each of the five measured individually before action` -> `2 fixed, 2 refuted, 1 tracked partial`

## Notes

Every instance now carries its OWN measurement, which the row required before
any of them earned action. Two dissolved on that measurement, which is the
result worth recording loudest: a relayed finding that survives to a plan row
reads exactly like a measured one.

REFUTED (2):
- The TUI route census is not built on a hardcoded literal set.
  `declared_destination_ids()` derives from `get_args` on the destination type
  alias, its docstring states why a literal was rejected, and the table is
  compared against it in BOTH directions. It is this family's counter-example,
  not an instance of it.
- The workspace remnant scanner's reported defect -- a pass condition
  contradicting a legitimate docstring -- was already resolved by three
  documented exclusions plus whole-filename matching.

FIXED (2):
- The staleness remediation that completed the defect it should prevent, closed
  under W01.P01.S104. The root cause was NOT the lane named in the message:
  naming any lane without knowing what the collected tests carry reproduces the
  empty selection for some other input. The banner now names the markers
  actually collected.
- The remnant scanner carried a STALE EXCLUSION, found while measuring the
  refuted claim above: `workspace.py` was excluded from the scan and no longer
  names the forbidden module at all, so the exclusion suppressed nothing and
  would have gone on suppressing that file the day it named the module again.
  Removed, and each surviving exclusion now asserts it still contains the
  token. Proven both directions against the real population.

TRACKED, NOT CLOSED (1): the strict-frozen config gate, the family's exemplar.
170 of ~222 production sites are repointed to the canonical constant under
W05.P23.S352, which stays open with a partial record for the remaining 26 and
the census gate. The row's own sequencing is why the gate is not landed here:
landing it first reds up to 221 sites and makes weakening the detector the
cheapest route to green.

The executor conformance matrix is the sixth item the row's own text
discounts; its census half landed under W07.P16.S333, whose partial record
names the three-deep payload blocker that keeps it open.

The counter-example the row points at held up under use: the marker scan's
scope gate asserts both directions, and when W01.P01.S100 widened a family it
FAILED loudly and correctly until its own probe moved with the change. A gate
that cannot go vacuous is one that breaks when the table it guards moves.
