---
tags:
  - '#exec'
  - '#secure-storage-performance-hardening'
date: '2026-08-23'
modified: '2026-08-23'
body_schema: 'body-v1'
body_hash: 'sha256:4f36b249015cf2b6251a32e41421a70980c5df97a82601e3f3f3a943da8dc181'
step_id: 'S54'
related:
  - "[[2026-08-22-secure-storage-performance-hardening-plan]]"
---

# Atomically hard-cut the complete root, group, and leaf surface to distributed production-authored CommandSpec as sole structural authority, project runtime assembly, help, completion, census, schema, operator, MCP/HITL, execution policy, and write routing from specs, make handlers behavior-only, and delete callback/decorator authority, lazy and path mirrors, both runtime JSON readers, both development generators, ignore entries, cache-parity tests, and stale prose with no fallback, shim, or partial coexistence

## Scope

- `repository CLI command-authority surface`

## Description

- Define distributed import-light CommandSpec modules and compile the complete runtime tree from their immutable graph.
- Route help, completion, census, schema, operator, MCP, HITL, execution policy, machine-secret semantics, and storage write policy through the same graph.
- Remove decorator and registrar structure, mutable lazy and schema registries, path and alias mirrors, generated JSON readers, development generators, ignore entries, compatibility fallbacks, retired passphrase behavior, and stale tests and prose.
- Preserve the frozen historical benchmark census as development evidence while proving it is neither imported nor shipped.
- Reconcile two independent architecture reviews and resolve every critical, high, medium, and low finding.
- Verify the effective diff against baseline `a453c9ddb41769b889cd710719a73f7351462208` and exclude concurrent Modelo registry changes from the S54 closure index.

## Outcome

- CommandSpec is the sole executable structural authority for 361 nodes: one root, 71 groups, and 289 leaves.
- Runtime construction remains lazy and node-local; behavior handlers and schema models resolve only through spec-owned public targets.
- Both generated command JSON files are absent and untracked. They were ignored generated caches, not Git-recoverable artifacts; their deleted generators must not be restored.
- The wheel contains all 27 distributed command-spec modules and contains no generated command JSON, development generator, schema-surface module, machine-secret registry, or development tree.
- Final checks passed: scoped Ruff, scoped type checking, compileall, 52 focused graph/runtime/schema/MCP/documentation tests, exact documentation parity, baseline whitespace validation, and a final independent review at zero findings.
## Notes

- Earlier partial commits `bd46d5b874`, `e766b6d7d2`, and `d5aa9ccd2b` landed concurrently during development. They are recorded as preparatory or mixed S54 state; the final effective review covers the complete diff from the approved baseline without rewriting history.
- S11 and S14 remain deliberately open because their evidence must be re-proved after this authority cut. S55 through S59 own exhaustive planted, clean-source, build, shipping, and cohort gates and were not started.
- Registry-backed broad tests currently encounter unrelated concurrent Modelo 200 registry-data validation failures. The S54 suites use isolated roots where applicable and pass; no shared cache or concurrent registry data was deleted or staged.
