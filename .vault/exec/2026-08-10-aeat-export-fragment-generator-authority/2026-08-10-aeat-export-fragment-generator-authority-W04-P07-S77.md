---
tags:
  - '#exec'
  - '#aeat-export-fragment-generator-authority'
date: '2026-08-13'
modified: '2026-08-14'
body_schema: 'body-v1'
body_hash: 'sha256:0e85125bac1cf4ed6c86f2cb6be35158ccdd7384d902f3ea5f54ff8f7731d1b1'
step_id: 'S77'
related:
  - "[[2026-08-10-aeat-export-fragment-generator-authority-plan]]"
---

# Delete the partial 34-endpoint completeness assumptions and every generic off_form_result, missing-value default, parallel resolver, per-slot scalar store, alias and compatibility reader, co-committed in the same atomic explicit-path commit as the replacement computed value path. The generic off-form result is the sole value source for the non-agricultural módulo importe anchor and backs 14 of the 28 declared module endpoints in all five revisions, so deleting it ahead of the replacement blanks 14 endpoints per epoch, which is the silent under-declaration this campaign exists to prevent. Sweep the projection-ref member, the filing-row model, every test module and all five revisions' projection-endpoint declarations

## Scope

- `src/cadrumo/core/`
- `src/cadrumo/domain/iva/`
- `src/cadrumo/domain/calculations/registry/`
- `src/cadrumo/application/filing/`
- `src/cadrumo/_data/registry/aeat/modelos/303/revisions/`

## Description

- Replace the retired generic module-result enum member with the typed calculated `cuota_devengada` projection value.
- Remove the generic module scalar, formula operators, formula runtime context, public caller plumbing, direct fixtures, and compatibility readers.
- Select the immutable S76 calculation result only after exact activity, annual Orden, module identity, and declared-quantity agreement.
- Migrate all five live revision endpoint declarations and remove their retired formula, construct, casilla, completeness, and reconciliation support.
- Prove the real `build_m303_filing_projection_plan` path projects each computed module value in all five epochs without blanks.
- Regenerate the 19 affected CLI-sequence goldens through the canonical sequence refresher and delete the non-authoritative parked formula residue.

## Outcome

The former generic `off_form_result` and casilla-formula channels no longer exist in the canonical production, revision, and generated-sequence surfaces. Calculated module endpoints now select `cuota_devengada` solely from the immutable simplified-regime result that S76 binds to filing facts, preserving exact row and annual Orden coordinates.

## Notes

Ruff, formatting, and basedpyright checks pass. The five-module real regression slice passes 44 tests, including the five-epoch no-blank production-plan proof. All 19 affected CLI-sequence goldens regenerated successfully through the live application. The only retained `off_form_result` token is an intentional negative compiler payload proving the retired discriminator is rejected. Locale scaffolding additionally reports unrelated missing CLI-overview keys outside this step. Independent review is recorded in the related S77 audit.
