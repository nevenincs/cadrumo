---
tags:
  - '#exec'
  - '#aeat-export-fragment-generator-authority'
date: '2026-08-10'
modified: '2026-08-10'
body_schema: 'body-v1'
body_hash: 'sha256:3ef7376f0c09dec02d54c53ce72e82d2d4f4c16931fd8085684010b0cb8f61d7'
step_id: 'S53'
related:
  - "[[2026-08-10-aeat-export-fragment-generator-authority-plan]]"
---
# Define and prove one parser-owned typed composite relative-closing contract for Modelo 220's exact six-row *** sequence after the Variable body and before the explicit Variable total, preserving every source row, ordinal, length, and content without concatenation, name selection, or extent inference, threading the typed composition through the intermediate representation and fixed-generation refusal boundary, and proving every pinned Modelo 220 binary plus malformed, incomplete, reordered, duplicate, and ambiguous variants

## Scope

- `src/cadrumo/domain/calculations/registry/`
- `src/cadrumo/_data/registry/aeat/legal/is.toml`
- `dev/registry/`
- Reconciled S43 lifecycle evidence in its existing execution and audit records.

## Description

- Add `RecordDesignCompositeRelativeClosing` as the single public production model for the six distinct Modelo 220 closing rows.
- Make `RecordDesignVariableEnvelope.closing` a hard-cut typed union of one exact relative marker or the composite closer, without a legacy field alias.
- Recognize the six-part official source shape through the existing parser and preserve its rows, ordinals, lengths, types, descriptions, validation, and content without concatenation.
- Restore S43 recognition to the final body-led or mixed-total boundary so registered M131, M232, and M390 partial-marker sources remain parseable.
- Add exact `record_design_epoch` authority to the three reviewed M220 source records and exercise the public SHA-verifying loader for 2023, 2024, and 2025.
- Project both closing variants through intermediate schema version 3 and reject provenance parser schema versions 1 and 2.
- Prove real M200, all five M303 epochs, all three M220 binaries, malformed composite variants, registered-source parseability, and pre-write fixed-generation refusal.
- Reconcile the stale S43 lifecycle prose through hash-guarded VaultSpec CLI writes while retaining the rejected interim union-trigger history.

## Outcome

The registry parser now owns one exact typed M220 composite closer. The `T220000000` envelope from each reviewed binary retains six separate source rows with lengths `(3, 3, 1, 4, 2, 5)` and contents `("</T", "220", "(*)[A|E|I|0]", null, "0A", "0000>")`. It neither concatenates those values nor selects the shape by modelo, sheet name, filename, source reference, inferred extent, or legacy tree.

Incomplete, over-complete, reordered, and content-ambiguous six-row compositions refuse through production validation. A Variable body or mixed fixed plus Variable total remains decisive; standalone relative closers and Variable-total facts do not activate envelope recognition because the official M131, M232, and M390 corpus proves they are not unique markers. M200 and all five M303 sources retain their existing single 18-byte closer behavior.

The typed composite survives the public source-SHA- and epoch-bound intermediate loader and semantic join. Fixed-width generation receives the retained envelope, refuses before target creation, and leaves no output directory. Intermediate schema version 3 makes the shape change explicit, and provenance rejects both preceding parser schema versions.

Executor verification passed 67 focused tests and 195 tests across the complete `dev/registry/tests` lane. Scoped Ruff passed. Scoped strict BasedPyright reported zero errors, warnings, and notes. The independent formal review passed 72 focused tests, reproduced the 195-test development lane, and closed with no open findings. Its final audit blob is `646d259c9733407d963f2fd7806159d6845444ef`.

The S43 execution and audit reconciliation passed feature-scoped frontmatter, execution-mapping, body-section, and Markdown checks. Their final blobs are `fa09f6d92900c61d7a1200123ad3eacc4261e42d` and `938b930cf07641332114eed7dc84e4787b7173f9`.

## Notes

Modified production and test paths are `src/cadrumo/domain/calculations/registry/_record_design_schema.py`, `src/cadrumo/domain/calculations/registry/_record_design.py`, the public registry facade, `src/cadrumo/_data/registry/aeat/legal/is.toml`, `dev/registry/_record_design_ir.py`, `src/cadrumo/domain/calculations/registry/tests/test_record_design.py`, `src/cadrumo/domain/calculations/registry/tests/test_record_design_intermediate_source_boundary.py`, `dev/registry/tests/test_record_design_ir.py`, `dev/registry/tests/test_variable_envelope_generation_gate.py`, and `dev/registry/tests/test_provenance_manifest.py`.

Concurrent S45 work changed header-token validation while the final broad domain retry was running. That retry reached 55 passes and 41 setup failures, all at unrelated M111 header-key hydration before S53 behavior. The stable pre-race S53 lanes and independent review remain green; S45 files were not changed. Concurrent `_export_tree.py`, registry-facade ordering, S44 ADR, plan, index, relation, authentication, TUI, and Modelo 303 changes remain peer-owned and are excluded from the S53 payload except for the exact public-facade promotion hunk.
