---
tags:
  - '#exec'
  - '#aeat-export-fragment-generator-authority'
date: '2026-08-13'
modified: '2026-08-13'
body_schema: 'body-v1'
body_hash: 'sha256:333ba47bf15b43d09d6bb3a1a3ad07bb65b37101856c5664cfcba0826783a1f2'
step_id: 'S72'
related:
  - "[[2026-08-10-aeat-export-fragment-generator-authority-plan]]"
---

# Persist the reviewed five-epoch Modelo 303 DP30302 semantic field matrix as a gated artefact rather than a scratch measurement, carrying per-epoch DP30302 totals and the agrícola, no agrícola, simplified, numbered, source-constant, reserve and producer partitions, the 87 distinct field semantics, and the repeating sub-indexed module descriptions that require a sub-index axis rather than ad-hoc endpoints. Measure through `load_record_design_intermediate` against the five hash-pinned binaries and read admissible homes from the loaded snapshot through `ValidatedRegistryAuthority`, never a directory listing. Gate the partition invariants so a drifting binary or a re-partitioned anchor reds, and correct the two constants the deficit audit fixed too early: the non-agricultural count is 114, 110, 116, 122 and 122 rather than a constant 122, and the agricultural cohort carries twelve fields per slot at 2024-late rather than ten

## Scope

- `dev/registry/`
- `dev/registry/tests/`

## Description

- Resolve each of the five M303 design epochs through its filing-context-selected `ValidatedRegistryAuthority` snapshot before parsing the hash-pinned record-design binary.
- Reuse the public snapshot-source admission gate for source catalogue membership, hash, epoch, and selected-revision ownership rather than redeclaring it for the matrix.
- Persist and validate the `simplified` partition alongside the six disjoint field partitions, preserving the reviewed 87 semantic descriptions and measured repeating-module sub-index cardinalities.
- Replace the patch-based reclassification test with a mutation of one field from the actual parsed 2023 DP30302 intermediate and prove the measured partition diverges from the artefact.
- Run the focused real-binary matrix and semantic-map validation suites, Ruff, and BasedPyright.

## Outcome

The persisted five-epoch matrix is gated by the validated M303 registry route. Every epoch records total, agricultural, non-agricultural, simplified, numbered, constant, reserve, and producer counts; `simplified` must equal the two activity cohorts. The measured artefact remains equal to the live five-binary census, including the corrected non-agricultural sequence `114, 110, 116, 122, 122` and the twelve-field agricultural cohort per activity slot at 2024-late.

## Notes

Focused verification passed: `33 passed` for `dev/registry/tests/test_dp30302_field_matrix.py` and `dev/registry/tests/test_semantic_map_validation.py`; Ruff and BasedPyright reported no findings. No production authority, export semantic home, endpoint declaration, or calculation path was changed.
