---
tags:
  - '#exec'
  - '#aeat-export-fragment-generator-authority'
date: '2026-08-13'
modified: '2026-08-13'
body_schema: 'body-v1'
body_hash: 'sha256:9237ad07308b6ae2ab0248c7b6047aabbaf37451b8faef6595611168e0faa08d'
step_id: 'S74'
related:
  - "[[2026-08-10-aeat-export-fragment-generator-authority-plan]]"
---

# Adjudicate and collapse the two Modelo 303 simplified-regime calculation mechanisms to one canonical mechanism, being the casilla-leaf formula-runtime ops and the filing-row projection, ruling which channel owns the computation given that the casilla channel is structurally single-activity at ten entries with no cohort or slot axis while the official design spans up to twelve activities, two cohorts and three records. Delete the non-canonical channel and sweep every consumer, fixture and registry TOML formula-op contract in one atomic explicit-path commit, leaving no re-export bridge, parallel resolver or compatibility reader

## Scope

- `.vault/adr/`
- `src/cadrumo/domain/calculations/registry/`
- `src/cadrumo/_data/registry/aeat/modelos/303/revisions/`

## Description

- Ground the mechanism decision in the accepted M303 simplified-regime and projection ADRs, the S74 plan contract, and the live formula-runtime and filing-row implementations.
- Select the row-indexed calculation-domain channel as the sole computation owner and retain projection as typed value selection only.
- Define the atomic retirement boundary for the single-activity casilla formula channel and its transitive consumers.
- Amend the existing accepted simplified-regime ADR in place instead of creating a competing decision record.

## Outcome

The accepted ADR now states unambiguously that immutable filing rows plus the exact annual-Orden snapshot feed one typed per-activity calculation result. The generic casilla formula runtime is non-canonical and must be removed with its operators, M303-specific runtime context and caller plumbing, and all five revisions' internal support-casilla contracts.

The decision preserves the annual-Orden authority, scope and filing-evidence owners, filing rows, projection-reference union, and coordinate-only projector. It orders S75 identity closure before S76 result construction and reserves the generic `off_form_result` and endpoint deletion for S77's replacement cutover, preventing a temporary blanking of admitted endpoints.

## Notes

The required ADR-researcher persona could not be dispatched because the shared agent-thread limit was already reached. The architecture owner completed the amendment locally from the same grounded evidence. No production implementation was changed in this step; the implementation fleet owns the explicit-path deletion sweep that follows this decision.
