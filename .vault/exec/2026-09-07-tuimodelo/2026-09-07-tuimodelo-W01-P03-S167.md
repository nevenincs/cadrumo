---
tags:
  - '#exec'
  - '#tuimodelo'
date: '2026-09-07'
modified: '2026-09-07'
body_schema: 'body-v2'
body_hash: 'sha256:ea6c8b875c9a228723c57ea2688c37a95c94da692d791d95fd05e0fe3502f8be'
step_id: 'S167'
related:
  - "[[2026-09-07-tuimodelo-plan]]"
---

# Record the gate state this campaign starts from, including the size budget already failing on two of its own target files, so no wave mistakes an inherited red for one it caused SCOPE CORRECTED BY MEASUREMENT: the deliverable is a vault audit, not a file under the development audit tree. Searched first and found no cross-gate known-red registry anywhere in the repository; that tree holds only per-gate ratchet data that a gate reads, and NOTHING would consume a campaign baseline placed there, so it would be an inert artefact in a code tree. The code-stands-alone mandate separately forbids development code from carrying campaign state. The baseline is therefore recorded as an audit beside this campaign's other audit, which is the sanctioned home for a measured record.

## Scope

- `.vault/audit`

## Changes

- `A` `.vault/audit/2026-09-07-tuimodelo-inherited-gate-baseline-audit.md`
- `M` `.vault/plan/2026-09-07-tuimodelo-plan.md`

## Notes

Eight inherited gate failures were recorded with their exact failing subject and owner, so a later
wave meeting a red can tell an inherited one from a regression it caused. The rule the record
establishes is symmetric and is the point of it: a red not listed is this campaign's to explain, a
red listed is not, and the burden shifts only when the owning lane fixes it.

THE STEP'S SCOPE WAS CORRECTED RATHER THAN FOLLOWED. It named the development audit tree, and
searching first showed why that is wrong: no cross-gate known-red registry exists anywhere in the
repository, that tree holds only per-gate ratchet data which a gate actually reads, and nothing
would consume a campaign baseline placed there. It would have been an inert artefact sitting in a
code tree, and the code-stands-alone mandate separately forbids development code from carrying
campaign state. The baseline is a measured record, so it was written as an audit beside this
campaign's other audit.

Two entries earn their place beyond merely being red. The destination-authority fixed point is
already violated in the module this campaign's reachability wave must admit destinations into, so
that wave does not start from a clean authority and would misread its first failure as its own. And
one session-resume failure asserts a refusal document nested one level deeper than the product now
emits; a test written earlier in this campaign copied that stale shape and briefly reported a
regression that does not exist, so it is recorded as a hazard rather than only as a count.

The cold-start entry is recorded structurally, not just as a failure. That contract asserts inside
its verb loop and therefore stops at the first failure, so a verb added after the failing one gets
no evidence from it at all. That is why a step in this campaign proved its case by direct dispatch
instead of relying on the suite it had just joined.

The record is explicitly a measurement at a moment rather than a standing guarantee, because
several lanes edit this tree concurrently and two of these entries were already refined during
execution.
