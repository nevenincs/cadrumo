---
tags:
  - '#exec'
  - '#source-casilla-integration'
date: '2026-08-25'
modified: '2026-08-25'
body_schema: 'body-v1'
body_hash: 'sha256:e4d43eba241bdfaa1d1e15280051428c1354470e19618817e4634ca54a008872'
step_id: 'S96'
related:
  - "[[2026-08-22-source-casilla-integration-plan]]"
---

<!-- FRONTMATTER RULES:
     tags: one directory tag (hardcoded #exec) and one feature tag.
     Replace source-casilla-integration with a kebab-case feature tag, e.g. #foo-bar.
     Additional tags may be appended below the required pair.

     modified: CLI-maintained last-modified stamp; set at scaffold time,
     refreshed by mutating CLI verbs and vault check fix; never hand-edit.

     step_id is the originating Step's canonical identifier, e.g. S01.
     The S96 and 2026-08-22-source-casilla-integration-plan placeholders are machine-filled by
     `vaultspec-core vault add exec`; do not fill them by hand.

     Related: use wiki-links as '[[yyyy-mm-dd-foo-bar-plan]]' and link the
     parent plan.

     DO NOT add fields beyond those scaffolded; metadata lives
     only in the frontmatter. -->

<!-- LINK RULES:
     - [[wiki-links]] are ONLY for .vault/ documents in the related: field above.
     - NEVER use [[wiki-links]] or markdown links in the document body.
     - NEVER reference file paths in the body. If you must name a source file,
       class, or function, use inline backtick code: `src/module.py`. -->

<!-- STEP RECORD:
     This file represents one Step from the originating plan. Identified
     by its canonical leaf identifier (S##) and ancestor display path.
     The adjudicate M360 row semantics and source ownership from official evidence and ## Scope

- `.vault/research/2026-08-22-m360-row-source-grounding-research.md` placeholders below are machine-filled
     by `vaultspec-core vault add exec` from the originating Step row;
     do not fill them by hand. -->

# adjudicate M360 row semantics and source ownership from official evidence

## Scope

- `.vault/research/2026-08-22-m360-row-source-grounding-research.md`

## Description

- Ground M360 request and row semantics in official AEAT material and compare them with the declared registry carrier.
- Confirm the worksheet assembler’s synthetic row identity and the deferred source-mesh/census ownership state.
- Retain the bounded ingress-blocked census predicate with the official axes and subsequent proof obligations made explicit.

## Outcome

The official row is document detail inside a refund request. No existing secure owner preserves its full axes or durable identity, so S96 does not enroll a resolver or claim a connected route. The census remains explicitly blocked pending a later secure owner and the S97-S99 proof program.
## Notes

The plan’s historical research path lacks the CLI-required feature segment; the canonical CLI scaffolded `2026-08-22-source-casilla-integration-m360-row-source-grounding-research.md` instead. No official artifact was bundled because the re-fetchable AEAT URLs are sufficient for this research-only adjudication.

Scoped census pytest collection was blocked before execution by concurrent user-profile work: `ProfilePersistedSessionPort` is absent from `_custody_ports` during import. `vault check all --feature source-casilla-integration` passed all errors; it reported only pre-existing template/reference hygiene warnings plus the new research’s unlinked-plan warning. This research-only step changes no Python file, so the path-scoped Ruff set is empty.
