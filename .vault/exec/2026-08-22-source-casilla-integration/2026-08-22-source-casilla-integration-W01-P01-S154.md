---
tags:
  - '#exec'
  - '#source-casilla-integration'
date: '2026-08-23'
modified: '2026-08-23'
body_schema: 'body-v1'
body_hash: 'sha256:4903dff141d3ba1a1d47dc4cfa1a280e2e82753aafa72d91b441b11194ba9769'
step_id: 'S154'
related:
  - "[[2026-08-22-source-casilla-integration-plan]]"
---

<!-- FRONTMATTER RULES:
     tags: one directory tag (hardcoded #exec) and one feature tag.
     Replace source-casilla-integration with a kebab-case feature tag, e.g. #foo-bar.
     Additional tags may be appended below the required pair.

     modified: CLI-maintained last-modified stamp; set at scaffold time,
     refreshed by mutating CLI verbs and vault check fix; never hand-edit.

     step_id is the originating Step's canonical identifier, e.g. S01.
     The S154 and 2026-08-22-source-casilla-integration-plan placeholders are machine-filled by
     `vaultspec-core vault add exec`; do not fill them by hand.

     Related: use wiki-links as '[[yyyy-mm-dd-foo-bar-plan]]' and link the
     parent plan.

     DO NOT add fields beyond those scaffolded; metadata lives
     only in the frontmatter. -->

<!-- LINK RULES:
     - [[wiki-links]] are ONLY for .vault/ documents in the related: field above.
     - NEVER use [[wiki-links]] or markdown links in the document body.
     - NEVER reference file paths in the body. If you must name a source file,
       class, or function, use inline backtick code: `src/module.py`. -->

<!-- STEP RECORD:
     This file represents one Step from the originating plan. Identified
     by its canonical leaf identifier (S##) and ancestor display path.
     The verify composite and direct provenance identity, encrypted round trips, mutation refusal, wallet lineage, foreign-asset blocking, and semantic sentinel non-duplication and ## Scope

- `src/cadrumo` placeholders below are machine-filled
     by `vaultspec-core vault add exec` from the originating Step row;
     do not fill them by hand. -->

# verify composite and direct provenance identity, encrypted round trips, mutation refusal, wallet lineage, foreign-asset blocking, and semantic sentinel non-duplication

## Scope

- `src/cadrumo`

## Description

- Verify every provenance axis participates in content-addressed revision identity.
- Prove contributor-only and orphaned graphs cannot satisfy encrypted connectivity authority.
- Run the Sol-provided semantic and regex sentinels across production code.
- Run focused source-mesh, wallet, encrypted-persistence, revision-identity, and authority suites.

## Outcome

The sentinel found one canonical provenance carrier per architectural layer and no parallel implementation of the new lineage vocabulary. Semantic analogues retain distinct meanings. All focused verification passed.

## Notes

Verification results: 85 selected tests passed across the principal migrated lane, followed by 27 adversarial identity and authority tests. Ruff passed on every S154-touched source and test. A broader 211-test sweep had 199 passes and 12 failures attributable to concurrent registry/export-layout and secure-session changes; the two provenance assertion failures from that sweep were corrected and are covered by the green focused lane.
