---
tags:
  - '#exec'
  - '#distribution-harness-identity'
date: '2026-07-18'
modified: '2026-07-19'
body_hash: 'sha256:f4f303191a56a64c5a094929a55bbac2a11ed0f32c7e5d81b3d4736c6be0cf68'
step_id: 'S08'
related:
  - "[[2026-07-18-distribution-harness-identity-plan]]"
---

# Wire the approved marketplace bilingual copy into the marketplace description source and enroll its approved English-Spanish pair in the verifier approval set

## Scope

- `src/cadrumo/agent/_workspace.py`

## Description

- Replaced `_MARKETPLACE_DESCRIPTION` in `src/cadrumo/agent/_workspace.py` with bilingual string: `English:` block followed by `\nEspañol:` block, containing the approved S06 copy verbatim.
- Updated `packaging/marketplace/.claude-plugin/marketplace.json` scaffold to match what `materialise_marketplace` generates with the new bilingual description (JSON `\n` escape for the EN/ES separator; accented chars in literal UTF-8 — both parse identically with Python `json.loads`).
- Added `_MARKETPLACE_DESCRIPTION_EN` and `_MARKETPLACE_DESCRIPTION_ES` Final[str] constants to `dev/packaging/verify_distribution_identity.py` matching the parser-extracted sections exactly.
- Added `("claude_marketplace_client_display", "description")` key to `_APPROVED_PRODUCT_DESCRIPTION_PAIRS` with the new `(EN, ES)` frozenset pair.
- Updated `test_marketplace_manifest_is_schema_shaped_and_resolves_to_the_plugin` in `src/cadrumo/agent/tests/test_marketplace_generation.py` to assert the bilingual prefix and the `\nEspañol:` separator.
- Rewrote row 1 assertions in `test_real_client_display_descriptions_report_missing_bilingual_claim_parity` to reflect `translation_approved=True`; updated `approved_pair_count` from 2 to 3; moved rows 3/4 (mcpb) to the not-yet-wired block.

## Outcome

S08 committed. Description-side assertions for the marketplace field pass:

- `test_real_client_display_descriptions_report_missing_bilingual_claim_parity`: PASS
- `test_marketplace_manifest_is_schema_shaped_and_resolves_to_the_plugin`: PASS

`approved_pair_count` advances from 2 to 3. Claims passing for the marketplace description: capability, safety, on_host_storage, never_files_live in both English and Spanish. Privacy and human_confirmation are absent from the short marketplace blurb.

## Notes

- The marketplace scaffold file (`packaging/marketplace/.claude-plugin/marketplace.json`) could not be regenerated by running the generator directly due to import-time errors in the working tree (sibling rename executor's in-flight ErrorCode registry changes). The file was constructed manually to match `json.dumps(document, indent=2)` output. The parity test `test_checked_in_marketplace_scaffold_matches_the_generator` uses Python `json.loads` on both sides, which normalizes `\n` escapes and literal newlines identically; the test will pass once the generator can run cleanly.
- Identity/namespace tests remain red (sibling rename executor in flight — expected).
