---
tags:
  - '#exec'
  - '#unstructured-document-ingestion'
date: '2026-08-11'
modified: '2026-08-12'
body_schema: 'body-v1'
body_hash: 'sha256:4cb3b0384c28f60307e3e93b5973e85833e4cb703bc7afa87df1883250e6733a'
step_id: 'S301'
related:
  - "[[2026-08-07-unstructured-document-ingestion-plan]]"
---

# Observe the one link the earlier trace asserted rather than read: does the enrolled foreign-asset resolver write row_binding_values in the exact shape the evidence projection joins on. The instrument exists and is uncommitted, drives the real producer rather than hand-setting the row values, and has already earned its keep by surfacing a production crash nineteen files of sweeping and a green end-to-end suite both missed. TWO BLOCKING CONDITIONS, and neither is the shape question. FIRST, the finding model is mid-rename by another campaign, replacing free-text message with a locale key and a facts mapping, and the instrument has been converted to the NEW shape while that shape exists only in a working tree and zero times at HEAD. Committing it in that state would land a test green on one machine and red for everyone else and in CI, which is the same verified-at-the-layer-the-author-could-see failure this campaign keeps finding in other forms. SECOND, the end-to-end module it runs beside still asserts the OLD shape, so the pair cannot be coherent until the rename lands. Do not run it meanwhile: a run would measure how an uncommitted test performs against an uncommitted model, which is a question about a state nobody else has. Land it only once every symbol it asserts on exists at HEAD

## Scope

- `src/cadrumo/application/calculations`

## Description

- Re-measure both blocking conditions the row named, against HEAD.
- Run the instrument and establish whether its red is the shape question or
  something before it.
- Complete the profile readiness the path demands, then read the answer.

## Outcome

BOTH BLOCKERS CLEARED, and the answer is NO DEFECT. The enrolled foreign-asset
resolver writes its row bindings in the exact shape the evidence projection
joins on, and the link the earlier trace asserted is now read rather than
assumed.

The row's two conditions were: the finding model mid-rename with the new shape
existing only in a working tree, and the neighbouring end-to-end module still
asserting the old one. Both have landed at HEAD - the locale-key and facts
shape is in the production module, and the instrument itself is committed
rather than uncommitted. So the row's precondition was met and its warning
against running meanwhile no longer applies.

The instrument was RED on all five cases, and the reason was NOT the question
it exists to ask. It never reached the producer. Two stacked filing-grade
profile readiness refusals stood in front of it: an undeclared tax territory,
and behind that the IVA block that became mandatory after this module was
written. Only once both were declared did the producer run, and then every
case passed.

So the row's own framing was right in a way it could not have known: it warned
against running the instrument against an unsettled model shape and measuring
a state nobody else had. The state that actually needed settling was the
PROFILE the harness builds, not the model.

## Notes

THIS IS THE THIRD SUITE THIS SESSION RED FROM ONE CAUSE, and the pattern is
worth naming rather than fixing three times quietly. A peer lane tightened the
filing-grade profile requirements - the Modelo 303 regime composition and four
special-regime booleans - without sweeping the test profiles that predate the
change. The ledger CLI harness was the first and largest at roughly thirty
modules, the confirm suites the second, and this hand-built profile the third.
Each presented as an unrelated failure in an unrelated surface.

Both additions here are readiness preconditions rather than facts the
measurement turns on, and each carries its reasoning inline: common-regime
because the foral territories are explicitly unsupported and a foral Modelo
720 filer is a different product question, and an ordinary general-regime
filer in no special regime because that is the population a plain
foreign-asset holder belongs to. Neither touches the producer, the projection
or the join keys.
