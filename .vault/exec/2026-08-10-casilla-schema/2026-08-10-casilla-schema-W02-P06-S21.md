---
tags:
  - '#exec'
  - '#casilla-schema'
date: '2026-08-11'
modified: '2026-08-11'
body_schema: 'body-v1'
body_hash: 'sha256:884ba59a935356609bd0afe9a59a975bdeb9b1a6acab94e58624fc9d68632871'
step_id: 'S21'
related:
  - "[[2026-08-10-casilla-schema-plan]]"
---
## Scope

- Preserve every verification finding's validated `message_facts` on the typed CLI Notice context.
- Exercise the real Modelo work verification envelope path and its native cross-period blocker codes.

## Description

- Extend the canonical `verification_report_notices` projection to stringify and copy every locale-neutral finding fact.
- Preserve seeded severity, kind, casilla, expectation, and provenance context values.
- Fail closed when a fact conflicts with an already-seeded native context value while admitting an identical duplicate idempotently.
- Update the real natural-key CLI fixture for the current required tax-residence jurisdiction scope.
- Assert that non-empty native `blocker_codes` reach the emitted JSON Notice context as data.
- Run the exact integration path, focused report projections, Ruff, formatting, diff hygiene, and formal review.

## Outcome

- Native verification blocker codes and all other validated finding facts now survive onto `Notice.context` instead of remaining only locale interpolation inputs.
- The real M130 natural-key verify integration passed through profile creation, calculation, persisted verification refusal, and JSON envelope emission.
- The exact integration node passed once locally and once independently under review; five unaffected report projection tests also passed.
- Ruff, format check, and scoped diff check passed.
- Formal review returned PASS with no findings and independently confirmed str, int, bool, and Decimal projection plus fail-closed conflict behavior.

## Notes

- The first integration attempt exposed a stale profile fixture missing the now-required `tax_residence.jurisdiction_scope`; the fixture now supplies canonical `common_regime` explicitly.
- One unrelated locale test remains red because all selected catalogues currently resolve the same English fallback.
- `_modelo_rendering.py` retains 388 legacy strict BasedPyright diagnostics, with zero diagnostics in the modified projection range.
- No action was inferred, and no fake, stub, patch, skip, compatibility path, or mirrored business logic was introduced.
