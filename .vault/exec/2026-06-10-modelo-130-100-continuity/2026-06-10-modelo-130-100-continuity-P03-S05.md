---
tags:
  - '#exec'
  - '#modelo-130-100-continuity'
date: '2026-07-04'
modified: '2026-07-08'
step_id: 'S05'
related:
  - "[[2026-06-10-modelo-130-100-continuity-plan]]"
---

# E2E test: autonoma profile files M130 Q1-Q4 (real adapters, isolated store) then the annual M100 credits the summed pagos fraccionados in the correct casilla

## Scope

- `assert exact reconciliation`
- `anti-tautology`
- `no mocks/skips`
- `application/modelo/tests`
- `entrypoints/cli/tests`

## Description

Verify-close: the E2E fold-in test exists at HEAD, exercises real adapters, and asserts exact reconciliation with an anti-tautology guard.

- Confirm `test_m100_0604_folds_in_four_m130_quarters_on_live_calculate` seeds four M130/2024 quarters as filed observations through the production persistence API and drives the live M100/2024 calculate.
- Confirm real adapters and isolated store: real encrypted-SQLite `SecureObjectRepository` with `EphemeralMasterKeyProvider`, real registry authority, real calculation engine, real relation resolver, per-test `tmp_path` bucket — no mocks, stubs, skips, or xfail.
- Confirm exact reconciliation: `0604 == 1520.00` (four quarters of 380.00), with the mutually-exclusive M131 leg resolving to explicit zero.
- Confirm the anti-tautology property: the test independently expects the summed persona value and asserts the fold wiring, not a reproduction of the registry formula under test.

## Outcome

The autónoma E2E leg is proven: four filed M130 quarters fold into the annual M100 `0604` on the live calculate path, exactly reconciled, with clean source diagnostics. Run: 4 passed. Scoped feature-surface collect-only is clean.

## Notes

The E2E is proven at the application live-calculate layer (the canonical operator calculate entrypoint) rather than through the CLI entrypoint; the plan Step named `entrypoints/cli/tests` as an additional scope hint, but the load-bearing fold-wiring contract is fully exercised by the live application-layer test with real adapters. A CLI-transport E2E remains an optional breadth addition, not a gap in the fold-in contract.
