---
tags:
  - '#exec'
  - '#cli-machine-secret-channel-unification'
date: '2026-08-23'
modified: '2026-08-23'
body_schema: 'body-v1'
body_hash: 'sha256:3bee2f23748ac1be8baf538583de2d637257c74bd649185687a5c330e89c5d1a'
step_id: 'S17'
related:
  - "[[2026-08-23-cli-machine-secret-channel-unification-plan]]"
---

# Run vaultspec-rag semantic code and ADR discovery, confirm exact symbols with rg, then re-read current HEAD, status, and scoped diff, and run focused and broad lint, type, import, test, subprocess, metadata, locale, generator, documentation, sequence, Sphinx, obsolete-code census, and Vault gates while recording honest triage for unrelated failures

## Scope

- `feature-scoped source tests generated documentation and Vault records`

## Description

- Reconstruct the feature-owned surface from the plan, semantic discovery, exact-symbol search, and per-commit path attribution while excluding mixed source-mesh, packaging, registry, and external-PDF work.
- Run scoped Ruff, formatter, and `ty` gates; normalize formatter drift and type every certificate handler context at the Typer boundary.
- Run every focused unit, metadata, help, schema, locale-diagnostic, and CLI-tree module plus the complete Windows subprocess matrix and real POSIX reader and collision probes.
- Correct the subprocess matrix's cross-package reach to use the owning public test facade.
- Run import, locale, documentation, sequence, Sphinx, obsolete-route, secret-leak, and feature-scoped Vault gates; classify every broad failure against current HEAD and exact ownership evidence.
- Request an independent SOL review of gate completeness and remediate every blocking finding before closure.

## Outcome

The feature-owned surface passes lint, formatting, focused `ty`, and 98 focused unit and discovery tests. The settled 69-case Windows subprocess matrix proves both leaf channels, both restore doors, keychain-free root proof, descriptor zero, Windows HANDLE bootstrap, collision-before-read behavior, hostile-environment non-interference, four-locale refusals, cleanup, and secret-free output. On Linux, 23 canonical reader tests exercise real `os.pipe` and `os.read` behavior, descriptor zero, reserved and unreadable descriptors, bounds, and closure; two real CLI subprocesses prove both cross-scope stdin/descriptor-zero aliases refuse before consuming stdin or mutating state.

The canonical data-access sequence is clean and the feature-scoped Vault audit passes every category. Exact censuses find one production reader authority in `_secure_input`, no CLI environment fallback, no live legacy payload fields, no unregistered flag adopter, and no secret-render pattern.

## Notes

- Focused behavior command: `uv run --no-sync pytest -q` over the five machine-secret `_config/tests` modules, seven custody and authentication `cli/tests` modules, `dev/locales/tests/test_machine_secret_diagnostics.py`, and `dev/docs/tests/test_cli_tree.py`; settled result after retiring forbidden patched cases and adding descriptor-zero alias coverage: 98 passed in 61.50 seconds.
- Windows runtime command: `uv run --no-sync pytest -q -m integration src/cadrumo/entrypoints/cli/tests/test_machine_secret_channels_subprocess.py`; all 69 cases pass. The final parallel run passed 64 and exposed five profile-target cases to the repository's documented shared-drive loader race; the mandated sequential rerun `-n 0 -k root_wrong_blank_target_or_secret_refuses_without_secret_disclosure` passed all five in 39.28 seconds.
- Linux reader command: `/tmp/cadrumo-s17-venv/bin/python -m pytest -n 0 -q src/cadrumo/entrypoints/cli/_config/tests/test_secure_input_machine_channels.py`; result: 23 passed in 10.88 seconds.
- Linux CLI probe: invoke both root-descriptor-zero plus leaf-stdin and root-stdin plus leaf-descriptor-zero cases through the production subprocess harness; result: `POSIX_FD0_ALIAS_2_PASSED`, with stdin unread and durable state unchanged in both directions.
- Static commands: scoped `uv run --no-sync ruff check`, `ruff format --check`, and `ty check`; each passed. Direct scoped Basedpyright retained only the unchanged `_certificate` reach to `_emit_envelope` described below.
- Import command: `uv run --no-sync pytest -q dev/tests/test_import_hygiene_gate.py dev/tests/test_import_edge_integrity_gate.py`; result: 51 passed and seven unrelated failures.
- Documentation command: `uv run --no-sync pytest -q -m integration` over documented-command conformance, environment reference, and sequence goldens; result: 356 passed and three failures classified below.
- Sequence command: `uv run --no-sync python -m dev.docs.sequences check --page how-to/protect-data-access --timeout 300`; result: clean.
- Vault command: `uv run --no-sync vaultspec-core vault check all --feature cli-machine-secret-channel-unification`; result: every category passed.
- Locale scaffold and audit report zero missing keys and 51 identical unrelated extra keys in each catalogue, caused by concurrent command and generated-document drift outside this feature.
- Whole-tree import tests pass 51 cases and fail seven on unrelated test-debt, deleted Modelo surfaces, wrapper, TUI-disposition, and dev-awareness drift. The sole feature-owned private reach was corrected before the final focused gates.
- Direct focused Basedpyright is clean for the new channel and profile-gate typing except for the long-standing `_certificate` import of `_emit_envelope`, an unchanged CLI-wide private-name convention outside this transport campaign; no new partial or unknown type remains.
- The full WSL subprocess matrix cannot reach CLI dispatch because the unrelated supervised KDF worker rejects WSL-inherited PTY descriptors during fixture registration. Sequential pytest and a direct isolated-venv invocation reproduce the same pre-dispatch attestation failure. The record therefore claims only the real Linux reader and two CLI collision probes above, not a full POSIX matrix pass.
- Independent SOL review found and closed two HIGH issues: stdin and descriptor zero were not treated as one cross-scope source identity, and feature-owned tests contained forbidden monkeypatch and runtime-skip shortcuts. Preflight now normalizes both source spellings, both directions have real-process unread and state-free proof, redundant patched cases were deleted in favor of the real matrix, and the OS-keychain lane fails closed when its required backend is unavailable.
- Earlier S06 and S10 records describe near-handler patched tests that no longer exist. Their behavioral claims are superseded by the S13 and S14 real-process authority; S18 owns final lifecycle-corpus reconciliation of that historical wording.
- Strict Sphinx parses and renders the target page but stops on unrelated generated Modelo 200 markup, a missing API index, and stale filing, Modelo 303, and verification sequence goldens. The target page itself emits no diagnostic.
- The broader documentation and sequence cohort passes 356 tests and fails three: one pre-existing inline profile-delete command and two repository-wide golden/coherence gates dominated by unrelated changed Modelo and command surfaces. The feature-owned data-access page remains clean in isolation.
- No skipped or mocked proof was introduced, and no unrelated dirty-worktree path was staged.
