---
tags:
  - '#exec'
  - '#cli-action-envelope-hardening'
date: '2026-08-10'
modified: '2026-08-10'
body_schema: 'body-v1'
body_hash: 'sha256:f34f87994e7e1fa8f3b1cd63e22e102b16e242cd75903dea6cb8db5fd7d0554e'
step_id: 'S17'
related:
  - "[[2026-08-09-cli-action-envelope-hardening-plan]]"
---

# Preserve requested live leaf identity before root guards and migrate every profile and taxpayer policy refusal in the shared CLI boundary to typed verdicts

## Scope

- `src/cadrumo/entrypoints/cli/_common.py`

## Description

- Calibrate semantic RAG discovery across code and Vault decisions, then
  confirm the exact producer and consumer denominator with `rg` and `fd`.
- Preserve the requested terminal leaf before root guards and retain it across
  shared-helper refusals for the lifetime of the real root invocation.
- Generalize the S16-only refusal bridge into a typed CLI policy projection
  without modifying the S18 generic error-envelope transport.
- Move profile selection, active-profile, persisted-session, taxpayer identity,
  and former-product precondition meaning into application-owned verdicts.
- Add the canonical zero-argument profile-list action and resolve every
  actionable verdict against the real live command schema.
- Represent former-product refusal as an explicit safety no-recovery outcome.
- Replace legacy suggestion assertions with canonical structured-action
  assertions and add real-root, exact-scenario, and sequential-no-leak tests.
- Run focused and adjacent tests, formatting, lint, strict typing, exact debt
  searches, and independent Terra xhigh review.

## Outcome

All 17 owned S17 scenarios and both S16 dependency outcomes now have canonical
typed meanings. Actionable refusals reference schema-resolved profile create,
login, list, edit, or repair actions with exact resolved and missing argument
bindings. Non-actionable former-product state is an explicit safety outcome.
The shared CLI boundary supplies observed facts and preserves presentation, but
no longer authors recovery command strings for these producers.

The machine-readable scenario table reconciles exact scenario identities
rather than a numeric count. Application, storage, and catalogue units passed
24 tests. The real root typed-projection integration suite passed all 11 tests.
Scoped Ruff and format checks passed across 14 files, targeted BasedPyright
reported zero errors, and configured BasedPyright reported no S16 or S17
diagnostics. Independent Terra xhigh review closed all findings and also closed
the prior S16 root-cutover finding. S17 is complete.

## Notes

- The broader producer integration lane passed 56 tests. Three peer-owned
  failures remain: one custody test still expects the legacy serialized
  `suggestion` field, and two root-guard census tests do not yet classify new
  ledger counterparty and evidence leaves.
- Configured BasedPyright currently reports 17 peer-WIP diagnostics outside
  S16 and S17. No owner diagnostic remains.
- Generic error-envelope serialization remains S18 work. S17 added only the
  typed attachment required for later transport and did not absorb S18.
- No compatibility shim, fake, mock, stub, patch, monkeypatch, skip, or xfail
  was introduced. No commit was made, and peer worktree changes were preserved.
