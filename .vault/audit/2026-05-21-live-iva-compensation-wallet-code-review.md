---
tags:
  - '#audit'
  - '#live-iva-compensation-wallet'
date: '2026-05-21'
related:
  - '[[2026-05-19-live-iva-compensation-wallet-plan]]'
  - '[[2026-05-21-live-iva-compensation-wallet-persona-testimonials]]'
---

# `live-iva-compensation-wallet` Code Review

LIVIVA-CR-001 | MEDIUM | Blocked-decision export and verification guards are not repository-injectable
`src/aeat/application/modelo/_actions.py:1907` checks blocked IVA wallet decisions during verification, and `src/aeat/application/modelo/_export.py:383` checks them before export. Both call the default `IvaWalletDecisionRepository` through `_persisted_blocked_iva_compensation_decision_for_work_unit`, while `calculate_modelo_revision` already accepts an injected decision repository for secure SQL-backed workflows. A caller that injects work-unit/calculation/event repositories backed by a non-default `SecureObjectRepository` cannot inject the matching IVA wallet decision repository into verify/export, so those paths may miss a blocked decision stored in the same non-default backend. Production default settings remain covered, but the service contract is inconsistent and weakens testability for the new safety gate.

Recommended fix: add `iva_compensation_decision_repository` injection to `verify_modelo_revision` and `export_modelo_revision`, thread it through the CLI only as the default, and add a real secure SQL-backed regression test that stores a blocked decision in the injected repository and proves verify/export refuse it.

LIVIVA-CR-002 | HIGH | Persona review found Modelo 303 readiness/calculation can diverge from ledger preflight
The W04 persona dry-run recorded `WALLET-048`: `ledger preflight --period 2026Q1` reported `ready false` for an incomplete business ledger row, while `modelo readiness --modelo 303 --revision-id 2009-y-siguientes --year 2026 --period 1T` reported `ready True` and calculation emitted a zero-valued draft. This is not introduced by the W03.P04 code, but it is directly in the broader IVA calculation-hardening scope and should be treated as the next safety implementation item before marking the wallet/operator wave complete.

Recommended fix: make Modelo readiness and ledger-backed calculation consult the ledger preflight/readiness result for ledger-owned Modelo 303 bindings, and refuse or clearly block calculation when relevant ledger evidence is incomplete.

LIVIVA-CR-003 | INFO | W04.F05 repository-injection follow-up review passed
Reviewed the W04.F05 delta for blocked IVA wallet decision authority, mutation ordering, and test quality. `verify_modelo_revision`, `file_modelo_revision`, and `export_modelo_revision` all consult the injected `IvaWalletDecisionRepository` before granting verification, mutating filing state, or writing export artifacts. The new tests use real encrypted SQL-backed repositories, store the blocked decision outside the default application database, assert the default lookup is empty, and then prove verify/file/export refuse through the injected repository. No critical, high, or medium issues remain for W04.F05.

LIVIVA-CR-004 | INFO | W04.F03 live wallet CLI safety-surfacing review passed
Reviewed the W04.F03 delta for operator-facing no-submit claims. The CLI help now states the fail-closed policy at the `iva-wallet` group, `pull`, and `capture-history` surfaces. Successful text output now includes explicit safety metrics before wallet values, and the tests exercise help rendering plus output-line construction without enabling live AEAT access. The change does not weaken the underlying adapter guard: wallet execute POSTs and non-own-name representation gates remain blocked in `src/aeat/adapters/outbound/aeat/sede/_iva_compensation_wallet.py`. No critical, high, or medium issues remain for W04.F03.

LIVIVA-CR-005 | INFO | W04.F04 carry-forward and authority-surface review passed
Reviewed the W04.F04 delta for calculation ownership, privacy, and CLI usefulness. The CLI does not calculate carry-forward amounts itself; `list_iva_compensation_history` calls the existing `build_iva_compensation_carry_forward_report` engine and only renders the returned lots. Persisted authority decisions are read from the encrypted decision repository and redacted with `taxpayer_ref`; tests assert the raw NIF is not present in the application report JSON. The CLI now exposes source period, age, expiry review state, remaining amount, selected authority, divergence, blocked/stale flags, and authority-source details. No critical, high, or medium issues remain for W04.F04.

LIVIVA-CR-006 | INFO | W04.F02 ledger diagnostic-surface review passed
Reviewed the W04.F02 delta for calculation ownership and operator usefulness. The status command reuses the existing ledger preflight service for readiness issues and only renders stored transaction facts alongside each issue; it does not duplicate IVA aggregation logic. The added CLI regression creates a real transaction through `aeat app ledger add` and proves `ledger status --period` exposes classification, category, taxable base, IVA rate, IVA amount, reason, and detail for blocked rows. No critical, high, or medium issues remain for W04.F02.

LIVIVA-CR-007 | INFO | W04.F06 external-constants centralisation review passed
Reviewed the W04.F06 delta for security, live-mutation risk, and test quality. The change does not add any AEAT navigation or submission path. It only moves read-only guard/parser route identifiers behind `external_constants.toml`: CSV verifier host pinning, declarations final-URL shape, cotejo CSV matching, and IRPF detail-year matching. The new AST guard checks executable string constants in the live auth/Sede/wallet/CSV modules while allowing docstrings, so it is a structural centralisation guard rather than a mirrored business-logic test. No critical, high, or medium issues remain for W04.F06.

LIVIVA-CR-008 | INFO | W04.F07 older Modelo 303 submitted-file extraction review passed
Reviewed the W04.F07 delta for extraction correctness, privacy, and test quality. The parser change is local-only and does not contact AEAT. It narrows the fallback by filing year so 2022 Modelo 303 page-03 records read casilla `71` from the official 2022 position, while 2023+ records retain the existing position. The regression grounds the 2022 position in the bundled official AEAT workbook and uses a redacted synthetic record, avoiding private captured values and avoiding a pure mirror of the production mapping. No critical, high, or medium issues remain for W04.F07.

LIVIVA-CR-009 | INFO | W04.F08 empty-wallet fail-closed review passed
Reviewed the W04.F08 delta for live-mutation safety, false-zero evidence risk, and test quality. The change does not introduce a new AEAT operation; it narrows acceptance after the already-guarded wallet read query. A no-table page that still exposes AEAT's executable `ejecutar` control now fails closed instead of becoming `total_pending=0`, and the post-query navigation path raises `external_shape_changed` for the same ambiguous shape. The new regression is structural: it varies the wallet HTML shape and asserts parser refusal/acceptance without hard-coding any private wallet amount or duplicating tax arithmetic. No critical, high, or medium issues remain for W04.F08.

LIVIVA-CR-010 | INFO | W04.F09 plan safety-wording review passed
Reviewed the W04.F09 vault-plan wording change for safety drift. The update does not loosen the live AEAT mutation boundary: it keeps filing, payment, confirmation, represented-taxpayer data, and operator-choice form submissions prohibited, while documenting the already-implemented guarded `CarteraCuotas` read-query POST as the single named exception. This removes stale absolute no-form wording that conflicted with the current CLI and guard policy. No critical, high, or medium issues remain for W04.F09.

LIVIVA-CR-011 | INFO | W04.F10 Cl@ve diagnostic-command review passed
Reviewed the W04.F10 delta for live-auth safety, privacy, and centralisation. The change does not add a new AEAT navigation, click, timeout, retry, or session mutation. It only enriches timeout diagnostics and persisted diagnostic detail with exact local CLI commands for the existing phone-state report workflow. The allowed phone-state values are now centralized in the Cl@ve auth module and reused by application diagnostics, avoiding a duplicated constant list. The emitted commands contain only the diagnostic id and closed phone-state labels; they do not expose NIF/NIE, support numbers, verification codes, URLs with query values, or wallet amounts. No critical, high, or medium issues remain for W04.F10.

LIVIVA-CR-012 | INFO | W04.F12 secure-object repair inventory mitigation review passed
Reviewed the W04.F12 delta for destructive-repair safety, privacy, and IVA reconciliation coverage. The repair integrity backend no longer routes unreadable secure-object rows directly to quarantine; both repair integrity diagnostics and the error registry now steer operators toward read-only inventory first. The inventory report adds namespace-level role, IVA reconciliation relevance, compensation-history participation, destructive-repair risk, and operator notes without decrypting or printing secure-object payloads. The classification is conservative: wallet observations, filed-declaration history, local IVA compensation history, Modelo state, filing/submission receipts, ledger transactions, invoices, bucket event history, profile/live snapshots, and auth namespaces are preserve-first categories, while unknown namespaces explicitly say not to quarantine blindly. No critical or high issues remain in this mitigation. Residual risk remains medium/high at the broader WALLET-059 level because row-level bucket/profile attribution is not implemented yet and destructive quarantine must remain out of scope until that evidence classification is complete.

LIVIVA-CR-013 | INFO | W04.F14 secure-object row-context review passed
Reviewed the W04.F14 delta for row-level attribution, privacy, and false precision. The repair list surface now carries active-bucket context and object-key context on each row without decrypting secure-object payloads or attempting to reverse HMAC digests. Safe hints are limited to repository contracts and current-key digest matches: singleton catalogues, workflow state, active-bucket transaction catalogue, usage-ratio profile, user-profile value, and apoderado config. Taxpayer, expediente, snapshot, diagnostic, submission, and period-derived keys remain redacted and report `unrecoverable_hmac_digest`. This avoids leaking NIF/NIE, expediente ids, snapshot ids, wallet amounts, or filing content, and it avoids pretending old-key unreadable rows can always be mapped to a natural key. No critical, high, or medium issues remain for W04.F14.

LIVIVA-CR-014 | INFO | W04.F13 dependency-sync diagnostic review passed
Reviewed the W04.F13 delta for local-runtime reliability and command safety. The change does not touch AEAT navigation, authentication, filing, payment, or wallet mutation paths. It narrows the Windows dependency-sync warning by consulting `uv sync --frozen --dry-run` only when the timestamp heuristic says the venv marker is stale. If uv reports no package changes, the diagnostic reports `ok` and includes the dry-run detail; otherwise it keeps the existing `uv sync` next action. The subprocess invocation resolves the local `uv` executable and passes fixed arguments only. The SQL package export for `SecureObjectUnreadable` restores the public import expected by `SecureBoundRepository` and unblocks repair diagnostics. No critical, high, or medium issues remain for W04.F13.

LIVIVA-CR-015 | INFO | W04.F20 relational-database integrity review passed
Reviewed the W04.F20 delta for database-drift safety, mutation risk, privacy, and test quality. The repair pipeline now inspects relational SQL integrity before secure repository access can create missing ORM tables. The check uses a fresh engine rather than the auto-creating storage singleton, reports missing expected tables, runs SQLite integrity and foreign-key checks, and keeps output to table names, row ids, parent table names, and SQLite diagnostic messages instead of row payloads or taxpayer values. Review initially found that a missing SQLite file was treated as first-run bootstrap even when an active profile existed; that was fixed so active-profile database loss fails closed while true first-run bootstrap remains `ok`. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F20.

LIVIVA-CR-016 | INFO | W04.F21 auth-status config-domain scoping review passed
Reviewed the W04.F21 delta for config-domain separation, persistence safety, and test quality. `auth status` and `auth test` still consume the canonical operator projection for active-profile/auth readiness, but they now opt out of workspace counters and pending-obligation calculation. Overview-style surfaces keep the full projection by default. The regression seeds a real encrypted filing draft under a rotated ephemeral key and proves `inspect_operator_auth("certificate")` still reports the active profile/provider instead of failing on unrelated draft decryptability. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F21.

LIVIVA-CR-017 | INFO | W04.F22 corrupt-session suggestion review passed
Reviewed the W04.F22 delta for operator recovery accuracy. The `CorruptAuthSessionError` registry suggestion now points to `aeat config auth clear --sessions`, matching the localized error message and the live CLI clear surface, rather than routing the operator to `aeat config auth test --provider certificate`. The registry contract test confirms the suggestion parses against the Click/Typer CLI. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F22.

LIVIVA-CR-018 | INFO | W04.F23 profile-list manifest-scan review passed
Reviewed the W04.F23 delta for profile lifecycle safety, privacy, and test quality. The profile bucket scanner now skips malformed manifests on the live profile enumeration path, so a legacy bucket missing lifecycle `status` cannot abort `aeat config profile list` or be switched into by label. The CLI reports only `skipped_invalid_manifests` as a count; it does not print malformed manifest contents, bucket UUIDs, profile labels, tax ids, or key material for skipped buckets. Valid live profiles remain visible, and existing tombstone filtering remains covered. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F23.

LIVIVA-CR-019 | INFO | W04.F24 auth-diagnostics inventory privacy review passed
Reviewed the W04.F24 delta for diagnostic privacy and operator usefulness. `auth diagnostics list` now builds list rows with private auth-attempt context disabled and the text renderer omits identity kind, profile label/id, and alignment. JSON list output keeps those fields present for schema stability but emits empty/null values. `auth diagnostics show <id>` remains the explicit detail surface and still returns redacted fingerprints and operator report commands for a chosen diagnostic. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F24.

LIVIVA-CR-020 | INFO | W04.F25 repair-list active-bucket privacy review passed
Reviewed the W04.F25 delta for secure-object repair inventory privacy. The repair-list row-context path still uses the active bucket id internally to test whether an HMAC digest matches a repository-contract key, but rendered row context now reports only `active_profile` and placeholders such as `transaction-catalogue:<active-profile>`. This preserves the useful `active_key_digest_match`/repository-contract confidence signal without printing active profile bucket UUIDs or embedding them in object-key hints. Taxpayer, expediente, diagnostic, submission, period, and receipt natural keys remain unrecovered HMAC contexts. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F25.

LIVIVA-CR-021 | INFO | W04.F26 top-level repair profile privacy review passed
Reviewed the W04.F26 delta for the top-level config repair report. The report still carries profile readiness counts, auth provider, and repair checks, but text rendering now displays `active_profile` instead of the active bucket UUID, `profile.storage` summaries use the same copy-safe marker, and the JSON payload returned by `build_config_repair_report` redacts the setup report's `active_profile` field. The smoke checks exercised both text and JSON CLI output for UUID-shaped leaks. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F26.

LIVIVA-CR-022 | INFO | W04.F27 public CLI repair privacy contract review passed
Reviewed the W04.F27 test delta for whether the privacy guarantees are architectural or merely helper-level. The new CLI contract creates a real operator profile through `config profile create`, writes a real encrypted secure-object row under the active storage backend, and then exercises `config repair` plus `config repair list` in both text and JSON modes. Assertions check the public command output for UUID-shaped active profile leaks while preserving readiness counts, row context, and placeholder object-key hints. The repair attribution invariant was also tightened to check that actual payload strings are absent rather than rejecting the legitimate metadata field name `payload_disclosure`. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F27.

LIVIVA-CR-023 | INFO | W04.F28 attribution CLI metadata-only contract review passed
Reviewed the W04.F28 delta against the expanded W05 reconciliation scope. The new public CLI assertion creates a real profile, writes an unreadable wallet-observation row under a different real encryption key, and exercises `config repair integrity attribution` in both text and JSON modes. It proves the operator-facing attribution output keeps namespace role, owner semantics, active-profile context, and metadata-only disclosure while excluding payload text, taxpayer ids, period tokens, active profile UUIDs, and wallet amount text. No live AEAT operation was added or run. No critical, high, or medium issues remain for W04.F28.
