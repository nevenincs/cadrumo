---
tags:
  - '#audit'
  - '#integration-fixture-drift'
date: '2026-07-08'
modified: '2026-07-17'
related:
  - "[[2026-07-08-integration-fixture-drift-plan]]"
  - "[[2026-07-08-gate-drift-reconciliation-audit]]"
---

# `integration-fixture-drift` audit: `fixture-drift burndown close (79 to 21)`

## Scope

The follow-up campaign to the gate-drift-reconciliation close, which had recorded (correcting an earlier passphrase-gated hypothesis) that the integration suite carried 79 real failures even with `AEAT_SECRET_PASSPHRASE` set, driven by concurrent campaigns tightening contracts without sweeping every test fixture. This campaign drained the mechanically-fixable share and classified the remainder. All work was test-only; a progressively-dispatched sonnet fleet did the sweeps under coordinator review, with a background lock janitor and lock-aware committers absorbing the persistent stale-`index.lock` churn in this shared worktree. Baseline at start: 79 failed / 2588 passed.

## Findings

### burndown-result | high | integration failures cut from 79 to 21, entirely test-only

Net result: 79 failed -> 21 failed (2647 passed), 58 failures drained across 14 test-only commits, zero production files touched (verified by `git show --stat` across every commit). Phase P01-P03 (`402c677e5c`, `05b540a288`, `355b0a5a28`, `5bf3ff7377`) cleared the three named fixture-drift root causes - non-UUID `profile_id`/`_BUCKET_ID` constants against the uuid-profile-identities sweep, residual profile-create identity-flag drift, and the bucket-session-activation gap in isolated fixtures - taking the count 79 -> 51 (the residual histogram afterward showed uuid 0, session 0, identity 1). Phase P04 triaged the remaining 51 and fixed 30 more (10 commits: `5fa0870651` M303/profile applicability, `26523be588` M111 retencion seeding, `eebd861835` process-state wording, `4e5ac84557` bare-invocation redaction, `87687eef9b` 2025 minimo bindings, `52d8ddf9e7` boe-layout source rename, `f78807aecd` OFX fixture, `e46d9ec452` output-surface allowlist, `8cb017f450` M130->M100 oracle, `6f369552d5` stale NIF in calendar fixtures, `2ca721d14b` secret-leak gate), taking 51 -> 21.

### residual-21-are-production-drift | high | the remaining 21 need production edits or product decisions, not fixture fixes

The 21 survivors are all bucket C/D (production-drift or environment/decision), correctly out of a test-only sweep's scope. They group as: (1) MECHANICAL PRODUCTION HYGIENE - 4 dead command-citation strings in production (`test_suggestion_command_conformance`), 4 verbs missing from the `OperatorSurfaceContract` (`test_operator_surface_contract_drift`), 10 inline Typer help strings not sourced from the locale catalogue (`test_audit_remediation`), a locale string that lost the printed-number/export-ref diagnostic distinction (`test_modelo_casilla_canonical_ids`, 2), and `ModeloReconcileResult` carrying a bespoke `advisories` field instead of the shared notices channel in violation of the cli-notices-are-the-only-diagnostic-channel rule (`test_json_schema_conformance`, 2). (2) ARCHITECTURAL / BEHAVIOURAL - a `--help`/bare-invocation eager-import regression that pulls ~130 registry/workflow modules through `_active_sandbox_notice` (`test_lazy_command_tree`, 2); 4 legacy direct `get_calculation_revision`/`get_work_unit` selector calls needing a shared CLI-support wrapper (`test_architecture_boundaries`); and an M202 first-period readiness-vs-calculate inconsistency where `state_projection` was not updated to match the zero-default `_relation_prefill` applies (`test_modelo_202_required_binding_gate`). (3) PRODUCT-BEHAVIOUR DECISIONS - the calendar now deliberately DEGRADES (notice) instead of refusing on an unreadable local evidence store per an ADR/audit-cited change, but three tests still assert hard refusal (`test_overview_calendar_verb` unreadable-store x2 and the related strict-mode set); M349 readiness became structurally unreproducible after `ac11025c15` widened `applicable_entity_types` to include the attribution entity (`test_modelo_work_readiness_ux`); and the agent-eval lifecycle-contradiction golden lost its premise when M347 rev `2008-y-siguientes` gained real bindings via `8220834c35`, so readiness and verify now genuinely agree and the manufactured-contradiction fixture must be rebuilt (`test_lifecycle_contradiction_golden`, 3). One (`test_modelo_work_ux` decimal-override) is not yet root-caused. None is caused by this campaign.

### transient-parallel-flakes | low | a few failures are -n auto isolation flakes, not real

The triage confirmed the certificate cluster (`test_certificate` register/select/secret-set x4, `test_apoderado` status) and two other node ids are green standalone and fail only under `-n auto` worker isolation - pre-existing parallel-run flakes per the aeat-local-execution re-run-sequentially guidance, not real regressions. They are counted in the 21 but are not actionable as failures; a sequential re-run is the confirmation path.

## Recommendations

Route the 21 by class, honouring the operator model policy (mechanical to sonnet, architectural to opus, ADR-level only with operator approval). The MECHANICAL PRODUCTION HYGIENE group (dead citations, missing contract verbs, inline-help-to-locale, the locale diagnostic string, the bespoke-advisories-to-notices move) is low-risk, rule-aligned, and can be swept by sonnet with production write scope - it was excluded here only because the triage was capped test-only. The ARCHITECTURAL group (eager-import lift, legacy-selector wrapper, M202 readiness/calculate reconciliation) is opus-appropriate and each touches a load-bearing surface. The PRODUCT-BEHAVIOUR DECISIONS (calendar degrade-vs-refuse test expectations, M349 readiness semantics, agent-eval golden rebuild) are ADR-level: the intended behaviour is a product call, so they require operator approval before a fixer changes either the test's asserted behaviour or the production contract. Re-run the certificate/apoderado cluster sequentially to confirm the parallel-flake classification before counting them against any gate. The registry-authority core remained verified-clean throughout; this campaign changed only test fixtures and expectations.

## Residual-21 remediation close (2026-07-09)

The operator authorised driving all three residual groups ("all of it"), including production edits, with the standing routing policy (sonnet mechanical, opus architectural, operator ruling on ADR-level product-behaviour). Three agents ran with disjoint file ownership to avoid collision.

### residual-remediation-result | high | arc 79 to 21 to 14; campaign's own attributable scope fully drained

Final integration state: 14 failed / 2654 passed (from the 21/2647 residual, and 79 at campaign start). Group 1 MECHANICAL HYGIENE (sonnet) landed six commits - `219b468573` + `634c2c9a8c` (dead command citations), `8897c6ee00` (four missing OperatorSurfaceContract verbs), `b46e1ecd33` (inline Typer help to locale), `1791259c3e` (split the noncanonical-casilla locale key into ambiguous vs single-target to restore the printed-number/export-ref distinction), `048268e820` (removed the bespoke `advisories` field from `ModeloReconcileResult`, folding it onto the shared Notice channel). Group 2 ARCHITECTURAL (opus; died once on a transient 529 Overloaded and was resumed on its intact working tree) landed three commits - `19bb5540ad` (legacy-selector shared `load_calculation_revision`/`load_work_unit` facades in `_modelo_cli_support.py` + the six review-package help-to-locale sites), `4ad81c6235` (the eager-import lift, which the audit had under-scoped: the leak chained `_emit_envelope` -> `_active_sandbox_notice` -> `bucket_maintenance`/`workflow` facade `__init__`s pulling ~130 registry modules, so the real fix read the plaintext manifest via light storage primitives and added a light `SANDBOX_LABEL_PREFIX` to `core.external_constants`, touching `_common.py`/`bucket_maintenance/_sandbox.py`/`external_constants.py` beyond the scoped set - peer-clean and regression-swept), `a8e7d605c7` (M202 readiness/calculate reconciled by extracting the calculate-side zero-default predicate `relation_prefill_period_zero_default_binding_ids` and consuming it in `state_projection`, so the two agree by construction). `lint-imports` stayed 5 kept / 0 broken throughout. Group 3 PRODUCT-BEHAVIOUR (sonnet) landed `1d196e6de6` - calendar tests updated to assert the deliberate degrade-with-notice behaviour (verified against `4adf391107`, including the intentional all-profiles notice-drop), and M349 readiness updated to the widened attribution-entity applicability (verified against `ac11025c15`).

### final-14-classification | medium | the 14 survivors are flakes, one flagged decision, and unrelated peer churn - none is unaddressed campaign scope

The 14 remaining failures decompose as: (1) FIVE confirmed `-n auto` PARALLEL-FLAKES - `test_certificate` (4) + `test_apoderado` (1) pass 14/14 when re-run sequentially (`-n0`), per the aeat-local-execution re-run-sequentially guidance; not real. (2) THREE agent-eval golden (`test_lifecycle_contradiction_golden`) - the one flagged product decision (see below); not fixed pending an operator ruling. (3) ~SIX unrelated PEER-CHURN / other-campaign failures that appeared or persisted as HEAD moved under concurrent campaigns - `test_risk_table_parity` (last touched by the mcp risk-table refactor `057744c473`), `test_ledger_validation_paths` (`22c074048e`), `test_modelo_work_ux` decimal-override (never root-caused, pre-dates this tranche), `test_modelo_requires_data_inventory`, `test_ledger_ux_defect_cluster` - none attributable to these campaigns. Separately, the arch agent honestly flagged one pre-existing `test_binding_prefill.py::test_modelo_390_prefill...` failure (a bienes-de-inversion registry-fact gap owned by another campaign), confirmed identical with its changes reverted. Net: the integration-fixture-drift campaign's own attributable scope is fully drained; what remains is flakes, one decision, and other campaigns' drift.

### agent-eval-golden-decision-required | high | the lifecycle-contradiction golden premise is permanently closed; needs an operator ruling

`test_lifecycle_contradiction_golden.py` (3 tests) manufactures a readiness-vs-verify contradiction that depends on a modelo revision having ZERO registry bindings (the one condition under which readiness can report ready while a required-but-unbound manual casilla leaves verify legitimately blocking). Commit `8220834c35` gave M347 `2008-y-siguientes` two real invoice-sourced bindings, permanently closing the contradiction for M347 - readiness now correctly reports not-ready too. A full registry search (TOML-parsed) for a replacement modelo/revision with manual-required casillas AND zero bindings found none usable via the CLI without violating the golden module's own documented design constraints (no clock dependency, no mocks): M145/M721 are CLI-unsupported for work-unit creation, M182/184 are already fully bound, and the next-best M180 needs four quarterly M115 filings through a deadline-gated `work file` (reintroducing the clock dependency the module deliberately avoids). Three options for the operator: (a) RETIRE/SUPERSEDE the three golden tests with a documented rationale (the contradiction is no longer CLI-reproducible after registry grounding) - the honest minimal move; (b) REWRITE them at the contradiction-DIMENSION level, unit-testing the scoring logic with a directly-constructed readiness/verify disagreement (needs relaxing the module's self-imposed no-mocks constraint); (c) pursue the M180 + 4x M115 route and accept the clock dependency. Recommendation: (a) or (b) - (a) if the CLI-observable scenario is considered genuinely obsolete, (b) if the contradiction-dimension logic still warrants direct coverage. No production or test change was made here pending the ruling.

### agent-eval-golden-resolved | high | resolved via a fable-advised hybrid restructure; module green, coverage preserved

The operator routed the ruling to a fable advisory agent, which corrected this audit's framing of option (b): it does NOT require relaxing the no-mocks constraint, because `check_contradiction_scenario` (`agent/eval/_runner.py:598`) is a PURE function over `readiness_ready: bool` / `blocking_step_refused: bool` / `trajectory`, and the module already tests it with directly-constructed booleans in two passing structural tests ("pure structural proof, no live dispatch needed" per their own docstrings). Fable recommended, and an opus executor implemented, a hybrid of (a)+(b) in commit `7415a5e4fb` (single test file, test-only): the "genuinely disagree" test was retired and replaced by `test_registry_grounding_closed_the_readiness_verify_contradiction` - a closure-regression test that drives the real CLI path and asserts readiness now reports not-ready for the casilla-empty M347 draft, citing `8220834c35` and serving as the reinstatement tripwire (if M347 is ever unbound, readiness reports ready again and this fails loudly); the two trajectory tests were rewritten as pure structural checker calls (booleans injected, every trajectory value and assertion including the "must stop and report" text preserved); the live-manifest test and the two pre-existing structural anti-tautology tests were kept unchanged. Together this restores full four-quadrant coverage of the contradiction dimension (pass / retry-fail / signals-agree-fail / never-reaches-boundary-fail) with zero clock dependency and no mocks. The dead `_dispatch_verify_refusal` helper was removed and the module docstring updated. Module state at HEAD: 6 passed. With this, the campaign's own attributable integration failures are zero - the remaining residual is the five confirmed parallel-flakes and the ~six unrelated other-campaign drift, neither in this campaign's scope. Fable also noted a codify candidate for a future pass: a registry-build gate asserting a CLI-supported revision with required manual casillas must declare at least one binding, which would close the latent contradiction class structurally.
