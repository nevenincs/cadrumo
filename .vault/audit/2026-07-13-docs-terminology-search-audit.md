---
tags:
  - '#audit'
  - '#docs-terminology-search'
date: '2026-07-13'
modified: '2026-07-13'
related:
  - "[[2026-07-13-docs-terminology-search-plan]]"
  - "[[2026-07-13-docs-terminology-search-adr]]"
---

# `docs-terminology-search` audit: `campaign close honesty review`

Fresh-context honesty review of the docs-terminology-search next-wave
campaign close (per the campaign-close honesty-review rule), performed by an
independent opus reviewer over the ADR (including Update 1), the plan, all
ten exec records, and the full commit range. Initial verdict: HOLD, three
SHARP findings; all three were actioned the same session, and the remediated
gate CHANGED THE CAMPAIGN'S HEADLINE DECISION. The reviewer re-verified the
remediation commit and LIFTED THE HOLD: PASS on all three SHARP findings,
with the service-pending NIT-1 checks disclosed follow-up, not a blocker.

### SHARP-1 (actioned): the rung-2 miss-rate gate was tautological

Every held-out case was a shipped vocabulary query whose expected id
included the concept's own seeded card, and the CI test additionally pinned
`miss_count == 0` — a measurement that could not register a miss regardless
of retrieval quality. Remediation: the held-out corpus now partitions into
`vocabulary` cases (wrangling-regression signal; a miss is a curated-surface
drop) and `out_of_sample` cases (genuine free-text palette phrasings,
gate-enforced NOT to be vocabulary rows), evaluated by lexical containment
over the compiled tiers, mirroring the shipped palette's matching. Twelve
out-of-sample cases were authored with independent expectations.

### SHARP-2 (actioned): enforced threshold was double the ratified one

`DEFAULT_RUNG2_MISS_RATE_THRESHOLD` was 0.20 while the ADR D3 ratified 0.10;
the committed reports carried 0.10 only because it was passed ad hoc. The
module default is now the ratified 0.10 and the gate asserts the equality
explicitly.

### SHARP-3 (actioned): the top-five bound was not implemented

Hits were counted anywhere in a mapping's target list (observed depth up to
13). The evaluator now bounds both case kinds to the top five targets per
the ADR D3 definition.

### The remediated gate fires: IMPLEMENT RUNG 2

Post-remediation measurement over the widened mapping: 32 cases, 26 hits,
miss-rate 0.1875 at threshold 0.10 → decision `implement-rung-2` (committed
as the canonical report, regenerated by the new
`python -m dev.docs.terminology.miss_rate report` writer, closing NIT-2).
Six misses, all out-of-sample: five phrasings the closed vocabulary cannot
reach (retenciones del alquiler de la oficina; impuesto de sociedades a
pagar este ano; factura de la luz deducible; dar de alta un autonomo en
hacienda; libro de ingresos y gastos) and one lexical-tier target mismatch
(vender a francia sin iva). Caveat recorded in the report: the offline
evaluation models the precompiled tiers only and understates the shipped
full-text tier, so the measured rate is an upper bound on shipped misses.

Disposition: per the ADR D3, rung 2 (the ~1-3 MB int8 static term-embedding
matrix, client-side cosine over the closed vocabulary plus query text) is
now COMMITTED FOLLOW-UP SCOPE. It is formally deferred out of this campaign
into its own pipeline (research → ADR → plan) under a new feature; the
2026-06-10 architecture ADR's rung-2 scoping and the licence-clean shipping
rule bound that work. ADR Update 2 records the fired gate.

### NIT-1 (open, blocked upstream)

The golden-query live suite and one incremental reindex were red at close
because the shared RAG service was down for team maintenance. Follow-up on
2026-07-14 root-caused the blocker upstream: after the vaultspec stack
update, the SERVICE-SIDE indexer never executes the document-preprocess
rules at all — a full reindex with `VAULTSPEC_RAG_PREPROCESS_ENABLED=1` set
at service start and a valid four-rule config produced zero preprocess
invocations in `~/.vaultspec-rag/service.log`, leaving the rule-fed binary
corpus (Diseños workbooks, PDFs) absent from the dev index, while per-file
`preprocess run-one` still works end to end (our adapter and rules are
proven). A reindex-throughput collapse was also observed (576/22216
sections in 77 minutes, job eb3d40bc). Both reported to the vaultspec-rag
team alongside the uv-tool torch regressions. Re-run
`uvx vaultspec-rag index --type code --port 8766` and
`uv run --no-sync pytest dev/docs/preprocess/tests -m integration` once the
team confirms service-side rule execution.

### NIT-2 (actioned)

Report artifacts were not reproducible from committed code; the `miss_rate`
developer CLI is now the committed writer.

### Reviewer confirmations (no action needed)

The sidecar-retirement rescission, the five adjudicated sweep-target losses,
the twenty concept promotions (prose, legal grounding, and the
taxpayer-facing rule all verified), the retargeted golden and index-coverage
gates, the rag-dev-only gate, and plan-closure exec-record completeness all
held up under independent inspection.

### Codify candidates

1. Verify what a derived-data surface FEEDS before scheduling its
   retirement (the sidecar-rescission lesson, carried from W02.S07).
2. A measurement gate must be able to fail: any adjudicating metric needs at
   least one case class whose failure is reachable, and CI must pin the
   gate's CONSISTENCY, never its VERDICT (this review's SHARP-1 lesson).
