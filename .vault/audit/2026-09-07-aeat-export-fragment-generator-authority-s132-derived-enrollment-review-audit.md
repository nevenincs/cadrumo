---
tags:
  - '#audit'
  - '#aeat-export-fragment-generator-authority'
date: '2026-09-07'
modified: '2026-09-07'
body_schema: 'body-v2'
body_hash: 'sha256:c7b02938dfec909a3022c4d3e3c272763d3192c72e341621ef20c06293ead56e'
related:
  - "[[2026-08-10-aeat-export-fragment-generator-authority-plan]]"
  - "[[2026-09-07-registry-temporal-coverage-enrolment-versus-declared-projection-research]]"
---

# `aeat-export-fragment-generator-authority` audit: `S132 derived generated-tree enrollment review`

## Scope

Reviewed the S132 change against the accepted completeness denominator, narrow-mechanism widening rule, generated-provenance contract, canonical law-selection coordinate derivation, filing-export residue authority, strict typing, and shared-worktree boundaries. The review distinguishes automatic enrollment from the broader filing-completeness predicate: this gate reproduces every published generated tree, while the existing filing-export classification retains an explicit owner, reconsideration condition, and detail for every filing revision that cannot materialize conformance proof.

## Findings

No HIGH or MEDIUM findings remain.

The deleted table duplicated identities already attested by each generated package. Its replacement walks validated modelo and revision declarations, admits only the canonical internal provenance member, validates manifest identity against its owning revision, and derives a real selectable coordinate through the registry temporal authority. A new publication therefore enrolls itself without changing a second list. The independent committed-versus-enrolled equality assertion prevents a future projection filter from hiding a manifest.

The change adds no casts, `Any`, type ignores, suppressions, copied selector, relaxed matcher, fixed corpus count, or modelo-specific enrollment branch. The one downstream M303 helper now identifies its generated authority by revision source and design epoch rather than restating a representative period that enrollment no longer owns.

The full reproduction gate exposes 27 existing artifact drifts after reaching the dynamic population. All include the internal provenance member; Modelo 347 additionally reports the record drift already declared in the pipeline disposition ledger. These failures are not introduced by enrollment derivation, but they prevent describing the whole generated corpus as reproduced at this HEAD. The canonical filing-export residue projection independently accounts for all 69 currently filing-graded revisions with non-empty ownership and reconsideration facts. Its higher-level two-channel test presently fails earlier in unrelated concurrent `snapshot_ref` construction work.

## Recommendations

Close S132 after preserving the full-gate failures as measured residue rather than weakening comparison semantics. Keep S32 blocked on its declared S84 dependency; when that temporal step opens, its census still needs the general yearless in-file enrollment-list detector and mutation fixtures even though this particular offending list has been deleted.
