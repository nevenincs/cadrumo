---
tags:
  - '#audit'
  - '#cli-authority-verb-conformance'
date: '2026-07-15'
modified: '2026-07-15'
related:
  - "[[2026-07-15-cli-authority-verb-conformance-plan]]"
---

# `cli-authority-verb-conformance` audit: `Wave W01 atomic Step reviews`

## Scope

Reviewed commit `36ff8f1850bac3f0335c78f73ab8499f3bbda035` against the accepted import-linter prerequisite in the ADR, the implementation grounding in the research and reference, plan Step `W01.P01.S01`, and its Step Record. The review covered safety, exact intent, tracked root-authority uniqueness, compatibility behavior, and attribution of every immediately observable remaining contract failure.

The review was grounded with `vaultspec-rag search "import-linter root package cadrumo architecture contracts" --type code`, then confirmed with commit-scoped `git show`, `git grep`, `git ls-tree`, package-tree probes, targeted `rg`, and fresh uncached import-linter runs.

## Findings

S01 has no actionable finding. Verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW issue was identified, and S02 is not blocked.

The commit changes only `.importlinter:2` from the nonexistent `aeat` package root to the real `cadrumo` package root and adds its Step Record. The commit tree contains `src/cadrumo/__init__.py`, contains no `src/aeat/__init__.py`, and contains exactly one tracked `.importlinter`. Packaging-smoke copies under `var` are ignored generated artifacts, not alternate authorities. The change adds no alias, fallback, compatibility parser, or second declaration.

An uncached review run constructed the live 3,421-file graph. The focused registry, domain-to-application, and domain-to-adapters contracts remained kept; the core contract reported only the helper-mediated path from `cadrumo.core.tests.test_isolation_fixture_state_root_coverage` through `cadrumo.tests.secure_sql`. The complete contract run stopped on the two expected unmatched entries for `cadrumo.application.live._censo` and `cadrumo.application.user_profile._censo_sync`. At the reviewed commit, the first source module is absent and the second has no adapter import, so those entries are correctly assigned to S02 and S03; the exact test-helper route is correctly assigned to S04.

The shared worktree advanced after the S01 commit and carried peer-owned source edits during review, so the current dependency count was 16,152 rather than the executor-time 16,153 recorded for S01. The package-root behavior, file count, contract verdicts, and failure attribution remained unchanged; this is worktree-context drift, not an S01 defect.

S06 has no actionable finding. Verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW issue was identified, and S07 is not blocked.

Commit `2a1eb45b1b0bcd72eba3ef8a9a5f2deb0137e2db` removes only the concrete `TransactionCatalogueRepository` adapter import and the optional ambient-construction branch from `aggregate_irnr_income_ledger_from_repositories`, makes `TransactionCatalogueRepositoryProtocol` required, preserves the bucket-identity guard and aggregation behavior, and adds the S06 Step Record. The resulting module imports inward through core and domain ports and contains no concrete adapter reference, fallback, alias, compatibility default, or second IRNR repository constructor.

The review was grounded with `vaultspec-rag search "IRNR income ledger injected transaction repository remove ambient fallback" --type code`. Exact commit-scoped caller enumeration found one production call in `LedgerIrnrIncomeAggregationSourceResolver` and two direct real-repository test calls; all three pass the repository argument. Structural substitutability is intact: both the concrete encrypted repository and the source mesh's `_MemoizedTransactionCatalogueRepository` implement the Protocol members consumed by the function (`bucket_id`, `load`, and `partition_by_date_range`). Focused Ruff passed, and the real encrypted-repository M210 suite passed two tests.

The only remaining IRNR composition door is the top-level-exported `LedgerIrnrIncomeAggregationSourceResolver` constructor, which still accepts `TransactionCatalogueRepositoryProtocol | None` and can forward `None` in ledger mode. Exact construction search found one production construction, and it already injects the source mesh's shared memoized repository; no alternate production construction or duplicate M210 aggregator exists. S07 therefore owns the complete remaining public-constructor hardening. Optional repository construction in other ledger families is not substitutable with the selected-code M210 IRNR authority and is outside this accepted defect's scope.

S09 has no actionable finding. Verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW issue was identified, and S10 is not blocked.

Commit `5695c6a0335ce7f63d5827c2ee56fc63b907e8a4` changes only the concrete invoice-repository import, four invoice-repository annotations, the directly affected public-argument documentation, and the S09 Step Record. Exact commit review found no changed condition, call, assignment, construction, exception path, or persistence behavior. The current verification module contains no `InvoiceCatalogueRepository(...)` construction, concrete invoice-repository annotation, or import from `adapters.persistence.profile.invoices`; every verification boundary now names the public `InvoiceCatalogueRepositoryProtocol` port.

The review was grounded with `vaultspec-rag search "modelo verification invoice repository protocol OSS IOSS resolver" --type code`, which returned the legacy Modelo 369 resolver call and all four Protocol-typed boundaries. Exact caller and symbol searches confirmed that verification only forwards the injected value through its finding collectors into `OssIossLedgerSourceResolver`. The runtime-checkable public Protocol declares `bucket_id`, `exists`, `load`, and `save`, and the concrete encrypted `InvoiceCatalogueRepository` implements those same members with compatible signatures, so existing concrete callers remain structurally valid without runtime coercion or a compatibility wrapper. Focused Ruff passed, and the real encrypted-storage dormant Modelo 369 suite passed five tests.

The only retained OSS/IOSS concrete composition edge is `oss_ioss_candidates_from_repositories` in `src/cadrumo/application/aggregation/_oss_ioss.py`, where one fallback constructs `InvoiceCatalogueRepository` when no repository is injected. Its helper, aggregate wrapper, and resolver constructor still carry concrete annotations but converge on that single fallback; production calculation and legacy verification both feed the same resolver path. S10 therefore owns the complete receiving-annotation widening while deliberately retaining that sole default construction authority.

S02 has no actionable finding. Verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW issue was identified, and S03 is not blocked.

Commit `69469505fe45ae786c01f7bcf0a9a8ae4b711ca3` removes exactly the single `cadrumo.application.live._censo -> cadrumo.adapters.**` layered-contract waiver and adds only the S02 Step Record, its plan closure, and regenerated feature-index links. The `.importlinter` diff changes no contract declaration, wildcard, neighboring ignore, root package, or `unmatched_ignore_imports_alerting = error` setting. Commit-scoped tree and symbol searches found no tracked `src/cadrumo/application/live/_censo.py`, import edge, lazy registration, `import_module`, `__import__`, or `find_spec` caller. The Spanish-stem rule example and UTF-8 debt-ratchet string naming the absent path are declarative text, not runtime or import-graph consumers.

The review was grounded with `vaultspec-rag search "stale live censo import-linter ignore no source module" --type code`, followed by commit-scoped `git show`, `git grep`, `git ls-tree`, `git ls-files`, `fd`, and targeted `rg`. A fresh full uncached import-linter invocation no longer reports the removed S02 waiver and stops only on the unmatched `cadrumo.application.user_profile._censo_sync -> cadrumo.adapters.**` entry owned by S03. A focused uncached four-contract run analyzed 3,421 files and 16,152 dependencies: the registry and both domain contracts were kept, while the core contract was broken only by `core.tests.test_isolation_fixture_state_root_coverage -> tests.secure_sql -> adapters`, the exact helper-mediated route owned by S04.

S03 has no actionable finding. Verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW issue was identified, and S04 is not blocked.

Commit `5dd41466c9e5018ed8736f1efc3ebedd10ccd41b` removes exactly the single `cadrumo.application.user_profile._censo_sync -> cadrumo.adapters.**` layered-contract waiver and adds only the S03 Step Record, its plan closure, and regenerated feature-index links. The `.importlinter` diff changes no contract declaration, wildcard, neighboring ignore, root package, or `unmatched_ignore_imports_alerting = error` setting. The tracked `_censo_sync.py` module contains no direct adapter import, `importlib`, `__import__`, `find_spec`, or string-based adapter target. Its PEP 562 facade branch loads `_censo_sync` itself, not an adapter; its function-local `_repository` import remains an application-owned edge, and that repository module owns the concrete storage construction boundary under its separate, still-matching ledger pin.

The review was grounded with `vaultspec-rag search "user profile censo sync stale adapter import-linter ignore" --type code`, followed by commit-scoped `git show`, `git ls-files`, `fd`, and targeted import, dynamic-import, facade, caller, and policy searches. The adapter-named `_censo_sync` rows in `test_lazy_import_policy.py` are static historical allowlist-superset data: the real-AST gate excludes the facade's PEP 562 branch and asserts that live sites remain a subset, without importing or dispatching those adapter targets. Three focused policy-gate tests passed. A fresh full uncached import-linter run analyzed 3,421 files and 16,152 dependencies: the registry contract, both domain contracts, and layered contract were kept; the sole broken contract was `Core must not import outer layers`, reporting only `core.tests.test_isolation_fixture_state_root_coverage -> tests.secure_sql -> adapters.persistence.storage{,.bucket}`. That exact helper-mediated route, and no censo-sync edge, is correctly and exclusively attributed to S04.

S04 has no actionable finding. Verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW issue was identified, and S05 is not blocked.

The S04 diff adds exactly one import-linter declaration: `cadrumo.core.tests.test_isolation_fixture_state_root_coverage -> cadrumo.tests.secure_sql` in the `core-not-outer` reporting contract. It does not change the layered contract, the forbidden source or target hierarchy, unmatched-ignore error policy, any production-core exemption, or either test implementation. Exact declaration search found the new route once. The only related existing declarations are the test-only `core.tests.** -> adapters.**` carveouts in both architecture contracts and the helper's own `cadrumo.tests.secure_sql -> cadrumo.adapters.**` layered-contract edge.

The review was grounded with `vaultspec-rag search "core isolation state root real secure sql helper import-linter reporting route" --type code`, the accepted test-carveout ADR, and targeted import, caller, contract, and duplicate-declaration searches. The core integration test directly imports `isolated_cli_backend`; loading that cross-cutting test helper imports genuine persistence-storage adapters and its fixture opens a real file-backed isolated storage route. Production core does not import the helper. Because `cadrumo.tests` is outside the declared layer hierarchy, exempting the direct test-to-helper edge is necessary to cut the reported transitive chain and cannot be replaced by a production waiver or a second layered-contract entry. The focused integration file passed two tests, and a complete fresh uncached graph analyzed 3,421 files and 16,152 dependencies with all five contracts kept and no unmatched ignore.

Independent S04 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S05 remains unblocked.

Commit `0f4d813f26a6a68bb03346bb4a90ba43e62b59a5` adds the exact helper route once, at the `core-not-outer` contract's reporting ledger. It adds no corresponding layered-contract route, changes no source/forbidden hierarchy or unmatched-ignore policy, and creates no production waiver. Repository-wide declaration search finds only that core-test-to-helper entry and the pre-existing `cadrumo.tests.secure_sql -> cadrumo.adapters.**` helper-to-adapter entry in the layered contract. Production core contains no secure-SQL-helper import; the only core importer is `core.tests.test_isolation_fixture_state_root_coverage`, which directly imports the shared fixture.

The mandatory semantic query returned the generic architecture-reporting implementation rather than the helper route itself, so exact source evidence carried the review. The imported helper directly loads real persistence-storage, bucket, master-key, runtime, engine, and secure-object adapters. Its integration test dynamically iterates the live `_STATE_ROOT_DERIVED_DIRS` taxonomy and asserts every setting relocates under the per-test root; it contains no fake, stub, mock, patch, monkeypatch, skip, xfail, or mirrored business logic. A fresh `pytest -q -n0 -m integration` run passed both tests in 1.80 seconds. A fresh `lint-imports --no-cache` run analyzed 3,421 files and 16,152 dependencies, kept all five contracts, broke none, and—because every contract retains error-level unmatched-ignore alerting—proved no unmatched ignore remains.

S05 has no actionable finding. Verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW issue was identified, and the narrowed import ledger remains trustworthy.

The S05 diff replaces exactly `cadrumo.application.diagnostics_run_health -> cadrumo.adapters.**` with `cadrumo.application.diagnostics_run_health -> cadrumo.adapters.outbound.llm`. It changes no importer, neighboring ignore, contract hierarchy, unmatched-ignore policy, source module, test, or caller. The accepted target is a strict descendant of the removed wildcard and remains the module's sole live adapter dependency.

The review was grounded with `vaultspec-rag search "diagnostics run health LLM adapter import-linter narrow wildcard" --type code`, exact caller and facade searches, and an AST walk of `diagnostics_run_health`. The AST reports one adapter import—the public outbound LLM facade supplying `LLMRunRecord` and `LLMRunTelemetryRecorder`—plus the inward application-auth import. It reports no `import_module`, `__import__`, `find_spec`, or string-dispatched adapter route. All application and lazy CLI consumers call the diagnostics API rather than constructing an alternate adapter path. The complete real encrypted-storage test module passed 24 tests, and a fresh uncached graph analyzed 3,421 files and 16,152 dependencies with all five contracts kept and no unmatched ignore.

Independent S05 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S07/P02 remain unblocked.

Commit `bf4e89b7241e69d47f55cf3d94c40460f3748c82` replaces the sole diagnostics run-health ledger row's `cadrumo.adapters.**` target with `cadrumo.adapters.outbound.llm`, a strict descendant of the former wildcard. It adds no second or broader source row and changes no contract, layer order, neighboring waiver, or error-level unmatched-ignore setting. Commit-scoped AST inspection finds exactly one adapter import, the public LLM facade's real `LLMRunRecord` and `LLMRunTelemetryRecorder` exports; the recorder is constructed and reads persisted records at runtime, so this is a live boundary rather than type-only debt. The module has no `import_module`, `__import__`, `find_spec`, importlib call, or non-docstring adapter target string.

The mandatory semantic query returned generic import-ledger, architecture-reporting, and packaging context rather than the diagnostics route, so exact commit-tree evidence carried the review. Production consumers consist of the diagnostics telemetry service and five lazy CLI handlers; each imports and calls the application API without another adapter path. The separate application-auth probe remains an application-to-application dependency whose adapter pins are independently owned. The focused test module uses the real encrypted isolated storage fixture and real telemetry recorder across 24 substantive behavior cases, with no fake, stub, mock, patch, monkeypatch, skip, xfail, or mirrored implementation. A fresh run passed all 24 tests in 7.95 seconds, and a fresh uncached graph analyzed 3,421 files and 16,152 dependencies with all five contracts kept and none broken; retained error-level unmatched-ignore enforcement proves the narrowed row remains live and matched. A concurrent docstring-only diagnostics edit was excluded from commit-scoped conclusions and did not alter imports, tests, configuration, or the reproduced graph.

Independent S07 review verdict: **PASS WITH LOW**. No CRITICAL, HIGH, or MEDIUM finding was identified; S08 is not blocked.

Commit `7033545be592ea9b845e19c9242870e76d4236fa` changes the public `LedgerIrnrIncomeAggregationSourceResolver` constructor from `TransactionCatalogueRepositoryProtocol | None = None` to required `TransactionCatalogueRepositoryProtocol`. The underlying `aggregate_irnr_income_ledger_from_repositories` boundary is already required, so the resolver can no longer forward `None`, construct a fallback, or preserve a compatibility default. The commit changes no sibling resolver default, overload, late construction route, adapter ignore, or production composition service.

The mandatory semantic query found the IRNR resolver, its already-required repository loader, and sibling defaults. Commit-tree construction search found one production instantiation in `_resolve_bucket_source_mesh`; it passes the single `_MemoizedTransactionCatalogueRepository` created around the resolved concrete repository. The only other class references are its public aggregation facade export and the resolver-enrollment declaration; no direct test or alternate production constructor exists. The runtime-checkable Protocol requires `bucket_id`, `exists`, `load`, `load_for_date_range`, `partition_by_date_range`, and `save`, and both the concrete encrypted repository and memoized wrapper implement that complete shape. Optional defaults on the IVA, Renta expense/income, impatriado, and gasto siblings remain intentionally unchanged and are not substitutable IRNR compatibility doors.

Focused Ruff passed. The exact evidence set—two real-storage M210 tests, five memoized-repository tests, and four resolver-enrollment tests—passed `11` tests in `44.22s`. Commit hygiene passed, and the sole production source path in the commit is `_modelo_bindings.py`. Current peer WIP in `_calculation_actions.py` remains a separate `21`-line worktree diff; review used the committed tree for construction evidence and did not modify or stage that file.

### s07-step-record-annotations | low | Scaffold annotations remain in the S07 execution record

The feature-scoped annotations check reports exactly three retained HTML scaffold blocks in `2026-07-15-cli-authority-verb-conformance-W01-P01-S07.md`. They do not change runtime behavior or invalidate the authored outcome, so S08 remains non-blocked, but the owning executor must remove them before Phase closure. The separate single annotation warning on the plan predates S07 and is not attributed to this commit.

Resolution: **CLOSED** by commit `e17890366ed8f95193da19c9efdbccddd8de7ee3`. Commit comparison proves it removed exactly the three S07 HTML scaffold blocks and no evidence prose or metadata: `step_id: S07`, the parent-plan relationship, scope, Description, Outcome, Notes, the recorded 11-test evidence, and the S08 boundary remain intact. The feature annotation and Markdown checks both report zero diagnostics. No runtime, test, plan, index, or other execution artifact changed in the remediation, so the LOW finding is fully resolved.

Independent S08 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S10 is not blocked.

Commit `1896d2db34f8b7b02ca7f8cb297f42744b6c36a7` adds one focused public-calculation scenario, its S08 execution record, the canonical plan closure, and the regenerated feature-index links. The test creates an injected SQLite engine through production `create_engine_from_settings`, materialises the real ORM schema, and binds the production `SecureObjectRepository` with the live namespace registry and active secure bucket session. The ambient runtime uses a separate database path for the same bucket id. Only the injected concrete `TransactionCatalogueRepository` receives the qualifying transaction, while the ambient transaction catalogue is asserted absent before and after calculation; both engines are closed through their owning lifecycle or an unconditional `finally` disposal.

The review was grounded with `vaultspec-rag search "W01 P01 S08 M210 injected transaction repository distinct encrypted object stores ambient default construction mutation sensitive public calculation source mesh"`, followed by commit-scoped inspection and exact resolver, aggregator, construction, test, and prohibited-pattern searches. There is one production `LedgerIrnrIncomeAggregationSourceResolver` authority and one repository-loading `aggregate_irnr_income_ledger_from_repositories` path. `_resolve_bucket_source_mesh` is the sole choice point between the injected repository and ambient construction, and it wraps the selected repository once for all ledger resolvers. No duplicate M210 aggregator, alternate production constructor, or competing distinct-store M210 scenario exists.

The scenario is mutation-sensitive: if the source mesh discards the injected repository and takes its ambient fallback, the empty ambient catalogue cannot produce the asserted `1234.56` gross value, injected transaction id, or `transaction:<id>` provenance. It exercises production transaction creation and the public calculation entrypoint without reproducing classification or aggregation logic. The target contains no fake, mock, stub, monkeypatch, test patch, skip, or xfail; its `ManualLedgerTransactionPatch` references are calls to the production domain mutation command. The S08 execution record accurately describes the store topology, RAG recovery boundary, verification results, and scoped worktree safety. Independent Ruff passed, and the exact new test passed once in `47.60s` against the real encrypted repositories.

Independent S10 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S11 is not blocked.

Commit `922fd4eb66487b291726616416045fbd73620b7c` changes only the public invoice-repository port import, exactly three injected repository annotations, their topology-focused documentation, the S10 execution record, the canonical plan closure, and regenerated feature-index links. The sole concrete `InvoiceCatalogueRepository(bucket_id=bucket_id)` construction remains byte-for-byte unchanged in the explicit `None` branch of `oss_ioss_candidates_from_repositories`; the adapter import and exact `_oss_ioss -> adapters.**` import-linter ledger row are retained. No condition, call, assignment, exception path, aggregation step, export, or persistence behavior changed.

The review was grounded with `vaultspec-rag search "OSS IOSS injected InvoiceCatalogueRepositoryProtocol sole default construction duplicate aggregator authority overlap"`, followed by exact commit, symbol, caller, construction, Protocol-shape, import-ledger, and duplicate-authority searches. The runtime-checkable `InvoiceCatalogueRepositoryProtocol` and concrete encrypted repository expose compatible `bucket_id`, `exists`, `load`, and `save` members; `_oss_ioss.py` consumes only `load`, so all existing concrete callers remain structurally substitutable without a wrapper or compatibility declaration. Production calculation and legacy verification both converge on the same `OssIossLedgerSourceResolver`, and repository-wide resolver-function searches plus the existing anti-parallel-aggregator guard found no competing OSS/IOSS aggregation authority.

The S10 execution record accurately states the three widened boundaries, one retained construction, unchanged runtime topology, real-test evidence, and S11 ownership of the distinct-store injected-boundary proof. Its checked plan row and feature-index link agree with the commit. Independent Ruff, ty, and pyright checks passed. The real encrypted-storage OSS/IOSS and dormant Modelo 369 suites passed all `22` tests in `72.78s`. A fresh import-linter run analyzed `3,421` files and `16,155` dependencies with all five contracts kept and none broken.

Independent S11 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and Phase W01.P01 is complete.

Commit `6d003dfddbca8d7bd785a318b09c6d279971919f` strengthens the existing positive Modelo 369 scenario, adds its S11 execution record, closes the canonical plan row, and regenerates the feature-index links. The test now opens two real SQLite-backed encrypted object stores for the same bucket: the active runtime retains the work-unit, calculation, transaction, profile, and empty ambient invoice catalogues, while a separately created engine contains the three OSS invoices. The injected engine is disposed in an unconditional `finally`, and the active runtime owns cleanup of the ambient engine and secure session.

The review was grounded with the healthy-port code query `vaultspec-rag search "M369 distinct ambient injected real encrypted invoice repository mutation sensitivity public calculation legacy verification duplicate OSS IOSS authority" --type code --port 8766`, followed by exact commit, source-mesh, verification-handoff, construction, definition, and prohibited-test-pattern searches. Public calculation forwards the injected repository through `_resolve_bucket_source_mesh` into `OssIossLedgerSourceResolver`; legacy verification forwards the same port through both finding collectors into that resolver. The scenario is mutation-sensitive at both boundaries: discarding injection during calculation leaves the three asserted cuotas and positive provenance unavailable, while discarding it during legacy verification produces `oss_no_live_source` against the already positive legacy provenance and refuses the asserted verification grant. The ambient catalogue is asserted absent before calculation and after verification.

Repository-wide definition search found one `OssIossLedgerSourceResolver`, one application candidate-projection module, and one registry `resolve_ledger_oss_aggregation_binding_values` authority. The existing anti-parallel-authority guard independently passed. The strengthened test calls production entrypoints and real repositories and adds no fake, mock, stub, patch, monkeypatch, skip, xfail, mirrored aggregation, compatibility route, default constructor, or second authority.

Independent Ruff passed. The exact strengthened real-store test plus the anti-parallel authority guard passed `2` tests in `54.36s`. Pyright reproduced zero errors and the five pre-existing private test-support warnings. Ty reproduced exactly the execution record's single pre-existing diagnostic on the unchanged `source_provenance` call at line `532`; blame attributes that line to commit `8d4cd1efce7`, and no S11 hunk reaches it. A fresh uncached import graph analyzed `3,421` files and `16,157` dependencies with all five contracts kept. Plan validation reports no findings, `11` completed Steps, Phase W01.P01 complete, next open Step `W01.P02.S12`, and no missing execution records; feature annotation and placeholder checks are clean.

Independent S12 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S13 is not blocked.

Commit `8aa043a2d65fa8d3996a75b6aa338ac908ce1449` changes only `_IGNORE_EDGE_RE` in `test_importlinter_ledger.py` from the removed product prefix `aeat` to the live package prefix `cadrumo`, then adds the S12 execution record, closes the canonical plan row, and regenerates the feature-index links. It does not change a ceiling, filtering rule, assertion, contract, configured ignore edge, or unmatched-ignore enforcement; S13 through S17 retain ownership of the already planned ratchet reductions and explicit non-vacuity assertions.

The review was grounded with the healthy-service semantic query `vaultspec-rag search "current import-linter ledger parser prefix inventory cadrumo endpoints duplicate parser authority" --json`, followed by commit-scoped inspection and exact parser, prefix, configuration, and duplicate-authority searches. Importing the codebase's real `_ignore_edges` and `_module_exists` helpers against `.importlinter` reports `265` total configured edges and `229` layered edges, comprising `199` application-to-adapter edges, `78` application-source wildcards, two domain-to-adapter test carveouts, and zero production-domain-to-adapter edges. All `265` source and target endpoints resolve, and no duplicate `(contract, source, target)` tuple exists. The only remaining `aeat` spelling in the relevant configuration is the legitimate external-authority package segment `cadrumo.adapters.outbound.aeat.auth`, not a product-root parser drift.

Repository-wide source search finds one tracked `test_importlinter_ledger.py`, one `_IGNORE_EDGE_RE`, and one `_ignore_edges` implementation; no competing live ledger parser or shadow inventory authority exists. Independent Ruff passed, the four focused ledger tests passed in `4.73s`, and a fresh uncached Import Linter run analyzed `3,421` files and `16,157` dependencies with all five contracts kept and none broken. The S12 execution record's inventory and gate results are therefore reproducible, the checked plan row and generated index link are truthful, plan validation is clean at `12` of `220` Steps, and the feature annotation check reports no retained scaffold diagnostics.

Independent S13 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S14 is not blocked.

Commit `a8ac6bf84b00f2116fa80ddbb008eabdf3418b4a` changes only `_APPLICATION_TO_ADAPTERS_BASELINE` from `840` to `199`, replaces its obsolete historical increment note with the truthful decrease-only ratchet policy, adds the S13 execution record, closes the canonical plan row, and regenerates the feature-index links. The source-wildcard and domain ceilings, parser, filters, assertions, `.importlinter`, and unmatched-ignore enforcement are unchanged.

Fresh live code RAG returned the exact current parser and `199` baseline before commit-scoped inspection and exact duplicate-authority searches. Importing the real ledger helper reports `265` configured edges, `229` layered edges, and exactly `199` application-to-adapter edges. The production assertion remains `len(application_adapter_edges) <= _APPLICATION_TO_ADAPTERS_BASELINE`, so one additional parsed edge produces `200 <= 199`, which is false; the ratchet is mutation-sensitive at its live boundary. Repository-wide source search finds one tracked ledger test, one parser, one application-edge baseline, and one helper authority. Independent Ruff passed, all four focused tests passed in `5.02s`, and a fresh uncached graph analyzed `3,421` files and `16,157` dependencies with all five contracts kept. The record, checked plan row, generated index link, `13`-of-`220` plan status, and clean annotation check are reproducible and truthful.

Independent S14 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S15 is not blocked.

Commit `3bbaaf7efeba07b66aceeefc1c2c53f49f9efaf1` preserves the numeric ceiling at `78` while renaming `_APPLICATION_SOURCE_MODULE_BASELINE` and its local inventory to `_APPLICATION_SOURCE_WILDCARD_BASELINE` and `source_wildcard_edges`. The accompanying comment and unchanged filter explicitly define the category as application edges whose target is `cadrumo.adapters.**`; the broader `199` application ceiling, domain ceiling, parser, assertions, and `.importlinter` configuration are otherwise unchanged. The commit also adds the S14 record, closes its canonical plan row, and regenerates the feature-index links.

Fresh live code RAG returned the exact renamed baseline and target-wildcard filter before exact commit and duplicate-authority searches. Importing the real helper reports `199` application-to-adapter edges, exactly `78` of which target `cadrumo.adapters.**`; the other `121` use narrower targets. The assertion remains `len(source_wildcard_edges) <= _APPLICATION_SOURCE_WILDCARD_BASELINE`, so a 79th matching edge produces `79 <= 78`, which is false. Repository-wide source search finds one tracked ledger test, one parser/helper authority, one renamed wildcard baseline, and no stale former name. Independent Ruff passed, all four focused tests passed in `4.77s`, and a fresh uncached graph analyzed `3,421` files and `16,157` dependencies with all five contracts kept. The record, plan, index, `14`-of-`220` status, and annotation state are truthful and clean.

Independent S15 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S16 is not blocked.

Commit `84897285d75636e8e796d73ae6f8c00480fa1c4a` changes only `_DOMAIN_TO_ADAPTERS_BASELINE` from `70` to the reconciled live ceiling `2` and replaces the obsolete increment note with a truthful decrease-only test-carveout policy. It adds the S15 record, closes the canonical plan row, and regenerates the feature-index links. The parser, domain filter, separate production-zero assertion, configured edges, and the identities that S17 will pin explicitly are unchanged.

Fresh live code RAG returned the exact `2` ceiling and production-zero gate before commit-scoped and duplicate-authority searches. The real helper reports exactly two layered domain-to-adapter entries—`cadrumo.domain.tests.** -> cadrumo.adapters.**` and `cadrumo.domain.**.tests.** -> cadrumo.adapters.**`—and zero production entries. The count assertion remains `len(domain_adapter_edges) <= _DOMAIN_TO_ADAPTERS_BASELINE`, so a third parsed edge produces `3 <= 2`, which is false, while the independent production predicate still hard-fails any non-test edge at zero. One tracked ledger test, parser/helper, baseline, and production-zero authority exist. Independent Ruff passed, all four focused tests passed in `6.16s`, and the uncached graph analyzed `3,421` files and `16,157` dependencies with all five contracts kept. The record, plan, index, `15`-of-`220` status, and annotations are truthful and clean.

Independent S16 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S17 is not blocked.

Commit `4dda121687378c5c192f8343bece2ac1de3a5747` adds one test that consumes the existing module-scoped `ignore_edges` and `layered_edges` fixtures and directly requires each tuple to be non-empty. Its separate messages accurately distinguish parser or product-prefix drift from layered contract-name or configuration drift. It adds no parser, fixture, helper, exact `265`/`229` inventory assertion, or S17-owned carveout-pair assertion; the commit otherwise contains only its record, canonical plan closure, and generated feature-index links.

Fresh live code RAG returned the exact new test and existing single parser authority before exact source and commit searches. The real helper supplies `265` ignore edges and `229` layered edges. Direct review invocation proves an empty parser result fails with `parsed ignore ledger is empty`, while a non-empty non-layered inventory fails independently with `parsed layered ledger is empty`; the real fixture passes both. This exercises the production parser output without copying its logic, and the decrease-tolerant truthiness checks are not tautological. Repository-wide source search finds one tracked ledger test and one parser implementation. Independent Ruff passed, the focused module passed all five tests in `5.36s`, and the uncached graph analyzed `3,421` files and `16,157` dependencies with all five contracts kept. The record, plan, index, `16`-of-`220` status, and annotations are truthful and clean.

Independent S17 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and Phase W01.P02 is complete.

Commit `bbd2903b77ca4f0ba5217bc3fd01eab80f2b6f84` adds one exact frozenset containing `cadrumo.domain.tests.** -> cadrumo.adapters.**` and `cadrumo.domain.**.tests.** -> cadrumo.adapters.**`, derives observed pairs from the existing layered-edge inventory, and rejects the observed-minus-sanctioned difference. The `2` ceiling remains unchanged, and commit comparison shows no hunk in the separate all-contract production-zero assertion. The commit otherwise adds its record, closes the canonical plan row, and regenerates the feature-index links.

Fresh live code RAG returned the sanctioned set before exact configuration, cross-contract, parser, and commit searches. Each identity legitimately appears once under `domain-not-adapters` and once under `layered`; the new identity check intentionally consumes only the two layered appearances, while the existing production-zero gate consumes every parsed contract and still reports zero production entries. Direct invocation proves an empty set and either sanctioned singleton pass—allowing debt reduction—while a replacement identity fails with the explicit unexpected-pair diagnostic; the unchanged count assertion rejects growth above two. The sanctioned constant is independent of parsed output, so the check is neither tautological nor duplicate parser logic. Independent Ruff passed, all five focused tests passed in `5.81s`, and the uncached graph analyzed `3,421` files and `16,157` dependencies with all five contracts kept. Plan queries show all six W01.P02 Steps closed and none open; the record, index, `17`-of-`220` status, and annotations are truthful and clean.

Independent S18 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S19 is not blocked.

Commit `64de1dbcde7051d92445d79d04a6ede46f2f0b7b` changes no source, test, or import-linter configuration. Its tree contains only the S18 execution record, the canonical plan closure, and regenerated feature-index links. The record names the exact serial command and states that the live inventories were derived by importing the codebase `_ignore_edges` helper rather than by copying its parser.

Fresh live code RAG preceded exact commit and sole-parser searches. Independent execution of `uv run --no-sync pytest -q -n 0 src/cadrumo/tests/test_importlinter_ledger.py` passed all five tests in `1.24s`, consistent with the recorded `1.19s` outcome. Importing `_ignore_edges` and filtering its returned records reproduced exactly `265` total, `229` layered, `199` application-to-adapter, `78` application-source wildcard, two layered domain-to-adapter, and zero production-domain counts. Repository-wide source search finds one tracked ledger test, one `_IGNORE_EDGE_RE`, and one `_ignore_edges`; no evidence-only duplicate parser was introduced. The record, checked plan row, generated index link, `18`-of-`220` plan status, and annotations are truthful and clean.

Independent S19 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S20 is not blocked.

Commit `c9d25187e07c4e520bf55058f09b8415284b73d9` changes no source or test file; it contains only the S19 record, canonical plan closure, and generated feature-index links. The record honestly distinguishes the initial marker-mismatched run from verification: with the repository default `-m unit`, the two integration nodes are collected but both deselected. It records the corrected explicit `-m integration` command and the two executed node names.

Fresh live code RAG preceded inspection of the live test, canonical `isolated_cli_backend`, `isolated_profile_storage_root`, and settings taxonomy. The helper uses the real file secure-store backend, an empty `tmp_path / "cadrumo-storage"` state root, and independent `tmp_path / "secrets"` sibling. The test dynamically iterates all `24` current `_STATE_ROOT_DERIVED_DIRS` fields, requires each value, and proves each equals or descends from `tmp_path`; its second node separately proves the yielded root boundary. Independent reproduction without the integration selector reported exactly `2 deselected`; the exact corrected command passed both nodes in `2.16s`. The peer-owned `core/config.py` WIP remains modified in the worktree but absent from the commit, while the test and helper remain clean. The record, plan, index, `19`-of-`220` status, and annotations are truthful and clean.

Independent S20 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S21 is not blocked.

Commit `1c01c1845beefafb8e2aca6571703e41e88edb57` changes no source or test file; it contains only the S20 record, canonical plan closure, and generated feature-index links. Live source inspection confirms `aggregate_irnr_income_ledger_from_repositories` and `LedgerIrnrIncomeAggregationSourceResolver` both require `TransactionCatalogueRepositoryProtocol` with no optional/default fallback. `_resolve_bucket_source_mesh` resolves one repository, wraps it once in `_MemoizedTransactionCatalogueRepository`, and supplies that wrapper to the M210 resolver.

Fresh live code RAG returned the required loader, resolver, composition route, and distinct-store test before exact commit and source inspection. The exact three-file serial lane collected and passed all `12` recorded nodes in `54.59s`: three M210 real-storage nodes, five memoized-repository nodes, and four resolver-enrollment nodes. The lane includes the mutation-sensitive distinct-store scenario that seeds only the injected SQL store, leaves the same-bucket ambient catalogue absent, and requires the public calculation's amount, transaction id, and provenance to come from injection. The peer-owned `_calculation_actions.py` WIP remains modified but outside the commit; every source and test target is otherwise clean. The record's complete node list, plan row, index link, `20`-of-`220` status, and annotations are truthful and clean.

Independent S21 review verdict: **PASS**. No CRITICAL, HIGH, MEDIUM, or LOW finding was identified, and S22 is not blocked.

Commit `99eb82a49799301a0e56fe4e6ce47417b1bb6308` changes no source or test file; it contains only the S21 record, canonical plan closure, and generated feature-index links. Live inspection confirms the OSS/IOSS loader, binding resolver, source-mesh resolver, public verification entrypoint, and both internal verification collectors receive `InvoiceCatalogueRepositoryProtocol` at their injected boundaries while retaining the one intentional default concrete construction in the loader.

Fresh live code RAG returned the distinct-store M369 calculation and verification assertions before exact commit, signature, and node searches. The positive node creates a separate injected SQL engine, stores all three OSS invoices only there, and asserts the same-bucket ambient invoice repository absent before calculation and after legacy verification. Public calculation receives the injected repository and must produce the three expected cuotas, total, and OSS provenance without no-source diagnostics; legacy verification receives the same port and must grant complete verification. The exact full-file serial command collected and passed all five recorded nodes in `69.98s`, including unresolved-source, legacy-unrouted, recalculation, and valid-zero behavior. Every source and test target remains clean. The record, plan, index, `21`-of-`220` status, and annotations are truthful and clean.

## Recommendations

Proceed to S02 with the atomic gate intact: remove only the stale live-censo ignore, preserve the corrected `cadrumo` root, and do not weaken unmatched-ignore alerting or any contract. S03 must remove the separate censo-sync ignore, and S04 must add only the exact test-to-helper route in the reporting core contract. Keep later Waves blocked until W01 reaches the planned uncached five-contract pass and non-vacuous `199/78/2` ratchet proof.

Proceed to S07 and make `LedgerIrnrIncomeAggregationSourceResolver` require `TransactionCatalogueRepositoryProtocol` without introducing a default, late constructor, or compatibility path. Retain the sole production composition through `_resolve_bucket_source_mesh` and its memoized repository; S08 must then prove the complete M210 path against the real encrypted repository.

Proceed to S10: widen the three OSS/IOSS receiving annotations and their documentation to `InvoiceCatalogueRepositoryProtocol`, retain exactly the existing `InvoiceCatalogueRepository(bucket_id=bucket_id)` fallback in `oss_ioss_candidates_from_repositories`, and add no second constructor, ignore, alias, or compatibility path.

Proceed to S05 by narrowing only the existing diagnostics run-health adapter wildcard to its live outbound LLM package. Do not add a second diagnostics pin, widen a production source, or alter the now-green core helper route.

Preserve the exact outbound LLM pin during the remaining Wave W01 work. Do not broaden it while hardening S07/S10 or repairing the later import-ledger parser and count ratchets.

Proceed to S08 with the required IRNR repository boundary intact and exercise the complete M210 path through the real injected repository. Before closing Phase W01.P01, remove the three S07 execution-record scaffold annotation blocks through the owning documentation/record cleanup path and rerun the annotations check; do not change S07 runtime code to address this LOW finding.
