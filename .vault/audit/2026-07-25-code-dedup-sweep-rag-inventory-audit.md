---
tags:
  - '#audit'
  - '#code-dedup-sweep'
date: '2026-07-25'
modified: '2026-07-25'
related: []
---

<!-- FRONTMATTER RULES:
     tags: one directory tag (hardcoded #audit) and one feature tag.
     Replace code-dedup-sweep with a kebab-case feature tag, e.g. #foo-bar.
     Additional tags may be appended below the required pair.

     Related: use wiki-links as '[[yyyy-mm-dd-foo-bar]]'.

     modified: CLI-maintained last-modified stamp; set at scaffold time,
     refreshed by mutating CLI verbs and vault check fix; never hand-edit.

     DO NOT add fields beyond those scaffolded; metadata lives
     only in the frontmatter. -->

<!-- LINK RULES:
     - [[wiki-links]] are ONLY for .vault/ documents in the related: field above.
     - NEVER use [[wiki-links]] or markdown links in the document body.
     - NEVER reference file paths in the body. If you must name a source file,
       class, or function, use inline backtick code: `src/module.py`. -->

# `code-dedup-sweep` audit: `semantic duplication sweep: 29-searcher RAG inventory`

## Scope

<!-- What was audited and why -->

## Findings

<!-- A rolling log of findings: append one subsection per finding, grouped or ordered by
     severity, using the heading form

       ### semantic duplication sweep: 29-searcher RAG inventory | {level} | {summary}

     followed by a paragraph carrying the detail. semantic duplication sweep: 29-searcher RAG inventory is a concise kebab-case slug,
     {level} is the severity (critical, high, medium, low), and {summary} is a one-line
     statement. Append continuously as findings surface; do not rewrite settled entries. -->

## Recommendations

<!-- Actionable recommendations, each tied to a finding above. An
     architecturally significant recommendation names the decision a
     follow-on ADR must make; the decision itself is never recorded here. -->

## Context

Campaign shape: 8 domain leads x 4 semantic searchers (32 dispatched, 29 returned, 3 killed by server-side throttling). Evidence channel was vaultspec-rag semantic search only - no file reads, no git, no tests. Every site below is RAG-reported and UNCONFIRMED against HEAD; each carries the symbols a targeted rg pass must confirm before any edit. Recall is partial: the code index was mid-rebuild throughout and one probe returned index_unavailable, so a thin domain means "no signal", never "no duplication".

Blind spots (rate-limit casualties, never searched): login/credentials/tokens (D4.2), taxpayer-identity validation (D4.3), censal facts and profile-fact bindings (D4.4).

TIER 1 - promotable, canonical home already exists: (1) schema-version ceiling hand-rolled in >=4 catalogue repositories while ensure_schema_version_readable is a strict superset; (2) _common.py::_parse_iso_date calls date.fromisoformat directly while its identically-named sibling routes through core.parsing.parse_iso8601_date - and a gate, test_no_bare_date_fromisoformat, claims zero bare calls survive; (3) lazy-Typer-subcommand materialiser hand-rolled 3x; (4) _money_round defined 3x instead of core.money.round_to_cents, with a sibling _quantize using default banker's rounding where neighbours use ROUND_HALF_UP; (5) _storage_state_sha256 re-rolls canonical-JSON+SHA256 instead of core.hashing.content_hash_hex; (6) two byte-shaped _parse_iso_date copies.

TIER 2 - cross-corroborated clusters, each found independently by 2-3 domains: decimal-string parsing (core.decimal self-declares canonical; >=6 bypassers, two using bare Decimal() with NO grammar validation on a --set path, i.e. silent under-validation); explicit-or-active bucket-id resolver (canonical declared in core/_bucket_pointer_io.py, >=4 re-derivations, one docstring admitting it "Mirrors" the fallback); bucket-event emission (~6 per-domain wrappers plus one emitting from the CLI layer, a hexagonal-boundary violation); FX rate resolution 4x with a comment admitting it behaves "exactly as the file-import path does"; fixed-width field encoders 3x; committed-TOML reader bypassed by 4 loaders; registry-tree fingerprint 2x plus a second snapshot cache with a coarser key and no fingerprint invalidation; scalar-parameter resolver 3x across M100/M131/M210 formula runtimes; sensitive-key redaction predicate 4x; accent-fold 4x under colliding generic names; _renta_ledger::_casilla_aggregation hand-rolls the per-casilla fold while its sibling income aggregator in the same module family calls the shared fold_casilla_observations; evidence-covers-snapshot contributor-coverage guard 3x including a private near-duplicate of the public exported one.

TIER 3 - structural, needs a ruling rather than a refactor: two coexisting filing-readiness pipelines (ModeloValidator/ModeloDraft vs verify_modelo_revision/CalculationRevision) with different finding taxonomies, the older possibly reachable only from internals; two parallel per-transaction evidence systems (PurchaseInvoiceEvidence CRUD service vs the generic content-addressed Attachment manifest, with attach_manual_transaction_evidence accepting both ids); cross-period not-applicable suppression set derived twice on the legally load-bearing path; _formats/__init__.py declaring itself superseded while _serialise() still ships; terminology lifecycle value set living in non-shippable dev/docs so production hardcodes the string "approved"; two parallel synthetic-PDF generator families for the same three modelos; the mandated grimp runtime import-graph audit axis apparently never built; ledger_transaction_review_payload with no call site found while the CLI recomposes it inline.

CONFIRMED CLEAN - do not re-flag: the sheets/workbook shared-plan two-transport pattern; the four retry policies (genuinely divergent - security throttle vs lock poll vs dev-tooling flakiness vs deliberately single-shot AEAT auth); prompter singularity (AST gate holds; blind spot is an indirect questionary re-export); help builder, command manifest, and risk classifier each single-homed; UnsecuredMasterKeyProvider env-gated and hostile-named; the jscpd false-green duplication runner already fixed and pinned to exactly one command constructor tree-wide.

ADDENDUM - D1 lead direct audit (binding resolvers, revision/period), recovered after the lead tier failed to receive its children's reports.

D1-1 dual binding-validator convention still live on four ledger source families. Every ledger aggregation source family (oss, iva, renta_income, renta_gasto) carries TWO validators: an accumulating `-> list[str]` variant registered in `_BINDING_VALIDATOR_REGISTRY`, and a raise-style `-> None` `*_binding_definition` sibling that does not appear in the dispatch table. Sites: `domain/calculations/registry/_ledger_bindings.py` at ~1364 vs ~168 (oss), ~1379 vs ~455 (iva), ~1409 vs ~887 (renta_income), and the renta_gasto pair (~1281). This is the exact dual convention the binding-validation-single-contract rule records as retired. Pattern confidence high (repeats 4x with identical shape); "definitely uncalled" is UNVERIFIED and is a grep job, since semantic search returns definitions and the dispatch table but not the call graph. Confirm with `rg "validate_ledger_(oss|iva|renta_income|renta_gasto)_aggregation_binding_definition\("`; if there are no callers outside the defining module, delete the four raise-style functions per no-legacy-compatibility.

D1-2 English/Spanish dual stem across sibling renta-expense binding source kinds. `BindingSourceKind.LEDGER_RENTA_EXPENSE_AGGREGATION` (English) and `LEDGER_RENTA_GASTO_AGGREGATION` (Spanish) both exist with separate resolvers in `application/aggregation/_modelo_bindings.py` (the gasto resolver at ~706). Functionally these look divergent-by-design - M100 first-slice expense vs M130 deductible gasto - but the English stem violates the Spanish-stem naming rule regardless. HAZARD for whoever actions this: a `BindingSourceKind` member value must stay byte-identical to the token stored in registry TOML, so a rename is not a code-only change - it must sweep the enum, every resolver and consumer, and the registry authoring tree in one atomic commit, and the registry validates the whole working tree on load. Enumerate consumers first with `rg "LEDGER_RENTA_EXPENSE_AGGREGATION|LEDGER_RENTA_GASTO_AGGREGATION"`. Unresolved: whether both source kinds are reachable on the same modelo/casilla, which would make this a functional duplicate rather than a naming defect - answerable only from the registry TOML.

CLEAN POSITIVE on the revision/period axis: seven queries returned coherent single-owner results for `select_revision`/`_temporal.py`, `Period.contains`/`core/_period.py`, `period_start_date`/`period_end_date`/`domain/period.py`, `validate_revision_windows`, and `apply_period_offset`/`_period_offset_math.py`. This is a clean signal, not an absence of signal - the period/revision resolution surface reads as genuinely single-canonical.

ADDENDUM - GATE BLIND SPOTS, a finding class more serious than any single duplication below.

Two independent structural gates report green against violations they are architecturally unable to see, because each matches a literal NAME rather than the shape of the thing it guards.

1. `src/cadrumo/tests/test_parsing_enrollment_inventory.py::test_no_bare_date_fromisoformat` asserts zero bare `date.fromisoformat(` calls survive in production modules. It PASSED (2 passed in 25.45s) while `entrypoints/cli/_common.py::_parse_iso_date` called `date.fromisoformat` directly - because that module imports `from datetime import date as _date` and calls `_date.fromisoformat(...)`, and the gate's AST helper `_fromisoformat_call_linenos` requires `func.value.id == "date"`. Any aliased import evades the gate silently. Confirmed empirically by the agent that fixed the call site (commit 57f8711287), not inferred.

2. `src/cadrumo/tests/test_wizard_prompter_singularity.py` enforces one live prompt authority tree-wide, but its detection rule keys on a DIRECT `questionary` import. A rival prompter reaching questionary indirectly through a re-export would not trip it. Reported by the D3.3 searcher independently of finding 1.

These are the same defect: a gate whose pattern does not fit the data's possible shapes returns a clean negative that reads as proof of compliance. Every "this surface is single-homed" claim backed by such a gate is unverified until the gate itself is checked against an aliased or indirect form. Remediation is to make the AST checks alias-aware and binding-aware (resolve import aliases, follow re-exports), and to add a self-test per gate that plants an aliased violation and proves the gate catches it - a gate with no anti-tautology proof cannot be trusted.

Audit implication: the whole class of AST-based structural gates in this repo needs the same review - the modelo-string-literal gate, the lazy-import policy gate, the import-hygiene scanner, the duplication runner, and the prompter-singularity gate all pattern-match names.

ADDENDUM - REFUTATIONS. Two findings from this sweep were investigated by Opus agents against HEAD and REFUTED. Both would have caused real regressions had they been actioned as written. Recording them so no future pass re-raises them.

REFUTED 1 - cross-period suppression "derived twice" (was Tier 3). Verdict: composition, not duplication. `_non_filer_modelos` (application/calculations/_cross_period_clean_state.py:369-385) is the sole owner of the suppression verdict, a three-way OR over `taxpayer_files_source`, `conditional_on_economic_activity` + `taxpayer_files_economic_activity is False`, and a caller-supplied set. The block at application/modelo/_verification_cross_period.py:212-221 filters on ONE condition into a local `conditional_source_modelos`, passes it through `derive_not_applicable_source_modelos`, and hands the result in as the `not_applicable_source_modelos=` kwarg - i.e. it produces the third OR-term ARGUMENT, not a verdict. Decisive discriminator: the inline set structurally excludes the suffered-retencion modelos (111/123/193, `taxpayer_files_source = false`) that the helper does suppress, so it cannot be the suppression set. The inline narrowing is LOAD-BEARING for fail-closed behaviour: any raised error or any verdict outside the accepted set collapses the whole result to None, so widening the query set can only preserve or destroy suppression, never add any - a naive consolidation risked an undetermined verdict on an unrelated modelo wiping the M130/M131 regime split. Gates on the untouched tree: 30 passed (test_cross_period_clean_state.py) and 31 passed (the three modelo-layer cross-period modules), both sequential, both unit-marked so selection was real. The undeclared/None fail-closed gate ALREADY EXISTS (test_m100_pagos_fraccionados_conditional_on_economic_activity, asserting False/True/None against a real registry snapshot) as does the suffered-vs-self-filed gate including the operator-visible not-applicable advisory. Nothing changed, no commit.

REFUTED 2 - "dual binding-validator convention still live" (was D1-1). Verdict: the premise is false. The four raise-style `validate_ledger_<family>_aggregation_binding_definition` functions are NOT uncalled siblings - they are the invariant bodies the registered accumulating validators delegate to, each via `return invariant_diagnostics(binding, "<family>", <raise-style check>)` at _ledger_bindings.py:1376, :1389, :1422, :1439. `invariant_diagnostics` (_binding_selector_utils.py:306-324) runs a raise-style check, catches RegistryValidationError, and converts it to one accumulating diagnostic string, so the `list[str]` never-raise contract is intact end to end. This two-layer shape is the norm, not a ledger anomaly: 15 of 24 dispatch entries use it (counterpart, invoice, the four detail-record families, donativo, previous-filing, irnr, impatriado, renta_expense), and `_BINDING_VALIDATOR_REGISTRY` holds exactly one accumulating entry per BindingSourceKind. Deleting the four would have deleted the op/fact enforcement itself - sum-only aggregation op, closed fact set, casilla scoping - a genuine under-validation regression. Gates: 83 passed sequential on the targeted binding suites, 3019 passed parallel across the whole registry test tree. Nothing changed, no commit.

TWO REAL DEFECTS SURFACED BY REFUTATION 2, both worth their own atomic change:

(a) FALSE DOCSTRINGS THAT MANUFACTURE AUDIT FINDINGS. The docstrings at _ledger_bindings.py:1370, :1384, :1414, :1431 and the dispatch-table comment at _bindings.py:931-932 claim the raise-style helpers "stay as a defence-in-depth resolve-time re-check". No resolver calls them - the resolvers re-parse through the private selector helpers (_ledger_bindings.py:226, 532, 955, 1322), so these op/fact invariants are BUILD-TIME ONLY. This inaccurate prose is the direct root cause of the refuted finding and will regenerate it on every future audit pass until corrected.

(b) INCONSISTENT VISIBILITY OF AN INTERNAL DELEGATE. For counterpart / detail-record / donativo / previous-filing the inner invariant check is PRIVATE (`_validated_counterpart_selector`, `_validate_previous_filing_invariants`). For ledger / invoice / irnr / impatriado it is PUBLIC, exported through _bindings.py and the registry package facade `__init__.py` and pinned by tests/test_public_api_boundaries.py. A public export that reads like a top-level validator but is an internal delegate is exactly what made this look like a live parallel convention. Demoting them to private is the durable fix; it requires a facade change, a public-API-boundary test update, and handling one cross-package test consumer (application/aggregation/tests/test_impatriado_income_ledger.py:414,428).

NEW FINDING, adjacent to refutation 1, not yet actioned: `_pagos_fraccionados_not_applicable_source_modelos` (application/calculations/_relation_prefill.py:257-285, used at :522 and :755) assembles the same not-applicable argument by a THIRD route - reading bucket profile-path values rather than a TaxpayerProfile, and HARDCODING M130/M131 instead of consulting `dependency_classifications`. It is not a duplicate of the classification derivation and it does fail closed (empty set on missing facts), but it re-implements the profile-to-M130/M131 applicability reasoning that `derive_taxpayer_files_economic_activity` + `derive_not_applicable_source_modelos` own. A hardcoded modelo pair on a suppression-adjacent path is exactly what the registry-classification grounding rule exists to prevent.

ADDENDUM - BUCKET-EVENT EMISSION, corrected inventory and a blocking hazard the original finding missed.

The cluster is 22 production construct-and-append sites, roughly three times what the sweep reported, and one reported site was a FALSE POSITIVE: all seven emissions in application/modelo/_review_package_collab_audit.py already route through `emit_bucket_event`, and that module's docstring cites the no-parallel-write-path rule explicitly. Also already routed: _work_lifecycle.py x3, _verification_actions.py, _m145_communication_records.py, _external_import_actions.py, _export.py, _amendment_actions.py.

Genuine re-derivations (hand-built id + BucketEvent + load/append/save): application/ledger/_business_operation_invoice.py:402; application/inventory/_service.py:143; application/user_profile/_lifecycle.py:408; application/user_profile/_bundle_export.py:637; application/workflow/_events.py:117 (the unconfirmed sixth site - CONFIRMED real); application/user_profile/_custody.py:198; application/user_profile/_orchestration.py:438; application/auth/_mutation.py:46; application/bucket_maintenance/_service.py at 330, 571, 714, 790, 1208; application/bucket_maintenance/_sandbox.py:699; application/modelo/_reconcile.py:751; application/modelo/_m036_lifecycle.py:319; application/modelo/_iva_wallet_seed.py:360 and :554; application/live/_justificante.py:716; application/live/_filed_observation_persistence.py:399; application/ledger/_actions_common.py:831; application/ledger/_evidence.py:287.

BLOCKING SUPERSET HAZARD - the reason a naive consolidation here would have been harmful. `emit_bucket_event` hardcodes `payload_version=_BUCKET_EVENT_PAYLOAD_VERSION` (=2), while every hand-rolled wrapper persists `payload_version=1` or its own per-domain constant. `payload_version` is NOT folded into `derive_bucket_event_id`, so routing the wrappers through the emitter as-is would silently rewrite the persisted schema version from 1 to 2 across roughly 15 event types with no payload change whatsoever - i.e. the audit trail would start lying about its own payload contract, in encrypted storage. Remediation: widen the emitter with `payload_version: int = _BUCKET_EVENT_PAYLOAD_VERSION`, have each routed site pass its current value explicitly, and pin the persisted version per event type with a test so no future consolidation can bump it silently. Every other axis (object_type, object_id, payload, actor) is a genuine superset; `actor.strip()` is a no-op for the id because `derive_bucket_event_id` strips internally.

HOUSE DEFECT - `emit_bucket_event` currently lives in application/modelo/_revision_persistence.py but composes only `domain.buckets` symbols (BucketEvent, derive_bucket_event_id, append_bucket_event, BucketEventHistoryRepositoryProtocol) plus one modelo-flavoured payload-version constant. Its correct home is domain/buckets, beside `append_bucket_event`. Routing the cross-package sites through application.modelo would make modelo a hub and mint NEW package edges from `inventory` and `ledger`, neither of which imports application.modelo today - edges that the relocation would then delete. Correct order is therefore: fix the CLI-layer violation, widen the emitter for payload_version, relocate the emitter to domain/buckets as one atomic `relocation:emit_bucket_event` commit including regenerated docs/api stubs, and only then route the wrappers.

SECOND CLI-LAYER VIOLATION, previously unnamed: entrypoints/cli/_ledger_ratios_cli.py:41 `_emit_ratios_event` and :88 `_emit_ratios_censo_override_warning` emit directly from the entrypoint layer, and there is NO application-layer ratios service at all - the whole mutation lives in the CLI. This is a larger gap than the profile-bundle case (which merely bypasses an existing service): closing it requires authoring a new application module, so it is its own decision and its own commit. The known first violation, entrypoints/cli/_config/_profile_bundle.py:831 `_emit_profile_lifecycle_event`, additionally hand-rolls the catalogue insert instead of calling `append_bucket_event`.

ADDENDUM - EXPENSE/GASTO SOURCE KINDS: not duplication, but a real and larger naming defect. Verdict from a full investigation against HEAD.

NOT A DUPLICATE, and structurally proven so. The two source kinds are disjoint by modelo and the disjointness is ENFORCED, not conventional: `_RentaLedgerExpenseSelector.modelo: Literal[Modelo.M100]` (registry/_ledger_bindings.py:680) versus `_RentaLedgerGastoSelector.modelo: Literal[Modelo.M130]` (:1267), with disjoint casilla domains (`_RENTA_100_FIRST_SLICE_CASILLAS`, 14 boxes, versus `_RENTA_130_GASTO_CASILLAS = {"02"}`). Registry-build validation refuses any binding that would cross them. Token map: `ledger_renta_expense_aggregation` appears only in M100 revisions 2024 and 2025 (28 binding files, 14 casillas x 2 years); `ledger_renta_gasto_aggregation` only in M130 `2019-y-siguientes` (1 binding, casilla 02).

The observations also diverge hard, so a collapse was never available: M100's `RentaDeductibleExpenseObservation` (domain/renta/_ledger_expenses.py:206) carries 27 fields, is category-routed SpendingCategory->CasillaId, and holds proportionality_kind / applied_ratio / invoice_evidence_status / reconciliation_status / legal_references, produced by `evaluate_renta_deductibility` against a CategoryProfile plus usage ratios, statutory caps, exclusive-use and residence CCAA, on an annual `0A` window. M130's `RentaGastoObservation` (application/aggregation/_renta_gasto_ledger.py:103) carries 4 fields: a flat taxable_base x business_proportion projection, no category axis, one fixed casilla, cumulative quarterly window. Forcing the 4-field path through 27-field constraints would have been the harm.

THE DEFECT THAT IS REAL, and it is broader than the sweep stated. The split is already deliberate and documented in three places (core/aggregation.py:252-261, which itself cites the Spanish-stem rule; registry/_ledger_bindings.py:1221-1231; application/aggregation/_renta_gasto_ledger.py:28-31) - so this is not forgotten cleanup. What survives is that BOTH names are wrong: the M100 token is English-stemmed, AND bare `gasto` on the M130 token is ambiguous, because the M100 first-slice boxes are gastos too - casilla 0203 is literally labelled "Gastos financieros" and all 14 sit under section ["rendimientos_actividades_economicas", "estimacion_directa"]. Renaming only the English one would leave the trap half-set.

Grandfathering question, resolved: `aeat-spanish-stem-naming` states that already-public pre-rule identifiers keep their names, and the M100 token predates the rule (ADR 2026-05-08 versus the rule of 2026-06-02), so the rename was discretionary rather than mandated. Operator directive overrides that clause - a Spanish/English pair naming one concept family is not acceptable - so both tokens are to be renamed, using vocabulary sourced from the registry itself: M100 -> `ledger_renta_gastos_estimacion_directa_aggregation` (the section tag on all 14 casillas), M130 -> `ledger_renta_gastos_pago_fraccionado_aggregation` (the AEAT term for M130).

HARD CONSTRAINTS on the rename. Enum VALUES must stay byte-identical to the tokens stored in registry TOML, so the enum and the authoring tree move in ONE commit or not at all - a partial sweep is strictly worse than nothing here, because the registry validates the whole working tree on load and a half-swept tree breaks suite collection for every concurrent agent. Both new tokens MUST retain the `ledger_` prefix: `LEDGER_BINDING_SOURCE_KINDS` (core/aggregation.py:420) is derived via `source.value.startswith("ledger_")` - correctly derived rather than hand-listed, which answers the parity question, but a prefix change would silently drop the kind out of ledger-readiness routing and fail OPEN.

FULL RENAME SURFACE, for whoever executes: registry/_ledger_bindings.py (67 sites), application/aggregation/_modelo_bindings.py (27), registry/_bindings.py (20, including the selector-model dispatch table at :869/:871 and the validator dispatch table at :959/:961, both keyed by the enum member), registry/__init__.py (12), 29 registry TOML files, core/aggregation.py, _source_mesh.py, _calculation_source_policy.py, _calculation_preparation.py, _modelo_discovery_cli.py, domain/renta/_ledger_expenses.py and _first_slice_routing_integrity.py, roughly 15 test modules, and 6 docs/_sequences/*.json recorded-output contracts which must be REGENERATED rather than hand-edited.

STANDALONE DEFECT AND RENAME TRAP, found independently: domain/renta/_ledger_expenses.py:39 and :210 hardcode `LEDGER_RENTA_EXPENSE_SOURCE = "ledger_renta_expense_aggregation"` as a raw string literal plus a `Literal[...]` annotation instead of deriving from `BindingSourceKind`. Fixing that to derive from the enum is worth doing on its own merits and de-risks the rename by removing a site a sweep could miss.

ADDENDUM - BUCKET-ID RESOLVER CLUSTER: landed as commit 58c02ee8e7, and the result reframes how much of this whole inventory is already-fixed work.

THREE OF THE FIVE REPORTED SITES WERE ALREADY CANONICAL. `adapters/persistence/profile/_modelo_runtime.py:39`, `adapters/persistence/profile/_filing_runtime.py:39`, and `application/filing/_runtime_repository.py:32` all import `resolve_repository_bucket_id` from `cadrumo.core` and delegate in one line at HEAD. The sweep reported `_filing_runtime.py` as having "no visible import of the canonical"; the import is at line 31. ADR `2026-06-13-semantic-dedup-epic-adr` (F3) already landed exactly these three. IMPLICATION FOR THIS AUDIT: a prior semantic-deduplication campaign exists, and some fraction of the Tier 1 and Tier 2 inventory in this document is re-discovery of work that campaign already completed. Any future pass over this inventory must re-read HEAD per site before acting - the searchers read a tree that was, in places, months stale relative to the fixes.

GENUINELY ROUTED: `adapters/persistence/profile/invoices.py:48` `_resolve_invoice_bucket_id`, an 11-line hand-rolled re-derivation collapsed to one canonical call, preserving the error class and message key and additionally gaining the structured `reason` context the copy had dropped.

A REAL DEFECT FOUND BY CONSOLIDATION, surfaced by no searcher: `entrypoints/cli/_modelo_work_lifecycle_cli.py:235` held an INLINE re-derivation, `bucket_id if bucket_id is not None else active_bucket_id_or_refuse()`, which did not trim - so `--bucket-id ""` propagated an EMPTY bucket id through the readiness guard and into storage scoping. Now routed so a blank option is treated as unset. This is the kind of find that justifies consolidation on its own: the duplicate was not merely redundant, it was subtly wrong.

DELIBERATELY KEPT AS A SUPERSET, with the difference stated rather than papered over: the CLI pair retains its own operator-facing refusal (`active_bucket_id_or_refuse` raising `CliRefusedBoundaryError`) instead of the core canonical's domain error, because a cold start must distinguish "no profile registered" (suggest create) from "registered but logged out" (suggest login), and `resolve_repository_bucket_id(error_type=X)` carries one fixed message key so that discrimination cannot cross the boundary. Residual divergence, flagged for whoever owns entrypoints/cli/_common.py: a blank explicit bucket id falls back to active in the CLI path rather than refusing with `blank_explicit_bucket_id` as core does; unifying it needs a new operator-facing locale key and changes refusal text at 7 call sites. The new shared CLI helper was homed in `_modelo_cli_support.py` rather than its natural home `_common.py` only because that file was peer-owned at the time - worth relocating later.

CORRECTLY EXCLUDED FROM THE CLUSTER (each a different concern, not a duplicate): `_bucket_archive.py:64` `_resolve_target_pointer` resolves a LABEL to a pointer record (different input, different return type); `_app_live_auth_preflight.py:29` `resolve_active_bucket` is a registration guard over an injected callable; `application/ledger/_llm_diagnostics.py:200` deliberately tolerates absence because the core contract requires diagnostics not to refuse; the `bucket_id.strip() if bucket_id is not None else None` constructor lines across filing_drafts/filing_amendments/modelos_*/participation_index and `application/filing/_history_repository.py` are lazy stores that then call an already-canonical resolver; the auth session/lock helpers construct token and lock paths rather than scoping repositories.

GATES: ruff and ty clean on 4 files; `pytest --collect-only -q` gave 14064/17332 collected with zero collection errors; 47 passed on the bucket-pointer/invoice/runtime-repository suites; 74 passed on the review-package and modelo-work CLI suites. Every selection used an explicit `-m "(unit or integration) and not external_tool and not os_keychain"` because the repo default of `-m 'unit and ...'` would have selected nothing for the integration-marked CLI modules and exited green - a vacuous pass, not a verification.

CROSS-AGENT CORRELATION, recorded because it nearly caused a mis-triage: this agent reported `src/cadrumo/tests/test_lazy_import_policy.py` failing 4 tests on 8 unallowlisted runtime edges and attributed them to peer campaigns. Five of those edges - inventory._service, ledger._business_operation_invoice, user_profile._bundle_export, user_profile._lifecycle, and workflow._events, each reaching `application.modelo` - are precisely the five bucket-event wrappers ANOTHER agent was concurrently routing via function-local imports at the `application.modelo` facade. The gate exists to catch exactly that class of edge. This is empirical confirmation of that agent's own house finding: relocating `emit_bucket_event` from application/modelo to domain/buckets does not merely avoid new coupling, it DELETES these five edges and is therefore the fix for the red gate, not a follow-up nicety. Lesson for the campaign: in a shared worktree, "pre-existing failure, not my surface" must be verified against concurrent agents' in-flight edits before it is believed.

ADDENDUM - REFUTED 3, and a correction to this audit's own Tier 1. This was the finding the compiled report rated highest-confidence and caveat-free. It was the most wrong item in the inventory.

THE CLAIM (as written in Tier 1 above): the schema-version ceiling is hand-rolled in >=4 catalogue repositories while `ensure_schema_version_readable` is a strict superset, therefore PROMOTABLE. FALSE on the decisive point.

THERE ARE TWO VERSION-CHECK LAYERS, and the finding conflated them. Layer 1, the outer secure-object ROW: `ensure_schema_version_readable` (adapters/persistence/storage/_schema_lineage.py:98) performs a ceiling check PLUS an upgrade chain keyed `(namespace, from_version)` in the outer-payload upgrader registry - and it is ALREADY applied on every one of these loads, via _secure_object_row_codec.py:156 and sql/secure_objects.py:210. Layer 2, the inner `Envelope`: the canonical contract is EQUALITY, not a ceiling - `_envelope.py:261` and `_secure_repository.py:195` both compare `!= max_supported_version`. That is correct by construction: every flagged repository calls `self._objects.load(..., max_supported_version=<CONST>)` first, so layer 1 has already chain-upgraded the payload to current, and any inner deviation is therefore drift or corruption rather than a lineage gap.

All 22 flagged sites are LAYER 2. So the canonical at that layer is STRICTER (equality) than the replacement the finding proposed (ceiling) - the exact inverse of the superset relationship the substitutability pre-filter requires. The pre-filter was never applied to this finding: the SIGNATURE is a superset, the CONSTRAINT SHAPE at layer 2 is not.

WHAT CONVERSION WOULD HAVE COST: the operator-facing refusal identity on 22 persisted-read paths; the per-object diagnostics that identify WHICH row is unreadable (transactions.py:316 asserts context including object_key and bucket_id, which the canonical gate cannot produce since it knows only namespace/schema_version/expected; filing_amendments, the four _observation_store sites, live/_verify and live/_snapshot_base likewise carry amendment_id / object_key / observation label / snapshot label); and the separation of two version axes, since every inner constant is literally `NAMESPACE.schema_version`, so routing layer 2 onto layer 1 puts both axes on one upgrader key and a future inner-only bump collides with `register_secure_object_schema_upgrader`'s duplicate guard. Empirical probe of the canonical gate: type `PersistenceError <- EnvelopeVersionError`, message key `errors.storage.namespace.schema_version_from_future` (and `...schema_upgrade_path_missing` below-current), context `{namespace, schema_version, expected}` - disjoint on all three axes from what the existing gates assert. Six of the ten profile sites raise `ModeloError`/`BucketsError` descendants, a DIFFERENT error branch from `EnvelopeVersionError`'s `PersistenceError -> StorageError`, each enrolled in core/errors/registry driving envelope and exit-code mapping, and caught narrowly by exact type in tests. usage_ratios.py:88 is additionally ordering-sensitive: its raise sits inside a `try` whose `except (ClassificationError, EnvelopeVersionError)` re-raises `UsageRatioPersistenceError`.

CORRECTED INVENTORY - 22 sites, not 4, and one suspected site does not exist. adapters/persistence/profile: modelos_filing.py:174, modelos_verification_reports.py:153, modelos_calculation.py:182, buckets.py:176, modelos_work_units.py:154, participation_index.py:130, transactions.py:316, filing_amendments.py:132, invoices.py:132, usage_ratios.py:88. adapters/outbound/aeat/sede/_observation_store.py:192, 219, 291, 320 (two namespaces). application: workflow/_persistence.py:146 and :411, user_profile/_repository.py:295 and :484 (own translated_message keys plus profile_id/snapshot_id context), user_profile/_bundle_encryption.py:91 (a different axis entirely - the encrypted-bundle format version, raising EncryptedProfileBundleError), live/_verify.py:252, live/_snapshot_base.py:489, bucket_maintenance/_service.py:149 (the portable-archive axis with its own `_ARCHIVE_DURABILITY_FLOOR` at :157, raising BucketImportError). `filing_drafts` has NO such check - that suspicion was wrong.

Facade note: `ensure_schema_version_readable` is deliberately ABSENT from the storage facade; its only two callers live inside storage.sql. Promoting it to `__all__` would advertise the wrong gate to layer-2 callers.

GATES (evidence for the refutation, tests genuinely executed - all four modules carry pytestmark unit + hex_persistence_adapter): 34 passed in 11.87s across test_schema_lineage.py, test_calculation_repository_roundtrip.py, test_event_history_roundtrip.py, test_transactions_repository_roundtrip.py. That run contains both gate kinds the conversion would have broken: save/load strict-equality roundtrips, and the mutate-the-payload-and-assert-refusal proofs - test_calculation_revision_catalogue_unsupported_storage_version_is_localized (future inner stamp, asserts CalculationRevisionPersistenceError plus its exact message key and `{reason: unsupported_envelope_version, stored_schema_version, max_supported_version}`), test_bucket_event_inner_schema_version_drift_is_structured (asserts BucketEventHistoryPersistenceError plus `{namespace, object_key, schema_version, expected}`), the transactions gate asserting object_key and bucket_id, and the event-history in-place ciphertext-tamper proof. Nothing was changed, nothing committed, no ceiling weakened.

NEW GENUINE FINDING, requiring a DECISION RECORD rather than a coder sweep: all 22 layer-2 checks use `>` where the layer-2 canonical contract is `!=`. Post-upgrade, an inner stamp BELOW current is drift that all 22 sites currently accept SILENTLY. Tightening to equality would harden a persisted-read boundary across 22 paths, but it changes a data-integrity refusal, so it belongs under the compatibility-lifecycle regime as an ADR - not a mechanical consolidation.

METHODOLOGICAL LESSON FOR THIS AUDIT, applying to every remaining unactioned finding above: the sweep's own confidence ratings were inversely predictive. This item was rated high-confidence precisely because signature similarity is the easiest thing for semantic search to see, while constraint shape, error-branch identity, diagnostic context, and layer position are invisible to it. Four findings have now been investigated and refuted, all four correctly, and two of those refutations prevented regressions. Treat every remaining PROMOTABLE claim in this document as unverified until an agent has read both sides at HEAD and compared constraint shape, not signature.

PROGRESS - commit bc87cbeaa90229e053f89a6d5749b255baa58458, the standalone rename-trap fix ahead of the expense/gasto sweep.

The hardcoded token `LEDGER_RENTA_EXPENSE_SOURCE = "ledger_renta_expense_aggregation"` at domain/renta/_ledger_expenses.py:39 was not merely re-pointed at the enum, it was RETIRED. Rationale, which improves on the instruction it was given: re-pointing would have left a publicly exported constant named `..._EXPENSE_...` holding an enum member renamed to `..._GASTOS_ESTIMACION_DIRECTA_...`, i.e. one further English-stemmed public site for the rename sweep to catch - the precise failure mode the fix exists to remove. Independently, a second public name for a canonical enum member is parallel authority. The field now follows the `modelo: Literal[Modelo.M100]` precedent already present in the same model, typed `Literal[BindingSourceKind.LEDGER_RENTA_EXPENSE_AGGREGATION]` with the member as its default. Swept 3 files (domain/renta/_ledger_expenses.py, domain/renta/__init__.py facade and __all__, tests/test_ledger_expenses.py), 6 insertions and 8 deletions, shrinking the pending rename surface by 3 sites.

Safety analysis that made enum-typing safe under strict validation: the model has exactly ONE constructor (:445, which does not pass source_kind) and ZERO `model_validate`/deserialization sites, so no raw-string hydration path exists. Contrast documented at registry/_schema.py:777, where registry TOML genuinely does feed raw strings and a `mode="before"` coercion is therefore required - no such coercion is needed on this model. `BindingSourceKind` is not a CORE_STRUCTS anchor, so the new import carries no docstring-cross-link obligation.

A TAUTOLOGICAL TEST WAS FIXED IN PASSING: the prior assertion in tests/test_ledger_expenses.py compared the model field against the very constant that supplied its default, so it would have passed no matter what value that constant held. It now asserts member identity against `BindingSourceKind.LEDGER_RENTA_EXPENSE_AGGREGATION`.

Gates, all sequential with an explicit `-m ''` and `-o addopts=""` because the repo default marker selection would have selected nothing and exited green: ruff check and format clean on 3 files; `ty check` clean; pyright strict on the domain reporting 0 errors; 130 passed across the renta domain, registry taxonomy, expense-binding and both aggregation suites; 11 passed on test_import_hygiene_gate.py; and `pytest --collect-only -q` giving 14140/17430 collected with zero collection errors, run immediately before the commit. Post-commit verification at HEAD confirmed the field default is the enum member, its `.value` is byte-identical to the stored token `ledger_renta_expense_aggregation`, and the retired alias is absent from the facade.

CROSS-CAMPAIGN RED GATE, not owned by this campaign and not actionable from it: `src/cadrumo/tests/test_docstring_core_struct_links.py` fails 2 tests, requiring cross-links from `cadrumo.application.user_profile._overview` to `:class:`SensitivityClass`` and `:class:`UserProfileRecord`` (the latter also from `build_profile_overview`). That module is UNTRACKED in git - a live peer's brand-new file, alongside further uncommitted WIP in user_profile/{__init__,_bundle,_bundle_export,_lifecycle}.py. No renta or registry module appears in the violation list. Whoever owns that campaign must add the two cross-links; until then the docs lane reds for anyone who runs it, and any agent seeing it should NOT triage it as their own surface.

PROGRESS - commit db28b65b0907f3dd63f92c5d977d4909764cd2f6, `relocation:ConceptLifecycle`, 24 files, 290 insertions / 58 deletions. The concept-lifecycle closed axis now has one declared home.

The enum lives at src/cadrumo/core/_concept_lifecycle.py, exported from cadrumo.core.__all__, following the existing per-axis convention (_refund_election.py, _rescate_type.py). Values byte-identical to the stored tokens; the bundled tree carries approved (49), draft (57), deprecated (11), and the enum adds retired. The dev-side declaration was RETIRED rather than parity-gated, with a sound justification: the shipped-to-dev direction was already load-bearing, since dev/docs/terminology_handbook/_schema.py:39 already imports OutputLanguage from cadrumo.core.external_constants and the old _enums.py docstring explicitly cited that precedent - "the one closed axis that IS cross-cutting ... is NOT redeclared here". Once the lifecycle acquired a shipped reader it met that stated criterion. With a single declaration drift is structurally impossible, so a parity gate would compare a set against itself. Fifteen dev consumers now import from cadrumo.core; _enums.py retains only ConceptDomain and TermStatus. No concept's lifecycle value was changed.

THE BRIEF UNDERCOUNTED THE SHIPPED HARDCODED SITES. Beyond the known `_APPROVED = "approved"` at application/corpus_search/_terminology.py:44, a SECOND shipped site carried `_APPROVED_LIFECYCLE = "approved"` with a bare `!=` string comparison at application/wizard/_copy_sources.py:40 and :91 - same closed axis, same defect. Both routed. Also a correction: dev/docs/terminology/_concept_cards.py imports the enum from the handbook package facade, not from _enums directly.

NEW BEHAVIOUR, deliberate and worth noting because it is a shipped read path: an unknown lifecycle token now RAISES CorpusSearchInputError carrying the offending token, the concept id, and the accepted set. Previously an unknown token silently dropped its concept from every search result. The test added (application/corpus_search/tests/test_terminology_lifecycle.py, six tests, no doubles) also reads every raw token in the bundled tree straight from TOML rather than through the projected models, making it a genuine drift gate rather than a self-consistency check.

PACKAGING FACTS ESTABLISHED HERE, which settle a question this campaign raised twice. pyproject.toml sheds tests from the wheel: `packages: ['src/cadrumo']` with `exclude: ['src/cadrumo/tests', 'src/cadrumo/tests/**', 'src/cadrumo/**/tests', 'src/cadrumo/**/tests/**', ...]`. Ten files under src/cadrumo/ import `dev.*` and ALL TEN are test modules - filtering for anything not under a tests/ path or named test_* returns nothing. ZERO production modules import dev. So the precedent modules can import unshipped tooling precisely BECAUSE they are not shipped, and an installed user never sees them. But a production module doing the same would raise ModuleNotFoundError for every installed user, and NOTHING enforces this today: neither the import-linter contracts nor dev/import_hygiene_scan.py mention `dev` at all. Packaging is the only constraint, and it is silent rather than enforcing. That gap is the durable finding; a structural gate deriving the shipped boundary from the packaging config is the fix, with the test-module case ruled on explicitly rather than left ambiguous.

TWO ADJACENT FINDINGS, same defect class, deliberately left out of scope: application/corpus_search/_terminology.py:125 compares `entry.get("term_status") == "preferred"` against a bare literal, and TerminologyConcept.domain is typed as free text. Both are shipped production reads of closed axes - TermStatus and ConceptDomain - that remain declared only in unshipped dev/, so both are genuinely unimportable today, exactly as the lifecycle was before this commit. Each needs the same treatment.

GATES: 62 passed on corpus_search plus the wizard copy-sources suite; 21 passed across the dev-docs glossary, pagefind-inject, palette-ranking and concept-cards suites; `apidocs scaffold --check` reported no drift; ruff check and format clean on 22 files; `pytest --collect-only -q` gave 14158/17451 collected. Nine wider dev-docs failures were BASELINED rather than asserted clean - the agent reverted all 17 of its dev/ files to HEAD content, re-ran, observed the identical nine failures, then restored its edits, establishing they are pre-existing (docs-prose redeclaration on 4 pages, the curation-backlog ratchet, relevance-data vocabulary, two scaffold-drift bootstrap tests, a live-service sweep). Two MCP handshake failures were shown to persist with _copy_sources.py at HEAD content and coincide with live peer WIP in wizard/{__init__,_commands}.py plus an untracked _results.py.

COMMIT-HYGIENE PRECEDENT WORTH REUSING: the agent's `apidocs scaffold` run regenerated stubs for peers' new modules and the core toctree also picked up a `_credentials` line belonging to pre-existing drift it did not own. It staged only its own toctree line via a HEAD-anchored `git apply --cached` patch, verified the staged set carried zero foreign hunks, and committed the verified index - leaving the peer's line unstaged and untouched. That is the correct shape for landing a change that a shared generator touches.

CAMPAIGN-LEVEL INCIDENT - THE SWEEP DESTROYED ITS OWN DISCOVERY SUBSTRATE, AND THE SERVICE REPORTED HEALTHY THROUGHOUT.

Measured directly at 2026-07-25T16:19 local: `code_count` = 214 against 3,634 tracked src/**/*.py files, down from 27,902 earlier in the same session. `degraded_reasons` = `[]`. The code generation job sat `state: running` with progress 109/109 - i.e. completing tiny incremental rebuilds that never converge on the tree.

MECHANISM: the file watcher is armed (`watch_enabled: true`, debounce 2000ms, cooldown 30s) while roughly thirteen concurrent coding agents write files continuously. Every edit and commit retriggers indexing; each pass truncates the collection and restarts. The campaign's own write volume was therefore consuming the semantic-search substrate the campaign's mandatory discovery gate depends on. Because the service reports no degradation, the discovery mandate's refuse-if-unavailable clause NEVER FIRES - the gate silently degrades from "search by meaning" to "return two arbitrary files".

HOW IT WAS CAUGHT, and how it should be caught in future: one agent noticed that two SEMANTICALLY UNRELATED probes ("compute a deterministic digest of a canonical json payload" and "parse a committed TOML file into a mapping") returned the SAME two irrelevant files, and that neither `core/hashing.py` nor `core/_toml.py` - the literal canonical owners of the two queried concepts - appeared at all. It also observed the section count oscillating 186 -> 133 -> 127 -> 214 across three job ids in about two minutes. Both are decisive tells. The durable lesson: `degraded_reasons: []` is not evidence of a usable index. Verify `code_count` against the tracked file count, and treat "two unrelated probes return the same handful of files" as proof of incapacity rather than weak recall.

CONSEQUENCE FOR THIS AUDIT'S PROVENANCE: every RAG discovery gate run during the high-concurrency window returned garbage while appearing to succeed. Any agent conclusion in this campaign that rests on SEMANTIC discovery alone is unreliable for that period. Conclusions resting on exhaustive `rg` sweeps and full-file reads at HEAD remain sound - and fortunately the strongest reports (the refutations in particular) were grounded that way. The original 29-searcher sweep also ran while the index was mid-rebuild at roughly 44k-49k chunks, which independently caps its recall: absence of a finding in this document is not evidence of absence, as recorded at the top.

REMEDIATION APPLIED: new agent dispatches paused to let the fleet drain and the watcher settle, so the index can converge. The service was deliberately NOT restarted - a restart discards the in-progress job and induces a perpetual-reindex state. No agent was permitted to reindex or restart either.

COORDINATOR ADJUDICATION RECORDED: for the canonical-JSON-then-SHA256 consolidation, the semantic gate was substituted by exhaustive grep coverage, on the grounds that the coverage is complete BY CONSTRUCTION rather than by diligence - any implementation of that concept must call `json.dumps`, and every `sort_keys=True` in production was swept. Byte-identity was established down to separators, key ordering, and encoding, so the persisted digest cannot move. The substitution is stated in the commit message rather than left implicit. This adjudication is narrow and does not generalise: it applies only where a concept has a mechanically exhaustive textual signature.

TOML CLUSTER, resolved without edits: `domain/categories/_registry.py` ALREADY delegates to `read_toml` with an `error_factory` - that finding was stale. `core/_bucket_pointer.py::from_toml` must NOT be converted, and the real reason is not the suspected datetime handling (its pair only ever handles two scalar keys) but a documented `tomllib.TOMLDecodeError` contract: verified in-environment, `rtoml.TomlParsingError` is a `ValueError` subclass but NOT a `TOMLDecodeError` subclass, so conversion would falsify a documented contract that `core/_bucket_pointer_io.py:70` also declares. `core/external_constants.py` and `agent/eval/_runner.py` call raw `tomllib` with NO except branch at all, so the decode error propagates untyped and no caller catches it; routing them through `read_toml`/`parse_toml_text` would ADD a refusal contract rather than preserve one, making "which typed error should each raise" a contract decision rather than a de-duplication. `external_constants` additionally parses one branch from `importlib.resources` text rather than a Path, so only `parse_toml_text` fits and the conversion would be partial.

FOURTH INSTANCE OF FALSE PROSE ASSERTING A GUARANTEE THAT DOES NOT HOLD: ADR `2026-07-07-registry-toml-parser-adr` states that `core/_toml.py` "is the SOLE TOML-parsing surface project-wide". That was already untrue when written - `core/external_constants.py`, `agent/eval/_runner.py`, `application/corpus_search/_terminology.py` and `application/workflow/_profile_bucket_scan.py` all use raw `tomllib`. The ADR's parser-correctness argument is sound and thoroughly evidenced (rtoml/tomllib parity proven across all 16,260 registry files plus offset and local datetime, date, time, inf/nan, large-exponent and duplicate-key probes), so parser behaviour is NOT the gap; the gap is that its safety argument rests on `error_factory` re-wrapping the parse failure, which is exactly what the unconverted sites lack. The ADR's sole-surface premise needs correcting or the sites need enrolling. This joins the false resolve-time-defence-in-depth docstrings, the ALT-FINGERPRINT rationale marker, and the two blind structural gates as a recurring pattern: prose in this repo asserts guarantees that were never or are no longer true, and those assertions actively manufacture false audit findings.

RESOLVED - GATE BLIND SPOTS, commit a5d21ced8a `test(gates): teach the structural gates to resolve aliases, and prove they can fail`. 4 files, +762/-62. This closes the highest-value finding in the campaign.

DEFECT 1 FIXED. `_fromisoformat_call_linenos` no longer matches a spelling; it resolves each call's dotted callee through the module's own import bindings and compares against one canonical origin, `datetime.date.fromisoformat`. Now catches eight forms: `date.fromisoformat`, `_date.fromisoformat` (the alias that evaded it), `dt.date.fromisoformat`, `datetime.date.fromisoformat`, a submodule import binding the root, a function-local aliased import, a handle rebound through a variable, and a multi-link rebinding chain. CRITICAL SUBTLETY, worth reusing: naive alias resolution would have NARROWED the gate, because a module reading `date.fromisoformat` whose binding cannot be resolved (a first-party re-export, a TYPE_CHECKING-only import) would stop resolving and silently drop out of scope. `_UNREBOUND_DATETIME_DEFAULTS` seeds the bare names so they stay governed, while a module genuinely rebinding one is judged on its own binding. Scope preserved in BOTH directions; the core/ exclusion, canonical-module exemption, and the datetime class remaining out of scope are untouched.

DEFECT 2 FIXED BEYOND THE AUTHORISED FLOOR. The brief authorised alias resolution plus ONE re-export hop with the residual documented; one hop proved not to be the honest limit, so the re-export graph is now closed TRANSITIVELY to a fixed point (`prompter_export_map` seeds from module-level runtime bindings and propagates across first-party from-import edges). A prompter handle laundered through a chain of re-exports resolves to its library. That is a new rule 2; the prior silhouette rule became rule 3 and is now alias-aware, having previously keyed on a bare reference spelled `questionary` so that `from ._reexports import questionary as q` was invisible to it.

RESIDUAL LIMITS NOW DOCUMENTED IN THE GATE'S OWN DOCSTRING, under a named section rather than left as an implied guarantee - the direct antidote to this repo's false-prose pattern: (a) a module string built at runtime and passed to `importlib.import_module` is not an import statement, so no static walk can see its target, which is exactly why the dynamic-import-targets rule is author-side discipline; (b) a PEP 562 module-level `__getattr__` serving a handle on attribute access binds no name any import statement declares, so such a facade sits outside the closure - no production facade in this tree does so, and rule 3's silhouette check is the named backstop if one appears.

ANTI-TAUTOLOGY PROOFS ADDED FOR EVERY TOUCHED GATE - `39 passed in 49.32s`, exit 0. Parsing gate: 8 parametrized planted violations, one per evading spelling, plus 4 out-of-scope shapes proving alias awareness did not widen reach. Prompter rule 2: a handle laundered TWO hops from the canonical owner, asserting both fire - the second resolves only under a transitive closure, so the test would fail against a one-hop implementation; plus a false-positive guard built from the tree's real shape, plus an anti-vacuity test proving that an empty export map would make rule 2 scan every module against nothing and report green. Prompter rule 3: the renamed-handle rival, AND a guard on the proof itself asserting that same rival is invisible without resolved handle names - the blindness is demonstrated, not merely asserted fixed. All planted violations are parsed from memory with synthetic paths; nothing was written to disk.

A THIRD GATE WITH NO FAILURE PROOF AT ALL, found in survey and fixed: `core/tests/test_modelo_string_usage.py` asserted only an empty offender list against a clean tree. Its inline loop was extracted into a pure `bare_modelo_code_offenders(relative, tree)` and given 5 planted violations in real identifier positions, 5 structurally-excluded shapes, and a proof that an allowlist entry is keyed by `(path, code)` and does not silence that code tree-wide. Its real evasion is NOT aliasing (it matches string constants, so aliasing is structurally irrelevant) but VALUE CONSTRUCTION: `"1" + "00"` computed at runtime, `str(100)`, or an f-string never materialises the constant. Adjacent literals (`"10" "0"`) ARE folded to one Constant by the parser and so are caught.

ZERO REAL VIOLATIONS IN SCOPE, and this was PROVEN rather than inferred: the new detector was run over the real tree with exclusions OFF, to demonstrate it fires on live files and not only synthetic ones. It fires on exactly six sites, every one inside the `core/` layer it correctly exempts (core/config.py:1216, core/parsing/_dates.py:56, core/setup_answers.py:597, :692, :705, :718). Rule 2 finds nothing - no prompter library outside the two canonical modules, no star imports in application/flows/. Defect 1's real call site was already fixed in 57f8711287; only the gate remained blind. ZERO allowlist entries were added anywhere.

SURVEY VERDICTS for the rest of the class. `tests/test_lazy_import_policy.py`: not alias-evadable (`_resolve_target` keys on the module target, so `import cadrumo.x as y` still resolves); re-export indirection inapplicable since it governs deferral SITES not symbol provenance; evadable only by a function-local `importlib.import_module`, the documented AST limit. Its failure proof is MISSING and was deliberately NOT added - that file carries live uncommitted peer WIP (18 insertions). The fix is cheap for its owner: `_ScopeWalker(consumer, package_base, is_init).visit(ast.parse(src))` is directly drivable against a synthetic tree. `dev/import_hygiene_scan.py` plus its gate: not alias-evadable (ownership keys on the imported module path; `_facade_export_origins` already tracks `alias.asname or alias.name`), re-export handled via `find_multi_sourced_symbols`, evadable only by a built module string which is the already-documented Ruling-6 limit; failure proof PRESENT via planted-source tests, with Family-1 covered by exact-named-baseline-set equality (a weaker but real signal). `dev/audit/duplication.py`: not an AST gate at all but a jscpd token matcher, so alias evasion is inapplicable; its failure proof is PRESENT and the strongest in the tree - unparseable output, missing npx, bad path, timeout and non-zero exit are each proven not to classify green, with `files_analyzed` as the discriminator so "nothing was looked at" cannot read as "no clones". That gate was already sound, and its historical false-green (a second jscpd command protected by a tautological test) is fully resolved including a pin that only one invocation may exist tree-wide.

THE src -> dev QUESTION, ANSWERED, and it is a DIFFERENT defect class from the above. NEITHER candidate gate governs that direction: `dev/import_hygiene_scan.py:314` opens `find_private_import_violations` with `if not site.target_mod.startswith("cadrumo"): continue`, structurally skipping every `dev.` target before any check, and its other families are computed over cadrumo `__init__.py` facades so never see it; `core/tests/test_boundary_contract.py` is scoped to core -> outer cadrumo layers, production-only and import-time-only, and never mentions `dev`. The scan gate's 11 green tests are therefore consistent with the edge being ungoverned - none of them looks. BUT THE PACKAGED-INSTALL HAZARD DOES NOT EXIST, and a different gate proves it: 0 PRODUCTION modules under src/cadrumo/ import `dev.` (all 18 sites are test modules under a tests/ directory), and `src/cadrumo/tests/test_wheel_content_boundary.py::test_wheel_excludes_every_test_member` asserts that no member under any tests/ package ships in the wheel - live and green at `6 passed in 33.96s`, carrying pytest.mark.unit so it runs in the default lane. A packaged install contains none of the modules that import `dev.` and cannot import it. CLASSIFICATION: the two confirmed defects were gates CLAIMING to forbid X while silently missing aliased X - a false green against their own stated contract. `src -> dev` is a direction no gate ever claimed to govern - an absence of coverage, not a lie. Worth closing cheaply by inverting that `startswith("cadrumo")` skip scoped to production modules, where the answer is already zero and it becomes a hard-zero pin; the bounded consequence today is that those test modules require the repo checkout and dev extras, which is already true and intended.

PRE-EXISTING RED NAMED, NOT PATCHED (per full-tree-gate-must-distinguish-owner): `tests/test_lazy_import_policy.py` is red at HEAD, `4 failed, 40 passed in 63.16s`. Every flagged site is in a production file owned by another live campaign - adapters/inbound/tui/{_manager_screen,_registration_screen,_theme}.py, application/user_profile/{_bundle,_bundle_export,_lifecycle}.py, application/inventory/_service.py, application/ledger/_business_operation_invoice.py, application/workflow/_events.py. Closing it requires, in the SAME commit as those deferrals: `ADAPTER_INTERNAL_DEFERRAL` 168 -> 176, `APPLICATION_DEFERRAL` 535 -> 544, `_ALLOWLIST_EDGE_CEILING` 487 -> 501, plus allowlist entries for 18 unclassified edges. Note this overlaps the bucket-event routing edges recorded earlier: relocating `emit_bucket_event` to domain/buckets would delete several of them rather than requiring an allowlist bump.

METHOD NOTE: the agent found its own pattern by searching first - `test_no_skip_xfail.py` already carried a complete alias-resolution inventory (module aliases, from-import aliases, assignment rebinding chains), so the tree already held a worked solution to this exact defect. It modelled the fixes on that rather than inventing a shape. Worth remembering: before building a structural gate here, look for the one that already solved the same problem.

COORDINATOR RULING - DECIMAL PARSING CANONICAL HOME, recorded here because a second campaign's agent found this document on its own and quoted it while asking exactly this question.

RULING: `cadrumo.core.decimal` owns the decimal GRAMMAR. `entrypoints/cli/_common.py`'s regex-based `parse_decimal_amount` / `parse_optional_decimal_amount` are a SECOND grammar for one concept and must be reconciled into the core primitive, becoming thin localised-refusal shells over it rather than independent implementations. Before that reconciliation lands, the two accepted-input sets must be proven identical; any input the regex accepts or rejects that `try_parse_canonical_decimal` does not is itself a finding to report, not a difference to paper over by widening or narrowing the CLI boundary. CLI-layer wrappers KEEP their own refusal shells - translation key, `typer.BadParameter`, optional and default behaviour - because a per-command message key is deliberate; only the grammar is shared.

WHY THE RULING WAS NEEDED, and it is the campaign's own risk realised: two agents from two different campaigns were dispatched onto this same cluster with CONTRADICTORY canonical targets - one briefed to consolidate onto `_common.py::parse_decimal_amount`, the other (this campaign's) onto `core.decimal`. Had both landed, the result would have been a THIRD authority for one concept: precisely the defect the sweep exists to remove. The second agent stopped without editing, on the shared-worktree rule that a non-empty `git diff` on a target file means stop and report, and it explicitly cited the contradiction between its brief and this audit document. That refusal prevented the regression. Recording the lesson: in a shared worktree running concurrent campaigns, a cluster spanning several files needs its canonical home DECLARED IN THE AUDIT before any agent is dispatched at it, or two correct-looking briefs will converge on different answers.

TREE STATE ESTABLISHED BY THAT AGENT (supersedes the inventory recorded earlier in this document): `entrypoints/cli/_modelo_cli_support.py::optional_decimal_option` has ALREADY been rewritten by a peer to delegate to `cadrumo.core.decimal.try_parse_canonical_decimal(raw, max_fraction_digits=2)`, keeping its own translation_key/default signature and its own `typer.BadParameter` raise, and NOT calling `_common.py`. `entrypoints/cli/_modelo_maritime_cli.py::_optional_decimal` has ALREADY been DELETED by the same peer, with its call sites migrated onto `optional_decimal_option` - so one of the two bare-`Decimal()` under-validation sites recorded in Tier 2 above may already be closed; verify at HEAD before acting. `entrypoints/cli/_ledger_support.py` is CLEAN and its `_parse_decimal` remains an unrouted duplicate. `_common.py` is dirty but only in `_emit_envelope`/`_active_profile_label` (an unrelated operator-output refactor); the decimal pair is untouched by that WIP.

STILL OPEN AND STILL THE PRIORITY: `application/modelo/_calculate_input.py::_decimal`, the bare `Decimal()` on the `modelo calculate --set` path. That is operator-supplied casilla values reaching a tax calculation with no canonical-grammar validation - a silent under-validation defect rather than a duplication nit, and the reason this cluster was ranked as correctness work.

RING-FENCE UNCHANGED: the bank-export (`adapters/inbound/financial/providers/_base.py::parse_amount_value`), AEAT-PDF (`adapters/inbound/pdf/_label_regex.py`, `adapters/inbound/justificante/_extract.py`), and oracle (`domain/calculations/registry/_renta_web_open_oracle.py`) parsers are DIFFERENT accepted-input sets parsing real AEAT artefacts. They are not part of this cluster and must not be merged into it. Where two sites share a grammar but differ only in error contract (raise-with-context versus best-effort `None`), the correct shape is one parser plus a thin `try_*` wrapper - not two implementations.

RESOLVED WITH A CORRECTION - the pagos-fraccionados applicability route, commit feb71e30e6 (2 files, 312 insertions, 11 deletions). This entry contains the campaign's most important single measurement: proof that the obvious consolidation would have STRIPPED REAL TAX CREDITS.

THE NEAR-MISS, measured against the real registry authority on M100 2025 rather than reasoned about:
  gate non-filer set     : 111, 123, 130, 131, 184, 190, 193
  resolver candidate set : 130, 131
  gate INTERSECT payer-filed : 111, 123, 184, 190, 193   <- reusing the gate's set WOULD zero these
Those five are suffered-retenciones sources that the PAYER files, not the taxpayer. Their value is a real credit the taxpayer must declare, so folding those legs in as zero strips the credit - a silent under-declaration. The two decisions look interchangeable and are not: `no evidence requirement` is NOT `no obligation, value is zero`. `_non_filer_modelos` carries a `taxpayer_files_source = false` arm that the relation-prefill resolver must NOT inherit. The pre-existing hardcoded {M130, M131} pair was safe only by coincidence, those two happening to be exactly the conditional-on-economic-activity set. The fix therefore derives the candidate set from `snapshot.revision.dependency_classifications` filtered to `conditional_on_economic_activity` and EXCLUDES the `taxpayer_files_source` arm, with that reasoning recorded in the docstring so a future consolidation pass cannot reuse the gate's set.

TWO PARTS OF THE LEAD WERE WRONG. (i) The bare-three-digit-literal concern was already satisfied: the site used `str(Modelo.M130)` / `str(Modelo.M131)` from the core enum before the change. (ii) More importantly, the call sites (~522, ~755) do NOT feed a suppression decision. The set decides whether an UNRESOLVED RELATION resolves to `Decimal("0")` with an operator-visible note versus `value=None`, a blank the operator fills. The cross-period clean-state gate is a separate evaluation that still runs at verify and file with its own independently-derived set, so this route cannot launder a filing past the evidence gate. Relation prefill is genuinely a different concern from clean-state gating.

BUT IT WAS NOT THE SAME SHAPE AS THE NEIGHBOURING FALSE POSITIVE. Unlike the refuted cross-period finding - where the second site merely assembled one OR-term of the canonical function's argument - this site computed its OWN independent set for a DIFFERENT consumer (`resolve_relations_from_local_store`) from a hardcoded candidate pair. That part of the finding stands, which is why it was actioned rather than refuted.

A DELIBERATE DESIGN CONSTRAINT, confirmed rather than overridden: the site reads bucket profile-path values instead of a `TaxpayerProfile` because `taxpayer_profile_from_mapping` (domain/deadlines/_profiles.py:55) calls `get_setup_flow()`, which raises `WizardCatalogueNotRegisteredError` in a non-CLI calculation context. A dedicated subprocess test (application/calculations/tests/test_first_year_modalidad_cuota_no_wizard_catalogue.py) pins exactly this for the sibling derivations. Anyone tempted to "simplify" that read should read the test first.

ZERO VERDICT DELTA, measured: the registry-declared conditional set is exactly {130, 131} on all five shipped M100 revisions (2021-2025), and no other modelo declares a conditional-on-economic-activity relation source. Fail-closed proven by test rather than asserted: three cases each assert `== frozenset()` (every source ENFORCED) for an absent profile, undeclared income categories, and economic-activity-with-undeclared-regime; plus `test_the_exclusion_is_not_vacuous_for_modelo_100` pinning that M100 really declares at least two payer-filed sources, so the exclusion guard bites rather than passing trivially. New module: application/calculations/tests/test_relation_prefill_not_applicable_registry_grounding.py.

Gates, sequential `-n 0`: 18 passed (new grounding module), 52 passed (clean-state + relation-prefill + parity + first-year), 532 passed (whole application/calculations/tests package, 2m48s), 65 passed (11 modelo cross-period and M100 fold-in live modules, 3m10s), 36 passed post-commit at HEAD; `pytest --collect-only -q` 14187/17482 collected with 0 errors; ruff, format and ty all clean; pyright 0 errors with 3 reportPrivateUsage warnings matching an existing sibling test's pattern.

OPEN FINDING, deliberately NOT fixed in that commit - A MEASURED BEHAVIOURAL DIVERGENCE, not a duplication. The verdict logic re-parses `taxpayer_type.irpf_income_categories` and `irpf.estimation_regime` as strings against a `_DIRECT_ESTIMATION_REGIMES` string set that duplicates the `IrpfEstimationRegime` enum, and it diverges from canonical for one REACHABLE persona:
  econ + regime UNDECLARED : current [] vs canonical ['131']  -> canonical newly suppresses 131
  legal_entity             : current [] vs canonical ['130','131']  -> unreachable (no M100 work unit)
  all other 7 personas     : identical
Canonical is the better answer: `derive_modelo_applicability` resolves an undeclared regime to estimación directa on cited law (LIRPF art. 16 / RIRPF art. 32, módulos being opt-in), and the clean-state gate ALREADY suppresses M131 in that case - so the resolver is the component out of step, not the gate. Because this changes a verdict rather than removing a duplicate, it must land as its own commit with a test pinning the new behaviour for that persona, flagged as a deliberate verdict change.

BLOCKER AND THE APPROVED PLAN. The canonical pair is unreachable from `application.calculations` without a new function-local import, and `tests/test_lazy_import_policy.py` currently carries live peer WIP editing `_SITE_CEILINGS[APPLICATION_DEFERRAL]` (523 -> 535) and `_ALLOWLIST_EDGE_CEILING` (477 -> 487); `test_ceilings_carry_no_slack_over_the_live_counts` demands EXACT equality, so two agents cannot both ratchet those constants without one redding the gate. APPROVED PLAN (held pending release of two peer-owned files): relocate `derive_taxpayer_files_economic_activity` and `derive_not_applicable_source_modelos` into `domain/calculations/registry/_applicability.py`, beside their siblings `derive_modelo_applicability`, `derive_modelo_202_modality` and `derive_tax_route`. They are pure `TaxpayerProfile -> verdict` functions with no verification coupling, so that is their proper home; the move lets both layers import EAGERLY, creating no new lazy edge and requiring no ratchet edit at all - avoiding the collision rather than negotiating it. One atomic `relocation:derive_taxpayer_files_economic_activity` commit across the canonical site, application/modelo/{__init__,_filing_actions,_export}.py, _modelo_work_verification_cli.py, the two verification modules, every `__all__` baseline, and regenerated docs/api stubs. Verified prerequisite: `projection_for_taxpayer` self-registers the wizard catalogue at function scope (probe showed registered False -> True across one call), so the relocation preserves the non-CLI property the subprocess test pins.

REFUTED 5 - THE FIXED-WIDTH EXPORT ENCODERS. No commits, consolidation proven unsafe, and the finding's own framing corrected by one structural level. This surface writes the bytes a human files with the tax authority, so the refusal matters more than a fix would have.

THE MODEL COMPARISON WAS WRONG BY A LEVEL. `ExportRecordDefinition.fields: tuple[ExportFieldDefinition, ...]` (_schema_surfaces.py:703 / :648) - the record CONTAINS the field. So application/filing/_export.py and application/modelo/_m145_communication_records.py are NOT two models; they are ONE model with TWO CONTRADICTORY CONSUMERS. `RecordFieldSpec` (_formats/_record_spec.py:136) is the only genuinely foreign model, differing on eight axes: offset and length optional-ge0 versus required-ge1 (1-based BOE); `kind` meaning value SOURCE (CASILLA/BINDING/LITERAL/HEADER/DRAFT/COMPUTED/FILLER) versus semantic TYPE (ALPHANUMERIC/NUMERIC/CURRENCY/DATE/RESERVED); semantic type as a separate `data_type` field versus folded into `kind`; `padding` with char and side FUSED plus a separate `justification` versus orthogonal `pad_char` + `justification` with no "none" member; `signed: bool` versus a `signed_mode` enum; free-form `date_format: str` versus a 2-member `DateFmt` enum; legal_refs/source_refs REQUIRED versus absent. Decisively, `RecordFieldSpec.kind` corresponds to `ExportFieldDefinition.data_type`, NOT to its `kind` - unifiable only through a lossy remap.

THE ACTUAL LATENT BUG, and it is independent of any consolidation: `ExportFieldDefinition` carries TWO pad axes and its two consumers disagree on which is authoritative. `_export.py:855` `_pad` derives the pad SIDE from `padding` and NEVER READS `justification` - verified, the only justification/padding references in that file are lines 858/860/862, so `justification` is DEAD INPUT on the live filing path. M145 derives the pad CHAR from `padding` and the SIDE from `justification`. Nine of twelve combinations diverge, several to exact opposites: (left_zero,left) gives '0000AB' versus 'AB0000'; (right_space,right) gives 'AB    ' versus '    AB'; (none,left) gives 'AB' versus a raised M145CommunicationRecordExportError. PROPOSED DURABLE FIX, approved in principle: a registry-build validator refusing any field whose padding-implied side conflicts with its declared justification. Today that refuses exactly one field (349/2020-y-siguientes modelo-349-decl-representante-nif-filler, right_space+right, which is `kind=filler` so `_format_field` returns spaces before `_pad` is reached - byte-neutral). It is a behaviour-preserving gate that makes the ambiguity structurally unrepresentable, and it must land BEFORE any shared primitive.

A SIGN-BYTE TRAP INSIDE THE ONE PROVABLE EQUIVALENCE. `_export.py signed=True` was proven byte-identical to `encode_currency(inline_sign=True)` (' 000123456', 'N000123456', ' 000000000'), and all three encoders agree on ROUND_HALF_UP because all three already delegate to `core.money.round_to_cents`. BUT `signed` means OPPOSITE things across the two APIs: `ExportFieldDefinition.signed` maps to `encode_currency(inline_sign=True)`, whereas `encode_currency(signed=True)` returns '0000123456' - a full-width bare magnitude with NO sign byte. A rename-level consolidation would have silently dropped the sign byte from every negative filing amount. Two further blockers: the equivalence crosses a layer `.importlinter` pins individually with `unmatched_ignore_imports_alerting = error`, and neither consumer module carries a pin to adapters.outbound.aeat.export; and the consolidation TARGET is itself unreached code, so routing live filing bytes onto it would make real filings depend on a package with zero production callers.

CORPUS EVIDENCE: 23 fixed_width layouts, 7,944 real export fields probed. Exactly ONE axis disagreement, and it is byte-neutral (the filler above). M145's own 53 fields are 51 consistent plus 2 exact-width `padding=none` literals, so M145 WOULD be byte-identical under _export.py semantics today - but it would lose two real safety properties: exact-length enforcement, and encoding-aware BYTE overflow measurement versus _export.py's character count. Losing a safety property is not a byte-identical refactor, so it was left alone. A hazard was also chased and CLEARED rather than left implied: `_pad` has no under-width check for `padding=none` (425 fields, 86 with dynamic values), but `_render_record` (_export.py:665) writes into a space-prefilled offset-positioned buffer so a short value cannot shift the record, and all 425 are text/boolean, never money or integer; all 339 `padding=none` literal fields are exact-width.

`_formats` PACKAGE IS DEAD - VERDICT CONFIRMED, WITH THREE CORRECTIONS ALL IN THE "MORE DEAD" DIRECTION. Every importer of `_serialise` is one of its own two test modules; `_formats/__init__.py` does not even re-export serialise/serialise_envelope (its `__all__` is _record_spec symbols only) and `export/__init__.py` imports only `._errors`, so the package is unreachable from the facade. (a) `_deserialise` is ALSO test-only - the live parse path is `domain.calculations.registry.parse_export_payload`, which has its own decoder and never touches _formats, refuting the premise that the decode direction had live callers. (b) `test_envelope.py` is NOT an M232 test - its own docstring states it uses a reduced two-segment layout specifically to avoid coupling to real registry offsets; M232 envelopes are handled in production at _export.py:791 via `computed_key == "envelope_closing_tag"`. (c) The enrolled roundtrip gate does not use serialise either - test_roundtrip_coverage.py:80 pins the fichero-BOE bytes to test_fichero_boe_roundtrip.py, which calls `application.filing.export_draft` and asserts a GOLDEN SHA256 (feaffb81...fe0120, a byte-exact 946-byte M130); it merely LIVES in the _formats tests directory while exercising the live path. Consequence: the enrolled gate SURVIVES deletion of the package.
  The `_formats/__init__.py` docstring is FALSE IN BOTH BRANCHES - it claims the primitives "remain useful when a caller already has adapter-local RecordFieldSpec or SegmentSpec tuples" (no such caller exists, in production or tests) and omits that it does not export the serialiser it describes. Correctly NOT edited: since the verdict is deletion, fixing prose on doomed code is the wrong move.
  RETIREMENT APPROVED IN PRINCIPLE, HELD FOR ITS OWN DISPATCH once the code index is real: retire the WHOLE package (_record_spec + _serialise + _deserialise are collectively unreached), repoint the test_roundtrip_coverage.py:80 pin, and clean two dev-tooling references - dev/audit/semantic.py::_VERIFIED_NON_LEAK_PATHS and test_dev_rename_audit_tools.py:52, which asserts every path starts with "src/cadrumo/adapters/outbound/aeat/". The agent declined to start this explicitly rather than half-landing it, which was correct.
  THIRD RECOMMENDATION, accepted: if the encoders are ever unified, the target must be `core/`, not the adapter package - `core.money.round_to_cents` is already the correctly-centralised piece and core is importable from every layer with no new .importlinter pin.

Gates on the untouched tree, marker `-m "unit or integration"` (explicitly not the repo default, which deselects integration modules and exits green): 183 collected, `183 passed in 105.45s` at one HEAD and `183 passed in 81.90s` re-run after HEAD moved mid-session under a peer ledger refactor - re-run specifically to keep the green claim honest. Coverage included the golden-SHA roundtrip, envelope, currency-edge-case, completeness-parity and completeness-set suites, so the blank-required-casilla refusal on the write path is confirmed still firing.

RESOLVED - ROUNDING DUPLICATION AND MODE ANOMALY, commit 961485c7c6ff98bb1f1428b5b12921a0075fb2d5. Seven local helpers retired onto `core.money.round_to_cents` across 7 files (~100 call sites, both local definitions removed, the support module's re-export dropped from its 3 importers, now-unused ROUND_HALF_UP imports cleaned).

THE ANOMALY WAS FOUR COPIES, NOT ONE, WITH WIDER REACH THAN REPORTED. All four were `return value.quantize(Decimal("0.01"))` with NO rounding argument, i.e. ROUND_HALF_EVEN: _modelo_131_modulos_engine_support.py:572 (and this one was RE-EXPORTED to three further test modules - test_modelo_131_modulos_engine, _food, _retail_services), test_modelo_131_modulos_engine_2024_backfill.py:90, test_modelo_131_modulos_engine_2026_rollforward.py:81, and test_modelo_303_regimen_simplificado_modulos_engine.py:74.

WHAT THEY ROUNDED, AND WHY IT MATTERED: EXPECTED VALUES asserted against engine output - not intermediates, not display. M131 `expected_previo` (Fase 1a rendimiento neto previo) and M303 `expected_devengada` / `_expected_cuota_derivada`, in assertions of the form `assert devengada == expected_devengada == Decimal("351.20")`. Meanwhile every casilla involved declares `rounding = "money-2"` in the registry (M131 0003-modulos-engine.toml for 2024/2025/2026 across all three phases; M303 2023-y-siguientes/formulas/0002-modulos-engine.toml for modulos-iva-cuota-devengada and -derivada), and `apply_rounding("money-2")` resolves to `core.money.round_to_cents`, i.e. ROUND_HALF_UP. So the tests were asserting against a rounding mode the engine does not apply - against a mode `core/money/__init__.py` explicitly documents as one that "does NOT match AEAT and must never be used at the euro-cent boundary".

REACHABILITY MEASURED, NOT GUESSED. Every real call site was instrumented via a probe plugin wrapping both helpers, deduped against double-recording, capturing all 144 calls the six modules make. `_quantize`: 56 calls, only 4 needing any rounding, ZERO exact-half ties (remainders .008/.0061/.001/.0025 - all unambiguous), the other 52 receiving values already at or below 2dp where quantize is a no-op. `_money_round`: 88 calls, 70 needing rounding, TEN exact-half ties of which four genuinely diverge between modes (16737.2850, 19787.7050, 6270.2850, 21138.5450 - e.g. .28 versus .29). VERDICT: no currently-asserted figure changes, proven twice - mathematically (no tie on the quantize path, and for non-ties the two modes are provably equal) and empirically (137 passed after the correction with every expected literal untouched). But the exact-half tie is DEMONSTRABLY REACHABLE in this very value space, landing ten times on the sibling path - so this was a live latent trap one test case or one new Orden coefficient away from silently asserting a wrong cent. No expected value was authored or recomputed: the figures remain the independently transcribed Orden tables (HFP/1359/2023, HAC/1347/2024, HAC/1425/2025), and only the rounding mode was corrected to the registry-declared one.

FOURTH GATE BLIND SPOT, and the most elegantly inverted one yet. `src/cadrumo/tests/test_decimal_enrollment_inventory.py::test_no_inline_quantize_round_half_up` exists to forbid exactly this pattern and misses it TWO ways: it iterates `production_ast_items(...)`, so test-local copies are structurally invisible; and `_is_cent_quantize_call` REQUIRES a `rounding=ROUND_HALF_UP` keyword, so a bare wrong-mode `quantize(Decimal("0.01"))` is invisible EVEN IN PRODUCTION. The gate catches the correct-mode duplicate and is blind to the incorrect-mode one - it forbids the safe spelling and permits the dangerous one. Remediation: widen it to test modules AND to the missing-keyword shape, with an anti-tautology proof planting a bare-mode call.

NO PRODUCTION MONEY BUG: four production bare-mode cent-quantize sites exist and all are non-euro - _run_telemetry.py:239 and diagnostics_run_health.py:365 and :577 (mean_duration_ms), _projection.py:781 (pct_change).

OPEN FOLLOW-UP, out of scope and not touched: 45 test-side bare-mode cent-quantize sites remain, several shaping tax figures under the same latent class - test_modelo_151_beckham_cuota_continuity.py:94 and :97, test_modelo_202_cuota_base_ejercicio_anterior_continuity.py:299, test_modelo_190_111_reconciliation_continuity.py:261, five sites in test_modelo_210_irnr_continuity.py, and test_e2e_ledger_m303_recargo_cross_period.py. Worth one dispatch alongside the gate widening.

Gates: `137 passed in 36.43s` sequential `-n 0 -m unit` across the six modulos modules plus core/money/tests/test_money.py plus the enrollment gate; `3019 passed, 2 warnings in 209.77s` parallel across the full registry test tree with zero FAILED lines; `pytest --collect-only -q` 14187/17482 collected with no errors; ruff check clean and format clean after a formatting pass; `dev.quality.types` and `dev.quality.relative_imports` both exit 0 with no diagnostics in the touched files. A peer's live WIP in test_modelo_303_registry.py appeared mid-session and was left untouched and out of the commit, with the staged set verified as exactly the seven authored files before an explicit-pathspec commit.

DECISION RECORD DELIVERED - inner-envelope version drift. ADR scaffolded at .vault/adr/2026-07-25-code-dedup-sweep-adr.md, status `proposed`, currently UNTRACKED and uncommitted. No source file was touched.

RECOMMENDATION: tighten the layer-2 check from `>` to `!=` across the sites - NOT because a live defect exists (it provably does not) but because the edit is a zero-behaviour no-op today and becomes a hard refusal against filed taxpayer data the moment the first version bump lands under the RELEASED regime. Land it as one shared NON-raising predicate each site calls while keeping its own error type, message key and per-object context, plus an AST gate so the loose form cannot re-enter by copy.

Q2 ANSWERED BY POSITIVE ENUMERATION, not by grep: all 66 registered `SecureObjectNamespaceDefinition` objects carry `schema_version = 1` (storage/_namespace_registry.py:19, `SECURE_OBJECT_SCHEMA_VERSION_V1 = 1`), and `Envelope.model_fields["schema_version"].metadata == [Ge(ge=1)]` (storage/envelope/_envelope.py:174). The only representable value is therefore >= 1 while current is 1, so `> 1` and `!= 1` are THE SAME PREDICATE today and the sites are already behaviourally identical to the canonical contract. Write-path evidence: at every site the writer stamps the identical constant its reader compares (e.g. modelos_filing.py:218 and :227 versus the check at :174), and all sites validate the canonical `Envelope[...]`. The only nearby version pass-through writer, workflow/_persistence.py:270, feeds a diagnostic reset fingerprint from ROW metadata and persists no envelope. So tightening is not a breaking change now or later.

THE REASON IT MATTERS, and this is the insight the original finding lacked: `_secure_object_row_codec.py:203` re-stamps the OUTER record to `max_supported_version` UNCONDITIONALLY, while the inner stamp lives inside the payload bytes and moves only if a registered upgrader rewrites that field. An upgrader that transforms shape but forgets the inner version therefore produces exactly a below-current inner stamp on a record the outer layer has already declared current - and the inner EQUALITY check is the ONLY place that half-written upgrader is detectable at read time. The `>` form disarms it. Nothing else guards this: the seven tests in storage/tests/test_schema_lineage.py cover layer 1 only.

Q3 CRUX RESOLVED: the compatibility regime argues FOR tightening, not against. `no-legacy-compatibility` separates "Refuse, do not tolerate" (mandated) from "invent handling for shapes nothing wrote" (forbidden); a tightened refusal is the blessed half, and the lifecycle rule states outright that gates and scaffolds are installable now.

TWO CORRECTIONS TO THIS AUDIT'S OWN INVENTORY - it over-counted by two, and both exclusions are instructive because they show what right looks like. `bucket_maintenance/_service.py:149` is NOT a defect: it is the canonical CORRECT shape, a ceiling at :149 PAIRED with a below-floor refusal at :157, with `_ARCHIVE_DURABILITY_FLOOR == _ARCHIVE_SCHEMA_VERSION == 3` (:118/:123), which is exactly equivalent to `!= 3`. `_bundle_encryption.py:91` likewise pairs its ceiling with a set-membership floor at :99 on a `ge=1` axis whose constant is 1. Both correct, both out of scope; the bundle tier at _bundle.py:121/:131 is the same two-sided shape. So the real site count is 20, not 22. Incidental confirmation that the durability machinery works: both the archive version and its floor have moved 2 -> 3 since the durability ADR was written, in lock-step, and the floor-pin gate held.

A FOURTH PERSISTED FORMAT WITH NO VERSION GATE AT ALL - found out of scope and STRONGER than the ADR's own subject. The bucket manifest carries a `schema_version` field (storage/bucket/_manifest.py:91), hardcoded to 2 at create (application/user_profile/_profile_repository.py:330), passed through UNCHANGED on every save (:478), and read at storage/bucket/_manifest_io.py:157 with NO version gate of any kind - not a ceiling, not a floor, nothing. A manifest written by a NEWER application version is accepted silently. Contrast every other persisted format in the tree, each of which carries at least a ceiling. The read surface beyond that one path was not audited. This wants its own dispatch, and under the compatibility-lifecycle rule a format must enrol its floor, version and upgrader registry AT BIRTH - this one never did.

VAULT VERIFICATION, done properly rather than assumed: `vault check all` exits 0 with 14,729 warnings that are all pre-existing vault-wide (overwhelmingly research docs missing a Sources section). The new document contributes zero, established by running all ten relevant checks individually against its stem AND by a POSITIVE CONTROL confirming the body-sections check does name 428 distinct ADR files in a matching form - so the empty result is a real absence rather than a pattern that never fits the data. Raw headings were verified by hand (one H1, seven H2s in template order, no duplicates, no placeholder residue) because `vault check` does not validate heading structure.

OPERATIONAL HAZARD RECORDED: `vault add adr` supports no `--topic` flag, so there is exactly ONE ADR filename slot per feature per date. With many agents dispatched under the `code-dedup-sweep` feature tag, any second agent wanting an ADR today collides on this filename. Future campaigns should either allocate distinct feature tags per decision or scaffold ADRs from the coordinator serially.

PENDING: both this ADR and this audit document are untracked. They need committing under an explicit pathspec by an agent authorised to run git, or they will be lost to a peer's broad `git add`.

REFUTED 6 - THE "TWO PARALLEL EVIDENCE SYSTEMS". Not substitutable, and the finding's premise was already satisfied by the code. Commit cd14309ddbcbe98500f0c946df9f4c08900f67a0 landed only the genuine cheap win (3 files, behaviour-preserving, evidence surface but NOT a persistence boundary).

THE PREMISE DID NOT HOLD. `PurchaseInvoiceEvidence` ALREADY delegates byte custody to the generic attachment store: its add path calls `store.put_file(resolved)` then `store.write_manifest(Attachment(...))` and records the digest. One byte store, one blob namespace, one content address. The "typed sidecar" that this audit named as the shape consolidation would require is what already ships.

THE RIGHT READING: the three types are a PERMISSIVENESS LADDER over ONE byte payload, not competing systems. `Attachment` (bytes only) -> `PurchaseInvoiceEvidence` (every fiscal field optional) -> `Invoice` (every field required), with `aeat app ledger evidence confirm` as the promotion verb from rung 2 to rung 3.

FOUR INDEPENDENT BLOCKERS, any one sufficient:
(a) IDENTITY IS STRICTER, NOT MORE PERMISSIVE - the inverse of the substitutability precondition. `Attachment` carries a model validator pinning `attachment_id == sha256` (domain/attachments/_models.py:248), making it strictly 1:1 with bytes. `PurchaseInvoiceEvidence` is deliberately 1:N over one payload - its `_ID_DISAMBIGUATION_CAP` loop exists precisely to keep two records over the same file distinct (its evidence_id is a 16-hex digest over METADATA plus created_at plus a disambiguator, not over the bytes). Absorbing collapses records the design keeps apart.
(b) THREE DECIMALS HAVE NO TYPED HOME: taxable_base, iva_rate and iva_amount do not exist on `Attachment`, whose `metadata` is `Mapping[str, str]`, so they could survive only as type-erased strings - barred by aeat-architecture-boundaries and exactly the field-for-tidiness trade the brief forbade.
(c) MUTABILITY: `evidence update` applies a typed patch, re-stamps `updated_at` and emits PURCHASE_INVOICE_EVIDENCE_REPLACED. `Attachment` is frozen with no patch verb, and has no `updated_at` at all.
(d) `Invoice` CANNOT ABSORB IT EITHER - the other plausible target, since the transaction field already accepts an Invoice id. `Invoice` REQUIRES counterparty_name, counterparty_tax_id, counterparty_country, base_total, iva_total, grand_total, currency, lines and payment_status, while every equivalent field on PIE is optional. A scanned receipt whose text layer yields nothing is a valid PIE and can never be an Invoice.
Derivable-only field: `media_kind` (pdf|image), already computed from mime_type by `_media_kind_from_mime`.

THE CHEAP WIN, LANDED: the two near-identical resolvers in `_evidence_input.py` differed only in the provenance id stamped plus a byte-custody guard. `resolve_attachment_evidence_input` gained `evidence_id: str | None = None`; `resolve_purchase_invoice_evidence_input` is now that guard plus a delegating call. One manifest-load / blob-read / EvidenceInput-construct block instead of two, no third name introduced, both module-private so no facade change.

A REAL CORRECTNESS DEFECT FOUND UNDERNEATH, and it costs the taxpayer money - NOT fixed, flagged, because it changes aggregation semantics. `Transaction.purchase_invoice_evidence_id` is DELIBERATELY POLYMORPHIC across two id spaces (documented at application/ledger/_actions_common.py:462-478): a PurchaseInvoiceEvidence id OR an imported InvoiceKind.RECEIVED id. But each consumer resolves only ONE space. `application/aggregation/_renta_ledger.py:619` resolves against the InvoiceCatalogue only, so a transaction attached with a VALID PIE id is dropped from the renta deductible-expense observations (:379-382) with reason MISSING_PURCHASE_INVOICE_EVIDENCE and the operator-facing message "absent from the invoice catalogue" - while the record exists, in the other space. The taxpayer silently loses a legitimate deduction and the diagnostic points the operator in the wrong direction. Symmetrically, application/ledger/_llm_classification.py:219 and _evidence_draft.py:393 call `PurchaseInvoiceEvidenceService.view`, which raises NotFound for an Invoice-space id. Either the field must carry which space its id belongs to, or both consumers must resolve both spaces, or the diagnostic must name the ambiguity. Needs its own dispatch.

A SECOND, SEPARATE FINDING: `PurchaseInvoiceEvidence.attachment_id: str | None` is a shape NOTHING WRITES - the add path always populates it, and the None branch is described in-code as covering records that "predate that contract". Under the PRE_RELEASE compatibility regime that is precisely the read-tolerance of a pre-current shape that no-legacy-compatibility forbids, and `_evidence_draft.py:698` works around the resulting Optional with a bare assert. Tightening the field to required touches a strict persisted model plus test_evidence_input.py:118 and the test_custody_store_matrix.py:773 seed.

TERMINOLOGY RESOLVED - no two of the four are interchangeable. `Attachment` (domain/attachments) is BYTE CUSTODY: immutable, content-addressed, refuses text/uri-list, and the only place evidence bytes live. `PurchaseInvoiceEvidence` (application/ledger/_evidence.py) is a MUTABLE OPERATOR-REGISTERED CLAIM about one such payload with every fiscal field optional. `EvidenceInput` (application/ledger/_evidence_input.py) is TRANSIENT in-memory decrypted bytes for one on-host read - model_dump, model_dump_json, __iter__ and __reduce_ex__ all raise so it can never be persisted. `EvidenceBundle` (application/evidence) is UNRELATED to documents: an audit-packaging manifest over WorkUnit / CalculationRevision / ModeloRecord ids behind `aeat app modelo audit`, and its facade already disclaims the name collision. The genuinely confusable pair was Attachment/PIE; their docstrings now separate them, and a wrong See Also pointer in the attachments facade was corrected (it attributed purchase-invoice evidence records to domain.invoices; PIE lives in application.ledger). A FOURTH invoice-ish store also exists - PayableInvoiceService / CollectibleInvoiceService - whose ids are deliberately REFUSED as evidence references.

HARD CONSTRAINT INTACT: no model field, validator, namespace or stored shape changed. The source_path no-fallback refusal and the text/uri-list rejection are both untouched and both covered by the green runs. Nothing permits a link or a bare source_path to stand in for encrypted stored bytes.

Gates, marker `-m "unit or integration"` on every run (never the repo default): 65 passed on the evidence and attachment suites; 442 passed across the full application/ledger tests plus the ledger-filing-evidence roundtrip, the custody store matrix and overview data-prep; 17 passed post-commit; 3 passed under `-m docs` on the core-struct docstring links; ruff check and format clean; `pytest --collect-only -q` 14106/17396 collected, exit 0.

TWO HONEST NEGATIVE RESULTS worth recording as method. First, the nitpicky Sphinx build did NOT complete - it ran over 45 minutes with a still-empty output file, so NO exit line was claimed for it. Instead every Sphinx role introduced was verified to have a green precedent in an already-autodoc'd production module (private modules ARE autodoc'd in this tree, so the check was necessary). Claiming an unfinished gate green would have been the easy path. Second, the first `--collect-only` run reported 112 COLLECTION ERRORS and an immediate re-run was clean at exit 0 - transient peer churn, not a real tree state. That is the same class as the earlier "a count at an old HEAD is not a measurement" lesson: in this worktree a single red reading must be re-run before it is believed, in either direction.

Also noted, not this campaign's surface: `just check-types` is red at HEAD with 39 advisory diagnostics, all in application/user_profile/*, entrypoints/cli/_config/__init__.py:888, entrypoints/cli/tests/, tests/_marker_hook.py and storage/master_key/tests/ - zero in the three committed files. The count moved 36 -> 39 mid-session with three new undefined-name errors in user_profile/_bundle.py, confirming a peer is mid-write there.

RESOLVED - canonical-JSON-then-SHA256 duplication, commit 921a78ce6d (2 files, 4 insertions, 9 deletions, one atomic commit covering both sites).

BOTH SITES ROUTED onto `core.hashing.content_hash_hex`: `adapters/outbound/aeat/auth/_session_store.py::_storage_state_sha256` and the sibling found out of scope at `application/aggregation/_atribucion_member.py:168`, which built `json.dumps(record.model_dump(mode="json"), sort_keys=True, separators=(",", ":")).encode(UTF_8_ENCODING)` - identical sorting, separators and encoding, differing only by a `"sha256:"` prefix that was preserved so the full string is unchanged. `_JSON_OBJECT_ADAPTER.validate_python` was PRESERVED at the session-store boundary: it is separate JSON-safety validation, not digest mechanics. No `__all__` promotion was needed - `content_hash_hex` is absent from `core/__init__.py` but `core.hashing` is a public module both files already imported, so no private-module violation arises.

BYTE-IDENTITY PROVEN EMPIRICALLY, NOT BY READING. The agent exercised both implementations across nesting, non-ASCII, escape sequences, floats, bools, nulls and key reordering - the non-ASCII case being what actually pins the `ensure_ascii` default, since both escaped identically. It then cross-checked the shipped digest against the deleted implementation directly, obtaining `dcbfcdab9989eddcd68fdfe131c719283e1960b866b600a3d36d6daff254f32b` from both. So the persisted `storage_state_sha256` compared on every session resume, and the pinned profile fingerprints, are provably unaffected. This is the standard a digest-touching consolidation needs: reading two functions that look alike is not a proof that their output bytes match.

THE ADJUDICATION IS RECORDED IN THE COMMIT MESSAGE, as instructed - that the semantic discovery gate could not be satisfied, the observed index numbers, and that exhaustive grep coverage was substituted by explicit coordinator adjudication on the grounds that any implementation of this concept must call `json.dumps`. The substitution is visible in history rather than implied.

A THIRD WAY A GREEN TEST RUN CAN LIE, newly identified and worth adding to the two already recorded in this audit. The 816-test suite run reported green, but ONE serial test (`test_ledger_scale_benchmark::test_iva_quarterly_aggregation_partitioned_p95_latency`) was HELD by the xdist scheduler because workers were active - it did NOT execute in that batch, and nothing in the output said so. The agent ran it separately (`1 passed in 122.79s` under `-n0`) rather than let a held test pass as covered. So the family is now: (i) the repo default marker selection silently deselecting integration-marked modules and exiting green; (ii) a structural gate whose pattern cannot match the violation it guards; (iii) a test HELD by the parallel scheduler, which looks covered in a green summary and never ran. All three produce a passing report that proves nothing.

TWO SELF-CORRECTIONS the agent volunteered, both material. First, it had earlier implied that the pinned `_PROFILE_FINGERPRINT = "sha256:a2163d..."` in test_source_mesh_profile_live.py guarded its `_atribucion_member` change. It does NOT - that constant belongs to `ProfileSourceResolver`, a THIRD site whose fingerprint is produced in `application/aggregation/_ledger_filing_snapshot.py`. The test passed but was never the gate on this edit; the actual gates are the 46-test AtribucionMember run and the general byte-identity proof. Second, and following from that: `application/aggregation/_ledger_filing_snapshot.py` ALSO defines a canonical-JSON fingerprint helper. It was NOT examined or touched, correctly, being outside the authorised scope after the instruction to stop opening new investigation. QUEUED: that third fingerprint site is the next candidate for this concept, and it should be assessed with the same byte-identity discipline - it is adjacent to the evidence-coverage guard already recorded above, so whoever takes it should read both together.

Gates, marker `-m "unit or integration"` (broader than the repo default, so nothing integration-marked was silently skipped): ruff check and format clean; `ty check` clean (the canonical checker here, mypy not being installed); `pytest --collect-only -q` 14187/17482 collected with no errors; `816 passed, 6 warnings in 236.32s` across the hashing, auth and aggregation suites; `30 passed in 20.00s` run verbosely to CONFIRM the load-bearing cases actually executed rather than assuming they were in the 816 - specifically the real-encryption session roundtrip, the non-JSON rejection proving the preserved validation still refuses, and the hash-mismatch resume path that exercises digest comparison; `46 passed in 20.18s` for the AtribucionMember resolver, run explicitly because those modules live outside the three directories already covered; and the held serial benchmark run separately. No keychain failures arose, so nothing environmental to discount and no guard was touched.

CLUSTER 2 (committed-TOML) closed with no edits, verdicts recorded earlier in this document: one site already delegates, one is genuinely unconvertible on a documented `tomllib.TOMLDecodeError` contract that `rtoml.TomlParsingError` does not satisfy, and two would ADD a refusal contract rather than preserve one - making "which typed error should each raise" a contract decision rather than a de-duplication.

REGISTRY CACHING - ONE REAL DEFECT FIXED, THREE CLAIMS REFUTED. Commit 31ce3018bf (2 files, explicit pathspec). This dispatch went one-for-four, and three of the coordinator's hypotheses were wrong, including the one rated sharpest.

REFUTED - THE `filing_year` CACHE-COLLISION HYPOTHESIS, which had been flagged as potentially the most serious bug in the sweep. The reported signature misleads: the key is `(modelo: str, period: _Period)` and `filing_year` is carried INSIDE `Period`, from which the function derives it. `Period` is `ConfigDict(frozen=True)` over `filing_year: int` plus `code`, and pydantic hashes it on both fields. Proof by construction: `Period(2024,'1T') == Period(2025,'1T')` is False, with hashes -545455879067548500 versus -9142051300812712064. Two tax years could never share a cache entry, so no filing was ever computed under another year's law by this route. Now also pinned behaviourally: the new test asserts 2023 and 2024 each resolve to the revision `authority.snapshot` selects for that year.

REFUTED - THE "FIFTH FALSE-PROSE INSTANCE". The `ALT-FINGERPRINT-RATIONALE-REGISTRY-TREE` marker at application/filing/runtime.py:469 and :480 has a REAL, WRITTEN rationale, found by grepping the repo AND the .vault corpus: plan `.vault/plan/2026-05-28-codebase-solidification-plan.md` step W15.P47.S618 directed either alignment with the canonical helper OR the addition of this exact marker, and `.vault/exec/2026-05-31-codebase-solidification-w15p47-exec.md` records the choice - option (b), relative-path keying distinct from the filename-keyed canonical, because refactoring to option (a) would require a canonical signature change rippling through four existing callers. A deliberate, plan-directed decision with its reasoning on record. The coordinator was pattern-matching on the earlier false-prose findings; this is not one. Nothing changed there.

REFUTED - THE FINGERPRINT DUPLICATION, and the brief paired the WRONG counterpart. The runtime helper's documented distinction is against `core.paths.file_stat_fingerprint` (filename-keyed, SINGLE file), not against the loader's. There are THREE genuinely different granularities serving three different caches: the loader's `(path, size, mtime, digest)` per-file structure keying a full recompile and covering directory-mode manifests, recursive revision fragments and `authorization.d/`; the runtime's `(relative_path, mtime_ns, size)` tree-walk probe keying `build_runtime_schema_provider`; and the canonical single-file triple. Two specific worries raised in the brief do not apply: the runtime probe uses `st_mtime_ns`, not whole-second `st_mtime`, so same-second edits ARE distinguished; and its 1-second TTL is bounded, documented in its docstring, and already covered by `test_registry_tree_fingerprint_ttl_cache`. The cheap probe is not used where the precise one is needed. Nothing consolidated, nothing written.

CONFIRMED AND FIXED - THE APPLICATION-LAYER SNAPSHOT MEMO. `application/filing/__init__.py:406` `_load_registry_snapshot` carried an `@lru_cache` with NO fingerprint and NO TTL - precisely the construct `aeat-registry-authority-flow` forbids in terms: "Do not introduce path-only registry caches that can serve stale TOML after source edits". Demonstrated empirically on a TEMP COPY of the registry (the tracked tree was never touched): after editing one casilla label, the second read returned the identical object, `edit visible via _load_registry_snapshot: False` while `edit visible via fresh authority: True`, and `CacheInfo(hits=1, misses=1)` proved the second call was a cache hit. Decisively, it SURVIVED the documented reset protocol - `resources.cache_clear()` plus `clear_fingerprint_cache()` - still returning the identical object.

A CORRECTION TO THE INVALIDATION MODEL, which both the brief and the agent's own first pass had wrong: a HELD authority does not re-derive after a source edit either (probe: `SAME authority instance sees edit: False`). Invalidation occurs when the authority is REBUILT - `resources()` is `@cache`d and holds one `StaticModeloRepository` with `_authority` memoized, and the rebuild's tree load is fingerprint-keyed. The application-layer memo was therefore the one link in the chain that NO documented reset could reach.

BLAST RADIUS SCOPED HONESTLY, not inflated: the registry root is `bundled_path("registry","aeat")`, fixed per process and NOT settings-overridable, so the only mutation vector is an in-place edit of `src/cadrumo/_data/registry/aeat/` while a process lives. That is the registry-authoring loop against a long-running MCP server or TUI session - routine activity in this repo - plus any test session rebuilding resources against changed sources. Short-lived one-shot CLI runs were never exposed. So: a real defect with a development-loop blast radius, not an end-user-filing one. And the cache was buying nothing for that hazard: 0.3 microseconds versus 0.7 microseconds per call, both sub-microsecond, because the layer beneath already caches.

THE FIX plus THE GATE WHOSE ABSENCE ALLOWED IT: the memo and its now-unused `lru_cache` import were removed, leaving the fingerprint-keyed chain as the single authority, with a docstring stating why that layer is deliberately uncached. Law-determined resolution is untouched - only `filing_year` and the bare period token are passed, never a revision id. New gate `application/filing/tests/test_registry_snapshot_freshness.py` uses two real registry trees and two real `ValidatedRegistryAuthority` instances over real TOML, substituting nothing about snapshot resolution itself (only which authority the module reaches, which is exactly what a resource reset changes) - no mocks of the subject, no skips, no xfail. PROVEN NON-TAUTOLOGICAL by re-applying the removed `lru_cache` in-process and confirming `GATE WOULD FAIL (stale served): True`. That anti-tautology discipline was applied unprompted, and it is the standard every gate added by this campaign has met.

Gates, all SEQUENTIAL (`-n 0 -p no:randomly`) with explicit `-m "unit or integration"` rather than the default selection: `3 passed in 163.50s` (new freshness gate), `41 passed in 150.48s` (registry compiled-cache, loader-cache-isolation, fingerprint-content-collision, cache-eviction, disk-cache-loader-fingerprint, temporal and validation-verdict-cache suites), `261 passed in 297.97s` (the whole application/filing tests suite), `14197/17492 collected` pre-commit, ruff check and format clean. No failures, so no parallel-versus-sequential re-triage was required. `lru_cache` confirmed absent at HEAD after the commit, as a peer-sweep check.
