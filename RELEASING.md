# Releasing

Release automation is defined in the repository `justfile`.

Minimum release entrypoint:

- Run `just release`.

The `justfile` also includes `release-apply` for apply-stage release flow.
